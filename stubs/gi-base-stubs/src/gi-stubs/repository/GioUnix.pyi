# pyright: reportIncompatibleMethodOverride=false
"""
Stub template for a GI Repository Module.

Generated by gi-stub-gen, version 0.0.1.
Module: GioUnix
Date: 2025-12-24
"""
# we disable method override report for pyright in generated stubs,
# causes too many false positives

from __future__ import annotations
from typing_extensions import deprecated  # noqa: F401
import typing_extensions  # noqa: F401
import builtins  # noqa: F401

import _thread
import typing

# gi.repository imports needed by this Stub
from gi.repository import GObject
from gi.repository import Gio
###############################################################
# Functions
###############################################################

@deprecated("deprecated")
@staticmethod
def desktop_app_info_lookup_get_default_for_uri_scheme(
    lookup: Gio.DesktopAppInfoLookup,
    uri_scheme: str,
) -> Gio.AppInfo | None:
    """
    Gets the default application for launching applications
    using this URI scheme for a particular [iface@Gio.DesktopAppInfoLookup]
    implementation.

    The [iface@Gio.DesktopAppInfoLookup] interface and this function is used
    to implement [func@Gio.AppInfo.get_default_for_uri_scheme] backends
    in a GIO module. There is no reason for applications to use it
    directly. Applications should use
    [func@Gio.AppInfo.get_default_for_uri_scheme].
    """
    ...

@staticmethod
def file_descriptor_based_get_fd(
    fd_based: Gio.FileDescriptorBased,
) -> int:
    """
    Gets the underlying file descriptor.
    """
    ...

@staticmethod
def is_mount_path_system_internal(
    mount_path: str,
) -> bool:
    """
    Determines if @mount_path is considered an implementation of the
    OS.

    This is primarily used for hiding mountable and mounted volumes
    that only are used in the OS and has little to no relevance to the
    casual user.
    """
    ...

@staticmethod
def is_system_device_path(
    device_path: str,
) -> bool:
    """
    Determines if @device_path is considered a block device path which is only
    used in implementation of the OS.

    This is primarily used for hiding mounted volumes that are intended as APIs
    for programs to read, and system administrators at a shell; rather than
    something that should, for example, appear in a GUI. For example, the Linux
    `/proc` filesystem.

    The list of device paths considered ‘system’ ones may change over time.
    """
    ...

@staticmethod
def is_system_fs_type(
    fs_type: str,
) -> bool:
    """
    Determines if @fs_type is considered a type of file system which is only
    used in implementation of the OS.

    This is primarily used for hiding mounted volumes that are intended as APIs
    for programs to read, and system administrators at a shell; rather than
    something that should, for example, appear in a GUI. For example, the Linux
    `/proc` filesystem.

    The list of file system types considered ‘system’ ones may change over time.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_at(
    mount_path: str,
) -> tuple[MountEntry | None, int | None]:
    """
    Gets a [struct@GioUnix.MountEntry] for a given mount path.

    If @time_read is set, it will be filled with a Unix timestamp for checking
    if the mounts have changed since with
    [func@GioUnix.mount_entries_changed_since].

    If more mounts have the same mount path, the last matching mount
    is returned.

    This will return `NULL` if there is no mount point at @mount_path.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_compare(
    mount1: MountEntry,
    mount2: MountEntry,
) -> int:
    """
    Compares two Unix mounts.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_copy(
    mount_entry: MountEntry,
) -> MountEntry:
    """
    Makes a copy of @mount_entry.
    """
    ...

@staticmethod
def mount_entries_changed_since(
    time: int,
) -> bool:
    """
    Checks if the Unix mounts have changed since a given Unix time.

    This can only work reliably if a [class@GioUnix.MountMonitor] is running in
    the process, otherwise changes in the mount entries file (such as
    `/proc/self/mountinfo` on Linux) cannot be detected and, as a result, this
    function has to conservatively always return `TRUE`.

    It is more efficient to use [signal@GioUnix.MountMonitor::mounts-changed] to
    be signalled of changes to the mount entries, rather than polling using this
    function. This function is more appropriate for infrequently determining
    cache validity.
    """
    ...

@staticmethod
def mount_entries_get() -> tuple[list, int | None]:
    """
    Gets a list of [struct@GioUnix.MountEntry] instances representing the Unix
    mounts.

    If @time_read is set, it will be filled with the mount timestamp, allowing
    for checking if the mounts have changed with
    [func@GioUnix.mount_entries_changed_since].
    """
    ...

@staticmethod
def mount_entries_get_from_file(
    table_path: str,
) -> tuple[list | None, int | None, int | None]:
    """
    Gets an array of [struct@Gio.UnixMountEntry]s containing the Unix mounts
    listed in @table_path.

    This is a generalized version of [func@GioUnix.mount_entries_get], mainly
    intended for internal testing use. Note that [func@GioUnix.mount_entries_get]
    may parse multiple hierarchical table files, so this function is not a direct
    superset of its functionality.

    If there is an error reading or parsing the file, `NULL` will be returned
    and both out parameters will be set to `0`.
    """
    ...

@staticmethod
def mount_entry_at(
    mount_path: str,
) -> tuple[MountEntry | None, int | None]:
    """
    Gets a [struct@GioUnix.MountEntry] for a given mount path.

    If @time_read is set, it will be filled with a Unix timestamp for checking
    if the mounts have changed since with
    [func@GioUnix.mount_entries_changed_since].

    If more mounts have the same mount path, the last matching mount
    is returned.

    This will return `NULL` if there is no mount point at @mount_path.
    """
    ...

@staticmethod
def mount_entry_compare(
    mount1: MountEntry,
    mount2: MountEntry,
) -> int:
    """
    Compares two Unix mounts.
    """
    ...

@staticmethod
def mount_entry_copy(
    mount_entry: MountEntry,
) -> MountEntry:
    """
    Makes a copy of @mount_entry.
    """
    ...

@staticmethod
def mount_entry_for(
    file_path: str,
) -> tuple[MountEntry | None, int | None]:
    """
    Gets a [struct@GioUnix.MountEntry] for a given file path.

    If @time_read is set, it will be filled with a Unix timestamp for checking
    if the mounts have changed since with
    [func@GioUnix.mount_entries_changed_since].

    If more mounts have the same mount path, the last matching mount
    is returned.

    This will return `NULL` if looking up the mount entry fails, if
    @file_path doesn’t exist or there is an I/O error.
    """
    ...

@staticmethod
def mount_entry_free(
    mount_entry: MountEntry,
) -> None:
    """
    Frees a Unix mount.
    """
    ...

@staticmethod
def mount_entry_get_device_path(
    mount_entry: MountEntry,
) -> str:
    """
    Gets the device path for a Unix mount.
    """
    ...

@staticmethod
def mount_entry_get_fs_type(
    mount_entry: MountEntry,
) -> str:
    """
    Gets the filesystem type for the Unix mount.
    """
    ...

@staticmethod
def mount_entry_get_mount_path(
    mount_entry: MountEntry,
) -> str:
    """
    Gets the mount path for a Unix mount.
    """
    ...

@staticmethod
def mount_entry_get_options(
    mount_entry: MountEntry,
) -> str | None:
    """
    Gets a comma separated list of mount options for the Unix mount.

    For example: `rw,relatime,seclabel,data=ordered`.

    This is similar to [func@GioUnix.MountPoint.get_options], but it takes
    a [struct@GioUnix.MountEntry] as an argument.
    """
    ...

@staticmethod
def mount_entry_get_root_path(
    mount_entry: MountEntry,
) -> str | None:
    """
    Gets the root of the mount within the filesystem. This is useful e.g. for
    mounts created by bind operation, or btrfs subvolumes.

    For example, the root path is equal to `/` for a mount created by
    `mount /dev/sda1 /mnt/foo` and `/bar` for
    `mount --bind /mnt/foo/bar /mnt/bar`.
    """
    ...

@staticmethod
def mount_entry_guess_can_eject(
    mount_entry: MountEntry,
) -> bool:
    """
    Guesses whether a Unix mount entry can be ejected.
    """
    ...

@staticmethod
def mount_entry_guess_icon(
    mount_entry: MountEntry,
) -> Gio.Icon:
    """
    Guesses the icon of a Unix mount entry.
    """
    ...

@staticmethod
def mount_entry_guess_name(
    mount_entry: MountEntry,
) -> str:
    """
    Guesses the name of a Unix mount entry.

    The result is a translated string.
    """
    ...

@staticmethod
def mount_entry_guess_should_display(
    mount_entry: MountEntry,
) -> bool:
    """
    Guesses whether a Unix mount entry should be displayed in the UI.
    """
    ...

@staticmethod
def mount_entry_guess_symbolic_icon(
    mount_entry: MountEntry,
) -> Gio.Icon:
    """
    Guesses the symbolic icon of a Unix mount entry.
    """
    ...

@staticmethod
def mount_entry_is_readonly(
    mount_entry: MountEntry,
) -> bool:
    """
    Checks if a Unix mount is mounted read only.
    """
    ...

@staticmethod
def mount_entry_is_system_internal(
    mount_entry: MountEntry,
) -> bool:
    """
    Checks if a Unix mount is a system mount.

    This is the Boolean OR of
    [func@GioUnix.is_system_fs_type], [func@GioUnix.is_system_device_path] and
    [func@GioUnix.is_mount_path_system_internal] on @mount_entry’s properties.

    The definition of what a ‘system’ mount entry is may change over time as new
    file system types and device paths are ignored.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_for(
    file_path: str,
) -> tuple[MountEntry | None, int | None]:
    """
    Gets a [struct@GioUnix.MountEntry] for a given file path.

    If @time_read is set, it will be filled with a Unix timestamp for checking
    if the mounts have changed since with
    [func@GioUnix.mount_entries_changed_since].

    If more mounts have the same mount path, the last matching mount
    is returned.

    This will return `NULL` if looking up the mount entry fails, if
    @file_path doesn’t exist or there is an I/O error.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_free(
    mount_entry: MountEntry,
) -> None:
    """
    Frees a Unix mount.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_get_device_path(
    mount_entry: MountEntry,
) -> str:
    """
    Gets the device path for a Unix mount.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_get_fs_type(
    mount_entry: MountEntry,
) -> str:
    """
    Gets the filesystem type for the Unix mount.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_get_mount_path(
    mount_entry: MountEntry,
) -> str:
    """
    Gets the mount path for a Unix mount.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_get_options(
    mount_entry: MountEntry,
) -> str | None:
    """
    Gets a comma separated list of mount options for the Unix mount.

    For example: `rw,relatime,seclabel,data=ordered`.

    This is similar to [func@GioUnix.MountPoint.get_options], but it takes
    a [struct@GioUnix.MountEntry] as an argument.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_get_root_path(
    mount_entry: MountEntry,
) -> str | None:
    """
    Gets the root of the mount within the filesystem. This is useful e.g. for
    mounts created by bind operation, or btrfs subvolumes.

    For example, the root path is equal to `/` for a mount created by
    `mount /dev/sda1 /mnt/foo` and `/bar` for
    `mount --bind /mnt/foo/bar /mnt/bar`.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_guess_can_eject(
    mount_entry: MountEntry,
) -> bool:
    """
    Guesses whether a Unix mount entry can be ejected.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_guess_icon(
    mount_entry: MountEntry,
) -> Gio.Icon:
    """
    Guesses the icon of a Unix mount entry.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_guess_name(
    mount_entry: MountEntry,
) -> str:
    """
    Guesses the name of a Unix mount entry.

    The result is a translated string.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_guess_should_display(
    mount_entry: MountEntry,
) -> bool:
    """
    Guesses whether a Unix mount entry should be displayed in the UI.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_guess_symbolic_icon(
    mount_entry: MountEntry,
) -> Gio.Icon:
    """
    Guesses the symbolic icon of a Unix mount entry.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_is_readonly(
    mount_entry: MountEntry,
) -> bool:
    """
    Checks if a Unix mount is mounted read only.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mount_is_system_internal(
    mount_entry: MountEntry,
) -> bool:
    """
    Checks if a Unix mount is a system mount.

    This is the Boolean OR of
    [func@GioUnix.is_system_fs_type], [func@GioUnix.is_system_device_path] and
    [func@GioUnix.is_mount_path_system_internal] on @mount_entry’s properties.

    The definition of what a ‘system’ mount entry is may change over time as new
    file system types and device paths are ignored.
    """
    ...

@staticmethod
def mount_point_at(
    mount_path: str,
) -> tuple[MountPoint | None, int | None]:
    """
    Gets a [struct@GioUnix.MountPoint] for a given mount path.

    If @time_read is set, it will be filled with a Unix timestamp for checking if
    the mount points have changed since with
    [func@GioUnix.mount_points_changed_since].

    If more mount points have the same mount path, the last matching mount point
    is returned.
    """
    ...

@staticmethod
def mount_point_compare(
    mount1: MountPoint,
    mount2: MountPoint,
) -> int:
    """
    Compares two Unix mount points.
    """
    ...

@staticmethod
def mount_point_copy(
    mount_point: MountPoint,
) -> MountPoint:
    """
    Makes a copy of @mount_point.
    """
    ...

@staticmethod
def mount_point_free(
    mount_point: MountPoint,
) -> None:
    """
    Frees a Unix mount point.
    """
    ...

@staticmethod
def mount_point_get_device_path(
    mount_point: MountPoint,
) -> str:
    """
    Gets the device path for a Unix mount point.
    """
    ...

@staticmethod
def mount_point_get_fs_type(
    mount_point: MountPoint,
) -> str:
    """
    Gets the file system type for the mount point.
    """
    ...

@staticmethod
def mount_point_get_mount_path(
    mount_point: MountPoint,
) -> str:
    """
    Gets the mount path for a Unix mount point.
    """
    ...

@staticmethod
def mount_point_get_options(
    mount_point: MountPoint,
) -> str | None:
    """
    Gets the options for the mount point.
    """
    ...

@staticmethod
def mount_point_guess_can_eject(
    mount_point: MountPoint,
) -> bool:
    """
    Guesses whether a Unix mount point can be ejected.
    """
    ...

@staticmethod
def mount_point_guess_icon(
    mount_point: MountPoint,
) -> Gio.Icon:
    """
    Guesses the icon of a Unix mount point.
    """
    ...

@staticmethod
def mount_point_guess_name(
    mount_point: MountPoint,
) -> str:
    """
    Guesses the name of a Unix mount point.

    The result is a translated string.
    """
    ...

@staticmethod
def mount_point_guess_symbolic_icon(
    mount_point: MountPoint,
) -> Gio.Icon:
    """
    Guesses the symbolic icon of a Unix mount point.
    """
    ...

@staticmethod
def mount_point_is_loopback(
    mount_point: MountPoint,
) -> bool:
    """
    Checks if a Unix mount point is a loopback device.
    """
    ...

@staticmethod
def mount_point_is_readonly(
    mount_point: MountPoint,
) -> bool:
    """
    Checks if a Unix mount point is read only.
    """
    ...

@staticmethod
def mount_point_is_user_mountable(
    mount_point: MountPoint,
) -> bool:
    """
    Checks if a Unix mount point is mountable by the user.
    """
    ...

@staticmethod
def mount_points_changed_since(
    time: int,
) -> bool:
    """
    Checks if the Unix mount points have changed since a given Unix time.

    Unlike [func@GioUnix.mount_entries_changed_since], this function can work
    reliably without a [class@GioUnix.MountMonitor] running, as it accesses the
    static mount point information (such as `/etc/fstab` on Linux), which has a
    valid modification time.

    It is more efficient to use [signal@GioUnix.MountMonitor::mountpoints-changed]
    to be signalled of changes to the mount points, rather than polling using
    this function. This function is more appropriate for infrequently determining
    cache validity.
    """
    ...

@staticmethod
def mount_points_get() -> tuple[list, int | None]:
    """
    Gets a list of [struct@GioUnix.MountPoint] instances representing the Unix
    mount points.

    If @time_read is set, it will be filled with the mount timestamp, allowing
    for checking if the mounts have changed with
    [func@GioUnix.mount_points_changed_since].
    """
    ...

@staticmethod
def mount_points_get_from_file(
    table_path: str,
) -> tuple[list | None, int | None, int | None]:
    """
    Gets an array of [struct@Gio.UnixMountPoint]s containing the Unix mount
    points listed in @table_path.

    This is a generalized version of [func@GioUnix.mount_points_get], mainly
    intended for internal testing use. Note that [func@GioUnix.mount_points_get]
    may parse multiple hierarchical table files, so this function is not a direct
    superset of its functionality.

    If there is an error reading or parsing the file, `NULL` will be returned
    and both out parameters will be set to `0`.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mounts_changed_since(
    time: int,
) -> bool:
    """
    Checks if the Unix mounts have changed since a given Unix time.
    """
    ...

@deprecated("deprecated")
@staticmethod
def mounts_get() -> tuple[list, int | None]:
    """
    Gets a list of [struct@GioUnix.MountEntry] instances representing the Unix
    mounts.

    If @time_read is set, it will be filled with the mount timestamp, allowing
    for checking if the mounts have changed with
    [func@GioUnix.mount_entries_changed_since].
    """
    ...

@deprecated("deprecated")
@staticmethod
def mounts_get_from_file(
    table_path: str,
) -> tuple[list | None, int | None, int | None]:
    """
    Gets an array of [struct@Gio.UnixMountEntry]s containing the Unix mounts
    listed in @table_path.

    This is a generalized version of [func@GioUnix.mount_entries_get], mainly
    intended for internal testing use. Note that [func@GioUnix.mount_entries_get]
    may parse multiple hierarchical table files, so this function is not a direct
    superset of its functionality.

    If there is an error reading or parsing the file, `NULL` will be returned
    and both out parameters will be set to `0`.
    """
    ...

###############################################################
# classes
###############################################################

class DesktopAppInfoClass(GObject.GPointer):
    # gi Fields
    @builtins.property
    def parent_class(self) -> GObject.ObjectClass | None: ...

    # gi Methods
    def __init__(self) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """

class DesktopAppInfoLookupIface(GObject.GPointer):
    """
    Interface that is used by backends to associate default
    handlers with URI schemes.
    """

    # gi Fields
    @builtins.property
    def g_iface(self) -> GObject.TypeInterface | None: ...

    # gi Methods
    def __init__(self) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """

class FDMessage(Gio.SocketControlMessage):
    """
    This [class@Gio.SocketControlMessage] contains a [class@Gio.UnixFDList].
    It may be sent using [method@Gio.Socket.send_message] and received using
    [method@Gio.Socket.receive_message] over UNIX sockets (ie: sockets in the
    `G_SOCKET_FAMILY_UNIX` family). The file descriptors are copied
    between processes by the kernel.

    For an easier way to send and receive file descriptors over
    stream-oriented UNIX sockets, see [method@Gio.UnixConnection.send_fd] and
    [method@Gio.UnixConnection.receive_fd].

    Note that `<gio/gunixfdmessage.h>` belongs to the UNIX-specific GIO
    interfaces, thus you have to use the `gio-unix-2.0.pc` pkg-config
    file or the `GioUnix-2.0` GIR namespace when using it.
    """

    class Props(Gio.SocketControlMessage.Props):
        fd_list: Gio.UnixFDList | None  # [fd-list]: changed because contained invalid characters
        """
        The [class@Gio.UnixFDList] object to send with the message.
        """

    @builtins.property
    def props(self) -> Props: ...

    # gi Methods
    def __init__(self, fd_list: Gio.UnixFDList | None = ...) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """
    @staticmethod
    def append_fd(message: FDMessage, fd: int) -> bool:
        """
            Adds a file descriptor to @message.

        The file descriptor is duplicated using dup(). You keep your copy
        of the descriptor and the copy contained in @message will be closed
        when @message is finalized.

        A possible cause of failure is exceeding the per-process or
        system-wide file descriptor limit.
        """
    @staticmethod
    def get_fd_list(message: FDMessage) -> Gio.UnixFDList:
        """
            Gets the #GUnixFDList contained in @message.  This function does not
        return a reference to the caller, but the returned list is valid for
        the lifetime of @message.
        """
    @classmethod
    def new(cls) -> Gio.SocketControlMessage:
        """
            Creates a new #GUnixFDMessage containing an empty file descriptor
        list.
        """
    @classmethod
    def new_with_fd_list(cls, fd_list: Gio.UnixFDList) -> Gio.SocketControlMessage:
        """
        Creates a new #GUnixFDMessage containing @list.
        """
    @staticmethod
    def steal_fds(message: FDMessage) -> tuple[list, int | None]:
        """
            Returns the array of file descriptors that is contained in this
        object.

        After this call, the descriptors are no longer contained in
        @message. Further calls will return an empty list (unless more
        descriptors have been added).

        The return result of this function must be freed with g_free().
        The caller is also responsible for closing all of the file
        descriptors.

        If @length is non-%NULL then it is set to the number of file
        descriptors in the returned array. The returned array is also
        terminated with -1.

        This function never returns %NULL. In case there are no file
        descriptors contained in @message, an empty array is returned.
        """

    # Signals
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::fd_list"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(  # type: ignore otherwise pylance will complain and we should repeat all parent overloads here..
        self, detailed_signal: str, handler: typing.Callable[..., typing.Any], *args: typing.Any
    ) -> int: ...

class FDMessageClass(GObject.GPointer):
    # gi Fields
    @builtins.property
    def parent_class(self) -> Gio.SocketControlMessageClass | None: ...

    # gi Methods
    def __init__(self) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """

class FDMessagePrivate(GObject.GPointer):
    # gi Methods
    def __init__(self) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """

class FileDescriptorBasedIface(GObject.GPointer):
    """
    An interface for file descriptor based io objects.
    """

    # gi Fields
    @builtins.property
    def g_iface(self) -> GObject.TypeInterface | None:
        """
        The parent interface.
        """
    @builtins.property
    def get_fd(self) -> get_fdFileDescriptorBasedIfaceCB:
        """
        Gets the underlying file descriptor.
        """

    # gi Methods
    def __init__(self) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """

class InputStream(GObject.Object):
    """
    `GUnixInputStream` implements [class@Gio.InputStream] for reading from a UNIX
    file descriptor, including asynchronous operations. (If the file
    descriptor refers to a socket or pipe, this will use `poll()` to do
    asynchronous I/O. If it refers to a regular file, it will fall back
    to doing asynchronous I/O in another thread.)

    Note that `<gio/gunixinputstream.h>` belongs to the UNIX-specific GIO
    interfaces, thus you have to use the `gio-unix-2.0.pc` pkg-config
    file or the `GioUnix-2.0` GIR namespace when using it.
    """

    class Props(GObject.Object.Props):
        close_fd: bool  # [close-fd]: changed because contained invalid characters
        """
        Whether to close the file descriptor when the stream is closed.
        """
        fd: int
        """
        The file descriptor that the stream reads from.
        """

    @builtins.property
    def props(self) -> Props: ...

    # gi Methods
    def __init__(self, close_fd: bool = ..., fd: int = ...) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """
    @staticmethod
    def get_close_fd(stream: InputStream) -> bool:
        """
            Returns whether the file descriptor of @stream will be
        closed when the stream is closed.
        """
    @staticmethod
    def get_fd(stream: InputStream) -> int:
        """
        Return the UNIX file descriptor that the stream reads from.
        """
    @classmethod
    def new(cls, fd: int, close_fd: bool) -> Gio.InputStream:
        """
            Creates a new #GUnixInputStream for the given @fd.

        If @close_fd is %TRUE, the file descriptor will be closed
        when the stream is closed.
        """
    @staticmethod
    def set_close_fd(stream: InputStream, close_fd: bool) -> None:
        """
            Sets whether the file descriptor of @stream shall be closed
        when the stream is closed.
        """

    # Signals
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::close_fd"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::fd"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(  # type: ignore otherwise pylance will complain and we should repeat all parent overloads here..
        self, detailed_signal: str, handler: typing.Callable[..., typing.Any], *args: typing.Any
    ) -> int: ...

class InputStreamClass(GObject.GPointer):
    # gi Fields
    @builtins.property
    def parent_class(self) -> Gio.InputStreamClass | None: ...

    # gi Methods
    def __init__(self) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """

class InputStreamPrivate(GObject.GPointer):
    # gi Methods
    def __init__(self) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """

class MountEntry(GObject.GBoxed):
    """
    Defines a Unix mount entry (e.g. `/media/cdrom`).
    This corresponds roughly to a mtab entry.
    """

    # gi Methods
    def __init__(self) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """
    @staticmethod
    def at(mount_path: str) -> tuple[MountEntry | None, int | None]:
        """
            Gets a [struct@GioUnix.MountEntry] for a given mount path.

        If @time_read is set, it will be filled with a Unix timestamp for checking
        if the mounts have changed since with
        [func@GioUnix.mount_entries_changed_since].

        If more mounts have the same mount path, the last matching mount
        is returned.

        This will return `NULL` if there is no mount point at @mount_path.
        """
    @staticmethod
    def compare(mount1: MountEntry, mount2: MountEntry) -> int:
        """
        Compares two Unix mounts.
        """
    @staticmethod
    def copy(mount_entry: MountEntry) -> MountEntry:
        """
        Makes a copy of @mount_entry.
        """
    @staticmethod
    def for_(file_path: str) -> tuple[MountEntry | None, int | None]: ...
    @staticmethod
    def free(mount_entry: MountEntry) -> None:
        """
        Frees a Unix mount.
        """
    @staticmethod
    def get_device_path(mount_entry: MountEntry) -> str:
        """
        Gets the device path for a Unix mount.
        """
    @staticmethod
    def get_fs_type(mount_entry: MountEntry) -> str:
        """
        Gets the filesystem type for the Unix mount.
        """
    @staticmethod
    def get_mount_path(mount_entry: MountEntry) -> str:
        """
        Gets the mount path for a Unix mount.
        """
    @staticmethod
    def get_options(mount_entry: MountEntry) -> str | None:
        """
            Gets a comma separated list of mount options for the Unix mount.

        For example: `rw,relatime,seclabel,data=ordered`.

        This is similar to [func@GioUnix.MountPoint.get_options], but it takes
        a [struct@GioUnix.MountEntry] as an argument.
        """
    @staticmethod
    def get_root_path(mount_entry: MountEntry) -> str | None:
        """
            Gets the root of the mount within the filesystem. This is useful e.g. for
        mounts created by bind operation, or btrfs subvolumes.

        For example, the root path is equal to `/` for a mount created by
        `mount /dev/sda1 /mnt/foo` and `/bar` for
        `mount --bind /mnt/foo/bar /mnt/bar`.
        """
    @staticmethod
    def guess_can_eject(mount_entry: MountEntry) -> bool:
        """
        Guesses whether a Unix mount entry can be ejected.
        """
    @staticmethod
    def guess_icon(mount_entry: MountEntry) -> Gio.Icon:
        """
        Guesses the icon of a Unix mount entry.
        """
    @staticmethod
    def guess_name(mount_entry: MountEntry) -> str:
        """
            Guesses the name of a Unix mount entry.

        The result is a translated string.
        """
    @staticmethod
    def guess_should_display(mount_entry: MountEntry) -> bool:
        """
        Guesses whether a Unix mount entry should be displayed in the UI.
        """
    @staticmethod
    def guess_symbolic_icon(mount_entry: MountEntry) -> Gio.Icon:
        """
        Guesses the symbolic icon of a Unix mount entry.
        """
    @staticmethod
    def is_readonly(mount_entry: MountEntry) -> bool:
        """
        Checks if a Unix mount is mounted read only.
        """
    @staticmethod
    def is_system_internal(mount_entry: MountEntry) -> bool:
        """
            Checks if a Unix mount is a system mount.

        This is the Boolean OR of
        [func@GioUnix.is_system_fs_type], [func@GioUnix.is_system_device_path] and
        [func@GioUnix.is_mount_path_system_internal] on @mount_entry’s properties.

        The definition of what a ‘system’ mount entry is may change over time as new
        file system types and device paths are ignored.
        """

class MountMonitor(GObject.Object):
    """
    Watches for changes to the set of mount entries and mount points in the
    system.

    Connect to the [signal@GioUnix.MountMonitor::mounts-changed] signal to be
    notified of changes to the [struct@GioUnix.MountEntry] list.

    Connect to the [signal@GioUnix.MountMonitor::mountpoints-changed] signal to
    be notified of changes to the [struct@GioUnix.MountPoint] list.
    """

    # gi Methods
    def __init__(self) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """
    @staticmethod
    def get() -> MountMonitor:
        """
            Gets the [class@GioUnix.MountMonitor] for the current thread-default main
        context.

        The mount monitor can be used to monitor for changes to the list of
        mounted filesystems as well as the list of mount points (ie: fstab
        entries).

        You must only call [method@GObject.Object.unref] on the return value from
        under the same main context as you called this function.
        """
    @deprecated("deprecated")
    @classmethod
    def new(cls) -> MountMonitor:
        """
            Deprecated alias for [func@GioUnix.MountMonitor.get].

        This function was never a true constructor, which is why it was
        renamed.
        """
    @deprecated("deprecated")
    @staticmethod
    def set_rate_limit(mount_monitor: MountMonitor, limit_msec: int) -> None:
        """
            This function does nothing.

        Before 2.44, this was a partially-effective way of controlling the
        rate at which events would be reported under some uncommon
        circumstances.  Since @mount_monitor is a singleton, it also meant
        that calling this function would have side effects for other users of
        the monitor.
        """

    # Signals
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["mountpoints-changed"],
        handler: typing.Callable[..., None],
        *args: typing.Any,
    ) -> int:
        """
        Emitted when the Unix mount points have changed.
        """
    @typing.overload
    def connect(
        self, detailed_signal: typing.Literal["mounts-changed"], handler: typing.Callable[..., None], *args: typing.Any
    ) -> int:
        """
        Emitted when the Unix mount entries have changed.
        """
    @typing.overload
    def connect(  # type: ignore otherwise pylance will complain and we should repeat all parent overloads here..
        self, detailed_signal: str, handler: typing.Callable[..., typing.Any], *args: typing.Any
    ) -> int: ...

class MountMonitorClass(GObject.GPointer):
    # gi Methods
    def __init__(self) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """

class MountPoint(GObject.GBoxed):
    """
    Defines a Unix mount point (e.g. `/dev`).
    This corresponds roughly to a fstab entry.
    """

    # gi Methods
    def __init__(self) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """
    @staticmethod
    def at(mount_path: str) -> tuple[MountPoint | None, int | None]:
        """
            Gets a [struct@GioUnix.MountPoint] for a given mount path.

        If @time_read is set, it will be filled with a Unix timestamp for checking if
        the mount points have changed since with
        [func@GioUnix.mount_points_changed_since].

        If more mount points have the same mount path, the last matching mount point
        is returned.
        """
    @staticmethod
    def compare(mount1: MountPoint, mount2: MountPoint) -> int:
        """
        Compares two Unix mount points.
        """
    @staticmethod
    def copy(mount_point: MountPoint) -> MountPoint:
        """
        Makes a copy of @mount_point.
        """
    @staticmethod
    def free(mount_point: MountPoint) -> None:
        """
        Frees a Unix mount point.
        """
    @staticmethod
    def get_device_path(mount_point: MountPoint) -> str:
        """
        Gets the device path for a Unix mount point.
        """
    @staticmethod
    def get_fs_type(mount_point: MountPoint) -> str:
        """
        Gets the file system type for the mount point.
        """
    @staticmethod
    def get_mount_path(mount_point: MountPoint) -> str:
        """
        Gets the mount path for a Unix mount point.
        """
    @staticmethod
    def get_options(mount_point: MountPoint) -> str | None:
        """
        Gets the options for the mount point.
        """
    @staticmethod
    def guess_can_eject(mount_point: MountPoint) -> bool:
        """
        Guesses whether a Unix mount point can be ejected.
        """
    @staticmethod
    def guess_icon(mount_point: MountPoint) -> Gio.Icon:
        """
        Guesses the icon of a Unix mount point.
        """
    @staticmethod
    def guess_name(mount_point: MountPoint) -> str:
        """
            Guesses the name of a Unix mount point.

        The result is a translated string.
        """
    @staticmethod
    def guess_symbolic_icon(mount_point: MountPoint) -> Gio.Icon:
        """
        Guesses the symbolic icon of a Unix mount point.
        """
    @staticmethod
    def is_loopback(mount_point: MountPoint) -> bool:
        """
        Checks if a Unix mount point is a loopback device.
        """
    @staticmethod
    def is_readonly(mount_point: MountPoint) -> bool:
        """
        Checks if a Unix mount point is read only.
        """
    @staticmethod
    def is_user_mountable(mount_point: MountPoint) -> bool:
        """
        Checks if a Unix mount point is mountable by the user.
        """

class OutputStream(GObject.Object):
    """
    `GUnixOutputStream` implements [class@Gio.OutputStream] for writing to a UNIX
    file descriptor, including asynchronous operations. (If the file
    descriptor refers to a socket or pipe, this will use `poll()` to do
    asynchronous I/O. If it refers to a regular file, it will fall back
    to doing asynchronous I/O in another thread.)

    Note that `<gio/gunixoutputstream.h>` belongs to the UNIX-specific GIO
    interfaces, thus you have to use the `gio-unix-2.0.pc` pkg-config file
    file or the `GioUnix-2.0` GIR namespace when using it.
    """

    class Props(GObject.Object.Props):
        close_fd: bool  # [close-fd]: changed because contained invalid characters
        """
        Whether to close the file descriptor when the stream is closed.
        """
        fd: int
        """
        The file descriptor that the stream writes to.
        """

    @builtins.property
    def props(self) -> Props: ...

    # gi Methods
    def __init__(self, close_fd: bool = ..., fd: int = ...) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """
    @staticmethod
    def get_close_fd(stream: OutputStream) -> bool:
        """
            Returns whether the file descriptor of @stream will be
        closed when the stream is closed.
        """
    @staticmethod
    def get_fd(stream: OutputStream) -> int:
        """
        Return the UNIX file descriptor that the stream writes to.
        """
    @classmethod
    def new(cls, fd: int, close_fd: bool) -> Gio.OutputStream:
        """
            Creates a new #GUnixOutputStream for the given @fd.

        If @close_fd, is %TRUE, the file descriptor will be closed when
        the output stream is destroyed.
        """
    @staticmethod
    def set_close_fd(stream: OutputStream, close_fd: bool) -> None:
        """
            Sets whether the file descriptor of @stream shall be closed
        when the stream is closed.
        """

    # Signals
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::close_fd"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::fd"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(  # type: ignore otherwise pylance will complain and we should repeat all parent overloads here..
        self, detailed_signal: str, handler: typing.Callable[..., typing.Any], *args: typing.Any
    ) -> int: ...

class OutputStreamClass(GObject.GPointer):
    # gi Fields
    @builtins.property
    def parent_class(self) -> Gio.OutputStreamClass | None: ...

    # gi Methods
    def __init__(self) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """

class OutputStreamPrivate(GObject.GPointer):
    # gi Methods
    def __init__(self) -> None:
        """
        Generated __init__ stub method. order not guaranteed.
        """

###############################################################
# Callbacks
###############################################################

class get_fdFileDescriptorBasedIfaceCB(typing.Protocol):
    """
    This callback was used in:
        FileDescriptorBasedIface.get_fd
    """
    #  fd_based
    def __call__(
        self,
        fd_based: Gio.FileDescriptorBased,
    ) -> int: ...

###############################################################
# Aliases
###############################################################

DesktopAppInfo = Gio.DesktopAppInfo
DesktopAppInfoLookup = Gio.DesktopAppInfoLookup
FileDescriptorBased = Gio.FileDescriptorBased
_lock = _thread._lock  # type: ignore
###############################################################
# Constants
###############################################################

DESKTOP_APP_INFO_LOOKUP_EXTENSION_POINT_NAME: str = ...
_namespace: str = ...
_version: str = ...
###############################################################
# Unknowns/Not Parsed
###############################################################
