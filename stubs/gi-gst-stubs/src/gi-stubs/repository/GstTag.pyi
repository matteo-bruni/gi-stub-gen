# pyright: reportIncompatibleMethodOverride=false
"""
Stub template for a GI Repository Module.

Generated by gi-stub-gen, version 0.0.1.
Module: GstTag
Date: 2026-07-14
"""
# we disable method override report for pyright in generated stubs,
# causes too many false positives

from __future__ import annotations
from typing_extensions import deprecated  # noqa: F401
import typing_extensions  # noqa: F401

import builtins
import typing

# gi.repository imports needed by this Stub
from gi.repository import GObject
from gi.repository import Gst
###############################################################
# Functions
###############################################################

def tag_check_language_code(
    lang_code: str,
) -> bool:
    """
    Check if a given string contains a known ISO 639 language code.

    This is useful in situations where it's not clear whether a given
    string is a language code (which should be put into a GST_TAG_LANGUAGE_CODE
    tag) or a free-form language name descriptor (which should be put into a
    GST_TAG_LANGUAGE_NAME tag instead).
    """
    ...

def tag_freeform_string_to_utf8(
    data: list[int],
    env_vars: list[str],
) -> str | None:
    """
    Convenience function to read a string with unknown character encoding. If
    the string is already in UTF-8 encoding, it will be returned right away.
    If not it tries to detect byte-order-mark for UTF-16/32 cases and use that.
    Otherwise, the environment will be searched for a number of environment
    variables (whose names are specified in the None-terminated string array
    `env_vars`) containing a list of character encodings to try/use. If none
    are specified, the current locale will be tried. If that also doesn't work,
    WINDOWS-1252/ISO-8859-1 is assumed (which will almost always succeed).
    """
    ...

def tag_from_id3_tag(
    id3_tag: str,
) -> str | None:
    """
    Looks up the GStreamer tag for a ID3v2 tag.
    """
    ...

def tag_from_id3_user_tag(
    type: str,
    id3_user_tag: str,
) -> str | None:
    """
    Looks up the GStreamer tag for an ID3v2 user tag (e.g. description in
    TXXX frame or owner in UFID frame).
    """
    ...

def tag_from_vorbis_tag(
    vorbis_tag: str,
) -> str | None:
    """
    Looks up the GStreamer tag for a vorbiscomment tag.
    """
    ...

def tag_get_id3v2_tag_size(
    buffer: Gst.Buffer,
) -> int:
    """
    Determines size of an ID3v2 tag on buffer containing at least ID3v2 header,
    i.e. at least GST_TAG_ID3V2_HEADER_SIZE (10) bytes;
    """
    ...

def tag_get_language_code_iso_639_1(
    lang_code: str,
) -> str | None:
    """
    Returns two-letter ISO-639-1 language code given a three-letter ISO-639-2
    language code or two-letter ISO-639-1 language code (both are accepted for
    convenience).

    Language codes are case-sensitive and expected to be lower case.
    """
    ...

def tag_get_language_code_iso_639_2B(
    lang_code: str,
) -> str | None:
    """
    Returns three-letter ISO-639-2 "bibliographic" language code given a
    two-letter ISO-639-1 language code or a three-letter ISO-639-2 language
    code (both are accepted for convenience).

    The "bibliographic" code is derived from the English name of the language
    (e.g. "ger" for German instead of "de" or "deu"). In most scenarios, the
    "terminological" codes are preferred.

    Language codes are case-sensitive and expected to be lower case.
    """
    ...

def tag_get_language_code_iso_639_2T(
    lang_code: str,
) -> str | None:
    """
    Returns three-letter ISO-639-2 "terminological" language code given a
    two-letter ISO-639-1 language code or a three-letter ISO-639-2 language
    code (both are accepted for convenience).

    The "terminological" code is derived from the local name of the language
    (e.g. "deu" for German instead of "ger"). In most scenarios, the
    "terminological" codes are preferred over the "bibliographic" ones.

    Language codes are case-sensitive and expected to be lower case.
    """
    ...

def tag_get_language_codes() -> list[str]:
    """
    Returns a list of known language codes (in form of two-letter ISO-639-1
    codes). This is useful for UIs to build a list of available languages for
    tagging purposes (e.g. to tag an audio track appropriately in a video or
    audio editor).
    """
    ...

def tag_get_language_name(
    language_code: str,
) -> str | None:
    """
    Returns the name of the language given an ISO-639 language code as
    found in a GST_TAG_LANGUAGE_CODE tag. The name will be translated
    according to the current locale (if the library was built against the
    iso-codes package, otherwise the English name will be returned).

    Language codes are case-sensitive and expected to be lower case.
    """
    ...

def tag_get_license_description(
    license_ref: str,
) -> str | None:
    """
    Get the description of a license, which is a translated description
    of the license's main features.
    """
    ...

def tag_get_license_flags(
    license_ref: str,
) -> TagLicenseFlags:
    """
    Get the flags of a license, which describe most of the features of
    a license in their most general form.
    """
    ...

def tag_get_license_jurisdiction(
    license_ref: str,
) -> str | None:
    """
    Get the jurisdiction code of a license. This is usually a two-letter
    ISO 3166-1 alpha-2 code, but there is also the special case of Scotland,
    for which no code exists and which is thus represented as "scotland".

    Known jurisdictions: ar, at, au, be, bg, br, ca, ch, cl, cn, co, de,
    dk, es, fi, fr, hr, hu, il, in, it, jp, kr, mk, mt, mx, my, nl, pe, pl,
    pt, scotland, se, si, tw, uk, us, za.
    """
    ...

def tag_get_license_nick(
    license_ref: str,
) -> str | None:
    """
    Get the nick name of a license, which is a short (untranslated) string
    such as e.g. "CC BY-NC-ND 2.0 UK".
    """
    ...

def tag_get_license_title(
    license_ref: str,
) -> str | None:
    """
    Get the title of a license, which is a short translated description
    of the license's features (generally not very pretty though).
    """
    ...

def tag_get_license_version(
    license_ref: str,
) -> str | None:
    """
    Get the version of a license.
    """
    ...

def tag_get_licenses() -> list[str]:
    """
    Returns a list of known license references (in form of URIs). This is
    useful for UIs to build a list of available licenses for tagging purposes
    (e.g. to tag an audio track appropriately in a video or audio editor, or
    an image in a camera application).
    """
    ...

def tag_id3_genre_count() -> int:
    """
    Gets the number of ID3v1 genres that can be identified. Winamp genres are
    included.
    """
    ...

def tag_id3_genre_get(
    id: int,
) -> str | None:
    """
    Gets the ID3v1 genre name for a given ID.
    """
    ...

def tag_image_data_to_image_sample(
    image_data: bytes,
    image_type: TagImageType,
) -> Gst.Sample | None:
    """
    Helper function for tag-reading plugins to create a GstSample suitable to
    add to a GstTag.List as an image tag (such as GST_TAG_IMAGE or
    GST_TAG_PREVIEW_IMAGE) from the encoded image data and an (optional) image
    type.

    Background: cover art and other images in tags are usually stored as a
    blob of binary image data, often accompanied by a MIME type or some other
    content type string (e.g. 'png', 'jpeg', 'jpg'). Sometimes there is also an
    'image type' to indicate what kind of image this is (e.g. front cover,
    back cover, artist, etc.). The image data may also be an URI to the image
    rather than the image itself.

    In GStreamer, image tags are GstSample<!-- -->s containing the raw image
    data, with the sample caps describing the content type of the image
    (e.g. image/jpeg, image/png, text/uri-list). The sample info may contain
    an additional 'image-type' field of GstTag.ImageType to describe
    the type of image (front cover, back cover etc.). GST_TAG_PREVIEW_IMAGE
    tags should not carry an image type, their type is already indicated via
    the special tag name.

    This function will do various checks and typefind the encoded image
    data (we can't trust the declared mime type).
    """
    ...

def tag_list_add_id3_image(
    tag_list: Gst.TagList,
    image_data: bytes,
    id3_picture_type: int,
) -> bool:
    """
    Adds an image from an ID3 APIC frame (or similar, such as used in FLAC)
    to the given tag list. Also see `gst_tag_image_data_to_image_sample` for
    more information on image tags in GStreamer.
    """
    ...

def tag_list_from_exif_buffer(
    buffer: Gst.Buffer,
    byte_order: int,
    base_offset: int,
) -> Gst.TagList:
    """
    Parses the IFD and IFD tags data contained in the buffer and puts it
    on a taglist. The base_offset is used to subtract from the offset in
    the tag entries and be able to get the offset relative to the buffer
    start
    """
    ...

def tag_list_from_exif_buffer_with_tiff_header(
    buffer: Gst.Buffer,
) -> Gst.TagList:
    """
    Parses the exif tags starting with a tiff header structure.
    """
    ...

def tag_list_from_id3v2_tag(
    buffer: Gst.Buffer,
) -> Gst.TagList | None:
    """
    Creates a new tag list that contains the information parsed out of a
    ID3 tag.
    """
    ...

def tag_list_from_vorbiscomment(
    data: bytes,
    id_data: bytes,
) -> tuple[Gst.TagList | None, str]:
    """
    Creates a new tag list that contains the information parsed out of a
    vorbiscomment packet.
    """
    ...

def tag_list_from_vorbiscomment_buffer(
    buffer: Gst.Buffer,
    id_data: bytes,
) -> tuple[Gst.TagList | None, str]:
    """
    Creates a new tag list that contains the information parsed out of a
    vorbiscomment packet.
    """
    ...

def tag_list_from_xmp_buffer(
    buffer: Gst.Buffer,
) -> Gst.TagList | None:
    """
    Parse a xmp packet into a taglist.
    """
    ...

def tag_list_new_from_id3v1(
    data: bytes,
) -> Gst.TagList | None:
    """
    Parses the data containing an ID3v1 tag and returns a GstTag.List from the
    parsed data.
    """
    ...

def tag_list_to_exif_buffer(
    taglist: Gst.TagList,
    byte_order: int,
    base_offset: int,
) -> Gst.Buffer:
    """
    Formats the tags in taglist on exif format. The resulting buffer contains
    the tags IFD and is followed by the data pointed by the tag entries.
    """
    ...

def tag_list_to_exif_buffer_with_tiff_header(
    taglist: Gst.TagList,
) -> Gst.Buffer:
    """
    Formats the tags in taglist into exif structure, a tiff header
    is put in the beginning of the buffer.
    """
    ...

def tag_list_to_vorbiscomment_buffer(
    list: Gst.TagList,
    id_data: bytes,
    vendor_string: str | None = None,
) -> Gst.Buffer:
    """
    Creates a new vorbiscomment buffer from a tag list.
    """
    ...

def tag_list_to_xmp_buffer(
    list: Gst.TagList,
    read_only: bool,
    schemas: list[str],
) -> Gst.Buffer | None:
    """
    Formats a taglist as a xmp packet using only the selected
    schemas. An empty list (None) means that all schemas should
    be used
    """
    ...

def tag_parse_extended_comment(
    ext_comment: str,
    fail_if_no_key: bool,
) -> tuple[bool, str | None, str | None, str]:
    """
    Convenience function to parse a GST_TAG_EXTENDED_COMMENT string and
    separate it into its components.

    If successful, `key`, `lang` and/or `value` will be set to newly allocated
    strings that you need to free with `g_free` when done. `key` and `lang`
    may also be set to None by this function if there is no key or no language
    code in the extended comment string.
    """
    ...

def tag_register_musicbrainz_tags() -> None:
    """
    Registers additional musicbrainz-specific tags with the GStreamer tag
    system. Plugins and applications that use these tags should call this
    function before using them. Can be called multiple times.
    """
    ...

def tag_to_id3_tag(
    gst_tag: str,
) -> str | None:
    """
    Looks up the ID3v2 tag for a GStreamer tag.
    """
    ...

def tag_to_vorbis_comments(
    list: Gst.TagList,
    tag: str,
) -> list[str]:
    """
    Creates a new tag list that contains the information parsed out of a
    vorbiscomment packet.
    """
    ...

def tag_to_vorbis_tag(
    gst_tag: str,
) -> str | None:
    """
    Looks up the vorbiscomment tag for a GStreamer tag.
    """
    ...

def tag_xmp_list_schemas() -> list[str]:
    """
    Gets the list of supported schemas in the xmp lib
    """
    ...

def vorbis_tag_add(
    list: Gst.TagList,
    tag: str,
    value: str,
) -> None:
    """
    Convenience function using `gst_tag_from_vorbis_tag`, parsing
    a vorbis comment string into the right type and adding it to the
    given taglist `list`.

    Unknown vorbiscomment tags will be added to the tag list in form
    of a GST_TAG_EXTENDED_COMMENT.
    """
    ...

###############################################################
# Enums/Flags
###############################################################

class TagDemuxResult(GObject.GEnum):
    """
    Result values from the parse_tag virtual function."""

    BROKEN_TAG = 0
    """
    cannot parse tag, just skip it
    """
    AGAIN = 1
    """
    call again with less or more data
    """
    OK = 2
    """
    parsed tag successfully
    """

class TagImageType(GObject.GEnum):
    """
    Type of image contained in an image tag (specified as "image-type" field in
    the info structure in the image's GstSample)"""

    NONE = -1
    """
    No image type. Can be used to
         tell functions such as `gst_tag_image_data_to_image_sample` that no
         image type should be set.
    """
    UNDEFINED = 0
    """
    Undefined/other image type
    """
    FRONT_COVER = 1
    """
    Cover (front)
    """
    BACK_COVER = 2
    """
    Cover (back)
    """
    LEAFLET_PAGE = 3
    """
    Leaflet page
    """
    MEDIUM = 4
    """
    Medium (e.g. label side of CD)
    """
    LEAD_ARTIST = 5
    """
    Lead artist/lead performer/soloist
    """
    ARTIST = 6
    """
    Artist/performer
    """
    CONDUCTOR = 7
    """
    Conductor
    """
    BAND_ORCHESTRA = 8
    """
    Band/orchestra
    """
    COMPOSER = 9
    """
    Composer
    """
    LYRICIST = 10
    """
    Lyricist/text writer
    """
    RECORDING_LOCATION = 11
    """
    Recording location
    """
    DURING_RECORDING = 12
    """
    During recording
    """
    DURING_PERFORMANCE = 13
    """
    During performance
    """
    VIDEO_CAPTURE = 14
    """
    Movie/video screen capture
    """
    FISH = 15
    """
    A fish as funny as the ID3v2 spec
    """
    ILLUSTRATION = 16
    """
    Illustration
    """
    BAND_ARTIST_LOGO = 17
    """
    Band/artist logotype
    """
    PUBLISHER_STUDIO_LOGO = 18
    """
    Publisher/studio logotype
    """

class TagLicenseFlags(GObject.GFlags):
    """
    See http://creativecommons.org/ns for more information."""

    PERMITS_REPRODUCTION = 1
    """
    making multiple copies
        is allowed
    """
    PERMITS_DISTRIBUTION = 2
    """
    distribution, public display
        and public performance are allowed
    """
    PERMITS_DERIVATIVE_WORKS = 4
    """
    distribution of derivative
        works is allowed
    """
    PERMITS_SHARING = 8
    """
    commercial derivatives are allowed,
        but only non-commercial distribution is allowed
    """
    REQUIRES_NOTICE = 256
    """
    copyright and license notices
        must be kept intact
    """
    REQUIRES_ATTRIBUTION = 512
    """
    credit must be given to
        copyright holder and/or author
    """
    REQUIRES_SHARE_ALIKE = 1024
    """
    derivative works must be
        licensed under the same terms or compatible terms as the original work
    """
    REQUIRES_SOURCE_CODE = 2048
    """
    source code (the preferred
        form for making modifications) must be provided when exercising some
        rights granted by the license
    """
    REQUIRES_COPYLEFT = 4096
    """
    derivative and combined works
        must be licensed under specified terms, similar to those of the original
        work
    """
    REQUIRES_LESSER_COPYLEFT = 8192
    """
    derivative works must be
        licensed under specified terms, with at least the same conditions as
        the original work; combinations with the work may be licensed under
        different terms
    """
    PROHIBITS_COMMERCIAL_USE = 65536
    """
    exercising rights for
        commercial purposes is prohibited
    """
    PROHIBITS_HIGH_INCOME_NATION_USE = 131072
    """
    use in a
        non-developing country is prohibited
    """
    CREATIVE_COMMONS_LICENSE = 16777216
    """
    this license was created
        by the Creative Commons project
    """
    FREE_SOFTWARE_FOUNDATION_LICENSE = 33554432
    """
    this license was
        created by the Free Software Foundation (FSF)
    """

###############################################################
# classes
###############################################################

class TagDemux(Gst.Element):
    """
    Provides a base class for demuxing tags at the beginning or end of a
    stream and handles things like typefinding, querying, seeking, and
    different modes of operation (chain-based, pull_range-based, and providing
    downstream elements with random access if upstream supports that). The tag
    is stripped from the output, and all offsets are adjusted for the tag
    sizes, so that to the downstream element the stream will appear as if
    there was no tag at all. Also, once the tag has been parsed, GstTagDemux
    will try to determine the media type of the resulting stream and add a
    source pad with the appropriate caps in order to facilitate auto-plugging.

    ## Deriving from GstTagDemux

    Subclasses have to do four things:

     * In their base init function, they must add a pad template for the sink
       pad to the element class, describing the media type they can parse in
       the caps of the pad template.
     * In their class init function, they must override
       GST_TAG_DEMUX_CLASS(demux_klass)->identify_tag with their own identify
       function.
     * In their class init function, they must override
     GST_TAG_DEMUX_CLASS(demux_klass)->parse_tag with their own parse
     function.
     * In their class init function, they must also set
       GST_TAG_DEMUX_CLASS(demux_klass)->min_start_size and/or
     GST_TAG_DEMUX_CLASS(demux_klass)->min_end_size to the minimum size required
     for the identify function to decide whether the stream has a supported tag
     or not. A class parsing ID3v1 tags, for example, would set min_end_size to
     128 bytes.
    """

    # gi Fields
    @builtins.property
    def element(self) -> Gst.Element | None:
        """
        parent element
        """
    @builtins.property
    def priv(self) -> TagDemuxPrivate | None: ...
    @builtins.property
    def reserved(self) -> list[object] | None: ...

    # gi Methods
    def __init__(self, name: str | None = None, parent: Gst.Object | None = None) -> None:
        """
        Initialize TagDemux object with properties.
        """

    # python methods (overrides?)
    def do_identify_tag(
        self,
        buffer: Gst.Buffer,
        start_tag: bool,
        tag_size: int,
    ) -> bool:
        """
        identify_tag(self, buffer:Gst.Buffer, start_tag:bool, tag_size:int) -> bool
        """
    def do_merge_tags(
        self,
        start_tags: Gst.TagList,
        end_tags: Gst.TagList,
    ) -> Gst.TagList:
        """
        merge_tags(self, start_tags:Gst.TagList, end_tags:Gst.TagList) -> Gst.TagList
        """
    def do_parse_tag(
        self,
        buffer: Gst.Buffer,
        start_tag: bool,
        tag_size: int,
        tags: Gst.TagList,
    ) -> TagDemuxResult:
        """
        parse_tag(self, buffer:Gst.Buffer, start_tag:bool, tag_size:int, tags:Gst.TagList) -> GstTag.TagDemuxResult
        """

    # Signals
    @typing.overload
    def connect(
        self, detailed_signal: typing.Literal["no-more-pads"], handler: typing.Callable[..., None], *args: typing.Any
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["pad-added"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Pad], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["pad-removed"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Pad], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["deep-notify"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Object, GObject.ParamSpec], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::name"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::parent"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(  # type: ignore otherwise pylance will complain and we should repeat all parent overloads here..
        self, detailed_signal: str, handler: typing.Callable[..., typing.Any], *args: typing.Any
    ) -> int: ...

class TagDemuxClass(GObject.GPointer, metaclass=GObject.GType):
    """
    The GstTag.DemuxClass structure.  See documentation at beginning of section
    for details about what subclasses need to override and do.
    """

    # gi Fields
    @builtins.property
    def identify_tag(self) -> identify_tagTagDemuxClassCB:
        """
            identify tag and determine the size required to parse the
        tag. Buffer may be larger than the specified minimum size.
        Subclassed MUST override this vfunc in their class_init function.
        """
    @builtins.property
    def merge_tags(self) -> merge_tagsTagDemuxClassCB:
        """
            merge start and end tags. Subclasses may want to override this
        vfunc to allow prioritising of start or end tag according to user
        preference.  Note that both start_tags and end_tags may be None. By default
        start tags are preferred over end tags.
        """
    @builtins.property
    def min_end_size(self) -> int:
        """
            minimum size required to identify a tag at the end and
        determine its total size. Set to 0 if not interested in end tags.
        Subclasses should set this in their class_init function.
        """
    @builtins.property
    def min_start_size(self) -> int:
        """
            minimum size required to identify a tag at the start and
        determine its total size. Set to 0 if not interested in start tags.
        Subclasses should set this in their class_init function.
        """
    @builtins.property
    def parent_class(self) -> Gst.ElementClass | None:
        """
        the parent class.
        """
    @builtins.property
    def parse_tag(self) -> parse_tagTagDemuxClassCB:
        """
            parse the tag. Buffer will be exactly of the size determined by
        the identify_tag vfunc before. The parse_tag vfunc may change the size
        stored in *tag_size and return GST_TAG_DEMUX_RESULT_AGAIN to request a
        larger or smaller buffer. It is also permitted to adjust the tag_size to a
        smaller value and then return GST_TAG_DEMUX_RESULT_OK in one go.
        Subclassed MUST override the parse_tag vfunc in their class_init function.
        """
    @builtins.property
    def reserved(self) -> list[object] | None: ...

class TagDemuxPrivate(GObject.GPointer, metaclass=GObject.GType): ...

class TagMux(Gst.Element):
    """
    Provides a base class for adding tags at the beginning or end of a
    stream.

    ## Deriving from GstTagMux

    Subclasses have to do the following things:

     * In their base init function, they must add pad templates for the sink
       pad and the source pad to the element class, describing the media type
       they accept and output in the caps of the pad template.
     * In their class init function, they must override the
       GST_TAG_MUX_CLASS(mux_klass)->render_start_tag and/or
       GST_TAG_MUX_CLASS(mux_klass)->render_end_tag vfuncs and set up a render
       function.
    """

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list[object] | None: ...
    @builtins.property
    def element(self) -> Gst.Element | None:
        """
        parent element
        """
    @builtins.property
    def priv(self) -> TagMuxPrivate | None: ...

    # gi Methods
    def __init__(self, name: str | None = None, parent: Gst.Object | None = None) -> None:
        """
        Initialize TagMux object with properties.
        """

    # python methods (overrides?)
    def do_render_end_tag(
        self,
        tag_list: Gst.TagList,
    ) -> Gst.Buffer:
        """
        render_end_tag(self, tag_list:Gst.TagList) -> Gst.Buffer
        """
    def do_render_start_tag(
        self,
        tag_list: Gst.TagList,
    ) -> Gst.Buffer:
        """
        render_start_tag(self, tag_list:Gst.TagList) -> Gst.Buffer
        """

    # Signals
    @typing.overload
    def connect(
        self, detailed_signal: typing.Literal["no-more-pads"], handler: typing.Callable[..., None], *args: typing.Any
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["pad-added"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Pad], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["pad-removed"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Pad], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["deep-notify"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Object, GObject.ParamSpec], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::name"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::parent"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(  # type: ignore otherwise pylance will complain and we should repeat all parent overloads here..
        self, detailed_signal: str, handler: typing.Callable[..., typing.Any], *args: typing.Any
    ) -> int: ...

class TagMuxClass(GObject.GPointer, metaclass=GObject.GType):
    """
    The GstTag.MuxClass structure. Subclasses need to override at least one
    of the two render vfuncs.
    """

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list[object] | None: ...
    @builtins.property
    def parent_class(self) -> Gst.ElementClass | None:
        """
        the parent class.
        """
    @builtins.property
    def render_end_tag(self) -> render_end_tagTagMuxClassCB:
        """
        create a tag buffer to add to the end of the
        input stream given a tag list, or None
        """
    @builtins.property
    def render_start_tag(self) -> render_start_tagTagMuxClassCB:
        """
        create a tag buffer to add to the beginning of the
        input stream given a tag list, or None
        """

class TagMuxPrivate(GObject.GPointer, metaclass=GObject.GType): ...

class TagXmpWriter(builtins.object):
    """
    This interface is implemented by elements that are able to do XMP serialization. Examples for
    such elements are #jifmux and #qtmux.

    Applications can use this interface to configure which XMP schemas should be used when serializing
    tags into XMP. Schemas are represented by their names, a full list of the supported schemas can be
    obtained from `gst_tag_xmp_list_schemas`. By default, all schemas are used.
    """

    class Props: ...

    @builtins.property
    def props(self) -> Props: ...

    # gi Methods
    def __init__(self) -> None:
        """
        Initialize TagXmpWriter object with properties.
        """
    def add_all_schemas(self) -> None:
        """
            Adds all available XMP schemas to the configuration. Meaning that
        all will be used.
        """
    def add_schema(self, schema: str) -> None:
        """
        Adds `schema` to the list schemas
        """
    def has_schema(self, schema: str) -> bool:
        """
        Checks if `schema` is going to be used
        """
    def remove_all_schemas(self) -> None:
        """
            Removes all schemas from the list of schemas to use. Meaning that no
        XMP will be generated.
        """
    def remove_schema(self, schema: str) -> None:
        """
            Removes a schema from the list of schemas to use. Nothing is done if
        the schema wasn't in the list
        """
    def tag_list_to_xmp_buffer(self, taglist: Gst.TagList, read_only: bool) -> Gst.Buffer: ...

class TagXmpWriterInterface(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    @builtins.property
    def parent(self) -> GObject.TypeInterface | None: ...

###############################################################
# Callbacks
###############################################################

class identify_tagTagDemuxClassCB(typing.Protocol):
    """
    This callback was used in:
        TagDemuxClass.identify_tag
    """
    #  demux
    def __call__(
        self,
        demux: TagDemux,
        buffer: Gst.Buffer,
        start_tag: bool,
        tag_size: int,
    ) -> bool: ...

class parse_tagTagDemuxClassCB(typing.Protocol):
    """
    This callback was used in:
        TagDemuxClass.parse_tag
    """
    #  demux
    def __call__(
        self,
        demux: TagDemux,
        buffer: Gst.Buffer,
        start_tag: bool,
        tag_size: int,
        tags: Gst.TagList,
    ) -> TagDemuxResult: ...

class merge_tagsTagDemuxClassCB(typing.Protocol):
    """
    This callback was used in:
        TagDemuxClass.merge_tags
    """
    #  demux
    def __call__(
        self,
        demux: TagDemux,
        start_tags: Gst.TagList,
        end_tags: Gst.TagList,
    ) -> Gst.TagList: ...

class render_start_tagTagMuxClassCB(typing.Protocol):
    """
    This callback was used in:
        TagMuxClass.render_start_tag
    """
    #  mux
    def __call__(
        self,
        mux: TagMux,
        tag_list: Gst.TagList,
    ) -> Gst.Buffer: ...

class render_end_tagTagMuxClassCB(typing.Protocol):
    """
    This callback was used in:
        TagMuxClass.render_end_tag
    """
    #  mux
    def __call__(
        self,
        mux: TagMux,
        tag_list: Gst.TagList,
    ) -> Gst.Buffer: ...

###############################################################
# Constants
###############################################################

TAG_ACOUSTID_FINGERPRINT: str = ...
TAG_ACOUSTID_ID: str = ...
TAG_CAPTURING_CONTRAST: str = ...
TAG_CAPTURING_DIGITAL_ZOOM_RATIO: str = ...
TAG_CAPTURING_EXPOSURE_COMPENSATION: str = ...
TAG_CAPTURING_EXPOSURE_MODE: str = ...
TAG_CAPTURING_EXPOSURE_PROGRAM: str = ...
TAG_CAPTURING_FLASH_FIRED: str = ...
TAG_CAPTURING_FLASH_MODE: str = ...
TAG_CAPTURING_FOCAL_LENGTH: str = ...
TAG_CAPTURING_FOCAL_LENGTH_35_MM: str = ...
TAG_CAPTURING_FOCAL_RATIO: str = ...
TAG_CAPTURING_GAIN_ADJUSTMENT: str = ...
TAG_CAPTURING_ISO_SPEED: str = ...
TAG_CAPTURING_LIGHT_SOURCE: str = ...
TAG_CAPTURING_METERING_MODE: str = ...
TAG_CAPTURING_SATURATION: str = ...
TAG_CAPTURING_SCENE_CAPTURE_TYPE: str = ...
TAG_CAPTURING_SHARPNESS: str = ...
TAG_CAPTURING_SHUTTER_SPEED: str = ...
TAG_CAPTURING_SOURCE: str = ...
TAG_CAPTURING_WHITE_BALANCE: str = ...
TAG_CDDA_CDDB_DISCID: str = ...
TAG_CDDA_CDDB_DISCID_FULL: str = ...
TAG_CDDA_MUSICBRAINZ_DISCID: str = ...
TAG_CDDA_MUSICBRAINZ_DISCID_FULL: str = ...
TAG_CMML_CLIP: str = ...
TAG_CMML_HEAD: str = ...
TAG_CMML_STREAM: str = ...
TAG_ID3V2_HEADER_SIZE: int = ...
TAG_IMAGE_HORIZONTAL_PPI: str = ...
TAG_IMAGE_VERTICAL_PPI: str = ...
TAG_MUSICAL_KEY: str = ...
TAG_MUSICBRAINZ_ALBUMARTISTID: str = ...
TAG_MUSICBRAINZ_ALBUMID: str = ...
TAG_MUSICBRAINZ_ARTISTID: str = ...
TAG_MUSICBRAINZ_RELEASEGROUPID: str = ...
TAG_MUSICBRAINZ_RELEASETRACKID: str = ...
TAG_MUSICBRAINZ_TRACKID: str = ...
TAG_MUSICBRAINZ_TRMID: str = ...
_namespace: str = ...
_version: str = ...
###############################################################
# Unknowns/Not Parsed
###############################################################

# type: lock, element not parsed are:
#     - _lock
