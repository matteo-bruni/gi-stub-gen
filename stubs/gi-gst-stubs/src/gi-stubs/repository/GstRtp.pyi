# pyright: reportIncompatibleMethodOverride=false
"""
Stub template for a GI Repository Module.

Generated by gi-stub-gen, version 0.0.1.
Module: GstRtp
Date: 2026-06-30
"""
# we disable method override report for pyright in generated stubs,
# causes too many false positives

from __future__ import annotations
from typing_extensions import deprecated  # noqa: F401
import typing_extensions  # noqa: F401

import _thread
import builtins
import enum
import typing

# gi.repository imports needed by this Stub
from gi.repository import GLib
from gi.repository import GObject
from gi.repository import Gst
from gi.repository import GstBase
###############################################################
# Functions
###############################################################

def buffer_add_rtp_source_meta(
    buffer: Gst.Buffer,
    ssrc: int | None = None,
    csrc: list[int] | None = None,
) -> RTPSourceMeta:
    """
    Attaches RTP source information to `buffer`.
    """
    ...

def buffer_get_rtp_source_meta(
    buffer: Gst.Buffer,
) -> RTPSourceMeta | None:
    """
    Find the GstRTPSourceMeta on `buffer`.
    """
    ...

def rtcp_buffer_map(
    buffer: Gst.Buffer,
    flags: Gst.MapFlags,
    rtcp: RTCPBuffer,
) -> bool:
    """
    Open `buffer` for reading or writing, depending on `flags`. The resulting RTCP
    buffer state is stored in `rtcp`.
    """
    ...

def rtcp_buffer_new(
    mtu: int,
) -> Gst.Buffer:
    """
    Create a new buffer for constructing RTCP packets. The packet will have a
    maximum size of `mtu`.
    """
    ...

def rtcp_buffer_new_copy_data(
    data: bytes,
) -> Gst.Buffer:
    """
    Create a new buffer and set the data to a copy of `len`
    bytes of `data` and the size to `len`. The data will be freed when the buffer
    is freed.
    """
    ...

def rtcp_buffer_new_take_data(
    data: bytes,
) -> Gst.Buffer:
    """
    Create a new buffer and set the data and size of the buffer to `data` and `len`
    respectively. `data` will be freed when the buffer is unreffed, so this
    function transfers ownership of `data` to the new buffer.
    """
    ...

def rtcp_buffer_validate(
    buffer: Gst.Buffer,
) -> bool:
    """
    Check if the data pointed to by `buffer` is a valid RTCP packet using
    `gst_rtcp_buffer_validate_data`.
    """
    ...

def rtcp_buffer_validate_data(
    data: bytes,
) -> bool:
    """
    Check if the `data` and `size` point to the data of a valid compound,
    non-reduced size RTCP packet.
    Use this function to validate a packet before using the other functions in
    this module.
    """
    ...

def rtcp_buffer_validate_data_reduced(
    data: bytes,
) -> bool:
    """
    Check if the `data` and `size` point to the data of a valid RTCP packet.
    Use this function to validate a packet before using the other functions in
    this module.

    This function is updated to support reduced size rtcp packets according to
    RFC 5506 and will validate full compound RTCP packets as well as reduced
    size RTCP packets.
    """
    ...

def rtcp_buffer_validate_reduced(
    buffer: Gst.Buffer,
) -> bool:
    """
    Check if the data pointed to by `buffer` is a valid RTCP packet using
    `gst_rtcp_buffer_validate_reduced`.
    """
    ...

def rtcp_ntp_to_unix(
    ntptime: int,
) -> int:
    """
    Converts an NTP time to UNIX nanoseconds. `ntptime` can typically be
    the NTP time of an SR RTCP message and contains, in the upper 32 bits, the
    number of seconds since 1900 and, in the lower 32 bits, the fractional
    seconds. The resulting value will be the number of nanoseconds since 1970.
    """
    ...

def rtcp_sdes_name_to_type(
    name: str,
) -> RTCPSDESType:
    """
    Convert `name` into a `GstRTCPSDESType`. `name` is typically a key in a
    GstStructure containing SDES items.
    """
    ...

def rtcp_sdes_type_to_name(
    type: RTCPSDESType,
) -> str:
    """
    Converts `type` to the string equivalent. The string is typically used as a
    key in a GstStructure containing SDES items.
    """
    ...

def rtcp_unix_to_ntp(
    unixtime: int,
) -> int:
    """
    Converts a UNIX timestamp in nanoseconds to an NTP time. The caller should
    pass a value with nanoseconds since 1970. The NTP time will, in the upper
    32 bits, contain the number of seconds since 1900 and, in the lower 32
    bits, the fractional seconds. The resulting value can be used as an ntptime
    for constructing SR RTCP packets.
    """
    ...

def rtp_buffer_allocate_data(
    buffer: Gst.Buffer,
    payload_len: int,
    pad_len: int,
    csrc_count: int,
) -> None:
    """
    Allocate enough data in `buffer` to hold an RTP packet with `csrc_count` CSRCs,
    a payload length of `payload_len` and padding of `pad_len`.
    `buffer` must be writable and all previous memory in `buffer` will be freed.
    If `pad_len` is >0, the padding bit will be set. All other RTP header fields
    will be set to 0/False.
    """
    ...

def rtp_buffer_calc_header_len(
    csrc_count: int,
) -> int:
    """
    Calculate the header length of an RTP packet with `csrc_count` CSRC entries.
    An RTP packet can have at most 15 CSRC entries.
    """
    ...

def rtp_buffer_calc_packet_len(
    payload_len: int,
    pad_len: int,
    csrc_count: int,
) -> int:
    """
    Calculate the total length of an RTP packet with a payload size of `payload_len`,
    a padding of `pad_len` and a `csrc_count` CSRC entries.
    """
    ...

def rtp_buffer_calc_payload_len(
    packet_len: int,
    pad_len: int,
    csrc_count: int,
) -> int:
    """
    Calculate the length of the payload of an RTP packet with size `packet_len`,
    a padding of `pad_len` and a `csrc_count` CSRC entries.
    """
    ...

def rtp_buffer_compare_seqnum(
    seqnum1: int,
    seqnum2: int,
) -> int:
    """
    Compare two sequence numbers, taking care of wraparounds. This function
    returns the difference between `seqnum1` and `seqnum2`.
    """
    ...

def rtp_buffer_default_clock_rate(
    payload_type: int,
) -> int:
    """
    Get the default clock-rate for the static payload type `payload_type`.
    """
    ...

def rtp_buffer_ext_timestamp(
    exttimestamp: int,
    timestamp: int,
) -> tuple[int, int]:
    """
    Update the `exttimestamp` field with the extended timestamp of `timestamp`
    For the first call of the method, `exttimestamp` should point to a location
    with a value of -1.

    This function is able to handle both forward and backward timestamps taking
    into account:
      - timestamp wraparound making sure that the returned value is properly increased.
      - timestamp unwraparound making sure that the returned value is properly decreased.
    """
    ...

def rtp_buffer_get_extension_onebyte_header_from_bytes(
    bytes: GLib.Bytes,
    bit_pattern: int,
    id: int,
    nth: int,
) -> tuple[bool, bytes]:
    """
    Similar to gst_rtp_buffer_get_extension_onebyte_header, but working
    on the GBytes you get from gst_rtp_buffer_get_extension_bytes.
    Parses RFC 5285 style header extensions with a one byte header. It will
    return the nth extension with the requested id.
    """
    ...

def rtp_buffer_map(
    buffer: Gst.Buffer,
    flags: Gst.MapFlags,
) -> tuple[bool, RTPBuffer]:
    """
    Map the contents of `buffer` into `rtp`.
    """
    ...

def rtp_buffer_new_allocate(
    payload_len: int,
    pad_len: int,
    csrc_count: int,
) -> Gst.Buffer:
    """
    Allocate a new GstBuffer with enough data to hold an RTP packet with
    `csrc_count` CSRCs, a payload length of `payload_len` and padding of `pad_len`.
    All other RTP header fields will be set to 0/False.
    """
    ...

def rtp_buffer_new_allocate_len(
    packet_len: int,
    pad_len: int,
    csrc_count: int,
) -> Gst.Buffer:
    """
    Create a new GstBuffer that can hold an RTP packet that is exactly
    `packet_len` long. The length of the payload depends on `pad_len` and
    `csrc_count` and can be calculated with `gst_rtp_buffer_calc_payload_len`.
    All RTP header fields will be set to 0/False.
    """
    ...

def rtp_buffer_new_copy_data(
    data: bytes,
) -> Gst.Buffer:
    """
    Create a new buffer and set the data to a copy of `len`
    bytes of `data` and the size to `len`. The data will be freed when the buffer
    is freed.
    """
    ...

def rtp_buffer_new_take_data(
    data: bytes,
) -> Gst.Buffer:
    """
    Create a new buffer and set the data and size of the buffer to `data` and `len`
    respectively. `data` will be freed when the buffer is unreffed, so this
    function transfers ownership of `data` to the new buffer.
    """
    ...

def rtp_get_header_extension_list() -> list[Gst.ElementFactory]:
    """
    Retrieve all the factories of the currently registered RTP header
    extensions.  Call `gst_element_factory_create` with each factory to create
    the associated GstRTPHeaderExtension.
    """
    ...

def rtp_hdrext_get_ntp_56(
    data: bytes,
) -> tuple[bool, int]:
    """
    Reads the NTP time from the `size` NTP-56 extension bytes in `data` and store the
    result in `ntptime`.
    """
    ...

def rtp_hdrext_get_ntp_64(
    data: bytes,
) -> tuple[bool, int]:
    """
    Reads the NTP time from the `size` NTP-64 extension bytes in `data` and store the
    result in `ntptime`.
    """
    ...

def rtp_hdrext_set_ntp_56(
    data: object | None,
    size: int,
    ntptime: int,
) -> bool:
    """
    Writes the NTP time in `ntptime` to the format required for the NTP-56 header
    extension. `data` must hold at least GST_RTP_HDREXT_NTP_56_SIZE bytes.
    """
    ...

def rtp_hdrext_set_ntp_64(
    data: object | None,
    size: int,
    ntptime: int,
) -> bool:
    """
    Writes the NTP time in `ntptime` to the format required for the NTP-64 header
    extension. `data` must hold at least GST_RTP_HDREXT_NTP_64_SIZE bytes.
    """
    ...

def rtp_payload_info_for_name(
    media: str,
    encoding_name: str,
) -> RTPPayloadInfo | None:
    """
    Get the GstRTPPayloadInfo for `media` and `encoding_name`. This function is
    mostly used to get the default clock-rate and bandwidth for dynamic payload
    types specified with `media` and `encoding` name.

    The search for `encoding_name` will be performed in a case insensitive way.
    """
    ...

def rtp_payload_info_for_pt(
    payload_type: int,
) -> RTPPayloadInfo | None:
    """
    Get the GstRTPPayloadInfo for `payload_type`. This function is
    mostly used to get the default clock-rate and bandwidth for static payload
    types specified with `payload_type`.
    """
    ...

def rtp_source_meta_api_get_type() -> GObject.GType: ...
def rtp_source_meta_get_info() -> Gst.MetaInfo: ...

###############################################################
# Enums/Flags
###############################################################

class RTCPFBType(GObject.GEnum, enum.IntEnum):
    """
    Different types of feedback messages."""

    FB_TYPE_INVALID = 0
    """
    Invalid type
    """
    RTPFB_TYPE_NACK = 1
    """
    Generic NACK
    """
    RTPFB_TYPE_TMMBR = 3
    """
    Temporary Maximum Media Stream Bit Rate Request
    """
    RTPFB_TYPE_TMMBN = 4
    """
    Temporary Maximum Media Stream Bit Rate
       Notification
    """
    RTPFB_TYPE_RTCP_SR_REQ = 5
    """
    Request an SR packet for early
       synchronization
    """
    RTPFB_TYPE_TWCC = 15
    PSFB_TYPE_PLI = 1
    """
    Picture Loss Indication
    """
    PSFB_TYPE_SLI = 2
    """
    Slice Loss Indication
    """
    PSFB_TYPE_RPSI = 3
    """
    Reference Picture Selection Indication
    """
    PSFB_TYPE_AFB = 15
    """
    Application layer Feedback
    """
    PSFB_TYPE_FIR = 4
    """
    Full Intra Request Command
    """
    PSFB_TYPE_TSTR = 5
    """
    Temporal-Spatial Trade-off Request
    """
    PSFB_TYPE_TSTN = 6
    """
    Temporal-Spatial Trade-off Notification
    """
    PSFB_TYPE_VBCN = 7
    """
    Video Back Channel Message
    """

class RTCPSDESType(GObject.GEnum, enum.IntEnum):
    """
    Different types of SDES content."""

    INVALID = -1
    """
    Invalid SDES entry
    """
    END = 0
    """
    End of SDES list
    """
    CNAME = 1
    """
    Canonical name
    """
    NAME = 2
    """
    User name
    """
    EMAIL = 3
    """
    User's electronic mail address
    """
    PHONE = 4
    """
    User's phone number
    """
    LOC = 5
    """
    Geographic user location
    """
    TOOL = 6
    """
    Name of application or tool
    """
    NOTE = 7
    """
    Notice about the source
    """
    PRIV = 8
    """
    Private extensions
    """
    H323_CADDR = 9
    """
    H.323 callable address
    """
    APSI = 10
    """
    Application Specific Identifier (RFC6776)
    """
    RGRP = 11
    """
    Reporting Group Identifier (RFC8861)
    """
    RTP_STREAM_ID = 12
    """
    RtpStreamId SDES item (RFC8852).
    """
    REPAIRED_RTP_STREAM_ID = 13
    """
    RepairedRtpStreamId SDES item (RFC8852).
    """
    CCID = 14
    """
    CLUE CaptId (RFC8849)
    """
    MID = 15
    """
    MID SDES item (RFC8843).
    """

class RTCPType(GObject.GEnum, enum.IntEnum):
    """
    Different RTCP packet types."""

    INVALID = 0
    """
    Invalid type
    """
    SR = 200
    """
    Sender report
    """
    RR = 201
    """
    Receiver report
    """
    SDES = 202
    """
    Source description
    """
    BYE = 203
    """
    Goodbye
    """
    APP = 204
    """
    Application defined
    """
    RTPFB = 205
    """
    Transport layer feedback.
    """
    PSFB = 206
    """
    Payload-specific feedback.
    """
    XR = 207
    """
    Extended report.
    """

class RTCPXRType(GObject.GEnum, enum.IntEnum):
    """
    Types of RTCP Extended Reports, those are defined in RFC 3611 and other RFCs
    according to the [IANA registry](https://www.iana.org/assignments/rtcp-xr-block-types/rtcp-xr-block-types.xhtml)."""

    INVALID = -1
    """
    Invalid XR Report Block
    """
    LRLE = 1
    """
    Loss RLE Report Block
    """
    DRLE = 2
    """
    Duplicate RLE Report Block
    """
    PRT = 3
    """
    Packet Receipt Times Report Block
    """
    RRT = 4
    """
    Receiver Reference Time Report Block
    """
    DLRR = 5
    """
    Delay since the last Receiver Report
    """
    SSUMM = 6
    """
    Statistics Summary Report Block
    """
    VOIP_METRICS = 7
    """
    VoIP Metrics Report Block
    """

class RTPBufferFlags(GObject.GFlags, enum.IntFlag):
    """
    Additional RTP buffer flags. These flags can potentially be used on any
    buffers carrying RTP packets.

    Note that these are only valid for GstCaps of type: application/x-rtp (x-rtcp).
    They can conflict with other extended buffer flags."""

    RETRANSMISSION = 1048576
    """
    The GstBuffer was once wrapped
              in a retransmitted packet as specified by RFC 4588.
    """
    REDUNDANT = 2097152
    """
    The packet represents redundant RTP packet.
              The flag is used in gstrtpstorage to be able to hold the packetback
              and use it only for recovery from packet loss.
              Since: 1.14
    """
    LAST = 268435456
    """
    Offset to define more flags.
    """

class RTPBufferMapFlags(GObject.GFlags, enum.IntFlag):
    """
    Additional mapping flags for `gst_rtp_buffer_map`."""

    SKIP_PADDING = 65536
    """
    Skip mapping and validation of RTP
              padding and RTP pad count when present. Useful for buffers where
              the padding may be encrypted.
    """
    LAST = 16777216
    """
    Offset to define more flags
    """

class RTPHeaderExtensionDirection(GObject.GFlags, enum.IntFlag):
    """
    Direction to which to apply the RTP Header Extension"""

    INACTIVE = 0
    """
    Neither send nor
    receive RTP Header Extensions
    """
    SENDONLY = 1
    """
    Only send RTP Header
    Extensions `GST_RTP_HEADER_EXTENSION_DIRECTION_RECVONLY`: Only
    receive RTP Header Extensions
    """
    RECVONLY = 2
    SENDRECV = 3
    """
    Send and receive RTP
    Header Extensions ext
    """
    INHERITED = 4
    """
    RTP header extension
    direction is inherited from the stream
    """

class RTPHeaderExtensionFlags(GObject.GFlags, enum.IntFlag):
    """
    Flags that apply to a RTP Audio/Video header extension."""

    ONE_BYTE = 1
    """
    The one byte rtp extension header.
                 1-16 data bytes per extension with a maximum of
                 14 extension ids in total.
    """
    TWO_BYTE = 2
    """
    The two byte rtp extension header.
                 256 data bytes per extension with a maximum of 255 (or 256
                 including appbits) extensions in total.
    """

class RTPPayload(GObject.GEnum, enum.IntEnum):
    """
    Standard predefined fixed payload types.

    The official list is at:
    http://www.iana.org/assignments/rtp-parameters

    Audio:
    reserved: 19
    unassigned: 20-23,

    Video:
    unassigned: 24, 27, 29, 30, 35-71, 77-95
    Reserved for RTCP conflict avoidance: 72-76"""

    PCMU = 0
    """
    ITU-T G.711. mu-law audio (RFC 3551)
    """
    _1016 = 1  # [1016]: changed because started with a number
    """
    RFC 3551 says reserved
    """
    G721 = 2
    """
    RFC 3551 says reserved
    """
    GSM = 3
    """
    GSM audio
    """
    G723 = 4
    """
    ITU G.723.1 audio
    """
    DVI4_8000 = 5
    """
    IMA ADPCM wave type (RFC 3551)
    """
    DVI4_16000 = 6
    """
    IMA ADPCM wave type (RFC 3551)
    """
    LPC = 7
    """
    experimental linear predictive encoding
    """
    PCMA = 8
    """
    ITU-T G.711 A-law audio (RFC 3551)
    """
    G722 = 9
    """
    ITU-T G.722 (RFC 3551)
    """
    L16_STEREO = 10
    """
    stereo PCM
    """
    L16_MONO = 11
    """
    mono PCM
    """
    QCELP = 12
    """
    EIA & TIA standard IS-733
    """
    CN = 13
    """
    Comfort Noise (RFC 3389)
    """
    MPA = 14
    """
    Audio MPEG 1-3.
    """
    G728 = 15
    """
    ITU-T G.728 Speech coder (RFC 3551)
    """
    DVI4_11025 = 16
    """
    IMA ADPCM wave type (RFC 3551)
    """
    DVI4_22050 = 17
    """
    IMA ADPCM wave type (RFC 3551)
    """
    G729 = 18
    """
    ITU-T G.729 Speech coder (RFC 3551)
    """
    CELLB = 25
    """
    See RFC 2029
    """
    JPEG = 26
    """
    ISO Standards 10918-1 and 10918-2 (RFC 2435)
    """
    NV = 28
    """
    nv encoding by Ron Frederick
    """
    H261 = 31
    """
    ITU-T Recommendation H.261 (RFC 2032)
    """
    MPV = 32
    """
    Video MPEG 1 & 2 (RFC 2250)
    """
    MP2T = 33
    """
    MPEG-2 transport stream (RFC 2250)
    """
    H263 = 34
    """
    Video H263 (RFC 2190)
    """

class RTPProfile(GObject.GEnum, enum.IntEnum):
    """
    The transfer profile to use."""

    UNKNOWN = 0
    """
    invalid profile
    """
    AVP = 1
    """
    the Audio/Visual profile (RFC 3551)
    """
    SAVP = 2
    """
    the secure Audio/Visual profile (RFC 3711)
    """
    AVPF = 3
    """
    the Audio/Visual profile with feedback (RFC 4585)
    """
    SAVPF = 4
    """
    the secure Audio/Visual profile with feedback (RFC 5124)
    """

###############################################################
# classes
###############################################################

class RTCPBuffer(GObject.GPointer, metaclass=GObject.GType):
    """
    Note: The API in this module is not yet declared stable.

    The GstRTPCBuffer helper functions makes it easy to parse and create regular
    GstBuffer objects that contain compound RTCP packets. These buffers are typically
    of 'application/x-rtcp' GstCaps.

    An RTCP buffer consists of 1 or more GstRTCPPacket structures that you can
    retrieve with `gst_rtcp_buffer_get_first_packet`. GstRTCPPacket acts as a pointer
    into the RTCP buffer; you can move to the next packet with
    `gst_rtcp_packet_move_to_next`.
    """

    # gi Fields
    buffer: Gst.Buffer | None = ...  # type: ignore

    # gi Methods
    def add_packet(self, type: RTCPType, packet: RTCPPacket) -> bool:
        """
            Add a new packet of `type` to `rtcp`. `packet` will point to the newly created
        packet.
        """
    def get_first_packet(self, packet: RTCPPacket) -> bool:
        """
            Initialize a new GstRTCPPacket pointer that points to the first packet in
        `rtcp`.
        """
    def get_packet_count(self) -> int:
        """
        Get the number of RTCP packets in `rtcp`.
        """
    @staticmethod
    def map(buffer: Gst.Buffer, flags: Gst.MapFlags, rtcp: RTCPBuffer) -> bool:
        """
            Open `buffer` for reading or writing, depending on `flags`. The resulting RTCP
        buffer state is stored in `rtcp`.
        """
    @staticmethod
    def new(mtu: int) -> Gst.Buffer:
        """
            Create a new buffer for constructing RTCP packets. The packet will have a
        maximum size of `mtu`.
        """
    @staticmethod
    def new_copy_data(data: bytes) -> Gst.Buffer:
        """
            Create a new buffer and set the data to a copy of `len`
        bytes of `data` and the size to `len`. The data will be freed when the buffer
        is freed.
        """
    @staticmethod
    def new_take_data(data: bytes) -> Gst.Buffer:
        """
            Create a new buffer and set the data and size of the buffer to `data` and `len`
        respectively. `data` will be freed when the buffer is unreffed, so this
        function transfers ownership of `data` to the new buffer.
        """
    def unmap(self) -> bool:
        """
            Finish `rtcp` after being constructed. This function is usually called
        after `gst_rtcp_buffer_map` and after adding the RTCP items to the new buffer.

        The function adjusts the size of `rtcp` with the total length of all the
        added packets.
        """
    @staticmethod
    def validate(buffer: Gst.Buffer) -> bool:
        """
            Check if the data pointed to by `buffer` is a valid RTCP packet using
        `gst_rtcp_buffer_validate_data`.
        """
    @staticmethod
    def validate_data(data: bytes) -> bool:
        """
            Check if the `data` and `size` point to the data of a valid compound,
        non-reduced size RTCP packet.
        Use this function to validate a packet before using the other functions in
        this module.
        """
    @staticmethod
    def validate_data_reduced(data: bytes) -> bool:
        """
            Check if the `data` and `size` point to the data of a valid RTCP packet.
        Use this function to validate a packet before using the other functions in
        this module.

        This function is updated to support reduced size rtcp packets according to
        RFC 5506 and will validate full compound RTCP packets as well as reduced
        size RTCP packets.
        """
    @staticmethod
    def validate_reduced(buffer: Gst.Buffer) -> bool:
        """
            Check if the data pointed to by `buffer` is a valid RTCP packet using
        `gst_rtcp_buffer_validate_reduced`.
        """

class RTCPPacket(GObject.GPointer, metaclass=GObject.GType):
    """
    Data structure that points to a packet at `offset` in `buffer`.
    The size of the structure is made public to allow stack allocations.
    """

    # gi Fields
    @builtins.property
    def count(self) -> int: ...
    @builtins.property
    def entry_offset(self) -> int: ...
    @builtins.property
    def item_count(self) -> int: ...
    @builtins.property
    def item_offset(self) -> int: ...
    @builtins.property
    def length(self) -> int: ...
    offset: int = ...
    """
    offset of packet in buffer data
    """
    @builtins.property
    def padding(self) -> bool: ...
    rtcp: RTCPBuffer | None = ...
    """
    pointer to RTCP buffer
    """
    @builtins.property
    def type(self) -> RTCPType: ...

    # gi Methods
    def add_profile_specific_ext(self, data: bytes) -> bool:
        """
            Add profile-specific extension `data` to `packet`. If `packet` already
        contains profile-specific extension `data` will be appended to the existing
        extension.
        """
    def add_rb(
        self, ssrc: int, fractionlost: int, packetslost: int, exthighestseq: int, jitter: int, lsr: int, dlsr: int
    ) -> bool:
        """
        Add a new report block to `packet` with the given values.
        """
    def app_get_data(self) -> int:
        """
        Get the application-dependent data attached to a RTPFB or PSFB `packet`.
        """
    def app_get_data_length(self) -> int:
        """
            Get the length of the application-dependent data attached to an APP
        `packet`.
        """
    def app_get_name(self) -> str:
        """
        Get the name field of the APP `packet`.
        """
    def app_get_ssrc(self) -> int:
        """
        Get the SSRC/CSRC field of the APP `packet`.
        """
    def app_get_subtype(self) -> int:
        """
        Get the subtype field of the APP `packet`.
        """
    def app_set_data_length(self, wordlen: int) -> bool:
        """
            Set the length of the application-dependent data attached to an APP
        `packet`.
        """
    def app_set_name(self, name: str) -> None:
        """
        Set the name field of the APP `packet`.
        """
    def app_set_ssrc(self, ssrc: int) -> None:
        """
        Set the SSRC/CSRC field of the APP `packet`.
        """
    def app_set_subtype(self, subtype: int) -> None:
        """
        Set the subtype field of the APP `packet`.
        """
    def bye_add_ssrc(self, ssrc: int) -> bool:
        """
        Add `ssrc` to the BYE `packet`.
        """
    def bye_add_ssrcs(self, ssrc: list[int]) -> bool:
        """
        Adds `len` SSRCs in `ssrc` to BYE `packet`.
        """
    def bye_get_nth_ssrc(self, nth: int) -> int:
        """
        Get the `nth` SSRC of the BYE `packet`.
        """
    def bye_get_reason(self) -> str | None:
        """
        Get the reason in `packet`.
        """
    def bye_get_reason_len(self) -> int:
        """
        Get the length of the reason string.
        """
    def bye_get_ssrc_count(self) -> int:
        """
        Get the number of SSRC fields in `packet`.
        """
    def bye_set_reason(self, reason: str) -> bool:
        """
        Set the reason string to `reason` in `packet`.
        """
    def copy_profile_specific_ext(self) -> tuple[bool, bytes]:
        """
            The profile-specific extension data is copied into a new allocated
        memory area `data`. This must be freed with `g_free` after usage.
        """
    def fb_get_fci(self) -> int:
        """
        Get the Feedback Control Information attached to a RTPFB or PSFB `packet`.
        """
    def fb_get_fci_length(self) -> int:
        """
            Get the length of the Feedback Control Information attached to a
        RTPFB or PSFB `packet`.
        """
    def fb_get_media_ssrc(self) -> int:
        """
        Get the media SSRC field of the RTPFB or PSFB `packet`.
        """
    def fb_get_sender_ssrc(self) -> int:
        """
        Get the sender SSRC field of the RTPFB or PSFB `packet`.
        """
    def fb_get_type(self) -> RTCPFBType:
        """
        Get the feedback message type of the FB `packet`.
        """
    def fb_set_fci_length(self, wordlen: int) -> bool:
        """
            Set the length of the Feedback Control Information attached to a
        RTPFB or PSFB `packet`.
        """
    def fb_set_media_ssrc(self, ssrc: int) -> None:
        """
        Set the media SSRC field of the RTPFB or PSFB `packet`.
        """
    def fb_set_sender_ssrc(self, ssrc: int) -> None:
        """
        Set the sender SSRC field of the RTPFB or PSFB `packet`.
        """
    def fb_set_type(self, type: RTCPFBType) -> None:
        """
        Set the feedback message type of the FB `packet`.
        """
    def get_count(self) -> int:
        """
        Get the count field in `packet`.
        """
    def get_length(self) -> int:
        """
            Get the length field of `packet`. This is the length of the packet in
        32-bit words minus one.
        """
    def get_padding(self) -> bool:
        """
        Get the packet padding of the packet pointed to by `packet`.
        """
    def get_profile_specific_ext(self) -> tuple[bool, bytes]: ...
    def get_profile_specific_ext_length(self) -> int: ...
    def get_rb(self, nth: int) -> tuple[int, int, int, int, int, int, int]:
        """
            Parse the values of the `nth` report block in `packet` and store the result in
        the values.
        """
    def get_rb_count(self) -> int:
        """
        Get the number of report blocks in `packet`.
        """
    def get_type(self) -> RTCPType:
        """
        Get the packet type of the packet pointed to by `packet`.
        """
    def move_to_next(self) -> bool:
        """
            Move the packet pointer `packet` to the next packet in the payload.
        Use `gst_rtcp_buffer_get_first_packet` to initialize `packet`.
        """
    def remove(self) -> bool:
        """
        Removes the packet pointed to by `packet` and moves pointer to the next one
        """
    def rr_get_ssrc(self) -> int:
        """
        Get the ssrc field of the RR `packet`.
        """
    def rr_set_ssrc(self, ssrc: int) -> None:
        """
        Set the ssrc field of the RR `packet`.
        """
    def sdes_add_entry(self, type: RTCPSDESType, data: bytes) -> bool:
        """
        Add a new SDES entry to the current item in `packet`.
        """
    def sdes_add_item(self, ssrc: int) -> bool:
        """
        Add a new SDES item for `ssrc` to `packet`.
        """
    def sdes_copy_entry(self, type: RTCPSDESType) -> tuple[bool, bytes]:
        """
            This function is like `gst_rtcp_packet_sdes_get_entry` but it returns a
        null-terminated copy of the data instead. use `g_free` after usage.
        """
    def sdes_first_entry(self) -> bool:
        """
        Move to the first SDES entry in the current item.
        """
    def sdes_first_item(self) -> bool:
        """
        Move to the first SDES item in `packet`.
        """
    def sdes_get_entry(self, type: RTCPSDESType) -> tuple[bool, bytes]:
        """
            Get the data of the current SDES item entry. `type` (when not None) will
        contain the type of the entry. `data` (when not None) will point to `len`
        bytes.

        When `type` refers to a text item, `data` will point to a UTF8 string. Note
        that this UTF8 string is NOT null-terminated. Use
        `gst_rtcp_packet_sdes_copy_entry` to get a null-terminated copy of the entry.
        """
    def sdes_get_item_count(self) -> int:
        """
        Get the number of items in the SDES packet `packet`.
        """
    def sdes_get_ssrc(self) -> int:
        """
        Get the SSRC of the current SDES item.
        """
    def sdes_next_entry(self) -> bool:
        """
        Move to the next SDES entry in the current item.
        """
    def sdes_next_item(self) -> bool:
        """
        Move to the next SDES item in `packet`.
        """
    def set_rb(
        self,
        nth: int,
        ssrc: int,
        fractionlost: int,
        packetslost: int,
        exthighestseq: int,
        jitter: int,
        lsr: int,
        dlsr: int,
    ) -> None:
        """
            Set the `nth` new report block in `packet` with the given values.

        Note: Not implemented.
        """
    def sr_get_sender_info(self) -> tuple[int, int, int, int, int]:
        """
        Parse the SR sender info and store the values.
        """
    def sr_set_sender_info(self, ssrc: int, ntptime: int, rtptime: int, packet_count: int, octet_count: int) -> None:
        """
        Set the given values in the SR packet `packet`.
        """
    def xr_first_rb(self) -> bool:
        """
        Move to the first extended report block in XR `packet`.
        """
    def xr_get_block_length(self) -> int: ...
    def xr_get_block_type(self) -> RTCPXRType:
        """
        Get the extended report block type of the XR `packet`.
        """
    def xr_get_dlrr_block(self, nth: int, ssrc: int, last_rr: int, delay: int) -> bool:
        """
        Parse the extended report block for DLRR report block type.
        """
    def xr_get_prt_by_seq(self, seq: int, receipt_time: int) -> bool:
        """
        Retrieve the packet receipt time of `seq` which ranges in [begin_seq, end_seq).
        """
    def xr_get_prt_info(self, ssrc: int, thinning: int, begin_seq: int, end_seq: int) -> bool:
        """
        Parse the Packet Recept Times Report Block from a XR `packet`
        """
    def xr_get_rle_info(self, ssrc: int, thinning: int, begin_seq: int, end_seq: int, chunk_count: int) -> bool:
        """
        Parse the extended report block for Loss RLE and Duplicated LRE block type.
        """
    def xr_get_rle_nth_chunk(self, nth: int, chunk: int) -> bool:
        """
        Retrieve actual chunk data.
        """
    def xr_get_rrt(self, timestamp: int) -> bool: ...
    def xr_get_ssrc(self) -> int:
        """
        Get the ssrc field of the XR `packet`.
        """
    def xr_get_summary_info(self, ssrc: int, begin_seq: int, end_seq: int) -> bool:
        """
        Extract a basic information from static summary report block of XR `packet`.
        """
    def xr_get_summary_jitter(self, min_jitter: int, max_jitter: int, mean_jitter: int, dev_jitter: int) -> bool:
        """
            Extract jitter information from the statistics summary. If the jitter flag in
        a block header is set as zero, all of jitters will be zero.
        """
    def xr_get_summary_pkt(self, lost_packets: int, dup_packets: int) -> bool:
        """
            Get the number of lost or duplicate packets. If the flag in a block header
        is set as zero, `lost_packets` or `dup_packets` will be zero.
        """
    def xr_get_summary_ttl(self, is_ipv4: bool, min_ttl: int, max_ttl: int, mean_ttl: int, dev_ttl: int) -> bool:
        """
        Extract the value of ttl for ipv4, or hop limit for ipv6.
        """
    def xr_get_voip_burst_metrics(
        self, burst_density: int, gap_density: int, burst_duration: int, gap_duration: int
    ) -> bool: ...
    def xr_get_voip_configuration_params(self, gmin: int, rx_config: int) -> bool: ...
    def xr_get_voip_delay_metrics(self, roundtrip_delay: int, end_system_delay: int) -> bool: ...
    def xr_get_voip_jitter_buffer_params(self, jb_nominal: int, jb_maximum: int, jb_abs_max: int) -> bool: ...
    def xr_get_voip_metrics_ssrc(self, ssrc: int) -> bool: ...
    def xr_get_voip_packet_metrics(self, loss_rate: int, discard_rate: int) -> bool: ...
    def xr_get_voip_quality_metrics(self, r_factor: int, ext_r_factor: int, mos_lq: int, mos_cq: int) -> bool: ...
    def xr_get_voip_signal_metrics(self, signal_level: int, noise_level: int, rerl: int, gmin: int) -> bool: ...
    def xr_next_rb(self) -> bool:
        """
        Move to the next extended report block in XR `packet`.
        """

class RTPBaseAudioPayload(RTPBasePayload):
    """
    Provides a base class for audio RTP payloaders for frame or sample based
    audio codecs (constant bitrate)

    This class derives from GstRTPBasePayload. It can be used for payloading
    audio codecs. It will only work with constant bitrate codecs. It supports
    both frame based and sample based codecs. It takes care of packing up the
    audio data into RTP packets and filling up the headers accordingly. The
    payloading is done based on the maximum MTU (mtu) and the maximum time per
    packet (max-ptime). The general idea is to divide large data buffers into
    smaller RTP packets. The RTP packet size is the minimum of either the MTU,
    max-ptime (if set) or available data. The RTP packet size is always larger or
    equal to min-ptime (if set). If min-ptime is not set, any residual data is
    sent in a last RTP packet. In the case of frame based codecs, the resulting
    RTP packets always contain full frames.

    ## Usage

    To use this base class, your child element needs to call either
    `gst_rtp_base_audio_payload_set_frame_based` or
    `gst_rtp_base_audio_payload_set_sample_based`. This is usually done in the
    element's ``_init`` function. Then, the child element must call either
    `gst_rtp_base_audio_payload_set_frame_options`,
    `gst_rtp_base_audio_payload_set_sample_options` or
    gst_rtp_base_audio_payload_set_samplebits_options. Since
    GstRTPBaseAudioPayload derives from GstRTPBasePayload, the child element
    must set any variables or call/override any functions required by that base
    class. The child element does not need to override any other functions
    specific to GstRTPBaseAudioPayload.
    """

    class Props(RTPBasePayload.Props):
        buffer_list: bool  # [buffer-list]: changed because contained invalid characters

    @builtins.property
    def props(self) -> Props: ...

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list | None: ...
    @builtins.property
    def base_ts(self) -> int: ...
    @builtins.property
    def frame_duration(self) -> int: ...
    @builtins.property
    def frame_size(self) -> int: ...
    @builtins.property
    def payload(self) -> RTPBasePayload | None: ...
    @builtins.property
    def priv(self) -> RTPBaseAudioPayloadPrivate | None: ...
    @builtins.property
    def sample_size(self) -> int: ...

    # gi Methods
    def __init__(
        self,
        auto_header_extension: bool | None = None,
        buffer_list: bool | None = None,
        max_ptime: int | None = None,
        min_ptime: int | None = None,
        mtu: int | None = None,
        name: str | None = None,
        onvif_no_rate_control: bool | None = None,
        parent: Gst.Object | None = None,
        perfect_rtptime: bool | None = None,
        pt: int | None = None,
        ptime_multiple: int | None = None,
        scale_rtptime: bool | None = None,
        seqnum_offset: int | None = None,
        source_info: bool | None = None,
        ssrc: int | None = None,
        timestamp_offset: int | None = None,
    ) -> None:
        """
        Initialize RTPBaseAudioPayload object with properties.
        """
    def flush(self, payload_len: int, timestamp: int) -> Gst.FlowReturn:
        """
            Create an RTP buffer and store `payload_len` bytes of the adapter as the
        payload. Set the timestamp on the new buffer to `timestamp` before pushing
        the buffer downstream.

        If `payload_len` is -1, all pending bytes will be flushed. If `timestamp` is
        -1, the timestamp will be calculated automatically.
        """
    def get_adapter(self) -> GstBase.Adapter:
        """
        Gets the internal adapter used by the depayloader.
        """
    def push(self, data: bytes, timestamp: int) -> Gst.FlowReturn:
        """
            Create an RTP buffer and store `payload_len` bytes of `data` as the
        payload. Set the timestamp on the new buffer to `timestamp` before pushing
        the buffer downstream.
        """
    def set_frame_based(self) -> None:
        """
            Tells GstRTPBaseAudioPayload that the child element is for a frame based
        audio codec
        """
    def set_frame_options(self, frame_duration: int, frame_size: int) -> None:
        """
        Sets the options for frame based audio codecs.
        """
    def set_sample_based(self) -> None:
        """
            Tells GstRTPBaseAudioPayload that the child element is for a sample based
        audio codec
        """
    def set_sample_options(self, sample_size: int) -> None:
        """
        Sets the options for sample based audio codecs.
        """
    def set_samplebits_options(self, sample_size: int) -> None:
        """
        Sets the options for sample based audio codecs.
        """

    # Signals
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["add-extension"],
        handler: typing.Callable[[typing_extensions.Self, RTPHeaderExtension], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["clear-extensions"],
        handler: typing.Callable[..., None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["request-extension"],
        handler: typing.Callable[[typing_extensions.Self, int, str], RTPHeaderExtension | None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self, detailed_signal: typing.Literal["no-more-pads"], handler: typing.Callable[..., None], *args: typing.Any
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["pad-added"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Pad], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["pad-removed"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Pad], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["deep-notify"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Object, GObject.ParamSpec], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::buffer_list"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::auto_header_extension"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::extensions"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::max_ptime"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::min_ptime"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::mtu"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::onvif_no_rate_control"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::perfect_rtptime"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::pt"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::ptime_multiple"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::scale_rtptime"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::seqnum"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::seqnum_offset"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::source_info"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::ssrc"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::stats"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::timestamp"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::timestamp_offset"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::name"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::parent"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(  # type: ignore otherwise pylance will complain and we should repeat all parent overloads here..
        self, detailed_signal: str, handler: typing.Callable[..., typing.Any], *args: typing.Any
    ) -> int: ...

class RTPBaseAudioPayloadClass(GObject.GPointer, metaclass=GObject.GType):
    """
    Base class for audio RTP payloader.
    """

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list | None: ...
    @builtins.property
    def parent_class(self) -> RTPBasePayloadClass | None:
        """
        the parent class
        """

class RTPBaseAudioPayloadPrivate(GObject.GPointer, metaclass=GObject.GType): ...

class RTPBaseDepayload(Gst.Element):
    """
    Provides a base class for RTP depayloaders

    In order to handle RTP header extensions correctly if the
    depayloader aggregates multiple RTP packet payloads into one output
    buffer this class provides the function
    `gst_rtp_base_depayload_set_aggregate_hdrext_enabled`. If the
    aggregation is enabled the virtual functions
    `GstRTPBaseDepayload`.process or
    `GstRTPBaseDepayload`.process_rtp_packet must tell the base class
    what happens to the current RTP packet. By default the base class
    assumes that the packet payload is used with the next output
    buffer.

    If the RTP packet will not be used with an output buffer
    `gst_rtp_base_depayload_dropped` must be called. A typical
    situation would be if we are waiting for a keyframe.

    If the RTP packet will be used but not with the current output
    buffer but with the next one `gst_rtp_base_depayload_delayed` must
    be called. This may happen if the current RTP packet signals the
    start of a new output buffer and the currently processed output
    buffer will be pushed first. The undelay happens implicitly once
    the current buffer has been pushed or
    `gst_rtp_base_depayload_flush` has been called.

    If `gst_rtp_base_depayload_flush` is called all RTP packets that
    have not been dropped since the last output buffer are dropped,
    e.g. if an output buffer is discarded due to malformed data. This
    may or may not include the current RTP packet depending on the 2nd
    parameter `keep_current`.

    Be aware that in case `gst_rtp_base_depayload_push_list` is used
    each buffer will see the same list of RTP header extensions.
    """

    class Props(Gst.Element.Props):
        auto_header_extension: bool  # [auto-header-extension]: changed because contained invalid characters
        """
        If enabled, the depayloader will automatically try to enable all the
        RTP header extensions provided in the sink caps, saving the application
        the need to handle these extensions manually using the
        GstRTPBaseDepayload::request-extension: signal.
        """
        extensions: Gst.ValueArray | None
        """
        A list of already enabled RTP header extensions. This may be useful for finding
        out which extensions are already enabled (with add-extension signal) and picking a non-conflicting
        ID for a new extension that needs to be added on top of the existing ones.

        Note that the value returned by reading this property is not dynamically updated when the set of
        enabled extensions changes by any of existing action signals. Rather, it represents the current state
        at the time the property is read.

        Dynamic updates of this property can be received by subscribing to its corresponding "notify" signal, i.e.
        "notify::extensions".
        """
        max_reorder: int  # [max-reorder]: changed because contained invalid characters
        """
        Max seqnum reorder before the sender is assumed to have restarted.

        When max-reorder is set to 0 all reordered/duplicate packets are
        considered coming from a restarted sender.
        """
        source_info: bool  # [source-info]: changed because contained invalid characters
        """
        Add RTP source information found in RTP header as meta to output buffer.
        """
        stats: Gst.Structure | None
        """
        Various depayloader statistics retrieved atomically (and are therefore
        synchroized with each other). This property return a GstStructure named
        application/x-rtp-depayload-stats containing the following fields relating to
        the last processed buffer and current state of the stream being depayloaded:

          * `clock-rate`: #G_TYPE_UINT, clock-rate of the stream
          * `npt-start`: #G_TYPE_UINT64, time of playback start
          * `npt-stop`: #G_TYPE_UINT64, time of playback stop
          * `play-speed`: #G_TYPE_DOUBLE, the playback speed
          * `play-scale`: #G_TYPE_DOUBLE, the playback scale
          * `running-time-dts`: #G_TYPE_UINT64, the last running-time of the
             last DTS
          * `running-time-pts`: #G_TYPE_UINT64, the last running-time of the
             last PTS
          * `seqnum`: #G_TYPE_UINT, the last seen seqnum
          * `timestamp`: #G_TYPE_UINT, the last seen RTP timestamp
        """

    @builtins.property
    def props(self) -> Props: ...

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list | None: ...
    @builtins.property
    def clock_rate(self) -> int: ...
    @builtins.property
    def need_newsegment(self) -> bool: ...
    @builtins.property
    def parent(self) -> Gst.Element | None: ...
    @builtins.property
    def priv(self) -> RTPBaseDepayloadPrivate | None: ...
    @builtins.property
    def segment(self) -> Gst.Segment | None: ...
    @builtins.property
    def sinkpad(self) -> Gst.Pad | None: ...
    @builtins.property
    def srcpad(self) -> Gst.Pad | None: ...

    # gi Methods
    def __init__(
        self,
        auto_header_extension: bool | None = None,
        max_reorder: int | None = None,
        name: str | None = None,
        parent: Gst.Object | None = None,
        source_info: bool | None = None,
    ) -> None:
        """
        Initialize RTPBaseDepayload object with properties.
        """
    def delayed(self) -> None:
        """
            Called from `GstRTPBaseDepayload`.process or
        `GstRTPBaseDepayload`.process_rtp_packet when the depayloader needs
        to keep the current input RTP header for use with the next output
        buffer.

        The delayed buffer will remain until the end of processing the
        current output buffer and then enqueued for processing with the
        next output buffer.

        A typical use-case is when the depayloader implementation will
        start a new output buffer for the current input RTP buffer but push
        the current output buffer first.

        Must be called with the stream lock held.
        """
    def dropped(self) -> None:
        """
            Called from `GstRTPBaseDepayload`.process or
        `GstRTPBaseDepayload`.process_rtp_packet if the depayloader does not
        use the current buffer for the output buffer. This will either drop
        the delayed buffer or the last buffer from the header extension
        cache.

        A typical use-case is when the depayloader implementation is
        dropping an input RTP buffer while waiting for the first keyframe.

        Must be called with the stream lock held.
        """
    def flush(self, keep_current: bool) -> None:
        """
            If `GstRTPBaseDepayload`.process or
        `GstRTPBaseDepayload`.process_rtp_packet drop an output buffer this
        function tells the base class to flush header extension cache as
        well.

        This will not drop an input RTP header marked as delayed from
        `gst_rtp_base_depayload_delayed`.

        If `keep_current` is True the current input RTP header will be kept
        and enqueued after flushing the previous input RTP headers.

        A typical use-case for `keep_current` is when the depayloader
        implementation invalidates the current output buffer and starts a
        new one with the current RTP input buffer.

        Must be called with the stream lock held.
        """
    def is_aggregate_hdrext_enabled(self) -> bool:
        """
        Queries whether header extensions will be aggregated per depayloaded buffers.
        """
    def is_source_info_enabled(self) -> bool:
        """
        Queries whether GstRTPSourceMeta will be added to depayloaded buffers.
        """
    def push(self, out_buf: Gst.Buffer) -> Gst.FlowReturn:
        """
            Push `out_buf` to the peer of `filter`. This function takes ownership of
        `out_buf`.

        This function will by default apply the last incoming timestamp on
        the outgoing buffer when it didn't have a timestamp already.
        """
    def push_list(self, out_list: Gst.BufferList) -> Gst.FlowReturn:
        """
            Push `out_list` to the peer of `filter`. This function takes ownership of
        `out_list`.
        """
    def set_aggregate_hdrext_enabled(self, enable: bool) -> None:
        """
        Enable or disable aggregating header extensions.
        """
    def set_source_info_enabled(self, enable: bool) -> None:
        """
        Enable or disable adding GstRTPSourceMeta to depayloaded buffers.
        """

    # python methods (overrides?)
    def do_handle_event(
        self,
        event: Gst.Event,
    ) -> bool:
        """
        handle_event(self, event:Gst.Event) -> bool
        """
    def do_packet_lost(
        self,
        event: Gst.Event,
    ) -> bool:
        """
        packet_lost(self, event:Gst.Event) -> bool
        """
    def do_process(
        self,
        in_: Gst.Buffer,
    ) -> Gst.Buffer:
        """
        process(self, in_:Gst.Buffer) -> Gst.Buffer
        """
    def do_process_rtp_packet(
        self,
        rtp_buffer: RTPBuffer,
    ) -> Gst.Buffer:
        """
        process_rtp_packet(self, rtp_buffer:GstRtp.RTPBuffer) -> Gst.Buffer
        """
    def do_set_caps(
        self,
        caps: Gst.Caps,
    ) -> bool:
        """
        set_caps(self, caps:Gst.Caps) -> bool
        """

    # Signals
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["add-extension"],
        handler: typing.Callable[[typing_extensions.Self, RTPHeaderExtension], None],
        *args: typing.Any,
    ) -> int:
        """
            Add `ext` as an extension for reading part of an RTP header extension from
        incoming RTP packets.
        """
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["clear-extensions"],
        handler: typing.Callable[..., None],
        *args: typing.Any,
    ) -> int:
        """
        Clear all RTP header extensions used by this depayloader.
        """
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["request-extension"],
        handler: typing.Callable[[typing_extensions.Self, int, str | None], RTPHeaderExtension | None],
        *args: typing.Any,
    ) -> int:
        """
            The returned `ext` must be configured with the correct `ext_id` and with the
        necessary attributes as required by the extension implementation.
        """
    @typing.overload
    def connect(
        self, detailed_signal: typing.Literal["no-more-pads"], handler: typing.Callable[..., None], *args: typing.Any
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["pad-added"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Pad], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["pad-removed"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Pad], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["deep-notify"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Object, GObject.ParamSpec], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::auto_header_extension"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::extensions"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::max_reorder"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::source_info"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::stats"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::name"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::parent"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(  # type: ignore otherwise pylance will complain and we should repeat all parent overloads here..
        self, detailed_signal: str, handler: typing.Callable[..., typing.Any], *args: typing.Any
    ) -> int: ...

class RTPBaseDepayloadClass(GObject.GPointer, metaclass=GObject.GType):
    """
    Base class for RTP depayloaders.
    """

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list | None: ...
    @builtins.property
    def handle_event(self) -> handle_eventRTPBaseDepayloadClassCB:
        """
        custom event handling
        """
    @builtins.property
    def packet_lost(self) -> packet_lostRTPBaseDepayloadClassCB:
        """
        signal the depayloader about packet loss
        """
    @builtins.property
    def parent_class(self) -> Gst.ElementClass | None:
        """
        the parent class
        """
    @builtins.property
    def process(self) -> processRTPBaseDepayloadClassCB:
        """
          process incoming rtp packets. Subclass must implement either
        this method or `process_rtp_packet` to process incoming rtp packets.
        If the child returns a buffer without a valid timestamp, the timestamp
        of the provided buffer will be applied to the result buffer and the
        buffer will be pushed. If this function returns None, nothing is pushed.
        """
    @builtins.property
    def process_rtp_packet(self) -> process_rtp_packetRTPBaseDepayloadClassCB:
        """
            Same as the process virtual function, but slightly more
        efficient, since it is passed the rtp buffer structure that has already
        been mapped (with GST_MAP_READ) by the base class and thus does not have
        to be mapped again by the subclass. Can be used by the subclass to process
        incoming rtp packets. If the subclass returns a buffer without a valid
        timestamp, the timestamp of the input buffer will be applied to the result
        buffer and the output buffer will be pushed out. If this function returns
        None, nothing is pushed out. Since: 1.6.
        """
    @builtins.property
    def set_caps(self) -> set_capsRTPBaseDepayloadClassCB:
        """
        configure the depayloader
        """

class RTPBaseDepayloadPrivate(GObject.GPointer, metaclass=GObject.GType): ...

class RTPBasePayload(Gst.Element):
    """
    Provides a base class for RTP payloaders
    """

    class Props(Gst.Element.Props):
        auto_header_extension: bool  # [auto-header-extension]: changed because contained invalid characters
        """
        If enabled, the payloader will automatically try to enable all the
        RTP header extensions provided in the src caps, saving the application
        the need to handle these extensions manually using the
        GstRTPBasePayload::request-extension: signal.
        """
        extensions: Gst.ValueArray | None
        """
        A list of already enabled RTP header extensions. This may be useful for finding
        out which extensions are already enabled (with add-extension signal) and picking a non-conflicting
        ID for a new extension that needs to be added on top of the existing ones.

        Note that the value returned by reading this property is not dynamically updated when the set of
        enabled extensions changes by any of existing action signals. Rather, it represents the current state
        at the time the property is read.

        Dynamic updates of this property can be received by subscribing to its corresponding "notify" signal, i.e.
        "notify::extensions".
        """
        max_ptime: int  # [max-ptime]: changed because contained invalid characters
        min_ptime: int  # [min-ptime]: changed because contained invalid characters
        """
        Minimum duration of the packet data in ns (can't go above MTU)
        """
        mtu: int
        onvif_no_rate_control: bool  # [onvif-no-rate-control]: changed because contained invalid characters
        """
        Make the payloader timestamp packets according to the Rate-Control=no
        behaviour specified in the ONVIF replay spec.
        """
        perfect_rtptime: bool  # [perfect-rtptime]: changed because contained invalid characters
        """
        Try to use the offset fields to generate perfect RTP timestamps. When this
        option is disabled, RTP timestamps are generated from GST_BUFFER_PTS of
        each payloaded buffer. The PTSes of buffers may not necessarily increment
        with the amount of data in each input buffer, consider e.g. the case where
        the buffer arrives from a network which means that the PTS is unrelated to
        the amount of data. Because the RTP timestamps are generated from
        GST_BUFFER_PTS this can result in RTP timestamps that also don't increment
        with the amount of data in the payloaded packet. To circumvent this it is
        possible to set the perfect rtptime option enabled. When this option is
        enabled the payloader will increment the RTP timestamps based on
        GST_BUFFER_OFFSET which relates to the amount of data in each packet
        rather than the GST_BUFFER_PTS of each buffer and therefore the RTP
        timestamps will more closely correlate with the amount of data in each
        buffer. Currently GstRTPBasePayload is limited to handling perfect RTP
        timestamps for audio streams.
        """
        pt: int
        ptime_multiple: int  # [ptime-multiple]: changed because contained invalid characters
        """
        Force buffers to be multiples of this duration in ns (0 disables)
        """
        scale_rtptime: bool  # [scale-rtptime]: changed because contained invalid characters
        """
        Make the RTP packets' timestamps be scaled with the segment's rate
        (corresponding to RTSP speed parameter). Disabling this property means
        the timestamps will not be affected by the set delivery speed (RTSP speed).

        Example: A server wants to allow streaming a recorded video in double
        speed but still have the timestamps correspond to the position in the
        video. This is achieved by the client setting RTSP Speed to 2 while the
        server has this property disabled.
        """
        seqnum: int
        seqnum_offset: int  # [seqnum-offset]: changed because contained invalid characters
        source_info: bool  # [source-info]: changed because contained invalid characters
        """
        Enable writing the CSRC field in allocated RTP header based on RTP source
        information found in the input buffer's GstRTPSourceMeta.
        """
        ssrc: int
        stats: Gst.Structure | None
        """
        Various payloader statistics retrieved atomically (and are therefore
        synchroized with each other), these can be used e.g. to generate an
        RTP-Info header. This property return a GstStructure named
        application/x-rtp-payload-stats containing the following fields relating to
        the last processed buffer and current state of the stream being payloaded:

          * `clock-rate` :#G_TYPE_UINT, clock-rate of the stream
          * `running-time` :#G_TYPE_UINT64, running time
          * `seqnum` :#G_TYPE_UINT, sequence number, same as GstRTPBasePayload:seqnum
          * `timestamp` :#G_TYPE_UINT, RTP timestamp, same as GstRTPBasePayload:timestamp
          * `ssrc` :#G_TYPE_UINT, The SSRC in use
          * `pt` :#G_TYPE_UINT, The Payload type in use, same as GstRTPBasePayload:pt
          * `seqnum-offset` :#G_TYPE_UINT, The current offset added to the seqnum
          * `timestamp-offset` :#G_TYPE_UINT, The current offset added to the timestamp
        """
        timestamp: int
        timestamp_offset: int  # [timestamp-offset]: changed because contained invalid characters

    @builtins.property
    def props(self) -> Props: ...

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list | None: ...
    @builtins.property
    def clock_rate(self) -> int: ...
    @builtins.property
    def current_ssrc(self) -> int: ...
    @builtins.property
    def dynamic(self) -> bool: ...
    @builtins.property
    def element(self) -> Gst.Element | None: ...
    @builtins.property
    def encoding_name(self) -> str: ...
    @builtins.property
    def max_ptime(self) -> int: ...
    @builtins.property
    def media(self) -> str: ...
    @builtins.property
    def min_ptime(self) -> int: ...
    @builtins.property
    def mtu(self) -> int: ...
    @builtins.property
    def priv(self) -> RTPBasePayloadPrivate | None: ...
    @builtins.property
    def pt(self) -> int: ...
    @builtins.property
    def ptime(self) -> int: ...
    @builtins.property
    def ptime_multiple(self) -> int: ...
    @builtins.property
    def segment(self) -> Gst.Segment | None: ...
    @builtins.property
    def seqnum(self) -> int: ...
    @builtins.property
    def seqnum_base(self) -> int: ...
    @builtins.property
    def seqnum_offset(self) -> int: ...
    @builtins.property
    def sinkpad(self) -> Gst.Pad | None: ...
    @builtins.property
    def srcpad(self) -> Gst.Pad | None: ...
    @builtins.property
    def ssrc(self) -> int: ...
    @builtins.property
    def timestamp(self) -> int: ...
    @builtins.property
    def ts_base(self) -> int: ...
    @builtins.property
    def ts_offset(self) -> int: ...

    # gi Methods
    def __init__(
        self,
        auto_header_extension: bool | None = None,
        max_ptime: int | None = None,
        min_ptime: int | None = None,
        mtu: int | None = None,
        name: str | None = None,
        onvif_no_rate_control: bool | None = None,
        parent: Gst.Object | None = None,
        perfect_rtptime: bool | None = None,
        pt: int | None = None,
        ptime_multiple: int | None = None,
        scale_rtptime: bool | None = None,
        seqnum_offset: int | None = None,
        source_info: bool | None = None,
        ssrc: int | None = None,
        timestamp_offset: int | None = None,
    ) -> None:
        """
        Initialize RTPBasePayload object with properties.
        """
    def allocate_output_buffer(self, payload_len: int, pad_len: int, csrc_count: int) -> Gst.Buffer:
        """
            Allocate a new GstBuffer with enough data to hold an RTP packet with
        minimum `csrc_count` CSRCs, a payload length of `payload_len` and padding of
        `pad_len`. If `payload` has GstRTPBasePayload:source-info True additional
        CSRCs may be allocated and filled with RTP source information.
        """
    def get_source_count(self, buffer: Gst.Buffer) -> int:
        """
            Count the total number of RTP sources found in the meta of `buffer`, which
        will be automically added by `gst_rtp_base_payload_allocate_output_buffer`.
        If GstRTPBasePayload:source-info is False the count will be 0.
        """
    def is_filled(self, size: int, duration: int) -> bool:
        """
            Check if the packet with `size` and `duration` would exceed the configured
        maximum size.
        """
    def is_source_info_enabled(self) -> bool:
        """
            Queries whether the payloader will add contributing sources (CSRCs) to the
        RTP header from GstRTPSourceMeta.
        """
    def push(self, buffer: Gst.Buffer) -> Gst.FlowReturn:
        """
            Push `buffer` to the peer element of the payloader. The SSRC, payload type,
        seqnum and timestamp of the RTP buffer will be updated first.

        This function takes ownership of `buffer`.
        """
    def push_list(self, list: Gst.BufferList) -> Gst.FlowReturn:
        """
            Push `list` to the peer element of the payloader. The SSRC, payload type,
        seqnum and timestamp of the RTP buffer will be updated first.

        This function takes ownership of `list`.
        """
    def set_options(self, media: str, dynamic: bool, encoding_name: str, clock_rate: int) -> None:
        """
            Set the rtp options of the payloader. These options will be set in the caps
        of the payloader. Subclasses must call this method before calling
        `gst_rtp_base_payload_push` or `gst_rtp_base_payload_set_outcaps`.
        """
    def set_outcaps_structure(self, s: Gst.Structure | None = None) -> bool:
        """
        Configure the output caps with the optional fields.
        """
    def set_source_info_enabled(self, enable: bool) -> None:
        """
            Enable or disable adding contributing sources to RTP packets from
        GstRTPSourceMeta.
        """

    # python methods (overrides?)
    def do_get_caps(
        self,
        pad: Gst.Pad,
        filter: Gst.Caps,
    ) -> Gst.Caps:
        """
        get_caps(self, pad:Gst.Pad, filter:Gst.Caps) -> Gst.Caps
        """
    def do_handle_buffer(
        self,
        buffer: Gst.Buffer,
    ) -> Gst.FlowReturn:
        """
        handle_buffer(self, buffer:Gst.Buffer) -> Gst.FlowReturn
        """
    def do_query(
        self,
        pad: Gst.Pad,
        query: Gst.Query,
    ) -> bool:
        """
        query(self, pad:Gst.Pad, query:Gst.Query) -> bool
        """
    def do_set_caps(
        self,
        caps: Gst.Caps,
    ) -> bool:
        """
        set_caps(self, caps:Gst.Caps) -> bool
        """
    def do_sink_event(
        self,
        event: Gst.Event,
    ) -> bool:
        """
        sink_event(self, event:Gst.Event) -> bool
        """
    def do_src_event(
        self,
        event: Gst.Event,
    ) -> bool:
        """
        src_event(self, event:Gst.Event) -> bool
        """

    # Signals
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["add-extension"],
        handler: typing.Callable[[typing_extensions.Self, RTPHeaderExtension], None],
        *args: typing.Any,
    ) -> int:
        """
            Add `ext` as an extension for writing part of an RTP header extension onto
        outgoing RTP packets.
        """
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["clear-extensions"],
        handler: typing.Callable[..., None],
        *args: typing.Any,
    ) -> int:
        """
        Clear all RTP header extensions used by this payloader.
        """
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["request-extension"],
        handler: typing.Callable[[typing_extensions.Self, int, str], RTPHeaderExtension | None],
        *args: typing.Any,
    ) -> int:
        """
            The returned `ext` must be configured with the correct `ext_id` and with the
        necessary attributes as required by the extension implementation.
        """
    @typing.overload
    def connect(
        self, detailed_signal: typing.Literal["no-more-pads"], handler: typing.Callable[..., None], *args: typing.Any
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["pad-added"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Pad], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["pad-removed"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Pad], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["deep-notify"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Object, GObject.ParamSpec], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::auto_header_extension"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::extensions"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::max_ptime"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::min_ptime"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::mtu"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::onvif_no_rate_control"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::perfect_rtptime"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::pt"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::ptime_multiple"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::scale_rtptime"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::seqnum"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::seqnum_offset"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::source_info"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::ssrc"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::stats"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::timestamp"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::timestamp_offset"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::name"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::parent"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(  # type: ignore otherwise pylance will complain and we should repeat all parent overloads here..
        self, detailed_signal: str, handler: typing.Callable[..., typing.Any], *args: typing.Any
    ) -> int: ...

class RTPBasePayloadClass(GObject.GPointer, metaclass=GObject.GType):
    """
    Base class for audio RTP payloader.
    """

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list | None: ...
    @builtins.property
    def get_caps(self) -> get_capsRTPBasePayloadClassCB:
        """
        get desired caps
        """
    @builtins.property
    def handle_buffer(self) -> handle_bufferRTPBasePayloadClassCB:
        """
        process data
        """
    @builtins.property
    def parent_class(self) -> Gst.ElementClass | None:
        """
        the parent class
        """
    @builtins.property
    def query(self) -> queryRTPBasePayloadClassCB:
        """
        custom query handling
        """
    @builtins.property
    def set_caps(self) -> set_capsRTPBasePayloadClassCB:
        """
        configure the payloader
        """
    @builtins.property
    def sink_event(self) -> sink_eventRTPBasePayloadClassCB:
        """
        custom event handling on the sinkpad
        """
    @builtins.property
    def src_event(self) -> src_eventRTPBasePayloadClassCB:
        """
        custom event handling on the srcpad
        """

class RTPBasePayloadPrivate(GObject.GPointer, metaclass=GObject.GType): ...

class RTPBuffer(GObject.GPointer, metaclass=GObject.GType):
    """
    The GstRTPBuffer helper functions makes it easy to parse and create regular
    GstBuffer objects that contain RTP payloads. These buffers are typically of
    'application/x-rtp' GstCaps.
    """

    # gi Fields
    buffer: Gst.Buffer | None = ...  # type: ignore
    """
    pointer to RTP buffer
    """
    data: list | None = ...
    """
    array of data
    """
    size: list | None = ...
    """
    array of size
    """
    state: int = ...
    """
    internal state
    """

    # gi Methods
    def add_extension_onebyte_header(self, id: int, data: bytes) -> bool:
        """
            Adds a RFC 5285 header extension with a one byte header to the end of the
        RTP header. If there is already a RFC 5285 header extension with a one byte
        header, the new extension will be appended.
        It will not work if there is already a header extension that does not follow
        the mechanism described in RFC 5285 or if there is a header extension with
        a two bytes header as described in RFC 5285. In that case, use
        `gst_rtp_buffer_add_extension_twobytes_header`
        """
    def add_extension_twobytes_header(self, appbits: int, id: int, data: bytes) -> bool:
        """
            Adds a RFC 5285 header extension with a two bytes header to the end of the
        RTP header. If there is already a RFC 5285 header extension with a two bytes
        header, the new extension will be appended.
        It will not work if there is already a header extension that does not follow
        the mechanism described in RFC 5285 or if there is a header extension with
        a one byte header as described in RFC 5285. In that case, use
        `gst_rtp_buffer_add_extension_onebyte_header`
        """
    @staticmethod
    def allocate_data(buffer: Gst.Buffer, payload_len: int, pad_len: int, csrc_count: int) -> None:
        """
            Allocate enough data in `buffer` to hold an RTP packet with `csrc_count` CSRCs,
        a payload length of `payload_len` and padding of `pad_len`.
        `buffer` must be writable and all previous memory in `buffer` will be freed.
        If `pad_len` is >0, the padding bit will be set. All other RTP header fields
        will be set to 0/False.
        """
    @staticmethod
    def calc_header_len(csrc_count: int) -> int:
        """
            Calculate the header length of an RTP packet with `csrc_count` CSRC entries.
        An RTP packet can have at most 15 CSRC entries.
        """
    @staticmethod
    def calc_packet_len(payload_len: int, pad_len: int, csrc_count: int) -> int:
        """
            Calculate the total length of an RTP packet with a payload size of `payload_len`,
        a padding of `pad_len` and a `csrc_count` CSRC entries.
        """
    @staticmethod
    def calc_payload_len(packet_len: int, pad_len: int, csrc_count: int) -> int:
        """
            Calculate the length of the payload of an RTP packet with size `packet_len`,
        a padding of `pad_len` and a `csrc_count` CSRC entries.
        """
    @staticmethod
    def compare_seqnum(seqnum1: int, seqnum2: int) -> int:
        """
            Compare two sequence numbers, taking care of wraparounds. This function
        returns the difference between `seqnum1` and `seqnum2`.
        """
    @staticmethod
    def default_clock_rate(payload_type: int) -> int:
        """
        Get the default clock-rate for the static payload type `payload_type`.
        """
    @staticmethod
    def ext_timestamp(exttimestamp: int, timestamp: int) -> tuple[int, int]:
        """
            Update the `exttimestamp` field with the extended timestamp of `timestamp`
        For the first call of the method, `exttimestamp` should point to a location
        with a value of -1.

        This function is able to handle both forward and backward timestamps taking
        into account:
          - timestamp wraparound making sure that the returned value is properly increased.
          - timestamp unwraparound making sure that the returned value is properly decreased.
        """
    def get_csrc(self, idx: int) -> int:
        """
        Get the CSRC at index `idx` in `buffer`.
        """
    def get_csrc_count(self) -> int:
        """
        Get the CSRC count of the RTP packet in `buffer`.
        """
    def get_extension(self) -> bool:
        """
        Check if the extension bit is set on the RTP packet in `buffer`.
        """
    def get_extension_data(self) -> tuple[GLib.Bytes | None, int]:
        """
            Get the extension data. `bits` will contain the extension 16 bits of custom
        data. `data` will point to the data in the extension and `wordlen` will contain
        the length of `data` in 32 bits words.

        If `buffer` did not contain an extension, this function will return False
        with `bits`, `data` and `wordlen` unchanged.
        """
    def get_extension_onebyte_header(self, id: int, nth: int) -> tuple[bool, bytes]:
        """
            Parses RFC 5285 style header extensions with a one byte header. It will
        return the nth extension with the requested id.
        """
    @staticmethod
    def get_extension_onebyte_header_from_bytes(
        bytes: GLib.Bytes, bit_pattern: int, id: int, nth: int
    ) -> tuple[bool, bytes]:
        """
            Similar to gst_rtp_buffer_get_extension_onebyte_header, but working
        on the GBytes you get from gst_rtp_buffer_get_extension_bytes.
        Parses RFC 5285 style header extensions with a one byte header. It will
        return the nth extension with the requested id.
        """
    def get_extension_twobytes_header(self, id: int, nth: int) -> tuple[bool, int, bytes]:
        """
            Parses RFC 5285 style header extensions with a two bytes header. It will
        return the nth extension with the requested id.
        """
    def get_header_len(self) -> int:
        """
            Return the total length of the header in `buffer`. This include the length of
        the fixed header, the CSRC list and the extension header.
        """
    def get_marker(self) -> bool:
        """
        Check if the marker bit is set on the RTP packet in `buffer`.
        """
    def get_packet_len(self) -> int:
        """
        Return the total length of the packet in `buffer`.
        """
    def get_padding(self) -> bool:
        """
        Check if the padding bit is set on the RTP packet in `buffer`.
        """
    def get_payload(self) -> GLib.Bytes | None:
        """
            Get a pointer to the payload data in `buffer`. This pointer is valid as long
        as a reference to `buffer` is held.
        """
    def get_payload_buffer(self) -> Gst.Buffer:
        """
            Create a buffer of the payload of the RTP packet in `buffer`. This function
        will internally create a subbuffer of `buffer` so that a memcpy can be
        avoided.
        """
    def get_payload_len(self) -> int:
        """
        Get the length of the payload of the RTP packet in `buffer`.
        """
    def get_payload_subbuffer(self, offset: int, len: int) -> Gst.Buffer:
        """
            Create a subbuffer of the payload of the RTP packet in `buffer`. `offset` bytes
        are skipped in the payload and the subbuffer will be of size `len`.
        If `len` is -1 the total payload starting from `offset` is subbuffered.
        """
    def get_payload_type(self) -> int:
        """
        Get the payload type of the RTP packet in `buffer`.
        """
    def get_seq(self) -> int:
        """
        Get the sequence number of the RTP packet in `buffer`.
        """
    def get_ssrc(self) -> int:
        """
        Get the SSRC of the RTP packet in `buffer`.
        """
    def get_timestamp(self) -> int:
        """
        Get the timestamp of the RTP packet in `buffer`.
        """
    def get_version(self) -> int:
        """
        Get the version number of the RTP packet in `buffer`.
        """
    @staticmethod
    def map(buffer: Gst.Buffer, flags: Gst.MapFlags) -> tuple[bool, RTPBuffer]:
        """
        Map the contents of `buffer` into `rtp`.
        """
    @staticmethod
    def new_allocate(payload_len: int, pad_len: int, csrc_count: int) -> Gst.Buffer:
        """
            Allocate a new GstBuffer with enough data to hold an RTP packet with
        `csrc_count` CSRCs, a payload length of `payload_len` and padding of `pad_len`.
        All other RTP header fields will be set to 0/False.
        """
    @staticmethod
    def new_allocate_len(packet_len: int, pad_len: int, csrc_count: int) -> Gst.Buffer:
        """
            Create a new GstBuffer that can hold an RTP packet that is exactly
        `packet_len` long. The length of the payload depends on `pad_len` and
        `csrc_count` and can be calculated with `gst_rtp_buffer_calc_payload_len`.
        All RTP header fields will be set to 0/False.
        """
    @staticmethod
    def new_copy_data(data: bytes) -> Gst.Buffer:
        """
            Create a new buffer and set the data to a copy of `len`
        bytes of `data` and the size to `len`. The data will be freed when the buffer
        is freed.
        """
    @staticmethod
    def new_take_data(data: bytes) -> Gst.Buffer:
        """
            Create a new buffer and set the data and size of the buffer to `data` and `len`
        respectively. `data` will be freed when the buffer is unreffed, so this
        function transfers ownership of `data` to the new buffer.
        """
    def pad_to(self, len: int) -> None:
        """
            Set the amount of padding in the RTP packet in `buffer` to
        `len`. If `len` is 0, the padding is removed.

        NOTE: This function does not work correctly.
        """
    def remove_extension_data(self) -> None:
        """
            Unsets the extension bit of the RTP buffer and removes the extension header
        and data.

        If the RTP buffer has no header extension data, the action has no effect.
        The RTP buffer must be mapped READWRITE only once and the underlying
        GstBuffer must be writable.
        """
    def set_csrc(self, idx: int, csrc: int) -> None:
        """
        Modify the CSRC at index `idx` in `buffer` to `csrc`.
        """
    def set_extension(self, extension: bool) -> None:
        """
        Set the extension bit on the RTP packet in `buffer` to `extension`.
        """
    def set_extension_data(self, bits: int, length: int) -> bool:
        """
            Set the extension bit of the rtp buffer and fill in the `bits` and `length` of the
        extension header. If the existing extension data is not large enough, it will
        be made larger.

        Will also shorten the extension data from 1.20.
        """
    def set_marker(self, marker: bool) -> None:
        """
        Set the marker bit on the RTP packet in `buffer` to `marker`.
        """
    def set_packet_len(self, len: int) -> None:
        """
            Set the total `rtp` size to `len`. The data in the buffer will be made
        larger if needed. Any padding will be removed from the packet.
        """
    def set_padding(self, padding: bool) -> None:
        """
        Set the padding bit on the RTP packet in `buffer` to `padding`.
        """
    def set_payload_type(self, payload_type: int) -> None:
        """
        Set the payload type of the RTP packet in `buffer` to `payload_type`.
        """
    def set_seq(self, seq: int) -> None:
        """
        Set the sequence number of the RTP packet in `buffer` to `seq`.
        """
    def set_ssrc(self, ssrc: int) -> None:
        """
        Set the SSRC on the RTP packet in `buffer` to `ssrc`.
        """
    def set_timestamp(self, timestamp: int) -> None:
        """
        Set the timestamp of the RTP packet in `buffer` to `timestamp`.
        """
    def set_version(self, version: int) -> None:
        """
        Set the version of the RTP packet in `buffer` to `version`.
        """
    def unmap(self) -> None:
        """
        Unmap `rtp` previously mapped with `gst_rtp_buffer_map`.
        """

class RTPHeaderExtension(Gst.Element):
    """
    Instance struct for a RTP Audio/Video header extension.
    """

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list | None: ...
    @builtins.property
    def parent(self) -> Gst.Element | None:
        """
        the parent GObject
        """

    # gi Methods
    def __init__(self, name: str | None = None, parent: Gst.Object | None = None) -> None:
        """
        Initialize RTPHeaderExtension object with properties.
        """
    @staticmethod
    def create_from_uri(uri: str) -> RTPHeaderExtension | None: ...
    def get_direction(self) -> RTPHeaderExtensionDirection:
        """
        Retrieve the direction
        """
    def get_id(self) -> int: ...
    def get_max_size(self, input_meta: Gst.Buffer) -> int:
        """
            This is used to know how much data a certain header extension will need for
        both allocating the resulting data, and deciding how much payload data can
        be generated.

        Implementations should return as accurate a value as is possible using the
        information given in the input `buffer`.
        """
    def get_sdp_caps_field_name(self) -> str: ...
    def get_supported_flags(self) -> RTPHeaderExtensionFlags: ...
    def get_uri(self) -> str | None: ...
    def read(self, read_flags: RTPHeaderExtensionFlags, data: bytes, buffer: Gst.Buffer) -> bool:
        """
        Read the RTP header extension from `data`.
        """
    def set_attributes_from_caps(self, caps: Gst.Caps) -> bool:
        """
            `gst_rtp_header_extension_set_id` must have been called with a valid
        extension id that is contained in these caps.

        The only current known caps format is based on the SDP standard as produced
        by `gst_sdp_media_attributes_to_caps`.
        """
    def set_caps_from_attributes(self, caps: Gst.Caps) -> bool:
        """
            `gst_rtp_header_extension_set_id` must have been called with a valid
        extension id that is contained in these caps.

        The only current known caps format is based on the SDP standard as produced
        by `gst_sdp_media_attributes_to_caps`.
        """
    def set_caps_from_attributes_helper(self, caps: Gst.Caps, attributes: str) -> bool:
        """
            Helper implementation for GstRTPExtensionClass::set_caps_from_attributes
        that sets the `ext` uri on caps with the specified extension id as required
        for sdp GstCaps.

        Requires that the extension does not have any attributes or direction
        advertised in `caps`.
        """
    def set_direction(self, direction: RTPHeaderExtensionDirection) -> None:
        """
            Set the direction that this header extension should be used in.
        If GST_RTP_HEADER_EXTENSION_DIRECTION_INHERITED is included, the
        direction will not be included in the caps (as it shouldn't be in the
        extmap line in the SDP).
        """
    def set_id(self, ext_id: int) -> None:
        """
        sets the RTP extension id on `ext`
        """
    def set_non_rtp_sink_caps(self, caps: Gst.Caps) -> bool:
        """
            Passes RTP payloader's sink (i.e. not payloaded) `caps` to the header
        extension.
        """
    def set_wants_update_non_rtp_src_caps(self, state: bool) -> None:
        """
            Call this function in a subclass from GstRTPHeaderExtensionClass::read to
        tell the depayloader whether the data just parsed from RTP packet require
        updating its src (non-RTP) caps. If `state` is True, GstRTPBaseDepayload will
        eventually invoke `gst_rtp_header_extension_update_non_rtp_src_caps` to
        have the caps update applied. Applying the update also flips the internal
        "wants update" flag back to False.
        """
    def update_non_rtp_src_caps(self, caps: Gst.Caps) -> bool:
        """
            Updates depayloader src caps based on the information received in RTP header.
        `caps` must be writable as this function may modify them.
        """
    def wants_update_non_rtp_src_caps(self) -> bool:
        """
            Call this function after `gst_rtp_header_extension_read` to check if
        the depayloader's src caps need updating with data received in the last RTP
        packet.
        """
    def write(
        self, input_meta: Gst.Buffer, write_flags: RTPHeaderExtensionFlags, output: Gst.Buffer, data: bytes
    ) -> int:
        """
            Writes the RTP header extension to `data` using information available from
        the `input_meta`.  `data` will be sized to be at least the value returned
        from `gst_rtp_header_extension_get_max_size`.
        """

    # python methods (overrides?)
    def do_get_max_size(
        self,
        input_meta: Gst.Buffer,
    ) -> int:
        """
        get_max_size(self, input_meta:Gst.Buffer) -> int
        """
    def do_get_supported_flags(
        self,
    ) -> RTPHeaderExtensionFlags:
        """
        get_supported_flags(self) -> GstRtp.RTPHeaderExtensionFlags
        """
    def do_read(
        self,
        read_flags: RTPHeaderExtensionFlags,
        data: list[int],
        buffer: Gst.Buffer,
    ) -> bool:
        """
        read(self, read_flags:GstRtp.RTPHeaderExtensionFlags, data:list, buffer:Gst.Buffer) -> bool
        """
    def do_set_attributes(
        self,
        direction: RTPHeaderExtensionDirection,
        attributes: str,
    ) -> bool:
        """
        set_attributes(self, direction:GstRtp.RTPHeaderExtensionDirection, attributes:str) -> bool
        """
    def do_set_caps_from_attributes(
        self,
        caps: Gst.Caps,
    ) -> bool:
        """
        set_caps_from_attributes(self, caps:Gst.Caps) -> bool
        """
    def do_set_non_rtp_sink_caps(
        self,
        caps: Gst.Caps,
    ) -> bool:
        """
        set_non_rtp_sink_caps(self, caps:Gst.Caps) -> bool
        """
    def do_update_non_rtp_src_caps(
        self,
        caps: Gst.Caps,
    ) -> bool:
        """
        update_non_rtp_src_caps(self, caps:Gst.Caps) -> bool
        """
    def do_write(
        self,
        input_meta: Gst.Buffer,
        write_flags: RTPHeaderExtensionFlags,
        output: Gst.Buffer,
        data: list[int],
    ) -> int:
        """
        write(self, input_meta:Gst.Buffer, write_flags:GstRtp.RTPHeaderExtensionFlags, output:Gst.Buffer, data:list) -> int
        """
    @classmethod
    def set_uri(
        cls,
        uri: str,
    ) -> None:
        """
        set_uri(self, uri:str)
        """

    # Signals
    @typing.overload
    def connect(
        self, detailed_signal: typing.Literal["no-more-pads"], handler: typing.Callable[..., None], *args: typing.Any
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["pad-added"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Pad], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["pad-removed"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Pad], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["deep-notify"],
        handler: typing.Callable[[typing_extensions.Self, Gst.Object, GObject.ParamSpec], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::name"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(
        self,
        detailed_signal: typing.Literal["notify::parent"],
        handler: typing.Callable[[typing_extensions.Self, GObject.ParamSpec, typing.Any], None],
        *args: typing.Any,
    ) -> int: ...
    @typing.overload
    def connect(  # type: ignore otherwise pylance will complain and we should repeat all parent overloads here..
        self, detailed_signal: str, handler: typing.Callable[..., typing.Any], *args: typing.Any
    ) -> int: ...

class RTPHeaderExtensionClass(GObject.GPointer, metaclass=GObject.GType):
    """
    Base class for RTP Header extensions.
    """

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list | None: ...
    @builtins.property
    def get_max_size(self) -> get_max_sizeRTPHeaderExtensionClassCB:
        """
        retrieve the maximum size for this extension based on the
        information available from input_meta.  Implementations should attempt
        to provide as accurate information as possible as the returned value
        will be used to control the amount of possible data in the payload.
        Implementations must return the maximum as the allocated size for
        writing the extension will be at least the size of the returned value.
        Return the amount of data read or <0 on failure.
        """
    @builtins.property
    def get_supported_flags(self) -> get_supported_flagsRTPHeaderExtensionClassCB:
        """
        retrieve the supported flags
        """
    @builtins.property
    def parent_class(self) -> Gst.ElementClass | None:
        """
        the parent class
        """
    @builtins.property
    def read(self) -> readRTPHeaderExtensionClassCB:
        """
        read from a rtp payloaded buffer and extract the extension
        information, optionally adding some meta onto the output buffer.
        """
    @builtins.property
    def set_attributes(self) -> set_attributesRTPHeaderExtensionClassCB:
        """
        set the necessary attributes that may be signaled e.g. with
        an SDP.
        """
    @builtins.property
    def set_caps_from_attributes(self) -> set_caps_from_attributesRTPHeaderExtensionClassCB:
        """
        write the necessary caps field/s for the configured
        attributes e.g. as signalled with SDP.
        """
    @builtins.property
    def set_non_rtp_sink_caps(self) -> set_non_rtp_sink_capsRTPHeaderExtensionClassCB:
        """
        read any information from sink caps that the header
        extension needs for its function.
        """
    @builtins.property
    def update_non_rtp_src_caps(self) -> update_non_rtp_src_capsRTPHeaderExtensionClassCB:
        """
        update depayloader non-RTP (depayloaded) caps with
        the information parsed from RTP header.
        """
    @builtins.property
    def write(self) -> writeRTPHeaderExtensionClassCB:
        """
        write into `data` the information for this extension.  Various
        information is provided to help writing extensions in particular cases.
        """

    # gi Methods
    def set_uri(self, uri: str) -> None:
        """
        Set the URI for this RTP header extension implementation.
        """

class RTPPayloadInfo(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure holding default payload type information.
    """

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list | None: ...
    bitrate: int = ...
    """
    the bitrate of the media. 0 = unknown/variable.
    """
    clock_rate: int = ...
    """
    default clock rate, 0 = unknown/variable
    """
    encoding_name: str = ...
    """
    the encoding name of `pt`
    """
    encoding_parameters: str = ...
    """
    encoding parameters. For audio this is the number of
    channels. None = not applicable.
    """
    media: str = ...
    """
    the media type(s), usually "audio", "video", "application", "text",
    "message".
    """
    payload_type: int = ...
    """
    payload type, -1 means dynamic
    """

    # gi Methods
    @staticmethod
    def for_name(media: str, encoding_name: str) -> RTPPayloadInfo | None:
        """
            Get the GstRTPPayloadInfo for `media` and `encoding_name`. This function is
        mostly used to get the default clock-rate and bandwidth for dynamic payload
        types specified with `media` and `encoding` name.

        The search for `encoding_name` will be performed in a case insensitive way.
        """
    @staticmethod
    def for_pt(payload_type: int) -> RTPPayloadInfo | None:
        """
            Get the GstRTPPayloadInfo for `payload_type`. This function is
        mostly used to get the default clock-rate and bandwidth for static payload
        types specified with `payload_type`.
        """

class RTPSourceMeta(GObject.GPointer, metaclass=GObject.GType):
    """
    Meta describing the source(s) of the buffer.
    """

    # gi Fields
    csrc: list | None = ...
    """
    pointer to the CSRCs
    """
    csrc_count: int = ...
    """
    number of elements in `csrc`
    """
    meta: Gst.Meta | None = ...  # type: ignore
    """
    parent GstMeta
    """
    ssrc: int = ...
    """
    the SSRC
    """
    ssrc_valid: bool = ...
    """
    whether `ssrc` is set and valid
    """

    # gi Methods
    def append_csrc(self, csrc: list[int]) -> bool:
        """
        Appends `csrc` to the list of contributing sources in `meta`.
        """
    @staticmethod
    def get_info() -> Gst.MetaInfo: ...
    def get_source_count(self) -> int:
        """
        Count the total number of RTP sources found in `meta`, both SSRC and CSRC.
        """
    def set_ssrc(self, ssrc: int | None = None) -> bool:
        """
        Sets `ssrc` in `meta`. If `ssrc` is None the ssrc of `meta` will be unset.
        """

###############################################################
# Callbacks
###############################################################

class set_capsRTPBaseDepayloadClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPBaseDepayloadClass.set_caps
    """
    #  filter
    def __call__(
        self,
        filter: RTPBaseDepayload,
        caps: Gst.Caps,
    ) -> bool: ...

class processRTPBaseDepayloadClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPBaseDepayloadClass.process
    """
    #  base
    def __call__(
        self,
        base: RTPBaseDepayload,
        in_: Gst.Buffer,
    ) -> Gst.Buffer: ...

class packet_lostRTPBaseDepayloadClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPBaseDepayloadClass.packet_lost
    """
    #  filter
    def __call__(
        self,
        filter: RTPBaseDepayload,
        event: Gst.Event,
    ) -> bool: ...

class handle_eventRTPBaseDepayloadClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPBaseDepayloadClass.handle_event
    """
    #  filter
    def __call__(
        self,
        filter: RTPBaseDepayload,
        event: Gst.Event,
    ) -> bool: ...

class process_rtp_packetRTPBaseDepayloadClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPBaseDepayloadClass.process_rtp_packet
    """
    #  base
    def __call__(
        self,
        base: RTPBaseDepayload,
        rtp_buffer: RTPBuffer,
    ) -> Gst.Buffer: ...

class get_capsRTPBasePayloadClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPBasePayloadClass.get_caps
    """
    #  payload
    def __call__(
        self,
        payload: RTPBasePayload,
        pad: Gst.Pad,
        filter: Gst.Caps,
    ) -> Gst.Caps: ...

class set_capsRTPBasePayloadClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPBasePayloadClass.set_caps
    """
    #  payload
    def __call__(
        self,
        payload: RTPBasePayload,
        caps: Gst.Caps,
    ) -> bool: ...

class handle_bufferRTPBasePayloadClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPBasePayloadClass.handle_buffer
    """
    #  payload
    def __call__(
        self,
        payload: RTPBasePayload,
        buffer: Gst.Buffer,
    ) -> Gst.FlowReturn: ...

class sink_eventRTPBasePayloadClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPBasePayloadClass.sink_event
    """
    #  payload
    def __call__(
        self,
        payload: RTPBasePayload,
        event: Gst.Event,
    ) -> bool: ...

class src_eventRTPBasePayloadClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPBasePayloadClass.src_event
    """
    #  payload
    def __call__(
        self,
        payload: RTPBasePayload,
        event: Gst.Event,
    ) -> bool: ...

class queryRTPBasePayloadClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPBasePayloadClass.query
    """
    #  payload
    def __call__(
        self,
        payload: RTPBasePayload,
        pad: Gst.Pad,
        query: Gst.Query,
    ) -> bool: ...

class get_supported_flagsRTPHeaderExtensionClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPHeaderExtensionClass.get_supported_flags
    """
    #  ext
    def __call__(
        self,
        ext: RTPHeaderExtension,
    ) -> RTPHeaderExtensionFlags: ...

class get_max_sizeRTPHeaderExtensionClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPHeaderExtensionClass.get_max_size
    """
    #  ext
    def __call__(
        self,
        ext: RTPHeaderExtension,
        input_meta: Gst.Buffer,
    ) -> int: ...

class writeRTPHeaderExtensionClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPHeaderExtensionClass.write
    """
    #  ext
    def __call__(
        self,
        ext: RTPHeaderExtension,
        input_meta: Gst.Buffer,
        write_flags: RTPHeaderExtensionFlags,
        output: Gst.Buffer,
        data: bytes,
    ) -> int: ...

class readRTPHeaderExtensionClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPHeaderExtensionClass.read
    """
    #  ext
    def __call__(
        self,
        ext: RTPHeaderExtension,
        read_flags: RTPHeaderExtensionFlags,
        data: bytes,
        buffer: Gst.Buffer,
    ) -> bool: ...

class set_non_rtp_sink_capsRTPHeaderExtensionClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPHeaderExtensionClass.set_non_rtp_sink_caps
    """
    #  ext
    def __call__(
        self,
        ext: RTPHeaderExtension,
        caps: Gst.Caps,
    ) -> bool: ...

class update_non_rtp_src_capsRTPHeaderExtensionClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPHeaderExtensionClass.update_non_rtp_src_caps
    """
    #  ext
    def __call__(
        self,
        ext: RTPHeaderExtension,
        caps: Gst.Caps,
    ) -> bool: ...

class set_attributesRTPHeaderExtensionClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPHeaderExtensionClass.set_attributes
    """
    #  ext
    def __call__(
        self,
        ext: RTPHeaderExtension,
        direction: RTPHeaderExtensionDirection,
        attributes: str,
    ) -> bool: ...

class set_caps_from_attributesRTPHeaderExtensionClassCB(typing.Protocol):
    """
    This callback was used in:
        RTPHeaderExtensionClass.set_caps_from_attributes
    """
    #  ext
    def __call__(
        self,
        ext: RTPHeaderExtension,
        caps: Gst.Caps,
    ) -> bool: ...

###############################################################
# Aliases
###############################################################

_lock = _thread._lock  # type: ignore
###############################################################
# Constants
###############################################################

RTCP_MAX_BYE_SSRC_COUNT: int = ...
RTCP_MAX_RB_COUNT: int = ...
RTCP_MAX_SDES: int = ...
RTCP_MAX_SDES_ITEM_COUNT: int = ...
RTCP_REDUCED_SIZE_VALID_MASK: int = ...
RTCP_VALID_MASK: int = ...
RTCP_VALID_VALUE: int = ...
RTCP_VERSION: int = ...
RTP_HDREXT_BASE: str = ...
RTP_HDREXT_ELEMENT_CLASS: str = ...
RTP_HDREXT_NTP_56: str = ...
RTP_HDREXT_NTP_56_SIZE: int = ...
RTP_HDREXT_NTP_64: str = ...
RTP_HDREXT_NTP_64_SIZE: int = ...
RTP_HEADER_EXTENSION_URI_METADATA_KEY: str = ...
RTP_PAYLOAD_1016_STRING: str = ...
RTP_PAYLOAD_CELLB_STRING: str = ...
RTP_PAYLOAD_CN_STRING: str = ...
RTP_PAYLOAD_DVI4_11025_STRING: str = ...
RTP_PAYLOAD_DVI4_16000_STRING: str = ...
RTP_PAYLOAD_DVI4_22050_STRING: str = ...
RTP_PAYLOAD_DVI4_8000_STRING: str = ...
RTP_PAYLOAD_DYNAMIC_STRING: str = ...
RTP_PAYLOAD_G721_STRING: str = ...
RTP_PAYLOAD_G722_STRING: str = ...
RTP_PAYLOAD_G723_53: int = ...
RTP_PAYLOAD_G723_53_STRING: str = ...
RTP_PAYLOAD_G723_63: int = ...
RTP_PAYLOAD_G723_63_STRING: str = ...
RTP_PAYLOAD_G723_STRING: str = ...
RTP_PAYLOAD_G728_STRING: str = ...
RTP_PAYLOAD_G729_STRING: str = ...
RTP_PAYLOAD_GSM_STRING: str = ...
RTP_PAYLOAD_H261_STRING: str = ...
RTP_PAYLOAD_H263_STRING: str = ...
RTP_PAYLOAD_JPEG_STRING: str = ...
RTP_PAYLOAD_L16_MONO_STRING: str = ...
RTP_PAYLOAD_L16_STEREO_STRING: str = ...
RTP_PAYLOAD_LPC_STRING: str = ...
RTP_PAYLOAD_MP2T_STRING: str = ...
RTP_PAYLOAD_MPA_STRING: str = ...
RTP_PAYLOAD_MPV_STRING: str = ...
RTP_PAYLOAD_NV_STRING: str = ...
RTP_PAYLOAD_PCMA_STRING: str = ...
RTP_PAYLOAD_PCMU_STRING: str = ...
RTP_PAYLOAD_QCELP_STRING: str = ...
RTP_PAYLOAD_TS41: int = ...
RTP_PAYLOAD_TS41_STRING: str = ...
RTP_PAYLOAD_TS48: int = ...
RTP_PAYLOAD_TS48_STRING: str = ...
RTP_SOURCE_META_MAX_CSRC_COUNT: int = ...
RTP_VERSION: int = ...
_namespace: str = ...
_version: str = ...
