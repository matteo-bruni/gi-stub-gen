# pyright: reportIncompatibleMethodOverride=false
"""
Stub template for a GI Repository Module.

Generated by gi-stub-gen, version 0.0.1.
Module: GstCodecParsers
Date: 2026-07-02
"""
# we disable method override report for pyright in generated stubs,
# causes too many false positives

from __future__ import annotations
from typing_extensions import deprecated  # noqa: F401
import typing_extensions  # noqa: F401

import _thread
import builtins
import enum

# gi.repository imports needed by this Stub
from gi.repository import GObject
from gi.repository import Gst
###############################################################
# Functions
###############################################################

def av1_bit_writer_frame_header_obu(
    frame_hdr: AV1FrameHeaderOBU,
    seq_hdr: AV1SequenceHeaderOBU,
    temporal_id: int,
    spatial_id: int,
    size_field: bool,
    size: int,
) -> tuple[AV1BitWriterResult, int, int]:
    """
    Generating the according AV1 bit stream OBU by providing the frame header.
    """
    ...

def av1_bit_writer_frame_header_obu_with_offsets(
    frame_hdr: AV1FrameHeaderOBU,
    seq_hdr: AV1SequenceHeaderOBU,
    temporal_id: int,
    spatial_id: int,
    size_field: bool,
    size_field_size: int,
    size: int,
) -> tuple[AV1BitWriterResult, int, int, int, int, int, int, int]:
    """
    While Generating the according AV1 bit stream OBU by providing the frame
    header, this function also return bit offsets of qindex, segmentation and
    cdef, etc. These offsets can help to change the content of these fields
    later. This function is useful if the encoder may change the content of
    the frame header after generating it. For example, some HW needs user to
    provide a frame header before the real encoding job, and it will change
    the according fields in the frame header during the real encoding job.
    """
    ...

def av1_bit_writer_metadata_obu(
    metadata: AV1MetadataOBU,
    temporal_id: int,
    spatial_id: int,
    size_field: bool,
    size: int,
) -> tuple[AV1BitWriterResult, int, int]:
    """
    Generating the according AV1 bit stream OBU by providing the meta data.
    """
    ...

def av1_bit_writer_sequence_header_obu(
    seq_hdr: AV1SequenceHeaderOBU,
    size_field: bool,
    size: int,
) -> tuple[AV1BitWriterResult, int, int]:
    """
    Generating the according AV1 bit stream OBU by providing the sequence header.
    """
    ...

def av1_bit_writer_temporal_delimiter_obu(
    size_field: bool,
    size: int,
) -> tuple[AV1BitWriterResult, int, int]:
    """
    Generating the according temporal delimiter AV1 bit stream OBU.
    """
    ...

def buffer_add_lcevc_meta(
    buffer: Gst.Buffer,
    enhancement_data: Gst.Buffer,
) -> LcevcMeta:
    """
    Attaches GstLcevcMeta metadata to `buffer`.
    """
    ...

def buffer_add_mpeg_video_meta(
    buffer: Gst.Buffer,
    seq_hdr: MpegVideoSequenceHdr,
    seq_ext: MpegVideoSequenceExt,
    disp_ext: MpegVideoSequenceDisplayExt,
    pic_hdr: MpegVideoPictureHdr,
    pic_ext: MpegVideoPictureExt,
    quant_ext: MpegVideoQuantMatrixExt,
) -> MpegVideoMeta:
    """
    Creates and adds a GstMpegVideoMeta to a `buffer`.
    Provided structures must either be None or GSlice-allocated.
    """
    ...

def buffer_get_lcevc_meta(
    buffer: Gst.Buffer,
) -> LcevcMeta | None:
    """
    Find the GstLcevcMeta on `buffer` with the lowest `id`.

    Buffers can contain multiple GstLcevcMeta metadata items when dealing with
    multiview buffers.
    """
    ...

def buffer_get_lcevc_meta_id(
    buffer: Gst.Buffer,
    id: int,
) -> LcevcMeta | None:
    """
    Find the GstLcevcMeta on `buffer` with the given `id`.

    Buffers can contain multiple GstLcevcMeta metadata items when dealing with
    multiview buffers.
    """
    ...

def h263_parse(
    packet: Mpeg4Packet,
    data: int,
    offset: int,
    size: int,
) -> Mpeg4ParseResult:
    """
    Parses `data` and fills `packet` with the information of the next packet
    found.

    Note that the type of the packet is meaningless in this case.
    """
    ...

def h264_bit_writer_aud(
    primary_pic_type: int,
    start_code: bool,
    size: int,
) -> tuple[H264BitWriterResult, int, int]:
    """
    Generating the according h264 bit stream of an aud.
    """
    ...

def h264_bit_writer_convert_to_nal(
    nal_prefix_size: int,
    packetized: bool,
    has_startcode: bool,
    add_trailings: bool,
    raw_data: int,
    raw_size: int,
    nal_size: int,
) -> tuple[H264BitWriterResult, int, int]:
    """
    Converting a bit stream into a real nal packet. If the bit stream already
    has a start code, it will be replaced by the new one specified by the
    `nal_prefix_size` and `packetized`. It is assured that the output aligns to
    the byte and the all the emulations are inserted.
    """
    ...

def h264_bit_writer_filler(
    start_code: bool,
    num: int,
    data: int,
    size: int,
) -> tuple[H264BitWriterResult, int, int]:
    """
    Generates the according h264 bit stream nal bitstream of a filler with `size`.
    """
    ...

def h264_bit_writer_pps(
    pps: H264PPS,
    start_code: bool,
    size: int,
) -> tuple[H264BitWriterResult, int, int]:
    """
    Generating the according h264 bit stream by providing the pps.
    """
    ...

def h264_bit_writer_sei(
    sei_messages: list[H264SEIMessage],
    start_code: bool,
    size: int,
) -> tuple[H264BitWriterResult, int, int]:
    """
    Generating the according h264 bit stream by providing sei messages.
    """
    ...

def h264_bit_writer_slice_hdr(
    slice: H264SliceHdr,
    start_code: bool,
    nal_type: H264NalUnitType,
    is_ref: bool,
    size: int,
) -> tuple[H264BitWriterResult, int, int, int]:
    """
    Generating the according h264 bit stream by providing the slice header.
    """
    ...

def h264_bit_writer_sps(
    sps: H264SPS,
    start_code: bool,
    size: int,
) -> tuple[H264BitWriterResult, int, int]:
    """
    Generating the according h264 bit stream by providing the sps.
    """
    ...

def h264_create_sei_memory(
    start_code_prefix_length: int,
    messages: list[H264SEIMessage],
) -> Gst.Memory:
    """
    Creates raw byte-stream format (a.k.a Annex B type) SEI nal unit data
    from `messages`
    """
    ...

def h264_create_sei_memory_avc(
    nal_length_size: int,
    messages: list[H264SEIMessage],
) -> Gst.Memory:
    """
    Creates raw packetized format SEI nal unit data from `messages`
    """
    ...

def h264_parse_pps(
    nalparser: H264NalParser,
    nalu: H264NalUnit,
    pps: H264PPS,
) -> H264ParserResult:
    """
    Parses `data`, and fills the `pps` structure.

    The resulting `pps` data structure shall be deallocated with the
    `gst_h264_pps_clear` function when it is no longer needed, or prior
    to parsing a new PPS NAL unit.
    """
    ...

def h264_parse_sps(
    nalu: H264NalUnit,
    sps: H264SPS,
) -> H264ParserResult:
    """
    Parses `data`, and fills the `sps` structure.
    """
    ...

def h264_parse_subset_sps(
    nalu: H264NalUnit,
    sps: H264SPS,
) -> H264ParserResult:
    """
    Parses `data`, and fills in the `sps` structure.

    This function fully parses `data` and allocates all the necessary
    data structures needed for MVC extensions. The resulting `sps`
    structure shall be deallocated with `gst_h264_sps_clear` when it is
    no longer needed.

    Note: if the caller doesn't need any of the MVC-specific data, then
    `gst_h264_parser_parse_sps` is more efficient because those extra
    syntax elements are not parsed and no extra memory is allocated.
    """
    ...

def h264_parser_identify_and_split_nalu_avc(
    nalparser: H264NalParser,
    data: int,
    offset: int,
    size: int,
    nal_length_size: int,
    nalus: list[H264NalUnit],
) -> tuple[H264ParserResult, int]:
    """
    Parses `data` for packetized (e.g., avc/avc3) bitstream and
    sets `nalus`. In addition to nal identifying process,
    this method scans start-code prefix to split malformed packet into
    actual nal chunks.
    """
    ...

def h264_parser_identify_nalu(
    nalparser: H264NalParser,
    data: int,
    offset: int,
    size: int,
    nalu: H264NalUnit,
) -> H264ParserResult:
    """
    Parses the headers of an Annex B coded NAL unit from `data` and puts the
    result into `nalu`.
    """
    ...

def h264_parser_identify_nalu_avc(
    nalparser: H264NalParser,
    data: int,
    offset: int,
    size: int,
    nal_length_size: int,
    nalu: H264NalUnit,
) -> H264ParserResult:
    """
    Parses the headers of an AVC coded NAL unit from `data` and puts the result
    into `nalu`.
    """
    ...

def h264_parser_identify_nalu_unchecked(
    nalparser: H264NalParser,
    data: int,
    offset: int,
    size: int,
    nalu: H264NalUnit,
) -> H264ParserResult:
    """
    Parses `data` and fills `nalu` from the next nalu data from `data`.

    This differs from `gst_h264_parser_identify_nalu` in that it doesn't
    check whether the packet is complete or not.

    Note: Only use this function if you already know the provided `data`
    is a complete NALU, else use `gst_h264_parser_identify_nalu`.
    """
    ...

def h264_parser_insert_sei(
    nalparser: H264NalParser,
    au: Gst.Buffer,
    sei: Gst.Memory,
) -> Gst.Buffer | None:
    """
    Copy `au` into new GstBuffer and insert `sei` into the GstBuffer.
    The validation for completeness of `au` and `sei` is caller's responsibility.
    Both `au` and `sei` must be byte-stream formatted
    """
    ...

def h264_parser_insert_sei_avc(
    nalparser: H264NalParser,
    nal_length_size: int,
    au: Gst.Buffer,
    sei: Gst.Memory,
) -> Gst.Buffer | None:
    """
    Copy `au` into new GstBuffer and insert `sei` into the GstBuffer.
    The validation for completeness of `au` and `sei` is caller's responsibility.
    Nal prefix type of both `au` and `sei` must be packetized, and
    also the size of nal length field must be identical to `nal_length_size`
    """
    ...

def h264_parser_parse_decoder_config_record(
    nalparser: H264NalParser,
    data: int,
    size: int,
) -> tuple[H264ParserResult, H264DecoderConfigRecord]:
    """
    Parses AVCDecoderConfigurationRecord data and fill into `config`.
    The caller must free `config` via `gst_h264_decoder_config_record_free`

    This method does not parse SPS and PPS and therefore the caller needs to
    parse each NAL unit via appropriate parsing method.
    """
    ...

def h264_parser_parse_nal(
    nalparser: H264NalParser,
    nalu: H264NalUnit,
) -> H264ParserResult:
    """
    This function should be called in the case one doesn't need to
    parse a specific structure. It is necessary to do so to make
    sure `nalparser` is up to date.
    """
    ...

def h264_parser_parse_pps(
    nalparser: H264NalParser,
    nalu: H264NalUnit,
    pps: H264PPS,
) -> H264ParserResult:
    """
    Parses `nalu` containing a Picture Parameter Set, and fills `pps`.

    The resulting `pps` data structure must be deallocated by the caller using
    `gst_h264_pps_clear`.
    """
    ...

def h264_parser_parse_sei(
    nalparser: H264NalParser,
    nalu: H264NalUnit,
    messages: list[H264SEIMessage],
) -> H264ParserResult:
    """
    Parses `nalu` containing one or more Supplementary Enhancement Information messages,
    and allocates and fills the `messages` array.
    """
    ...

def h264_parser_parse_slice_hdr(
    nalparser: H264NalParser,
    nalu: H264NalUnit,
    slice: H264SliceHdr,
    parse_pred_weight_table: bool,
    parse_dec_ref_pic_marking: bool,
) -> H264ParserResult:
    """
    Parses `nalu` containing a coded slice, and fills `slice`.
    """
    ...

def h264_parser_parse_sps(
    nalparser: H264NalParser,
    nalu: H264NalUnit,
    sps: H264SPS,
) -> H264ParserResult:
    """
    Parses `nalu` containing a Sequence Parameter Set, and fills `sps`.
    """
    ...

def h264_parser_parse_subset_sps(
    nalparser: H264NalParser,
    nalu: H264NalUnit,
    sps: H264SPS,
) -> H264ParserResult:
    """
    Parses `data`, and fills in the `sps` structure.

    This function fully parses `data` and allocates all the necessary
    data structures needed for MVC extensions. The resulting `sps`
    structure shall be deallocated with `gst_h264_sps_clear` when it is
    no longer needed.

    Note: if the caller doesn't need any of the MVC-specific data, then
    `gst_h264_parser_parse_sps` is more efficient because those extra
    syntax elements are not parsed and no extra memory is allocated.
    """
    ...

def h264_parser_update_pps(
    nalparser: H264NalParser,
    pps: H264PPS,
) -> H264ParserResult:
    """
    Replace internal Picture Parameter Set struct corresponding to id of `pps`
    with `pps`. `nalparser` will mark `pps` as last parsed pps.
    """
    ...

def h264_parser_update_sps(
    nalparser: H264NalParser,
    sps: H264SPS,
) -> H264ParserResult:
    """
    Replace internal Sequence Parameter Set struct corresponding to id of `sps`
    with `sps`. `nalparser` will mark `sps` as last parsed sps.
    """
    ...

def h264_profile_from_string(
    profile: str,
) -> H264Profile:
    """
    Returns a GstH264Profile for the `string`.
    """
    ...

def h264_quant_matrix_4x4_get_raster_from_zigzag(
    quant: int,
) -> int:
    """
    Converts quantization matrix `quant` from zigzag scan order to
    raster scan order and store the resulting factors into `out_quant`.

    Note: it is an error to pass the same table in both `quant` and
    `out_quant` arguments.
    """
    ...

def h264_quant_matrix_4x4_get_zigzag_from_raster(
    quant: int,
) -> int:
    """
    Converts quantization matrix `quant` from raster scan order to
    zigzag scan order and store the resulting factors into `out_quant`.

    Note: it is an error to pass the same table in both `quant` and
    `out_quant` arguments.
    """
    ...

def h264_quant_matrix_8x8_get_raster_from_zigzag(
    quant: int,
) -> int:
    """
    Converts quantization matrix `quant` from zigzag scan order to
    raster scan order and store the resulting factors into `out_quant`.

    Note: it is an error to pass the same table in both `quant` and
    `out_quant` arguments.
    """
    ...

def h264_quant_matrix_8x8_get_zigzag_from_raster(
    quant: int,
) -> int:
    """
    Converts quantization matrix `quant` from raster scan order to
    zigzag scan order and store the resulting factors into `out_quant`.

    Note: it is an error to pass the same table in both `quant` and
    `out_quant` arguments.
    """
    ...

def h264_sei_clear(
    sei: H264SEIMessage,
) -> None:
    """
    sei: The GstH264SEIMessage to clear
    Frees allocated data in `sei` if any.
    """
    ...

def h264_slice_type_to_string(
    slice_type: H264SliceType,
) -> str | None:
    """
    Returns the descriptive name for the GstH264SliceType.
    """
    ...

def h264_video_calculate_framerate(
    sps: H264SPS,
    field_pic_flag: int,
    pic_struct: int,
) -> tuple[int, int]:
    """
    Calculate framerate of a video sequence using `sps` VUI information,
    `field_pic_flag` from a slice header and `pic_struct` from GstH264PicTiming SEI
    message.

    If framerate is variable or can't be determined, `fps_num` will be set to 0
    and `fps_den` to 1.
    """
    ...

def h265_create_sei_memory(
    layer_id: int,
    temporal_id_plus1: int,
    start_code_prefix_length: int,
    messages: list[H265SEIMessage],
) -> Gst.Memory:
    """
    Creates raw byte-stream format (a.k.a Annex B type) SEI nal unit data
    from `messages`
    """
    ...

def h265_create_sei_memory_hevc(
    layer_id: int,
    temporal_id_plus1: int,
    nal_length_size: int,
    messages: list[H265SEIMessage],
) -> Gst.Memory:
    """
    Creates raw packetized format SEI nal unit data from `messages`
    """
    ...

def h265_get_profile_from_sps(
    sps: H265SPS,
) -> H265Profile:
    """
    Return the H265 profile from `sps`.
    """
    ...

def h265_parse_pps(
    parser: H265Parser,
    nalu: H265NalUnit,
    pps: H265PPS,
) -> H265ParserResult:
    """
    Parses `data`, and fills the `pps` structure.
    """
    ...

def h265_parse_sps(
    parser: H265Parser,
    nalu: H265NalUnit,
    sps: H265SPS,
    parse_vui_params: bool,
) -> H265ParserResult:
    """
    parser: The GstH265Parser
    Parses `data`, and fills the `sps` structure.
    """
    ...

def h265_parse_sps_ext(
    parser: H265Parser,
    nalu: H265NalUnit,
    sps: H265SPS,
    sps_ext: H265SPSEXT,
    parse_vui_params: bool,
) -> H265ParserResult:
    """
    parser: The GstH265Parser
    Parses `data`, and fills the `sps` structure.
    """
    ...

def h265_parse_vps(
    nalu: H265NalUnit,
    vps: H265VPS,
) -> H265ParserResult:
    """
    Parses `data`, and fills the `vps` structure.
    """
    ...

def h265_profile_from_string(
    string: str,
) -> H265Profile:
    """
    Returns a GstH265Profile for the `string`.
    """
    ...

def h265_profile_to_string(
    profile: H265Profile,
) -> str | None:
    """
    Returns the descriptive name for the GstH265Profile.
    """
    ...

def h265_quant_matrix_4x4_get_raster_from_uprightdiagonal(
    quant: int,
) -> int:
    """
    Converts quantization matrix `quant` from uprightdiagonal scan order to
    raster scan order and store the resulting factors into `out_quant`.

    Note: it is an error to pass the same table in both `quant` and
    `out_quant` arguments.
    """
    ...

def h265_quant_matrix_4x4_get_raster_from_zigzag(
    quant: int,
) -> int:
    """
    Converts quantization matrix `quant` from zigzag scan order to
    raster scan order and store the resulting factors into `out_quant`.

    Note: it is an error to pass the same table in both `quant` and
    `out_quant` arguments.
    """
    ...

def h265_quant_matrix_4x4_get_uprightdiagonal_from_raster(
    quant: int,
) -> int:
    """
    Converts quantization matrix `quant` from raster scan order to
    uprightdiagonal scan order and store the resulting factors
    into `out_quant`.

    Note: it is an error to pass the same table in both `quant` and
    `out_quant` arguments.
    """
    ...

def h265_quant_matrix_4x4_get_zigzag_from_raster(
    quant: int,
) -> int:
    """
    Converts quantization matrix `quant` from raster scan order to
    zigzag scan order and store the resulting factors into `out_quant`.

    Note: it is an error to pass the same table in both `quant` and
    `out_quant` arguments.
    """
    ...

def h265_quant_matrix_8x8_get_raster_from_uprightdiagonal(
    quant: int,
) -> int:
    """
    Converts quantization matrix `quant` from uprightdiagonal scan order to
    raster scan order and store the resulting factors into `out_quant`.

    Note: it is an error to pass the same table in both `quant` and
    `out_quant` arguments.
    """
    ...

def h265_quant_matrix_8x8_get_raster_from_zigzag(
    quant: int,
) -> int:
    """
    Converts quantization matrix `quant` from zigzag scan order to
    raster scan order and store the resulting factors into `out_quant`.

    Note: it is an error to pass the same table in both `quant` and
    `out_quant` arguments.
    """
    ...

def h265_quant_matrix_8x8_get_uprightdiagonal_from_raster(
    quant: int,
) -> int:
    """
    Converts quantization matrix `quant` from raster scan order to
    uprightdiagonal scan order and store the resulting factors
    into `out_quant`.

    Note: it is an error to pass the same table in both `quant` and
    `out_quant` arguments.
    """
    ...

def h265_quant_matrix_8x8_get_zigzag_from_raster(
    quant: int,
) -> int:
    """
    Converts quantization matrix `quant` from raster scan order to
    zigzag scan order and store the resulting factors into `out_quant`.

    Note: it is an error to pass the same table in both `quant` and
    `out_quant` arguments.
    """
    ...

def h265_sei_copy(
    dest_sei: H265SEIMessage,
    src_sei: H265SEIMessage,
) -> bool:
    """
    Copies `src_sei` into `dst_sei`
    """
    ...

def h265_sei_free(
    sei: H265SEIMessage,
) -> None:
    """
    sei: The GstH265SEIMessage to free
    Frees `sei` fields.
    """
    ...

def h265_slice_type_to_string(
    slice_type: H265SliceType,
) -> str | None:
    """
    Returns the descriptive name for the GstH265SliceType.
    """
    ...

def h266_parse_aps(
    parser: H266Parser,
    nalu: H266NalUnit,
    aps: H266APS,
) -> H266ParserResult:
    """
    Parses `data`, and fills the `aps` structure.
    """
    ...

def h266_parse_pps(
    parser: H266Parser,
    nalu: H266NalUnit,
    pps: H266PPS,
) -> H266ParserResult:
    """
    Parses `data`, and fills the `pps` structure.
    """
    ...

def h266_parse_sps(
    parser: H266Parser,
    nalu: H266NalUnit,
    sps: H266SPS,
) -> H266ParserResult:
    """
    Parses `data`, and fills the `sps` structure.
    """
    ...

def h266_parse_vps(
    nalu: H266NalUnit,
    vps: H266VPS,
) -> H266ParserResult:
    """
    Parses `data`, and fills the `vps` structure.
    """
    ...

def h266_profile_from_string(
    string: str,
) -> H266Profile:
    """
    Returns a GstH266Profile for the `string`.
    """
    ...

def h266_profile_to_string(
    profile: H266Profile,
) -> str | None:
    """
    Returns the descriptive name for the GstH266Profile.
    """
    ...

def jpeg2000_colorspace_from_string(
    colorspace_string: str,
) -> JPEG2000Colorspace: ...
def jpeg2000_colorspace_to_string(
    colorspace: JPEG2000Colorspace,
) -> str: ...
def jpeg2000_sampling_from_string(
    sampling_string: str,
) -> JPEG2000Sampling: ...
def jpeg2000_sampling_is_mono(
    sampling: JPEG2000Sampling,
) -> bool: ...
def jpeg2000_sampling_is_rgb(
    sampling: JPEG2000Sampling,
) -> bool: ...
def jpeg2000_sampling_is_yuv(
    sampling: JPEG2000Sampling,
) -> bool: ...
def jpeg2000_sampling_to_string(
    sampling: JPEG2000Sampling,
) -> str: ...
def jpeg_bit_writer_frame_header(
    frame_hdr: JpegFrameHdr,
    marker: JpegMarker,
    size: int,
) -> tuple[JpegBitWriterResult, int, int]:
    """
    Generating the according JPEG bit stream by providing the frame header.
    """
    ...

def jpeg_bit_writer_huffman_table(
    huff_tables: JpegHuffmanTables,
    size: int,
) -> tuple[JpegBitWriterResult, int, int]:
    """
    Generating the according JPEG bit stream by providing the huffman tables.
    """
    ...

def jpeg_bit_writer_quantization_table(
    quant_tables: JpegQuantTables,
    size: int,
) -> tuple[JpegBitWriterResult, int, int]:
    """
    Generating the according JPEG bit stream by providing the quantization tables.
    """
    ...

def jpeg_bit_writer_restart_interval(
    interval: int,
    size: int,
) -> tuple[JpegBitWriterResult, int, int]:
    """
    Generating the according JPEG bit stream by providing the interval value.
    """
    ...

def jpeg_bit_writer_scan_header(
    scan_hdr: JpegScanHdr,
    size: int,
) -> tuple[JpegBitWriterResult, int, int]:
    """
    Generating the according JPEG bit stream by providing the scan header.
    """
    ...

def jpeg_bit_writer_segment_with_data(
    marker: JpegMarker,
    seg_data: int | None,
    seg_size: int,
    size: int,
) -> tuple[JpegBitWriterResult, int, int]:
    """
    Generating the bit stream for a JPEG segment.
    """
    ...

def jpeg_get_default_huffman_tables() -> JpegHuffmanTables:
    """
    Fills in `huf_tables` with the default AC/DC Huffman tables, as
    specified by the JPEG standard.
    """
    ...

def jpeg_get_default_quantization_tables(
    quant_tables: JpegQuantTables,
) -> None: ...
def jpeg_parse(
    data: int,
    size: int,
    offset: int,
) -> tuple[bool, JpegSegment]:
    """
    Parses the JPEG bitstream contained in `data`, and returns the
    detected segment as a GstJpegSegment.

    Note that a valid segment may be returned with a length that exceeds
    the available data. It is up to the caller to make sure there's enough
    data available when parsing the segment.
    """
    ...

def lcevc_meta_api_get_type() -> GObject.GType:
    """
    Gets the GType of the LCEVC meta API.
    """
    ...

def lcevc_meta_get_info() -> Gst.MetaInfo:
    """
    Gets the GstMetaInfo of the LCEVC meta.
    """
    ...

def mpeg4_parse(
    packet: Mpeg4Packet,
    skip_user_data: bool,
    vop: Mpeg4VideoObjectPlane,
    data: int,
    offset: int,
    size: int,
) -> Mpeg4ParseResult:
    """
    Parses `data` and fills `packet` with the information of the next packet
    found.
    """
    ...

def mpeg4_parse_group_of_vop(
    gov: Mpeg4GroupOfVOP,
    data: int,
    size: int,
) -> Mpeg4ParseResult:
    """
    Parses `data` containing the group of video object plane packet, and fills
    the `gov` structure.
    """
    ...

def mpeg4_parse_video_object_layer(
    vol: Mpeg4VideoObjectLayer,
    vo: Mpeg4VisualObject,
    data: int,
    size: int,
) -> Mpeg4ParseResult:
    """
    Parses `data` containing the video object layer packet, and fills
    the `vol` structure.
    """
    ...

def mpeg4_parse_video_object_plane(
    vop: Mpeg4VideoObjectPlane,
    sprite_trajectory: Mpeg4SpriteTrajectory,
    vol: Mpeg4VideoObjectLayer,
    data: int,
    size: int,
) -> Mpeg4ParseResult:
    """
    Parses `data` containing the video object plane packet, and fills the `vol`
    structure.
    """
    ...

def mpeg4_parse_video_packet_header(
    videopackethdr: Mpeg4VideoPacketHdr,
    vol: Mpeg4VideoObjectLayer,
    vop: Mpeg4VideoObjectPlane,
    sprite_trajectory: Mpeg4SpriteTrajectory,
    data: int,
    size: int,
) -> Mpeg4ParseResult:
    """
    Parsers `data` containing the video packet header
    and fills the `videopackethdr` structure
    """
    ...

def mpeg4_parse_video_plane_short_header(
    shorthdr: Mpeg4VideoPlaneShortHdr,
    data: int,
    size: int,
) -> Mpeg4ParseResult: ...
def mpeg4_parse_visual_object(
    vo: Mpeg4VisualObject,
    signal_type: Mpeg4VideoSignalType,
    data: int,
    size: int,
) -> Mpeg4ParseResult:
    """
    Parses `data` containing the visual object packet, and fills
    the `vo` structure.
    """
    ...

def mpeg4_parse_visual_object_sequence(
    vos: Mpeg4VisualObjectSequence,
    data: int,
    size: int,
) -> Mpeg4ParseResult:
    """
    Parses `data` containing the visual object sequence packet, and fills
    the `vos` structure.
    """
    ...

def mpeg_video_finalise_mpeg2_sequence_header(
    hdr: MpegVideoSequenceHdr,
    seqext: MpegVideoSequenceExt,
    displayext: MpegVideoSequenceDisplayExt,
) -> bool: ...
def mpeg_video_meta_api_get_type() -> GObject.GType: ...
def mpeg_video_meta_get_info() -> Gst.MetaInfo: ...
def mpeg_video_parse(
    packet: MpegVideoPacket,
    data: int,
    size: int,
    offset: int,
) -> bool:
    """
    Parses the MPEG 1/2 video bitstream contained in `data`, and returns the
    offset, and if known also the size, in `packet`. This function will scan
    the data to find the next packet if needed.
    """
    ...

def mpeg_video_quant_matrix_get_raster_from_zigzag(
    quant: int,
) -> int:
    """
    Converts quantization matrix `quant` from zigzag scan order to
    raster scan order and store the resulting factors into `out_quant`.

    Note: it is an error to pass the same table in both `quant` and
    `out_quant` arguments.
    """
    ...

def mpeg_video_quant_matrix_get_zigzag_from_raster(
    quant: int,
) -> int:
    """
    Converts quantization matrix `quant` from raster scan order to
    zigzag scan order and store the resulting factors into `out_quant`.

    Note: it is an error to pass the same table in both `quant` and
    `out_quant` arguments.
    """
    ...

def vc1_bitplanes_ensure_size(
    bitplanes: VC1BitPlanes,
    seqhdr: VC1SeqHdr,
) -> bool:
    """
    Fills the `bitplanes` structure from `seqhdr`, this function
    should be called after #gst_vc1_parse_sequence_header if
    in simple or main mode, or after #gst_vc1_parse_entry_point_header
    if in advanced mode.
    """
    ...

def vc1_bitplanes_free(
    bitplanes: VC1BitPlanes,
) -> None: ...
def vc1_bitplanes_free_1(
    bitplanes: VC1BitPlanes,
) -> None: ...
def vc1_identify_next_bdu(
    data: int,
    size: int,
) -> tuple[VC1ParserResult, VC1BDU]:
    """
    Parses `data` and fills `bdu` fields
    """
    ...

def vc1_parse_entry_point_header(
    data: int,
    size: int,
    seqhdr: VC1SeqHdr,
) -> tuple[VC1ParserResult, VC1EntryPointHdr]:
    """
    Parses `data`, and sets `entrypoint` fields.
    """
    ...

def vc1_parse_field_header(
    data: int,
    size: int,
    fieldhdr: VC1FrameHdr,
    seqhdr: VC1SeqHdr,
    bitplanes: VC1BitPlanes,
) -> VC1ParserResult:
    """
    Parses `data`, and fills `fieldhdr` fields.
    """
    ...

def vc1_parse_frame_header(
    data: int,
    size: int,
    framehdr: VC1FrameHdr,
    seqhdr: VC1SeqHdr,
    bitplanes: VC1BitPlanes,
) -> VC1ParserResult:
    """
    Parses `data`, and fills `entrypoint` fields.
    """
    ...

def vc1_parse_frame_layer(
    data: int,
    size: int,
    framelayer: VC1FrameLayer,
) -> VC1ParserResult:
    """
    Parses `data`, and fills `framelayer` fields.
    """
    ...

def vc1_parse_sequence_header(
    data: int,
    size: int,
    seqhdr: VC1SeqHdr,
) -> VC1ParserResult:
    """
    Parses `data`, and fills `seqhdr` fields.
    """
    ...

def vc1_parse_sequence_header_struct_a(
    data: int,
    size: int,
    structa: VC1SeqStructA,
) -> VC1ParserResult:
    """
    Parses `data`, and fills `structa` fields.
    """
    ...

def vc1_parse_sequence_header_struct_b(
    data: int,
    size: int,
    structb: VC1SeqStructB,
) -> VC1ParserResult:
    """
    Parses `data`, and fills `structb` fields.
    """
    ...

def vc1_parse_sequence_header_struct_c(
    data: int,
    size: int,
    structc: VC1SeqStructC,
) -> VC1ParserResult:
    """
    Parses `data`, and fills `structc` fields.
    """
    ...

def vc1_parse_sequence_layer(
    data: int,
    size: int,
    seqlayer: VC1SeqLayer,
) -> VC1ParserResult:
    """
    Parses `data`, and fills `seqlayer` fields.
    """
    ...

def vc1_parse_slice_header(
    data: int,
    size: int,
    slicehdr: VC1SliceHdr,
    seqhdr: VC1SeqHdr,
) -> VC1ParserResult:
    """
    Parses `data`, and fills `slicehdr` fields.
    """
    ...

###############################################################
# Enums/Flags
###############################################################

class AV1BitWriterResult(enum.IntEnum):
    """
    The result of writing AV1 data into bit stream."""

    OK = 0
    """
    The writing succeeded
    """
    INVALID_DATA = 1
    """
    The input data to write is invalid
    """
    NO_MORE_SPACE = 2
    """
    The output does not have enough size
    """
    ERROR = 3
    """
    An general error occurred when writing
    """

class AV1ChromaSamplePositions(enum.IntEnum):
    UNKNOWN = 0
    """
    Unknown (in this case the source video transfer
     function must be signaled outside the AV1 bitstream).
    """
    VERTICAL = 1
    """
    Horizontally co-located with (0, 0) luma sample,
     vertical position in the middle between two luma samples.
    """
    COLOCATED = 2
    """
    co-located with (0, 0) luma sample.
    """
    RESERVED = 3
    """
    For future use.
    """

class AV1ColorPrimaries(enum.IntEnum):
    BT_709 = 1
    """
    BT.709
    """
    UNSPECIFIED = 2
    """
    Unspecified
    """
    BT_470_M = 4
    """
    BT.470 System M (historical)
    """
    BT_470_B_G = 5
    """
    BT.470 System B, G (historical),
    """
    BT_601 = 6
    """
    BT.601
    """
    SMPTE_240 = 7
    """
    SMPTE 240
    """
    GENERIC_FILM = 8
    """
    Generic film (color filters using illuminant C,
    """
    BT_2020 = 9
    """
    BT.2020, BT.2100,
    """
    XYZ = 10
    """
    SMPTE 428 (CIE 1921 XYZ),
    """
    SMPTE_431 = 11
    """
    SMPTE RP 431-2
    """
    SMPTE_432 = 12
    """
    SMPTE EG 432-1
    """
    EBU_3213 = 22
    """
    EBU Tech. 3213-E
    """

class AV1FrameRestorationType(enum.IntEnum):
    NONE = 0
    """
    no filtering is applied
    """
    WIENER = 1
    """
    Wiener filter process is invoked
    """
    SGRPROJ = 2
    """
    self guided filter proces is invoked
    """
    SWITCHABLE = 3
    """
    restoration filter is swichtable
    """

class AV1FrameType(enum.IntEnum):
    KEY_FRAME = 0
    """
    Key Frame
    """
    INTER_FRAME = 1
    """
    InterFrame
    """
    INTRA_ONLY_FRAME = 2
    """
    Intra-Only Frame
    """
    SWITCH_FRAME = 3
    """
    Switch Frame
    """

class AV1InterpolationFilter(enum.IntEnum):
    EIGHTTAP = 0
    """
    Eighttap
    """
    EIGHTTAP_SMOOTH = 1
    """
    Eighttap Smooth
    """
    EIGHTTAP_SHARP = 2
    """
    Eighttap Sharp
    """
    BILINEAR = 3
    """
    Bilinear
    """
    SWITCHABLE = 4
    """
    Filter is swichtable
    """

class AV1MatrixCoefficients(enum.IntEnum):
    IDENTITY = 0
    """
    Identity matrix
    """
    BT_709 = 1
    """
    BT.709
    """
    UNSPECIFIED = 2
    """
    Unspecified
    """
    RESERVED_3 = 3
    """
    For future use
    """
    FCC = 4
    """
    US FCC 73.628
    """
    BT_470_B_G = 5
    """
    BT.470 System B, G (historical)
    """
    BT_601 = 6
    """
    BT.601
    """
    SMPTE_240 = 7
    """
    SMPTE 240 M
    """
    SMPTE_YCGCO = 8
    """
    YCgCo
    """
    BT_2020_NCL = 9
    """
    BT.2020 non-constant luminance, BT.2100 YCbCr
    """
    BT_2020_CL = 10
    """
    BT.2020 constant luminance
    """
    SMPTE_2085 = 11
    """
    SMPTE ST 2085 YDzDx
    """
    CHROMAT_NCL = 12
    """
    Chromaticity-derived non-constant luminance
    """
    CHROMAT_CL = 13
    """
    Chromaticity-derived constant luminancw
    """
    ICTCP = 14
    """
    BT.2100 ICtCp
    """

class AV1MetadataType(enum.IntEnum):
    RESERVED_0 = 0
    """
    Reserved 0
    """
    HDR_CLL = 1
    """
    Metadata high dynamic range content
      light level semantics
    """
    HDR_MDCV = 2
    """
    Metadata high dynamic range mastering
      display color volume semantics
    """
    SCALABILITY = 3
    """
    Metadata scalability semantics
    """
    ITUT_T35 = 4
    """
    Metadata ITUT T35 semantics
    """
    TIMECODE = 5
    """
    Timecode semantics
    """

class AV1OBUType(enum.IntEnum):
    """
    Defines all the possible OBU types"""

    RESERVED_0 = 0
    """
    Reserved 0
    """
    SEQUENCE_HEADER = 1
    """
    Sequence Header OBU
    """
    TEMPORAL_DELIMITER = 2
    """
    Temporal Delimiter OBU
    """
    FRAME_HEADER = 3
    """
    Frame Header OBU
    """
    TILE_GROUP = 4
    """
    Tile Group OBU
    """
    METADATA = 5
    """
    Metadata OBU
    """
    FRAME = 6
    """
    Frame OBU (includes Frame Header and one Tile Group)
    """
    REDUNDANT_FRAME_HEADER = 7
    """
    Redundant Frame Header OBU
    """
    TILE_LIST = 8
    """
    Tile LIst OBU
    """
    RESERVED_9 = 9
    """
    Reserved 9
    """
    RESERVED_10 = 10
    """
    Reserved 10
    """
    RESERVED_11 = 11
    """
    Reserved 11
    """
    RESERVED_12 = 12
    """
    Reserved 12
    """
    RESERVED_13 = 13
    """
    Reserved 13
    """
    RESERVED_14 = 14
    """
    Reserved 14
    """
    PADDING = 15
    """
    Padding
    """

class AV1ParserResult(enum.IntEnum):
    """
    Defines the result of parser process"""

    OK = 0
    """
    successful return
    """
    NO_MORE_DATA = 1
    """
    the parser needs more data for one OBU
    """
    DROP = 2
    """
    no need to handle this OBU, skip it
    """
    BITSTREAM_ERROR = 3
    """
    stream error, for example, include invalid bits
    """
    MISSING_OBU_REFERENCE = 4
    """
    no reference, for example, no sequence found
    """
    INVALID_OPERATION = 5
    """
    something like invalid parameters
    """

class AV1Profile(enum.IntEnum):
    """
    Defines the AV1 profiles"""

    _0 = 0  # [0]: changed because started with a number
    """
    8-bit and 10-bit 4:2:0 and 4:0:0 only.
    """
    _1 = 1  # [1]: changed because started with a number
    """
    8-bit and 10-bit 4:4:4.
    """
    _2 = 2  # [2]: changed because started with a number
    """
    8-bit and 10-bit 4:2:2, 12-bit 4:0:0 4:2:2 and 4:4:4
    """
    UNDEFINED = 3
    """
    unknow AV1 profile
    """

class AV1ReferenceFrame(enum.IntEnum):
    REF_INTRA_FRAME = 0
    """
    Intra Frame Reference
    """
    REF_LAST_FRAME = 1
    """
    Last Reference Frame
    """
    REF_LAST2_FRAME = 2
    """
    Last2 Reference Frame
    """
    REF_LAST3_FRAME = 3
    """
    Last3 Reference Frame
    """
    REF_GOLDEN_FRAME = 4
    """
    Golden Reference Frame
    """
    REF_BWDREF_FRAME = 5
    """
    BWD Reference Frame
    """
    REF_ALTREF2_FRAME = 6
    """
    Alternative2 Reference Frame
    """
    REF_ALTREF_FRAME = 7
    """
    Alternative Reference Frame
    """
    NUM_REF_FRAMES = 8
    """
    Total Reference Frame Number
    """

class AV1ScalabilityModes(enum.IntEnum):
    L1T2 = 0
    """
    1 spatial layer, 2 temporal layers
    """
    L1T3 = 1
    """
    1 spatial layer, 3 temporal layers
    """
    L2T1 = 2
    """
    2 spatial layer (ratio 2:1), 1 temporal layer,
     inter-layer dependency
    """
    L2T2 = 3
    """
    2 spatial layer (ratio 2:1), 2 temporal layer,
     inter-layer dependency
    """
    L2T3 = 4
    """
    2 spatial layer (ratio 2:1), 3 temporal layer,
     inter-layer dependency
    """
    S2T1 = 5
    """
    2 spatial layer (ratio 2:1), 1 temporal layer
    """
    S2T2 = 6
    """
    2 spatial layer (ratio 2:1), 2 temporal layer
    """
    S2T3 = 7
    """
    2 spatial layer (ratio 2:1), 3 temporal layer
    """
    L2T1H = 8
    """
    2 spatial layer (ratio 1.5:1), 1 temporal layer,
     inter-layer dependency
    """
    L2T2H = 9
    """
    2 spatial layer (ratio 1.5:1), 2 temporal layer,
     inter-layer dependency
    """
    L2T3H = 10
    """
    2 spatial layer (ratio 1.5:1), 3 temporal layer,
     inter-layer dependency
    """
    S2T1H = 11
    """
    2 spatial layer (ratio 1.5:1), 1 temporal layer
    """
    S2T2H = 12
    """
    2 spatial layer (ratio 1.5:1), 2 temporal layer
    """
    S2T3H = 13
    """
    2 spatial layer (ratio 1.5:1), 3 temporal layer
    """
    SS = 14
    """
    Use scalability structure GstAV1MetadataScalability
    """

class AV1SeqLevels(enum.IntEnum):
    """
    Defines all the possible OBU types"""

    LEVEL_2_0 = 0
    """
    Level 2.0
    """
    LEVEL_2_1 = 1
    """
    Level 2.1
    """
    LEVEL_2_2 = 2
    """
    Level 2.2
    """
    LEVEL_2_3 = 3
    """
    Level 2.3
    """
    LEVEL_3_0 = 4
    """
    Level 3.0
    """
    LEVEL_3_1 = 5
    """
    Level 3.1
    """
    LEVEL_3_2 = 6
    """
    Level 3.2
    """
    LEVEL_3_3 = 7
    """
    Level 3.3
    """
    LEVEL_4_0 = 8
    """
    Level 4.0
    """
    LEVEL_4_1 = 9
    """
    Level 4.1
    """
    LEVEL_4_2 = 10
    """
    Level 4.2
    """
    LEVEL_4_3 = 11
    """
    Level 4.3
    """
    LEVEL_5_0 = 12
    """
    Level 5.0
    """
    LEVEL_5_1 = 13
    """
    Level 5.1
    """
    LEVEL_5_2 = 14
    """
    Level 5.2
    """
    LEVEL_5_3 = 15
    """
    Level 5.3
    """
    LEVEL_6_0 = 16
    """
    Level 6.0
    """
    LEVEL_6_1 = 17
    """
    Level 6.1
    """
    LEVEL_6_2 = 18
    """
    Level 6.2
    """
    LEVEL_6_3 = 19
    """
    Level 6.3
    """
    LEVEL_7_0 = 20
    """
    Level 7.0
    """
    LEVEL_7_1 = 21
    """
    Level 7.1
    """
    LEVEL_7_2 = 22
    """
    Level 7.2
    """
    LEVEL_7_3 = 23
    """
    Level 7.3
    """
    LEVELS = 24
    """
    all valid levels
    """
    LEVEL_MAX = 31
    """
    Maximum parameters
    """

class AV1TXModes(enum.IntEnum):
    ONLY_4X4 = 0
    """
    the inverse transform will use only 4x4 transforms.
    """
    LARGEST = 1
    """
    the inverse transform will use the largest transform
      size that fits inside the block.
    """
    SELECT = 2
    """
    the choice of transform size is specified explicitly
      for each block.
    """

class AV1TransferCharacteristics(enum.IntEnum):
    RESERVED_0 = 0
    """
    For future use
    """
    BT_709 = 1
    """
    BT.709
    """
    UNSPECIFIED = 2
    """
    Unspecified
    """
    RESERVED_3 = 3
    """
    For future use
    """
    BT_470_M = 4
    """
    BT.470 System M (historical)
    """
    BT_470_B_G = 5
    """
    BT.470 System B, G (historical)
    """
    BT_601 = 6
    """
    BT.601
    """
    SMPTE_240 = 7
    """
    SMPTE 240 M
    """
    LINEAR = 8
    """
    Linear
    """
    LOG_100 = 9
    """
    Logarithmic (100 : 1 range)
    """
    LOG_100_SQRT10 = 10
    """
    Logarithmic (100 * Sqrt(10) : 1 range)
    """
    IEC_61966 = 11
    """
    IEC 61966-2-4
    """
    BT_1361 = 12
    """
    BT.1361
    """
    SRGB = 13
    """
    sRGB or sYCC
    """
    BT_2020_10_BIT = 14
    """
    BT.2020 10-bit systems
    """
    BT_2020_12_BIT = 15
    """
    BT.2020 12-bit systems
    """
    SMPTE_2084 = 16
    """
    SMPTE ST 2084, ITU BT.2100 PQ
    """
    SMPTE_428 = 17
    """
    SMPTE ST 428
    """
    HLG = 18
    """
    BT.2100 HLG, ARIB STD-B67
    """

class AV1WarpModelType(enum.IntEnum):
    IDENTITY = 0
    """
    Warp model is just an identity transform
    """
    TRANSLATION = 1
    """
    Warp model is a pure translation
    """
    ROTZOOM = 2
    """
    Warp model is a rotation + symmetric zoom
        + translation
    """
    AFFINE = 3
    """
    Warp model is a general affine transform
    """

class CtType(enum.IntEnum):
    PROGRESSIVE = 0
    INTERLACED = 1
    UNKNOWN = 2

class H264BitWriterResult(enum.IntEnum):
    """
    The result of writing H264 data into bit stream."""

    OK = 0
    """
    The writing succeeded
    """
    INVALID_DATA = 1
    """
    The input data to write is invalid
    """
    NO_MORE_SPACE = 2
    """
    The output does not have enough size
    """
    ERROR = 3
    """
    An general error occurred when writing
    """

class H264FramePackingType(enum.IntEnum):
    """
    Frame packing arrangement types."""

    FRAME_PACKING_NONE = 6
    """
    A complete 2D frame without any frame packing
    """
    FRAME_PACKING_CHECKERBOARD_INTERLEAVING = 0
    """
    Checkerboard
      based interleaving
    """
    FRAME_PACKING_COLUMN_INTERLEAVING = 1
    """
    Column based interleaving
    """
    FRAME_PACKING_ROW_INTERLEAVING = 2
    """
    Row based interleaving
    """
    FRAME_PACKING_SIDE_BY_SIDE = 3
    """
    Side-by-side packing
    """
    FRMAE_PACKING_TOP_BOTTOM = 4
    """
    Deprecated; use GST_H264_FRAME_PACKING_TOP_BOTTOM instead
    """
    FRAME_PACKING_TOP_BOTTOM = 4
    """
    Top-Bottom packing
    """
    FRAME_PACKING_TEMPORAL_INTERLEAVING = 5
    """
    Temporal interleaving
    """

class H264Level(enum.IntEnum):
    """
    H.264 level"""

    L1 = 10
    """
    Level 1
    """
    L1B = 9
    """
    Level 1b
    """
    L1_1 = 11
    """
    Level 1.1
    """
    L1_2 = 12
    """
    Level 1.2
    """
    L1_3 = 13
    """
    Level 1.3
    """
    L2 = 20
    """
    Level 2
    """
    L2_1 = 21
    """
    Level 2.1
    """
    L2_2 = 22
    """
    Level 2.2
    """
    L3 = 30
    """
    Level 3
    """
    L3_1 = 31
    """
    Level 3.1
    """
    L3_2 = 32
    """
    Level 3.2
    """
    L4 = 40
    """
    Level 4
    """
    L4_1 = 41
    """
    Level 4.1
    """
    L4_2 = 42
    """
    Level 4.2
    """
    L5 = 50
    """
    Level 5
    """
    L5_1 = 51
    """
    Level 5.1
    """
    L5_2 = 52
    """
    Level 5.2
    """
    L6 = 60
    """
    Level 6
    """
    L6_1 = 61
    """
    Level 6.1
    """
    L6_2 = 62
    """
    Level 6.2
    """

class H264NalUnitExtensionType(enum.IntEnum):
    """
    Indicates the type of H.264 NAL unit extension."""

    NONE = 0
    """
    No NAL unit header extension is available
    """
    SVC = 1
    """
    NAL unit header extension for SVC (Annex G)
    """
    MVC = 2
    """
    NAL unit header extension for MVC (Annex H)
    """

class H264NalUnitType(enum.IntEnum):
    """
    Indicates the type of H264 Nal Units"""

    UNKNOWN = 0
    """
    Unknown nal type
    """
    SLICE = 1
    """
    Slice nal
    """
    SLICE_DPA = 2
    """
    DPA slice nal
    """
    SLICE_DPB = 3
    """
    DPB slice nal
    """
    SLICE_DPC = 4
    """
    DPC slice nal
    """
    SLICE_IDR = 5
    """
    DPR slice nal
    """
    SEI = 6
    """
    Supplemental enhancement information (SEI) nal unit
    """
    SPS = 7
    """
    Sequence parameter set (SPS) nal unit
    """
    PPS = 8
    """
    Picture parameter set (PPS) nal unit
    """
    AU_DELIMITER = 9
    """
    Access unit (AU) delimiter nal unit
    """
    SEQ_END = 10
    """
    End of sequence nal unit
    """
    STREAM_END = 11
    """
    End of stream nal unit
    """
    FILLER_DATA = 12
    """
    Filler data nal lunit
    """
    SPS_EXT = 13
    """
    Sequence parameter set (SPS) extension NAL unit
    """
    PREFIX_UNIT = 14
    """
    Prefix NAL unit
    """
    SUBSET_SPS = 15
    """
    Subset sequence parameter set (SSPS) NAL unit
    """
    DEPTH_SPS = 16
    """
    Depth parameter set (DPS) NAL unit
    """
    RSV_1 = 17
    """
    First reserved parameter
    """
    RSV_2 = 18
    """
    Second reserved parameter
    """
    SLICE_AUX = 19
    """
    Auxiliary coded picture without partitioning NAL unit
    """
    SLICE_EXT = 20
    """
    Coded slice extension NAL unit
    """
    SLICE_DEPTH = 21
    """
    Coded slice extension for depth or 3D-AVC texture view
    """

class H264ParserResult(enum.IntEnum):
    """
    The result of parsing H264 data."""

    OK = 0
    """
    The parsing succeeded
    """
    BROKEN_DATA = 1
    """
    The data to parse is broken
    """
    BROKEN_LINK = 2
    """
    The link to structure needed for the parsing couldn't be found
    """
    ERROR = 3
    """
    An error occurred when parsing
    """
    NO_NAL = 4
    """
    No NAL unit found during the parsing
    """
    NO_NAL_END = 5
    """
    Start of the NAL unit found, but not the end.
        This will be returned if no start/sync marker for the next NAL unit was
        found. In this case the parser will assume that the end of the data is
        also the end of the NAL unit. Whether this assumption is correct or not
        depends on the context, which only the caller can know, which is why a
        special result value is returned in this case. If the data is NAL-aligned
        then GST_H264_PARSER_NO_NAL_END can be treated just like
        GST_H264_PARSER_OK. If the data is not guaranteed to be NAL-aligned,
        then the caller probably wants to collect more data until there's another
        sync marker or the end of the stream has been reached.
    """

class H264Profile(enum.IntEnum):
    """
    H.264 Profiles."""

    BASELINE = 66
    """
    Baseline profile (A.2.1)
    """
    MAIN = 77
    """
    Main profile (A.2.2)
    """
    EXTENDED = 88
    """
    Extended profile (A.2.3)
    """
    HIGH = 100
    """
    High profile (A.2.4),
    or Progressive High profile (A.2.4.1), or Constrained High profile (A.2.4.2)
    depending on constraint_set4_flag and constraint_set5_flag
    """
    HIGH10 = 110
    """
    High 10 profile (A.2.5) or High 10 Intra
      profile (A.2.8), or Progressive High 10 profile (A.2.5.1) depending on
      constraint_set3_flag and constraint_set4_flag
    """
    HIGH_422 = 122
    """
    High 4:2:2 profile (A.2.6) or High
      4:2:2 Intra profile (A.2.9), depending on constraint_set3_flag
    """
    HIGH_444 = 244
    """
    High 4:4:4 Predictive profile (A.2.7)
      or High 4:4:4 Intra profile (A.2.10), depending on the value of
      constraint_set3_flag
    """
    MULTIVIEW_HIGH = 118
    """
    Multiview High profile (H.10.1.1)
    """
    STEREO_HIGH = 128
    """
    Stereo High profile (H.10.1.2)
    """
    SCALABLE_BASELINE = 83
    """
    Scalable Baseline profile (G.10.1.1)
    """
    SCALABLE_HIGH = 86
    """
    Scalable High profile (G.10.1.2)
      or Scalable High Intra profile (G.10.1.3), depending on the value
      of constraint_set3_flag
    """
    INVALID = -1
    """
    Invalid H264 profile
    """

class H264SEIPayloadType(enum.IntEnum):
    """
    The type of SEI message."""

    BUF_PERIOD = 0
    """
    Buffering Period SEI Message
    """
    PIC_TIMING = 1
    """
    Picture Timing SEI Message
    """
    REGISTERED_USER_DATA = 4
    """
    Registered user data (D.2.5)
    """
    USER_DATA_UNREGISTERED = 5
    """
    User Data Unregistered (D.2.6)
    """
    RECOVERY_POINT = 6
    """
    Recovery Point SEI Message (D.2.7)
    """
    STEREO_VIDEO_INFO = 21
    """
    stereo video info SEI message (Since: 1.6)
    """
    FRAME_PACKING = 45
    """
    Frame Packing Arrangement (FPA) message that
        contains the 3D arrangement for stereoscopic 3D video (Since: 1.6)
    """
    MASTERING_DISPLAY_COLOUR_VOLUME = 137
    """
    Mastering display colour volume information SEI message (D.2.29) (Since: 1.18)
    """
    CONTENT_LIGHT_LEVEL = 144
    """
    Content light level information SEI message (D.2.31) (Since: 1.18)
    """
    UNHANDLED_PAYLOAD = -1
    """
    Unhandled SEI message. This may or may not
        be defined by spec (Since 1.18)
    ...
    """

class H264SEIPicStructType(enum.IntEnum):
    """
    SEI pic_struct type"""

    FRAME = 0
    """
    Picture is a frame
    """
    TOP_FIELD = 1
    """
    Top field of frame
    """
    BOTTOM_FIELD = 2
    """
    Bottom field of frame
    """
    TOP_BOTTOM = 3
    """
    Top bottom field of frame
    """
    BOTTOM_TOP = 4
    """
    bottom top field of frame
    """
    TOP_BOTTOM_TOP = 5
    """
    top bottom top field of frame
    """
    BOTTOM_TOP_BOTTOM = 6
    """
    bottom top bottom field of frame
    """
    FRAME_DOUBLING = 7
    """
    indicates that the frame should
     be displayed two times consecutively
    """
    FRAME_TRIPLING = 8
    """
    indicates that the frame should be
     displayed three times consecutively
    """

class H264SliceType(enum.IntEnum):
    """
    Type of Picture slice"""

    P_SLICE = 0
    B_SLICE = 1
    I_SLICE = 2
    SP_SLICE = 3
    SI_SLICE = 4
    S_P_SLICE = 5
    S_B_SLICE = 6
    S_I_SLICE = 7
    S_SP_SLICE = 8
    S_SI_SLICE = 9

class H265Level(enum.IntEnum):
    """
    H.265 level"""

    L1 = 30
    """
    Level 1
    """
    L2 = 60
    """
    Level 2
    """
    L2_1 = 63
    """
    Level 2.1
    """
    L3 = 90
    """
    Level 3
    """
    L3_1 = 93
    """
    Level 3.1
    """
    L4 = 120
    """
    Level 4
    """
    L4_1 = 123
    """
    Level 4.1
    """
    L5 = 150
    """
    Level 5
    """
    L5_1 = 153
    """
    Level 5.1
    """
    L5_2 = 156
    """
    Level 5.2
    """
    L6 = 180
    """
    Level 6
    """
    L6_1 = 183
    """
    Level 6.1
    """
    L6_2 = 186
    """
    Level 6.2
    """

class H265NalUnitType(enum.IntEnum):
    """
    Indicates the type of H265 Nal Units"""

    SLICE_TRAIL_N = 0
    """
    Slice nal of a non-TSA, non-STSA trailing picture
    """
    SLICE_TRAIL_R = 1
    """
    Slice nal of a non-TSA, non-STSA trailing picture
    """
    SLICE_TSA_N = 2
    """
    Slice nal of a TSA picture
    """
    SLICE_TSA_R = 3
    """
    Slice nal of a TSA picture
    """
    SLICE_STSA_N = 4
    """
    Slice nal of a STSA picture
    """
    SLICE_STSA_R = 5
    """
    Slice nal of a STSA picture
    """
    SLICE_RADL_N = 6
    """
    Slice nal of a RADL picture
    """
    SLICE_RADL_R = 7
    """
    Slice nal of a RADL piicture
    """
    SLICE_RASL_N = 8
    """
    Slice nal of a RASL picture
    """
    SLICE_RASL_R = 9
    """
    Slice nal of a RASL picture
    """
    SLICE_BLA_W_LP = 16
    """
    Slice nal of a BLA picture
    """
    SLICE_BLA_W_RADL = 17
    """
    Slice nal of a BLA picture
    """
    SLICE_BLA_N_LP = 18
    """
    Slice nal of a BLA picture
    """
    SLICE_IDR_W_RADL = 19
    """
    Slice nal of an IDR picture
    """
    SLICE_IDR_N_LP = 20
    """
    Slice nal of an IDR picture
    """
    SLICE_CRA_NUT = 21
    """
    Slice nal of a CRA picture
    """
    VPS = 32
    """
    Video parameter set(VPS) nal unit
    """
    SPS = 33
    """
    Sequence parameter set (SPS) nal unit
    """
    PPS = 34
    """
    Picture parameter set (PPS) nal unit
    """
    AUD = 35
    """
    Access unit (AU) delimiter nal unit
    """
    EOS = 36
    """
    End of sequence (EOS) nal unit
    """
    EOB = 37
    """
    End of bitstream (EOB) nal unit
    """
    FD = 38
    """
    Filler data (FD) nal lunit
    """
    PREFIX_SEI = 39
    """
    Supplemental enhancement information prefix nal unit
    """
    SUFFIX_SEI = 40
    """
    Suppliemental enhancement information suffix nal unit
    """

class H265ParserResult(enum.IntEnum):
    """
    The result of parsing H265 data."""

    OK = 0
    """
    The parsing succeeded
    """
    BROKEN_DATA = 1
    """
    The data to parse is broken
    """
    BROKEN_LINK = 2
    """
    The link to structure needed for the parsing couldn't be found
    """
    ERROR = 3
    """
    An error accured when parsing
    """
    NO_NAL = 4
    """
    No nal found during the parsing
    """
    NO_NAL_END = 5
    """
    Start of the nal found, but not the end.
    """

class H265Profile(enum.IntEnum):
    """
    H.265 Profiles."""

    INVALID = -1
    MAIN = 1
    """
    Main profile (A.3.2)
    """
    MAIN_10 = 2
    """
    Main 10 profile (A.3.3)
    """
    MAIN_STILL_PICTURE = 3
    """
    Main Still Picture profile (A.3.4)
    """
    MONOCHROME = 4
    """
    Monochrome profile (A.3.4)
    """
    MONOCHROME_12 = 5
    """
    Monochrome 12-bits profile (A.3.4)
    """
    MONOCHROME_16 = 6
    """
    Monochrome 16-bits profile (A.3.4)
    """
    MAIN_12 = 7
    """
    Main profile 12-bits (A.3.4)
    """
    MAIN_422_10 = 8
    """
    Main 4:2:2 profile 10-bits (A.3.4)
    """
    MAIN_422_12 = 9
    """
    Main 4:2:2 profile 12-bits (A.3.4)
    """
    MAIN_444 = 10
    """
    Main 4:4:4 profile (A.3.4)
    """
    MAIN_444_10 = 11
    """
    Main 4:4:4 10-bits profile (A.3.4)
    """
    MAIN_444_12 = 12
    """
    Main 4:4:4 12-bits profile (A.3.4)
    """
    MAIN_INTRA = 13
    """
    Main Intra profile (A.3.4)
    """
    MAIN_10_INTRA = 14
    """
    Main Intra 10-bits profile (A.3.4)
    """
    MAIN_12_INTRA = 15
    """
    Main Intra 12-bits profile (A.3.4)
    """
    MAIN_422_10_INTRA = 16
    """
    Main Intra 4:2:2 10-bits profile (A.3.4)
    """
    MAIN_422_12_INTRA = 17
    """
    Main Intra 4:2:2 12-bits profile (A.3.4)
    """
    MAIN_444_INTRA = 18
    """
    Main Intra 4:4:4 profile (A.3.4)
    """
    MAIN_444_10_INTRA = 19
    """
    Main Intra 4:4:4 10-bits profile (A.3.4)
    """
    MAIN_444_12_INTRA = 20
    """
    Main Intra 4:4:4 12-bits profile (A.3.4)
    """
    MAIN_444_16_INTRA = 21
    """
    Main Intra 4:4:4 16-bits profile (A.3.4)
    """
    MAIN_444_STILL_PICTURE = 22
    """
    Main 4:4:4 Still Picture profile (A.3.4)
    """
    MAIN_444_16_STILL_PICTURE = 23
    """
    Main 4:4:4 16-bits Still Picture profile (A.3.4)
    """
    MONOCHROME_10 = 24
    """
    Monochrome 10-bits profile (A.3.5) (Since: 1.18)
    """
    HIGH_THROUGHPUT_444 = 25
    """
    High Throughput 4:4:4 profile (A.3.6) (Since: 1.18)
    """
    HIGH_THROUGHPUT_444_10 = 26
    """
    High Throughput 4:4:4 10-bits profile (A.3.6) (Since: 1.18)
    """
    HIGH_THROUGHPUT_444_14 = 27
    """
    High Throughput 4:4:4 14-bits profile (A.3.6) (Since: 1.18)
    """
    HIGH_THROUGHPUT_444_16_INTRA = 28
    """
    High Throughput 4:4:4 16-bits Intra profile (A.3.6) (Since: 1.18)
    """
    SCREEN_EXTENDED_MAIN = 29
    """
    Screen-Extended Main profile (A.3.7) (Since: 1.18)
    """
    SCREEN_EXTENDED_MAIN_10 = 30
    """
    Screen-Extended Main 10-bits profile (A.3.7) (Since: 1.18)
    """
    SCREEN_EXTENDED_MAIN_444 = 31
    """
    Screen-Extended Main 4:4:4 profile (A.3.7) (Since: 1.18)
    """
    SCREEN_EXTENDED_MAIN_444_10 = 32
    """
    Screen-Extended Main 4:4:4 10-bits profile (A.3.7) (Since: 1.18)
    """
    SCREEN_EXTENDED_HIGH_THROUGHPUT_444 = 33
    """
    Screen-Extended High Throughput 4:4:4 profile (A.3.7) (Since: 1.18)
    """
    SCREEN_EXTENDED_HIGH_THROUGHPUT_444_10 = 34
    """
    Screen-Extended High Throughput 4:4:4 10-bits profile (A.3.7) (Since: 1.18)
    """
    SCREEN_EXTENDED_HIGH_THROUGHPUT_444_14 = 35
    """
    Screen-Extended High Throughput 4:4:4 14-bits profile (A.3.7) (Since: 1.18)
    """
    MULTIVIEW_MAIN = 36
    """
    Multiview Main profile (G.11.1) (Since: 1.18)
    """
    SCALABLE_MAIN = 37
    """
    Scalable Main profile (H.11.1) (Since: 1.18)
    """
    SCALABLE_MAIN_10 = 38
    """
    Scalable Main 10-bits profile (H.11.1) (Since: 1.18)
    """
    SCALABLE_MONOCHROME = 39
    """
    Scalable Monochrome profile (H.11.1) (Since: 1.18)
    """
    SCALABLE_MONOCHROME_12 = 40
    """
    Scalable Monochrome 12-bits profile (H.11.1) (Since: 1.18)
    """
    SCALABLE_MONOCHROME_16 = 41
    """
    Scalable Monochrome 16-bits profile (H.11.1) (Since: 1.18)
    """
    SCALABLE_MAIN_444 = 42
    """
    Scalable Main 4:4:4 profile (H.11.1) (Since: 1.18)
    """
    _3D_MAIN = 43  # [3d_main]: changed because started with a number
    """
    3D Main 4:4:4 profile (I.11.1) (Since: 1.18)
    """
    MAX = 44

class H265ProfileIDC(enum.IntEnum):
    """
    Valid values for the profile_idc field. This is different from
    GstH265Profile as an extension idc can be used to encode a whole variety of
    profiles."""

    MAIN = 1
    """
    Main profile (A.3.2)
    """
    MAIN_10 = 2
    """
    Main 10 profile (A.3.3)
    """
    MAIN_STILL_PICTURE = 3
    """
    Main Still Picture profile (A.3.4)
    """
    FORMAT_RANGE_EXTENSION = 4
    """
    Format range extensions profile (A.3.5)
    """
    HIGH_THROUGHPUT = 5
    """
    High throughput profiles (A.3.6)
    """
    MULTIVIEW_MAIN = 6
    """
    Multiview Main profiles (G.11.1) (Since: 1.18)
    """
    SCALABLE_MAIN = 7
    """
    Scalable Main and Scalable Main 10 profile (H.11.1) (Since: 1.18)
    """
    _3D_MAIN = 8  # [3d_main]: changed because started with a number
    """
    3D Main profile (I.11.1) (Since: 1.18)
    """
    SCREEN_CONTENT_CODING = 9
    """
    Screen content coding extensions profiles (A.3.7)
    """
    SCALABLE_FORMAT_RANGE_EXTENSION = 10
    """
    Scalable Format range extensions profiles (H.11.1) (Since: 1.18)
    """
    HIGH_THROUGHPUT_SCREEN_CONTENT_CODING_EXTENSION = 11
    """
    High throughput screen content coding extensions profiles (A.3.8) (Since: 1.18)
    """

class H265QuantMatrixSize(enum.IntEnum):
    _4X4 = 0  # [4x4]: changed because started with a number
    _8X8 = 1  # [8x8]: changed because started with a number
    _16X16 = 2  # [16x16]: changed because started with a number
    _32X32 = 3  # [32x32]: changed because started with a number

class H265SEIPayloadType(enum.IntEnum):
    """
    The type of SEI message."""

    BUF_PERIOD = 0
    """
    Buffering Period SEI Message
    """
    PIC_TIMING = 1
    """
    Picture Timing SEI Message
    """
    REGISTERED_USER_DATA = 4
    """
    Registered user data (D.2.5)
    """
    USER_DATA_UNREGISTERED = 5
    """
    User data unregistered (D.2.7)
    """
    RECOVERY_POINT = 6
    """
    Recovery Point SEI Message (D.3.8)
    """
    TIME_CODE = 136
    """
    Time code SEI message (D.2.27) (Since: 1.16)
    """
    MASTERING_DISPLAY_COLOUR_VOLUME = 137
    """
    Mastering display colour volume information SEI message (D.2.28) (Since: 1.18)
    """
    CONTENT_LIGHT_LEVEL = 144
    """
    Content light level information SEI message (D.2.35) (Since: 1.18)
    ...
    """

class H265SEIPicStructType(enum.IntEnum):
    """
    SEI pic_struct type"""

    FRAME = 0
    """
    Picture is a frame
    """
    TOP_FIELD = 1
    """
    Top field of frame
    """
    BOTTOM_FIELD = 2
    """
    Botom field of frame
    """
    TOP_BOTTOM = 3
    """
    Top bottom field of frame
    """
    BOTTOM_TOP = 4
    """
    bottom top field of frame
    """
    TOP_BOTTOM_TOP = 5
    """
    top bottom top field of frame
    """
    BOTTOM_TOP_BOTTOM = 6
    """
    bottom top bottom field of frame
    """
    FRAME_DOUBLING = 7
    """
    indicates that the frame should
     be displayed two times consecutively
    """
    FRAME_TRIPLING = 8
    """
    indicates that the frame should be
     displayed three times consecutively
    """
    TOP_PAIRED_PREVIOUS_BOTTOM = 9
    """
    top field paired with
     previous bottom field in output order
    """
    BOTTOM_PAIRED_PREVIOUS_TOP = 10
    """
    bottom field paried with
     previous top field in output order
    """
    TOP_PAIRED_NEXT_BOTTOM = 11
    """
    top field paired with next
     bottom field in output order
    """
    BOTTOM_PAIRED_NEXT_TOP = 12
    """
    bottom field paired with
     next top field in output order
    """

class H265SliceType(enum.IntEnum):
    """
    Type of Picture slice"""

    B_SLICE = 0
    P_SLICE = 1
    I_SLICE = 2

class H266APSType(enum.IntEnum):
    """
    Indicates the types of Adaptation parameter set (APS) as
    Table 6 - APS parameters type codes and types of APS parameters."""

    ALF_APS = 0
    """
    ALF parameters.
    """
    LMCS_APS = 1
    """
    LMCS parameters.
    """
    SCALING_APS = 2
    """
    Scaling list parameters.
    """
    APS_TYPE_MAX = 3

class H266Level(enum.IntEnum):
    """
    H.266 level."""

    L1_0 = 16
    L2_0 = 32
    L2_1 = 35
    L3_0 = 48
    L3_1 = 51
    L4_0 = 64
    L4_1 = 67
    L5_0 = 80
    L5_1 = 83
    L5_2 = 86
    L6_0 = 96
    L6_1 = 99
    L6_2 = 102
    L6_3 = 105

class H266NalUnitType(enum.IntEnum):
    """
    Indicates the types of H266 Nal Units as
    table 5 - NAL unit type codes and NAL unit type classes."""

    SLICE_TRAIL = 0
    """
    Coded slice of a trailing picture or subpicture.
    """
    SLICE_STSA = 1
    """
    Coded slice of an STSA picture or subpicture.
    """
    SLICE_RADL = 2
    """
    Coded slice of a RADL picture or subpicture.
    """
    SLICE_RASL = 3
    """
    Coded slice of a RASL picture or subpicture.
    """
    SLICE_IDR_W_RADL = 7
    """
    Coded slice of an IDR picture or subpicture.
    """
    SLICE_IDR_N_LP = 8
    """
    Coded slice of an IDR picture or subpicture.
    """
    SLICE_CRA = 9
    """
    Coded slice of a CRA picture or subpicture.
    """
    SLICE_GDR = 10
    """
    Coded slice of a GDR picture or subpicture.
    """
    OPI = 12
    """
    Operating point information.
    """
    DCI = 13
    """
    Decoding capability information.
    """
    VPS = 14
    """
    Video parameter set(VPS).
    """
    SPS = 15
    """
    Sequence parameter set (SPS).
    """
    PPS = 16
    """
    Picture parameter set (PPS).
    """
    PREFIX_APS = 17
    """
    Prefix Adaptation parameter set (APS).
    """
    SUFFIX_APS = 18
    """
    Suffix Adaptation parameter set (APS).
    """
    PH = 19
    """
    Picture header (PH).
    """
    AUD = 20
    """
    AU delimiter.
    """
    EOS = 21
    """
    End of sequence (EOS).
    """
    EOB = 22
    """
    End of bitstream (EOB).
    """
    PREFIX_SEI = 23
    """
    Prefix Supplemental enhancement information.
    """
    SUFFIX_SEI = 24
    """
    Suffix Suppliemental enhancement information.
    """
    FD = 25
    """
    Filler data (FD).
    """

class H266ParserResult(enum.IntEnum):
    """
    The result of parsing H266 data."""

    OK = 0
    """
    The parsing succeeded.
    """
    BROKEN_DATA = 1
    """
    The data to parse is broken.
    """
    BROKEN_LINK = 2
    """
    The link to structure needed for the parsing
     couldn't be found.
    """
    ERROR = 3
    """
    An error accured when parsing.
    """
    NO_NAL = 4
    """
    No nal found during the parsing.
    """
    NO_NAL_END = 5
    """
    Start of the nal found, but not the end.
    """

class H266Profile(enum.IntEnum):
    """
    H.266 Profiles."""

    INVALID = -1
    NONE = 0
    INTRA = 8
    STILL_PICTURE = 64
    MAIN_10 = 1
    """
    Main 10 profile (A.3.1).
    """
    MAIN_10_STILL_PICTURE = 65
    """
    Main 10 Still Picture profile (A.3.1).
    """
    MULTILAYER_MAIN_10 = 17
    """
    MultiLayer Main 10 profile (A.3.3).
    """
    MULTILAYER_MAIN_10_STILL_PICTURE = 81
    """
    MultiLayer Main 10 Still
     Picture profile (A.3.3).
    """
    MAIN_10_444 = 33
    """
    Main 10 4:4:4 profile (A.3.2).
    """
    MAIN_10_444_STILL_PICTURE = 97
    """
    Main 10 4:4:4 Still Picture
     profile (A.3.2).
    """
    MULTILAYER_MAIN_10_444 = 49
    """
    MultiLayer Main 10 4:4:4
     profile (A.3.4).
    """
    MULTILAYER_MAIN_10_444_STILL_PICTURE = 113
    """
    MultiLayer Main 10
     4:4:4 Still Picture profile (A.3.4).
    """
    MAIN_12 = 2
    """
    Main 12 profile (A.3.5).
    """
    MAIN_12_444 = 34
    """
    Main 12 4:4:4 profile (A.3.5).
    """
    MAIN_16_444 = 35
    """
    Main 16 4:4:4 profile (A.3.5).
    """
    MAIN_12_INTRA = 10
    """
    Main 12 Intra profile (A.3.5).
    """
    MAIN_12_444_INTRA = 42
    """
    Main 12 4:4:4 Intra profile (A.3.5).
    """
    MAIN_16_444_INTRA = 43
    """
    Main 16 4:4:4 Intra profile (A.3.5).
    """
    MAIN_12_STILL_PICTURE = 66
    """
    Main 12 Still Picture profile (A.3.5).
    """
    MAIN_12_444_STILL_PICTURE = 98
    """
    Main 12 4:4:4 Still Picture
     profile (A.3.5).
    """
    MAIN_16_444_STILL_PICTURE = 99
    """
    Main 16 4:4:4 Still Picture
     profile (A.3.5).
    """
    MAX = 100

class H266SEIPayloadType(enum.IntEnum):
    """
    The type of SEI message.
    More other SEIs are specified in Rec.ITU-T H.274 | ISO/IEC 23002-7."""

    BUF_PERIOD = 0
    """
    Buffering Period SEI Message.
    """
    PIC_TIMING = 1
    """
    Picture Timing SEI Message.
    """
    REGISTERED_USER_DATA = 4
    """
    Registered user data.
    """
    USER_DATA_UNREGISTERED = 5
    """
    User data Unregistered.
    """
    DU_INFO = 130
    """
    DU Information SEI Message.
    """
    SCALABLE_NESTING = 133
    FRAME_FIELD_INFO = 168
    """
    Frame Field Info SEI Message.
    """
    SUBPIC_LEVEL_INFO = 203
    """
    Subpicture Level Information SEI.
    """

class H266SliceType(enum.IntEnum):
    """
    Types of Picture slice."""

    B_SLICE = 0
    """
    B slice type.
    """
    P_SLICE = 1
    """
    P slice type.
    """
    I_SLICE = 2
    """
    I slice type.
    """

class JPEG2000Colorspace(enum.IntEnum):
    NONE = 0
    """
    no color space
    """
    RGB = 1
    """
    standard RGB color space
    """
    YUV = 2
    """
    standard YUV color space
    """
    GRAY = 3
    """
    monochrome color space
    """

class JPEG2000Sampling(enum.IntEnum):
    """
    Sampling values from RF 5371 for JPEG 2000 over RTP : https://datatracker.ietf.org/doc/rfc5371/C
    Note: sampling extensions that are not listed in the RFC are signified by an _EXT at the end of the enum"""

    NONE = 0
    """
    no sampling
    """
    RGB = 1
    """
    standard Red, Green, Blue color space.
    """
    BGR = 2
    """
    standard Blue, Green, Red color space.
    """
    RGBA = 3
    """
    standard Red, Green, Blue, Alpha color space.
    """
    BGRA = 4
    """
    standard Blue, Green, Red, Alpha color space.
    """
    YBR444 = 5
    """
    standard YCbCr color space; no subsampling.
    """
    YBR422 = 6
    """
    standard YCbCr color space; Cb and Cr are subsampled horizontally by 1/2.
    """
    YBR420 = 7
    """
    standard YCbCr color space; Cb and Cr are subsampled horizontally and vertically by 1/2.
    """
    YBR410 = 8
    """
    standard YCbCr color space; Cb and Cr are subsampled vertically by 1/4 alternating the Cb and Cr component.
    """
    GRAYSCALE = 9
    """
    basically, a single component image of just multilevels of grey.
    """
    YBRA4444_EXT = 10
    """
    standard YCbCr color space, alpha channel, no subsampling,
    """
    YBR411 = 11
    """
    standard YCbCr color space; Cb and Cr are subsampled vertically by 1/4
    """

class JpegBitWriterResult(enum.IntEnum):
    """
    The result of writing JPEG data into bit stream."""

    OK = 0
    """
    The writing succeeded
    """
    INVALID_DATA = 1
    """
    The input data to write is invalid
    """
    NO_MORE_SPACE = 2
    """
    The output does not have enough size
    """
    ERROR = 3
    """
    An general error occurred when writing
    """

class JpegEntropyCodingMode(enum.IntEnum):
    """
    JPEG entropy coding mode."""

    HUFFMAN = 0
    """
    Huffman coding
    """
    ARITHMETIC = 8
    """
    arithmetic coding
    """

class JpegMarker(enum.IntEnum):
    """
    Indicates the type of JPEG segment."""

    SOF0 = 192
    """
    Start of frame marker code (Baseline)
    """
    SOF1 = 193
    """
    Start of frame marker code (Extended Sequential, Huffman)
    """
    SOF2 = 194
    """
    Start of frame marker code (Progressive, Huffman)
    """
    SOF3 = 195
    """
    Start of frame marker code (Lossless, Huffman)
    """
    SOF5 = 197
    """
    Start of frame marker code (Differential Sequential, Huffman)
    """
    SOF6 = 198
    """
    Start of frame marker code (Differential Progressive, Huffman)
    """
    SOF7 = 199
    """
    Start of frame marker code (Differential Lossless, Huffman)
    """
    SOF9 = 201
    """
    Start of frame marker code (Extended Sequential, Arithmetic)
    """
    SOF10 = 202
    """
    Start of frame marker code (Progressive, Arithmetic)
    """
    SOF11 = 203
    """
    Start of frame marker code (Lossless, Arithmetic)
    """
    SOF13 = 205
    """
    Start of frame marker code (Differential Sequential, Arithmetic)
    """
    SOF14 = 206
    """
    Start of frame marker code (Differential Progressive, Arithmetic)
    """
    SOF15 = 207
    """
    Start of frame marker code (Differential Lossless, Arithmetic)
    """
    DHT = 196
    """
    Huffman table marker code
    """
    DAC = 204
    """
    Arithmetic coding marker code
    """
    RST0 = 208
    RST1 = 209
    RST2 = 210
    RST3 = 211
    RST4 = 212
    RST5 = 213
    RST6 = 214
    RST7 = 215
    SOI = 216
    """
    Start of image marker code
    """
    EOI = 217
    """
    End of image marker code
    """
    SOS = 218
    """
    Start of scan marker code
    """
    DQT = 219
    """
    Define quantization table marker code
    """
    DNL = 220
    """
    Define number of lines marker code
    """
    DRI = 221
    """
    Define restart interval marker code
    """
    APP0 = 224
    """
    Application segment 0 marker code
    """
    APP1 = 225
    """
    Application segment 1 marker code
    """
    APP2 = 226
    """
    Application segment 2 marker code
    """
    APP3 = 227
    """
    Application segment 3 marker code
    """
    APP4 = 228
    """
    Application segment 4 marker code
    """
    APP5 = 229
    """
    Application segment 5 marker code
    """
    APP6 = 230
    """
    Application segment 6 marker code
    """
    APP7 = 231
    """
    Application segment 7 marker code
    """
    APP8 = 232
    """
    Application segment 8 marker code
    """
    APP9 = 233
    """
    Application segment 9 marker code
    """
    APP10 = 234
    """
    Application segment 10 marker code
    """
    APP11 = 235
    """
    Application segment 11 marker code
    """
    APP12 = 236
    """
    Application segment 12 marker code
    """
    APP13 = 237
    """
    Application segment 13 marker code
    """
    APP14 = 238
    """
    Application segment 14 marker code
    """
    APP15 = 239
    """
    Application segment 15 marker code
    """
    COM = 254
    """
    Comment marker code
    """

class JpegProfile(enum.IntEnum):
    """
    JPEG encoding processes."""

    BASELINE = 0
    """
    Baseline DCT
    """
    EXTENDED = 1
    """
    Extended sequential DCT
    """
    PROGRESSIVE = 2
    """
    Progressive DCT
    """
    LOSSLESS = 3
    """
    Lossless (sequential)
    """

class Mpeg4AspectRatioInfo(enum.IntEnum):
    """
    Defines the different pixel aspect ratios as
    defined in: Table 6-12 -- Meaning of pixel aspect ratio"""

    SQUARE = 1
    """
    1:1 square
    """
    _625_TYPE_4_3 = 2  # [625_type_4_3]: changed because started with a number
    """
    12:11 (625-type for 4:3 picture)
    """
    _525_TYPE_4_3 = 3  # [525_type_4_3]: changed because started with a number
    """
    10:11 (525-type for 4:3 picture)
    """
    _625_TYPE_16_9 = 4  # [625_type_16_9]: changed because started with a number
    """
    16:11 (625-type stretched for 16:9 picture)
    """
    _525_TYPE_16_9 = 5  # [525_type_16_9]: changed because started with a number
    """
    40:33 (525-type stretched for 16:9 picture)
    """
    EXTENDED_PAR = 15
    """
    Extended par
    """

class Mpeg4ChromaFormat(enum.IntEnum):
    """
    The chroma format in use as
    defined in: Table 6-13 -- Meaning of chroma_format"""

    MPEG4_CHROMA_4_2_0 = 1

class Mpeg4Level(enum.IntEnum):
    """
    Different levels as defined in:
    9- Profiles and levels

    It is computed using:
    Table G.1 — FLC table for profile_and_level_indication"""

    LEVEL0 = 0
    LEVEL1 = 1
    LEVEL2 = 2
    LEVEL3 = 3
    LEVEL3B = 4
    LEVEL4 = 5
    LEVEL5 = 6
    LEVEL_RESERVED = 7

class Mpeg4ParseResult(enum.IntEnum):
    """
    Result type of any parsing function."""

    OK = 0
    """
    The parsing went well
    """
    BROKEN_DATA = 1
    """
    The bitstream was broken
    """
    NO_PACKET = 2
    """
    There was no packet in the buffer
    """
    NO_PACKET_END = 3
    """
    There was no packet end in the buffer
    """
    ERROR = 4

class Mpeg4Profile(enum.IntEnum):
    """
    Different defined profiles as defined in:
    9- Profiles and levels

    It is computed using:
    Table G.1 — FLC table for profile_and_level_indication"""

    CORE = 0
    MAIN = 1
    N_BIT = 2
    SIMPLE = 3
    HYBRID = 4
    RESERVED = 5
    SIMPLE_FBA = 6
    CORE_STUDIO = 7
    SIMPLE_STUDIO = 8
    CORE_SCALABLE = 9
    ADVANCED_CORE = 10
    ADVANCED_SIMPLE = 11
    SIMPLE_SCALABLE = 12
    SCALABLE_TEXTURE = 13
    SIMPLE_FACE_ANIMATION = 14
    BASIC_ANIMATED_TEXTURE = 15
    ADVANCED_REALTIME_SIMPLE = 16
    ADVANCED_SCALABLE_TEXTURE = 17
    FINE_GRANULARITY_SCALABLE = 18
    ADVANCED_CODING_EFFICIENCY = 19

class Mpeg4SpriteEnable(enum.IntEnum):
    """
    Indicates the usage of static sprite coding
    or global motion compensation (GMC) as defined in:
    Table V2 - 2 -- Meaning of sprite_enable codewords"""

    UNUSED = 0
    STATIC = 1
    GMG = 2

class Mpeg4StartCode(enum.IntEnum):
    """
    Defines the different startcodes present in the bitstream as
    defined in: Table 6-3 — Start code values"""

    VIDEO_OBJ_FIRST = 0
    VIDEO_OBJ_LAST = 31
    VIDEO_LAYER_FIRST = 32
    VIDEO_LAYER_LAST = 47
    VISUAL_OBJ_SEQ_START = 176
    VISUAL_OBJ_SEQ_END = 177
    USER_DATA = 178
    GROUP_OF_VOP = 179
    VIDEO_SESSION_ERR = 180
    VISUAL_OBJ = 181
    VIDEO_OBJ_PLANE = 182
    FBA = 186
    FBA_PLAN = 187
    MESH = 188
    MESH_PLAN = 189
    STILL_TEXTURE_OBJ = 190
    TEXTURE_SPATIAL = 191
    TEXTURE_SNR_LAYER = 192
    TEXTURE_TILE = 193
    SHAPE_LAYER = 194
    STUFFING = 195
    SYSTEM_FIRST = 198
    SYSTEM_LAST = 255
    RESYNC = 4095

class Mpeg4VideoObjectCodingType(enum.IntEnum):
    """
    The vop coding types as defined in:
    Table 6-20 -- Meaning of vop_coding_type"""

    I_VOP = 0
    """
    intra-coded (I)
    """
    P_VOP = 1
    """
    predictive-coded (P)
    """
    B_VOP = 2
    """
    bidirectionally-predictive-coded (B)
    """
    S_VOP = 3
    """
    sprite (S)
    """

class Mpeg4VideoObjectLayerShape(enum.IntEnum):
    """
    The different video object layer shapes as defined in:
    Table 6-16 — Video Object Layer shape type"""

    RECTANGULAR = 0
    BINARY = 1
    BINARY_ONLY = 2
    GRAYSCALE = 3

class Mpeg4VisualObjectType(enum.IntEnum):
    """
    Defines the different visual object types as
    defined in: Table 6-5 -- Meaning of visual object type"""

    VIDEO_ID = 1
    STILL_TEXTURE_ID = 2
    STILL_MESH_ID = 3
    STILL_FBA_ID = 4
    STILL_3D_MESH_ID = 5

class MpegVideoChromaFormat(enum.IntEnum):
    """
    Chroma subsampling type."""

    RES = 0
    """
    Invalid (reserved for future use)
    """
    _420 = 1  # [420]: changed because started with a number
    """
    4:2:0 subsampling
    """
    _422 = 2  # [422]: changed because started with a number
    """
    4:2:2 subsampling
    """
    _444 = 3  # [444]: changed because started with a number
    """
    4:4:4 (non-subsampled)
    """

class MpegVideoLevel(enum.IntEnum):
    """
    Mpeg-2 Levels."""

    HIGH_P = 2
    """
    High-P level (HL Progressive)
    """
    HIGH = 4
    """
    High level (HL)
    """
    HIGH_1440 = 6
    """
    High 1440 level (H-14)
    """
    MAIN = 8
    """
    Main level (ML)
    """
    LOW = 10
    """
    Low level (LL)
    """

class MpegVideoPacketExtensionCode(enum.IntEnum):
    """
    Indicates what type of packets are in this block, some are mutually
    exclusive though - ie, sequence packs are accumulated separately. GOP &
    Picture may occur together or separately."""

    SEQUENCE = 1
    """
    Sequence extension code
    """
    SEQUENCE_DISPLAY = 2
    """
    Sequence Display extension code
    """
    QUANT_MATRIX = 3
    """
    Quantization Matrix extension code
    """
    SEQUENCE_SCALABLE = 5
    """
    Sequence Scalable extension code
    """
    PICTURE = 8
    """
    Picture coding extension
    """

class MpegVideoPacketTypeCode(enum.IntEnum):
    """
    Indicates the type of MPEG packet"""

    PICTURE = 0
    """
    Picture packet starting code
    """
    SLICE_MIN = 1
    """
    Slice min packet starting code
    """
    SLICE_MAX = 175
    """
    Slice max packet starting code
    """
    USER_DATA = 178
    """
    User data packet starting code
    """
    SEQUENCE = 179
    """
    Sequence packet starting code
    """
    EXTENSION = 181
    """
    Extension packet starting code
    """
    SEQUENCE_END = 183
    """
    Sequence end packet code
    """
    GOP = 184
    """
    Group of Picture packet starting code
    """
    NONE = 255
    """
    None packet code
    """

class MpegVideoPictureStructure(enum.IntEnum):
    """
    Picture structure type."""

    TOP_FIELD = 1
    """
    Top field
    """
    BOTTOM_FIELD = 2
    """
    Bottom field
    """
    FRAME = 3
    """
    Frame picture
    """

class MpegVideoPictureType(enum.IntEnum):
    """
    Picture type."""

    I = 1
    """
    Intra-coded (I) frame
    """
    P = 2
    """
    Predictive-codec (P) frame
    """
    B = 3
    """
    Bidirectionally predictive-coded (B) frame
    """
    D = 4
    """
    D frame
    """

class MpegVideoProfile(enum.IntEnum):
    """
    Mpeg-2 Profiles."""

    _422 = 0  # [422]: changed because started with a number
    """
    4:2:2 profile (422)
    """
    HIGH = 1
    """
    High profile (HP)
    """
    SPATIALLY_SCALABLE = 2
    """
    Spatially Scalable profile (Spatial)
    """
    SNR_SCALABLE = 3
    """
    SNR Scalable profile (SNR)
    """
    MAIN = 4
    """
    Main profile (MP)
    """
    SIMPLE = 5
    """
    Simple profile (SP)
    """

class MpegVideoSequenceScalableMode(enum.IntEnum):
    DATA_PARTITIONING = 0
    """
    Data partitioning
    """
    SPATIAL = 1
    """
    Spatial Scalability
    """
    SNR = 2
    """
    SNR Scalability
    """
    TEMPORAL = 3
    """
    Temporal Scalability
    """

class VC1Condover(enum.IntEnum):
    NONE = 0
    ALL = 1
    SELECT = 2

class VC1DQProfile(enum.IntEnum):
    FOUR_EDGES = 0
    DOUBLE_EDGES = 1
    SINGLE_EDGE = 2
    ALL_MBS = 3

class VC1FrameCodingMode(enum.IntEnum):
    FRAME_PROGRESSIVE = 0
    FRAME_INTERLACE = 16
    FIELD_INTERLACE = 17

class VC1Level(enum.IntEnum):
    LOW = 0
    MEDIUM = 2
    HIGH = 4
    L0 = 0
    L1 = 1
    L2 = 2
    L3 = 3
    L4 = 4
    UNKNOWN = 255

class VC1MvMode(enum.IntEnum):
    _1MV_HPEL_BILINEAR = 0  # [1mv_hpel_bilinear]: changed because started with a number
    _1MV = 1  # [1mv]: changed because started with a number
    _1MV_HPEL = 2  # [1mv_hpel]: changed because started with a number
    MIXED_MV = 3
    INTENSITY_COMP = 4

class VC1ParserResult(enum.IntEnum):
    OK = 0
    BROKEN_DATA = 1
    NO_BDU = 2
    NO_BDU_END = 3
    ERROR = 4

class VC1PictureType(enum.IntEnum):
    P = 0
    B = 1
    I = 2
    BI = 3
    SKIPPED = 4

class VC1Profile(enum.IntEnum):
    SIMPLE = 0
    MAIN = 1
    RESERVED = 2
    ADVANCED = 3

class VC1QuantizerSpec(enum.IntEnum):
    IMPLICITLY = 0
    EXPLICITLY = 1
    NON_UNIFORM = 2
    UNIFORM = 3

class VC1StartCode(enum.IntEnum):
    END_OF_SEQ = 10
    SLICE = 11
    FIELD = 12
    FRAME = 13
    ENTRYPOINT = 14
    SEQUENCE = 15
    SLICE_USER = 27
    FIELD_USER = 28
    FRAME_USER = 29
    ENTRY_POINT_USER = 30
    SEQUENCE_USER = 31

class VP9Profile(enum.IntEnum):
    _0 = 0  # [0]: changed because started with a number
    _1 = 1  # [1]: changed because started with a number
    _2 = 2  # [2]: changed because started with a number
    _3 = 3  # [3]: changed because started with a number
    UNDEFINED = 4

class Vp8FrameType(enum.IntEnum):
    """
    VP8 frame types"""

    KEY_FRAME = 0
    """
    Key frame, only have intra blocks
    """
    INTER_FRAME = 1
    """
    Inter frame, both intra and inter blocks
    """

class Vp8ParserResult(enum.IntEnum):
    """
    The result of parsing VP8 data."""

    OK = 0
    """
    The parsing succeeded
    """
    BROKEN_DATA = 1
    """
    The data to parse is broken
    """
    ERROR = 2
    """
    An error accured when parsing
    """

class Vp8RefFrameType(enum.IntEnum):
    """
    Reference Frame types"""

    INTRA = 0
    """
    Intra reference frame
    """
    LAST = 1
    """
    Last Reference frame
    """
    GOLDEN = 2
    """
    Golden Reference frame
    """
    ALTREF = 3
    """
    Alternate Reference frame
    """
    MAX = 4

class Vp9BitDepth(enum.IntEnum):
    """
    Bit depths of encoded frames"""

    _8 = 8  # [8]: changed because started with a number
    """
    Bit depth is 8
    `GST_VP9_BIT_DEPTH_10` Bit depth is 10
    """
    _10 = 10  # [10]: changed because started with a number
    _12 = 12  # [12]: changed because started with a number
    """
    Bit depth is 12
    """

class Vp9ColorRange(enum.IntEnum):
    """
    Possible color value ranges"""

    LIMITED = 0
    """
    Y range is [16-235], UV range is [16-240]
    """
    FULL = 1
    """
    Full range for Y,U and V [0-255]
    """

class Vp9ColorSpace(enum.IntEnum):
    """
    Supported ColorSpace standards"""

    UNKNOWN = 0
    """
    Unknown color space
    """
    BT_601 = 1
    """
    BT.601
    """
    BT_709 = 2
    """
    BT.709
    """
    SMPTE_170 = 3
    """
    SMPTE.170
    """
    SMPTE_240 = 4
    """
    SMPTE.240
    """
    BT_2020 = 5
    """
    BT.2020
    """
    RESERVED_2 = 6
    SRGB = 7
    """
    sRGB
    """

class Vp9FrameType(enum.IntEnum):
    """
    VP9 frame types"""

    KEY_FRAME = 0
    """
    Key frame, only have intra blocks
    """
    INTER_FRAME = 1
    """
    Inter frame, both intra and inter blocks
    """

class Vp9InterpolationFilter(enum.IntEnum):
    """
    Interpolation Filters Types"""

    EIGHTTAP = 0
    """
    EightTap interpolation filter
    """
    EIGHTTAP_SMOOTH = 1
    """
    Smooth interpolation filter
    """
    EIGHTTAP_SHARP = 2
    """
    Shart interpolation filter
    """
    BILINEAR = 3
    """
    Bilinear interpolation filter
    """
    SWITCHABLE = 4
    """
    Selectable interpolation filter
    """

class Vp9ParserResult(enum.IntEnum):
    OK = 0
    BROKEN_DATA = 1
    ERROR = 2

class Vp9RefFrameType(enum.IntEnum):
    """
    Reference Frame types"""

    INTRA = 0
    """
    Intra reference frame
    """
    LAST = 1
    """
    Last Reference frame
    """
    GOLDEN = 2
    """
    Golden Reference frame
    """
    ALTREF = 3
    """
    Alternate Reference frame
    """
    MAX = 4

###############################################################
# classes
###############################################################

class AV1CDEFParams(GObject.GPointer, metaclass=GObject.GType):
    """
    Parameters of Constrained Directional Enhancement Filter (CDEF).
    """

    # gi Fields
    cdef_bits: int = ...
    """
    specifies the number of bits needed to specify which CDEF filter to apply.
    """
    cdef_damping: int = ...
    """
    controls the amount of damping in the deringing filter.
    """
    cdef_uv_pri_strength: bytes | None = ...
    """
    specify the strength of the primary filter (UV components).
    """
    cdef_uv_sec_strength: bytes | None = ...
    """
    specify the strength of the secondary filter (UV components).
    """
    cdef_y_pri_strength: bytes | None = ...
    """
    specify the strength of the primary filter (Y component)
    """
    cdef_y_sec_strength: bytes | None = ...
    """
    specify the strength of the secondary filter (Y component).
    """

class AV1ColorConfig(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    chroma_sample_position: AV1ChromaSamplePositions = ...
    color_description_present_flag: bool = ...
    """
    equal to 1 specifies that color_primaries,
      `transfer_characteristics`, and `matrix_coefficients` are present.
      `color_description_present_flag` equal to 0 specifies that `color_primaries`,
      `transfer_characteristics` and `matrix_coefficients` are not present.
    """
    color_primaries: AV1ColorPrimaries = ...
    """
    is an integer that is defined by the "Color primaries" section of
      ISO/IEC 23091-4/ITU-T H.273.
    """
    color_range: bool = ...
    """
    is a binary value that is associated with the VideoFullRangeFlag variable
      specified in ISO/IEC 23091-4/ITU-T H.273. color range equal to 0 shall be referred to
      as the studio swing representation and color range equal to 1 shall be referred to as
      the full swing representation for all intents relating to this specification.
    `subsampling_x`, `subsampling_y`: specify the chroma subsampling format. If
      `matrix_coefficients` is equal to GST_AV1_MC_IDENTITY, it is a requirement of bitstream
      conformance that `subsampling_x` is equal to 0 and `subsampling_y` is equal to 0.
    `chroma_sample_position` specifies the sample position for subsampled streams:
    """
    high_bitdepth: bool = ...
    """
    syntax element which, together with `seq_profile`, determine the bit depth.
    """
    matrix_coefficients: AV1MatrixCoefficients = ...
    """
    is an integer that is defined by the "Matrix coefficients" section
      of ISO/IEC 23091-4/ITU-T H.273.
    """
    mono_chrome: bool = ...
    """
    equal to 1 indicates that the video does not contain U and V color planes.
      `mono_chrome` equal to 0 indicates that the video contains Y, U, and V color planes.
    """
    separate_uv_delta_q: bool = ...
    """
    equal to 1 indicates that the U and V planes may have separate
     delta quantizer values. `separate_uv_delta_q` equal to 0 indicates that the U and V
     planes will share the same delta quantizer value.
    """
    subsampling_x: int = ...
    subsampling_y: int = ...
    transfer_characteristics: AV1TransferCharacteristics = ...
    """
    is an integer that is defined by the "Transfer characteristics"
      section of ISO/IEC 23091-4/ITU-T H.273.
    """
    twelve_bit: bool = ...
    """
    is syntax elements which, together with `seq_profile` and `high_bitdepth`,
      determines the bit depth.
    """

class AV1DecoderModelInfo(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    buffer_delay_length_minus_1: int = ...
    """
    plus 1 specifies the length of the
      `decoder_buffer_delay` and the `encoder_buffer_delay` syntax elements,
      in bits.
    """
    buffer_removal_time_length_minus_1: int = ...
    """
    plus 1 specifies the length of the
      `buffer_removal_time` syntax element, in bits.
    """
    frame_presentation_time_length_minus_1: int = ...
    """
    plus 1 specifies the length of the
      `frame_presentation_time` syntax element, in bits.
    """
    num_units_in_decoding_tick: int = ...
    """
    is the number of time units of a decoding clock
      operating at the frequency `time_scale` Hz that corresponds to one increment
      of a clock tick counter.
    """

class AV1FilmGrainParams(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    apply_grain: bool = ...
    """
    equal to 1 specifies that film grain should be added to this frame.
      apply_grain equal to 0 specifies that film grain should not be added.
    """
    ar_coeff_lag: int = ...
    """
    specifies the number of auto-regressive coefficients for luma and chroma.
    """
    ar_coeff_shift_minus_6: int = ...
    """
    specifies the range of the auto-regressive coefficients. Values
      of 0, 1, 2, and 3 correspond to the ranges for auto-regressive coefficients of [-2, 2),
      [-1, 1), [-0.5, 0.5) and [-0.25, 0.25) respectively.
    """
    ar_coeffs_cb_plus_128: bytes | None = ...
    """
    specifies auto-regressive coefficients used for the U plane.
    """
    ar_coeffs_cr_plus_128: bytes | None = ...
    """
    specifies auto-regressive coefficients used for the V plane.
    """
    ar_coeffs_y_plus_128: bytes | None = ...
    """
    specifies auto-regressive coefficients used for the Y plane.
    """
    cb_luma_mult: int = ...
    """
    represents a multiplier for the average luma component used in derivation
      of the input index to the cb component scaling function.
    """
    cb_mult: int = ...
    """
    represents a multiplier for the cb component used in derivation of the input
      index to the cb component scaling function.
    """
    cb_offset: int = ...
    """
    represents an offset used in derivation of the input index to the cb component
      scaling function.
    """
    chroma_scaling_from_luma: int = ...
    """
    specifies that the chroma scaling is inferred from the luma scaling.
    """
    clip_to_restricted_range: bool = ...
    """
    equal to 1 indicates that clipping to the restricted (studio)
      range shall be applied to the sample values after adding the film grain (see the
      semantics for color_range for an explanation of studio swing). clip_to_restricted_range
      equal to 0 indicates that clipping to the full range shall be applied to the sample
      values after adding the film grain.
    """
    cr_luma_mult: int = ...
    """
    represents a multiplier for the average luma component used in derivation
      of the input index to the cr component scaling function.
    """
    cr_mult: int = ...
    """
    represents a multiplier for the cr component used in derivation of the input
      index to the cr component scaling function.
    """
    cr_offset: int = ...
    """
    represents an offset used in derivation of the input index to the cr component
      scaling function.
    """
    film_grain_params_ref_idx: int = ...
    """
    indicates which reference frame contains the film grain
      parameters to be used for this frame.
    """
    grain_scale_shift: int = ...
    """
    specifies how much the Gaussian random numbers should be scaled down
      during the grain synthesis process.
    """
    grain_scaling_minus_8: int = ...
    """
    represents the shift - 8 applied to the values of the chroma
      component. The `grain_scaling_minus_8` can take values of 0..3 and determines the range and
      quantization step of the standard deviation of film grain.
    """
    grain_seed: int = ...
    """
    specifies the starting value for the pseudo-random numbers used during film
      grain synthesis.
    """
    num_cb_points: int = ...
    """
    specifies the number of points for the piece-wise linear scaling function
      of the cb component. It is a requirement of bitstream conformance that `num_cb_points` is
      less than or equal to 10.
    """
    num_cr_points: int = ...
    """
    specifies represents the number of points for the piece-wise linear scaling
      function of the cr component. It is a requirement of bitstream conformance that
      num_cr_points is less than or equal to 10. If subsampling_x is equal to 1 and
      `subsampling_y` is equal to 1 and num_cb_points is equal to 0, it is a requirement of
      bitstream conformance that num_cr_points is equal to 0. If `subsampling_x` is equal to 1
      and `subsampling_y` is equal to 1 and `num_cb_points` is not equal to 0, it is a requirement
      of bitstream conformance that `num_cr_points` is not equal to 0.
    """
    num_y_points: int = ...
    """
    specifies the number of points for the piece-wise linear scaling function
      of the luma component. It is a requirement of bitstream conformance that `num_y_points` is
      less than or equal to 14.
    """
    overlap_flag: bool = ...
    """
    equal to 1 indicates that the overlap between film grain blocks shall be
      applied. overlap_flag equal to 0 indicates that the overlap between film grain blocks
      shall not be applied.
    """
    point_cb_scaling: bytes | None = ...
    """
    represents the scaling (output) value for the i-th point of the
      piecewise linear scaling function for cb component.
    """
    point_cb_value: bytes | None = ...
    """
    represents the x coordinate for the i-th point of the piece-wise linear
      scaling function for cb component. The values are signaled on the scale of 0..255. If i
      is greater than 0, it is a requirement of bitstream conformance that point_cb_value[ i ]
      is greater than point_cb_value[ i - 1 ].
    """
    point_cr_scaling: bytes | None = ...
    """
    represents the scaling (output) value for the i-th point of the
      piecewise linear scaling function for cr component.
    """
    point_cr_value: bytes | None = ...
    """
    represents the x coordinate for the i-th point of the piece-wise linear
      scaling function for cr component. The values are signaled on the scale of 0..255. If i
      is greater than 0, it is a requirement of bitstream conformance that `point_cr_value`[ i ]
      is greater than `point_cr_value`[ i - 1 ].
    """
    point_y_scaling: bytes | None = ...
    """
    represents the scaling (output) value for the i-th point of the
      piecewise linear scaling function for luma component.
    """
    point_y_value: bytes | None = ...
    """
    represents the x (luma value) coordinate for the i-th point of the
      piecewise linear scaling function for luma component. The values are signaled on the
      scale of 0..255. (In case of 10 bit video, these values correspond to luma values divided
      by 4. In case of 12 bit video, these values correspond to luma values divided by 16.)
      If i is greater than 0, it is a r equirement of bitstream conformance that
      `point_y_value`[ i ] is greater than `point_y_value`[ i - 1 ] (this ensures the x coordinates
      are specified in increasing order).
    """
    update_grain: bool = ...
    """
    equal to 1 means that a new set of parameters should be sent. `update_grain`
      equal to 0 means that the previous set of parameters should be used.
    """

class AV1FrameHeaderOBU(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    all_lossless: int = ...
    """
    is a variable that is equal to 1 when `coded_lossless` is equal to 1 and
      `frame_width` is equal to `upscaled_width`. This indicates that the frame is fully lossless
      at the upscaled resolution.
    """
    allow_high_precision_mv: bool = ...
    """
    equal to 0 specifies that motion vectors are specified to quarter
      pel precision; `allow_high_precision_mv` equal to 1 specifies that motion vectors are
      specified to eighth pel precision.
    """
    allow_intrabc: bool = ...
    """
    equal to 1 indicates that intra block copy may be used in this frame.
      allow_intrabc equal to 0 indicates that intra block copy is not allowed in this frame.
    """
    allow_screen_content_tools: int = ...
    """
    equal to 1 indicates that intra blocks may use palette encoding;
      `allow_screen_content_tools` equal to 0 indicates that palette encoding is never used.
    """
    allow_warped_motion: bool = ...
    """
    equal to 1 indicates that the syntax element `motion_mode` may be
      present. `allow_warped_motion` equal to 0 indicates that the syntax element motion_mode
      will not be present (this means that LOCALWARP cannot be signaled if `allow_warped_motion`
      is equal to 0).
    """
    buffer_removal_time: list | None = ...
    """
    specifies the frame removal time in units of DecCT clock ticks
      counted from the removal time of the last frame with frame_type equal to KEY_FRAME for
      operating point opNum. `buffer_removal_time` is signaled as a fixed length unsigned integer
      with a length in bits given by `buffer_removal_time_length_minus_1` + 1. `buffer_removal_time`
      is the remainder of a modulo 1 << ( `buffer_removal_time_length_minus_1` + 1 ) counter.
    """
    buffer_removal_time_present_flag: bool = ...
    """
    equal to 1 specifies that `buffer_removal_time` is present
      in the bitstream. `buffer_removal_time_present_flag` equal to 0 specifies that
      `buffer_removal_time` is not present in the bitstream.
    """
    cdef_params: AV1CDEFParams | None = ...
    """
    a GstAV1CDEFParams holding the CDEF paramters.
    """
    coded_lossless: int = ...
    """
    is a variable that is equal to 1 when all segments use lossless encoding.
    """
    current_frame_id: int = ...
    """
    specifies the frame id number for the current frame. Frame id numbers
      are additional information that do not affect the decoding process, but provide decoders
      with a way of detecting missing reference frames so that appropriate action can be taken.
    """
    disable_cdf_update: bool = ...
    """
    specifies whether the CDF update in the symbol decoding process should
      be disabled.
    """
    disable_frame_end_update_cdf: bool = ...
    """
    equal to 1 indicates that the end of frame CDF update is
      disabled; `disable_frame_end_update_cdf` equal to 0 indicates that the end of frame CDF
      update is enabled.
    """
    display_frame_id: int = ...
    """
    provides the frame id number for the frame to output. It is a requirement
      of bitstream conformance that whenever `display_frame_id` is read, the value matches
      `ref_frame_id`[ `frame_to_show_map_idx` ] (the value of `current_frame_id` at the time that the
      frame indexed by `frame_to_show_map_idx` was stored), and that
      `ref_valid`[ `frame_to_show_map_idx` ] is equal to 1. It is a requirement of bitstream
      conformance that the number of bits needed to read `display_frame_id` does not exceed 16.
      This is equivalent to the constraint that idLen <= 16
    """
    error_resilient_mode: bool = ...
    """
    equal to 1 indicates that error resilient mode is enabled;
      `error_resilient_mode` equal to 0 indicates that error resilient mode is disabled.
    """
    expected_frame_id: list | None = ...
    """
    specifies the frame id for each frame used for reference. (Since: 1.24)
    """
    film_grain_params: AV1FilmGrainParams | None = ...
    """
    a GstAV1FilmGrainParams holding the Film Grain parameters.
    """
    force_integer_mv: bool = ...
    """
    equal to 1 specifies that motion vectors will always be integers.
      `force_integer_mv` equal to 0 specifies that motion vectors can contain fractional bits.
    """
    frame_height: int = ...
    """
    the frame height.
    """
    frame_is_intra: int = ...
    """
    if equal to 0 indicating that this frame may use inter prediction.
    """
    frame_presentation_time: int = ...
    """
    specifies the presentation time of the frame in clock ticks
      DispCT counted from the removal time of the last frame with frame_type equal to KEY_FRAME
      for the operating point that is being decoded. The syntax element is signaled as a fixed
      length unsigned integer with a length in bits given by
      `frame_presentation_time_length_minus_1` + 1. The `frame_presentation_time` is the remainder
      of a modulo 1 << (`frame_presentation_time_length_minus_1` + 1) counter.
    """
    frame_refs_short_signaling: bool = ...
    """
    equal to 1 indicates that only two reference frames are
      explicitly signaled. frame_refs_short_signaling equal to 0 indicates that all reference
      frames are explicitly signaled.
    """
    frame_size_override_flag: bool = ...
    """
    equal to 0 specifies that the frame size is equal to the size in
      the sequence header. `frame_size_override_flag` equal to 1 specifies that the frame size
      will either be specified as the size of one of the reference frames, or computed from the
      `frame_width_minus_1` and `frame_height_minus_1` syntax elements.
    """
    frame_to_show_map_idx: int = ...
    """
    specifies the frame to be output. It is only available if
      `show_existing_frame` is 1.
    """
    frame_type: AV1FrameType = ...
    """
    specifies the type of the frame.
    """
    frame_width: int = ...
    """
    the frame width.
    """
    global_motion_params: AV1GlobalMotionParams | None = ...
    """
    a GstAV1GlobalMotionParams holding the global motion parameters.
    """
    gold_frame_idx: int = ...
    """
    specifies the reference frame to use for GOLDEN_FRAME.
    `ref_frame_idx`[i]: specifies which reference frames are used by inter frames.
    `delta_frame_id_minus_1` is used to calculate `delta_frame_id`.
    """
    interpolation_filter: AV1InterpolationFilter = ...
    """
    a GstAV1InterpolationFilter that specifies the filter selection used
      for performing inter prediction.
    """
    is_filter_switchable: bool = ...
    """
    equal to 1 indicates that the filter selection is signaled at the
      block level; `is_filter_switchable` equal to 0 indicates that the filter selection is
      signaled at the frame level.
    """
    is_motion_mode_switchable: bool = ...
    """
    equal to 0 specifies that only the SIMPLE motion mode will be used.
    """
    last_frame_idx: int = ...
    """
    specifies the reference frame to use for LAST_FRAME.
    """
    loop_filter_params: AV1LoopFilterParams | None = ...
    """
    a GstAV1LoopFilterParams holding the loop filter parameters.
    """
    loop_restoration_params: AV1LoopRestorationParams | None = ...
    """
    a GstAV1LoopRestorationParams holding the loop restoration parameters.
    """
    lossless_array: bytes | None = ...
    """
    whether the segmentation is lossless.
    """
    order_hint: int = ...
    """
    is used to compute order_hint.
    """
    order_hints: list | None = ...
    """
    specifies the expected output order for each reference frame.
    """
    primary_ref_frame: int = ...
    """
    specifies which reference frame contains the CDF values and other state
      that should be loaded at the start of the frame.
    """
    quantization_params: AV1QuantizationParams | None = ...
    """
    a GstAV1QuantizationParams holding the quantization parameters.
    """
    reduced_tx_set: bool = ...
    """
    equal to 1 specifies that the frame is restricted to a reduced subset of
      the full set of transform types.
    """
    ref_frame_idx: list | None = ...
    ref_frame_sign_bias: list | None = ...
    """
    specifies the intended direction of the motion vector in time for
      each reference frame.
    """
    ref_global_motion_params: AV1GlobalMotionParams | None = ...
    """
    specifies the global motion parameters of the reference. (Since: 1.24)
    """
    ref_order_hint: list | None = ...
    """
    specifies the expected output order hint for each reference buffer.
    """
    reference_select: bool = ...
    """
    equal to 1 specifies that the mode info for inter blocks contains the
      syntax element comp_mode that indicates whether to use single or compound reference
      prediction. Reference_select equal to 0 specifies that all interblocks will use single
      prediction.
    """
    refresh_frame_flags: int = ...
    """
    contains a bitmask that specifies which reference frame slots will be
      updated with the current frame after it is decoded. If `frame_type` is equal to
      GST_AV1_INTRA_ONLY_FRAME, it is a requirement of bitstream conformance that
      `refresh_frame_flags` is not equal to 0xff.
    """
    render_and_frame_size_different: bool = ...
    """
    equal to 0 means that the render width and height are
      inferred from the frame width and height. `render_and_frame_size_different` equal to 1
      means that the render width and height are explicitly coded in the bitstream.
    """
    render_height: int = ...
    """
    the frame height to be rendered.
    """
    render_width: int = ...
    """
    the frame width to be rendered.
    """
    seg_qm_Level: bytes | None = ...
    """
    the segmentation's qm level.
    """
    segmentation_params: AV1SegmentationParams | None = ...
    """
    a GstAV1SegmentationParams holding the segmentation parameters.
    """
    show_existing_frame: bool = ...
    """
    equal to 1, indicates the frame indexed by `frame_to_show_map_idx` is
      to be output; `show_existing_frame` equal to 0 indicates that further processing is required.
      If `obu_type` is equal to GST_AV1_OBU_FRAME, it is a requirement of bitstream conformance that
      `show_existing_frame` is equal to 0.
    """
    show_frame: bool = ...
    """
    equal to 1 specifies that this frame should be immediately output once decoded.
      show_frame equal to 0 specifies that this frame should not be immediately output. (It may
      be output later if a later uncompressed header uses `show_existing_frame` equal to 1).
    """
    showable_frame: bool = ...
    """
    equal to 1 specifies that the frame may be output using the
      `show_existing_frame` mechanism. showable_frame equal to 0 specifies that this frame will
      not be output using the `show_existing_frame` mechanism. It is a requirement of bitstream
      conformance that when `show_existing_frame` is used to show a previous frame, that the
      value of `showable_frame` for the previous frame was equal to 1. It is a requirement of
      bitstream conformance that a particular showable frame is output via the
      `show_existing_frame` mechanism at most once.
    """
    skip_mode_frame: bytes | None = ...
    """
    specifies the frames to use for compound prediction when `skip_mode` is 1.
    """
    skip_mode_present: bool = ...
    """
    equal to 1 specifies that the syntax element `skip_mode` will be coded
      in the bitstream. `skip_mode_present` equal to 0 specifies that `skip_mode` will not be used
      for this frame.
    """
    superres_denom: int = ...
    """
    is the denominator of a fraction that specifies the ratio between the
      superblock width before and after upscaling.
    """
    tile_info: AV1TileInfo | None = ...
    """
    a GstAV1TileInfo holding the tile info.
    """
    tu_presentation_delay: int = ...
    """
    is a syntax element used by the decoder model. It does not affect
      the decoding process.
    """
    tx_mode: AV1TXModes = ...
    """
    specifies how the transform size is determined.
    """
    tx_mode_select: bool = ...
    """
    is used to compute TxMode.
    """
    upscaled_width: int = ...
    """
    the upscaled width.
    """
    use_ref_frame_mvs: bool = ...
    """
    equal to 1 specifies that motion vector information from a previous
      frame can be used when decoding the current frame. `use_ref_frame_mvs` equal to 0 specifies
      that this information will not be used.
    """
    use_superres: bool = ...
    """
    equal to 0 indicates that no upscaling is needed. `use_superres` equal to 1
      indicates that upscaling is needed.
    """

class AV1FrameOBU(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    frame_header: AV1FrameHeaderOBU | None = ...
    """
    a GstAV1FrameHeaderOBU holding frame_header data.
    """
    tile_group: AV1TileGroupOBU | None = ...
    """
    a GstAV1TileGroupOBU holding tile_group data.
    """

class AV1GlobalMotionParams(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    gm_params: list | None = ...
    """
    is set equal to SavedGmParams[ frame_to_show_map_idx ][ ref ][ j ] for
      ref = LAST_FRAME..ALTREF_FRAME, for j = 0..5.
    """
    gm_type: list | None = ...
    """
    specifying the type of global motion.
    """
    invalid: list | None = ...
    """
    whether this global motion parameters is invalid. (Since: 1.20)
    """
    is_global: list | None = ...
    """
    specifies whether global motion parameters are present for a particular
      reference frame.
    """
    is_rot_zoom: list | None = ...
    """
    specifies whether a particular reference frame uses rotation and zoom
      global motion.
    """
    is_translation: list | None = ...
    """
    specifies whether a particular reference frame uses translation
      global motion.
    """

class AV1LoopFilterParams(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    delta_lf_multi: int = ...
    """
    equal to 1 specifies that separate loop filter deltas are sent for
      horizontal luma edges, vertical luma edges, the U edges, and the V edges. `delta_lf_multi`
      equal to 0 specifies that the same loop filter delta is used for all edges.
    """
    delta_lf_present: bool = ...
    """
    specifies whether loop filter delta values are present in the bitstream.
    """
    delta_lf_res: int = ...
    """
    specifies the left shift which should be applied to decoded loop filter
      delta values.
    """
    loop_filter_delta_enabled: bool = ...
    """
    equal to 1 means that the filter level depends on the mode and
      reference frame used to predict a block. `loop_filter_delta_enabled` equal to 0 means that
      the filter level does not depend on the mode and reference frame.
    """
    loop_filter_delta_update: bool = ...
    """
    equal to 1 means that the bitstream contains additional syntax
      elements that specify which mode and reference frame deltas are to be updated.
      `loop_filter_delta_update` equal to 0 means that these syntax elements are not present.
    """
    loop_filter_level: bytes | None = ...
    """
    is an array containing loop filter strength values. Different loop
      filter strength values from the array are used depending on the image plane being
      filtered, and the edge direction (vertical or horizontal) being filtered.
    """
    loop_filter_mode_deltas: list | None = ...
    """
    contains the adjustment needed for the filter level based on
      the chosen mode. If this syntax element is not present in the bitstream, it maintains
      its previous value.
    """
    loop_filter_ref_deltas: list | None = ...
    """
    contains the adjustment needed for the filter level based on
      the chosen reference frame. If this syntax element is not present in the bitstream,
      it maintains its previous value.
    """
    loop_filter_sharpness: int = ...
    """
    indicates the sharpness level. The `loop_filter_level` and
      `loop_filter_sharpness` together determine when a block edge is filtered, and by how much
      the filtering can change the sample values. The loop filter process is described in AV1
      Bitstream Spec. section 7.14.
    """

class AV1LoopRestorationParams(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    frame_restoration_type: list | None = ...
    """
    specifies the type of restoration used for each plane.
    """
    loop_restoration_size: list | None = ...
    """
    specifies the size of loop restoration units in units of samples in
      the current plane.
    """
    lr_unit_shift: int = ...
    """
    specifies if the luma restoration size should be halved.
    """
    lr_uv_shift: bool = ...
    """
    is only present for 4:2:0 formats and specifies if the chroma size should be
      half the luma size.
    """
    uses_lr: int = ...
    """
    indicates if any plane uses loop restoration.
    """

class AV1MetadataHdrCll(GObject.GPointer, metaclass=GObject.GType):
    """
    High Dynamic Range content light level syntax metadata.
    """

    # gi Fields
    max_cll: int = ...
    """
    specifies the maximum content light level as specified in CEA-861.3, Appendix A.
    """
    max_fall: int = ...
    """
    specifies the maximum frame-average light level as specified in CEA-861.3, Appendix A.
    """

class AV1MetadataHdrMdcv(GObject.GPointer, metaclass=GObject.GType):
    """
    High Dynamic Range mastering display color volume metadata.
    """

    # gi Fields
    luminance_max: int = ...
    """
    is a 24.8 fixed-point maximum luminance, represented in candelas per
      square meter.
    """
    luminance_min: int = ...
    """
    is a 18.14 fixed-point minimum luminance, represented in candelas per
      square meter.
    """
    primary_chromaticity_x: list | None = ...
    """
    specifies a 0.16 fixed-point X chromaticity coordinate as
      defined by CIE 1931, where i = 0,1,2 specifies Red, Green, Blue respectively.
    """
    primary_chromaticity_y: list | None = ...
    """
    specifies a 0.16 fixed-point Y chromaticity coordinate as
      defined by CIE 1931, where i = 0,1,2 specifies Red, Green, Blue respectively.
    """
    white_point_chromaticity_x: int = ...
    """
    specifies a 0.16 fixed-point white X chromaticity coordinate
      as defined by CIE 1931.
    """
    white_point_chromaticity_y: int = ...
    """
    specifies a 0.16 fixed-point white Y chromaticity coordinate
      as defined by CIE 1931.
    """

class AV1MetadataITUT_T35(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    itu_t_t35_country_code: int = ...
    """
    shall be a byte having a value specified as a country code by
      Annex A of Recommendation ITU-T T.35.
    """
    itu_t_t35_country_code_extention_byte: int = ...
    itu_t_t35_payload_bytes: int = ...
    """
    shall be bytes containing data registered as specified in
      Recommendation ITU-T T.35.
    """

class AV1MetadataOBU(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    metadata_type: AV1MetadataType = ...
    """
    type of metadata
    """

class AV1MetadataScalability(GObject.GPointer, metaclass=GObject.GType):
    """
    The scalability metadata OBU is intended for use by intermediate
    processing entities that may perform selective layer elimination.
    """

    # gi Fields
    scalability_mode_idc: AV1ScalabilityModes = ...
    """
    indicates the picture prediction structure of the bitstream.
    """
    spatial_layer_description_present_flag: bool = ...
    """
    indicates when set to 1 that the
      spatial_layer_ref_id is present for each of the (`spatial_layers_cnt_minus_1` + 1) layers,
      or that it is not present when set to 0.
    """
    spatial_layer_dimensions_present_flag: bool = ...
    """
    indicates when set to 1 that the
      `spatial_layer_max_width` and `spatial_layer_max_height` parameters are present for each of
      the (`spatial_layers_cnt_minus_1` + 1) layers, or that it they are not present when set to 0.
    """
    spatial_layer_max_height: list | None = ...
    """
    specifies the maximum frame height for the frames with
      `spatial_id` equal to i. This number must not be larger than `max_frame_height_minus_1` + 1.
    """
    spatial_layer_max_width: list | None = ...
    """
    specifies the maximum frame width for the frames with
      `spatial_id` equal to i. This number must not be larger than `max_frame_width_minus_1` + 1.
    """
    spatial_layer_ref_id: bytes | None = ...
    """
    specifies the `spatial_id` value of the frame within the current
      temporal unit that the frame of layer i uses for reference. If no frame within the
      current temporal unit is used for reference the value must be equal to 255.
    """
    spatial_layers_cnt_minus_1: int = ...
    """
    indicates the number of spatial layers present in the video
      sequence minus one.
    """
    temporal_group_description_present_flag: bool = ...
    """
    indicates when set to 1 that the temporal
      dependency information is present, or that it is not when set to 0.
    """
    temporal_group_ref_cnt: bytes | None = ...
    """
    indicates the number of reference pictures used by the i-th
      picture in the temporal group.
    """
    temporal_group_ref_pic_diff: bytes | None = ...
    """
    indicates, for the i-th picture in the temporal group,
      the temporal distance between the i-th picture and the j-th reference picture used by
      the i-th picture. The temporal distance is measured in frames, counting only frames of
      identical `spatial_id` values.
    """
    temporal_group_size: int = ...
    """
    indicates the number of pictures in a temporal picture group. If the
      `temporal_group_size` is greater than 0, then the scalability structure data allows the
      inter-picture temporal dependency structure of the video sequence to be specified. If the
      `temporal_group_size` is greater than 0, then for `temporal_group_size` pictures in the
      temporal group, each picture's temporal layer id (`temporal_id`), switch up points
      (`temporal_group_temporal_switching_up_point_flag` and
      `temporal_group_spatial_switching_up_point_flag`), and the reference picture indices
      (`temporal_group_ref_pic_diff`) are specified. The first picture specified in a temporal
      group must have `temporal_id` equal to 0. If the parameter `temporal_group_size` is not
      present or set to 0, then either there is only one temporal layer or there is no fixed
      inter-picture temporal dependency present going forward in the video sequence. Note that
      for a given picture, all frames follow the same inter-picture temporal dependency
      structure. However, the frame rate of each layer can be different from each other. The
      specified dependency structure in the scalability structure data must be for the highest
      frame rate layer.
    """
    temporal_group_spatial_switching_up_point_flag: bytes | None = ...
    """
    is set to 1 if spatial layers of the
      current picture in the temporal group (i.e., pictures with a spatial_id higher than zero)
      do not depend on any picture preceding the current picture in the temporal group.
    """
    temporal_group_temporal_id: bytes | None = ...
    """
    specifies the temporal_id value for the i-th picture in
      the temporal group.
    """
    temporal_group_temporal_switching_up_point_flag: bytes | None = ...
    """
    is set to 1 if subsequent (in decoding
      order) pictures with a `temporal_id` higher than `temporal_group_temporal_id`[i] do not
      depend on any picture preceding the current picture (in coding order) with `temporal_id`
      higher than `temporal_group_temporal_id`[ i ].
    """

class AV1MetadataTimecode(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    cnt_dropped_flag: bool = ...
    """
    specifies the skipping of one or more values of `n_frames` using the
      counting method specified by counting_type.
    """
    counting_type: int = ...
    """
    specifies the method of dropping values of the n_frames syntax element as
      specified in AV1 Spec 6.1.1. `counting_type` should be the same for all pictures in the
      coded video sequence.
    """
    discontinuity_flag: bool = ...
    """
    equal to 0 indicates that the difference between the current value
      of clockTimestamp and the value of clockTimestamp computed from the previous set of
      timestamp syntax elements in output order can be interpreted as the time difference
      between the times of origin or capture of the associated frames or fields.
      `discontinuity_flag` equal to 1 indicates that the difference between the current value of
      clockTimestamp and the value of clockTimestamp computed from the previous set of clock
      timestamp syntax elements in output order should not be interpreted as the time difference
      between the times of origin or capture of the associated frames or fields.
    """
    full_timestamp_flag: bool = ...
    """
    equal to 1 indicates that the the `seconds_value`, `minutes_value`,
      `hours_value` syntax elements will be present. `full_timestamp_flag` equal to 0 indicates
      that there are flags to control the presence of these syntax elements.
    """
    hours_flag: bool = ...
    """
    equal to 1 specifies that `hours_value` is present when `full_timestamp_flag` is
      equal to 0 and `seconds_flag` is equal to 1 and `minutes_flag` is equal to 1.
    """
    hours_value: int = ...
    """
    is used to compute clockTimestamp and shall be in the range of 0 to 23,
      inclusive. When `hours_value` is not present, its value is inferred to be equal to the
      value of `hours_value` for the previous set of clock timestamp syntax elements in decoding
      order, and it is required that such a previous `hours_value` shall have been present.
    """
    minutes_flag: bool = ...
    """
    equal to 1 specifies that `minutes_value` and `hours_flag` are present when
      `full_timestamp_flag` is equal to 0 and `seconds_flag` is equal to 1. `minutes_flag` equal to 0
      specifies that `minutes_value` and `hours_flag` are not present.
    """
    minutes_value: int = ...
    """
    specifies the value of mm used to compute clockTimestamp and shall be in
      the range of 0 to 59, inclusive. When minutes_value is not present, its value is inferred
      to be equal to the value of `minutes_value` for the previous set of clock timestamp syntax
      elements in decoding order, and it is required that such a previous `minutes_value` shall
      have been present.
    """
    n_frames: int = ...
    """
    is used to compute clockTimestamp. When `timing_info_present_flag` is equal to 1,
      `n_frames` shall be less than maxFps, where maxFps is specified by
      maxFps = ceil( time_scale / ( 2 * `num_units_in_display_tick` ) ).
    """
    seconds_flag: bool = ...
    """
    equal to 1 specifies that `seconds_value` and `minutes_flag` are present when
      `full_timestamp_flag` is equal to 0. `seconds_flag` equal to 0 specifies that `seconds_value`
      and `minutes_flag` are not present.
    """
    seconds_value: int = ...
    """
    is used to compute clockTimestamp and shall be in the range of 0 to 59.
      When `seconds_value` is not present, its value is inferred to be equal to the value of
      `seconds_value` for the previous set of clock timestamp syntax elements in decoding order,
      and it is required that such a previous `seconds_value` shall have been present.
    """
    time_offset_length: int = ...
    """
    greater than 0 specifies the length in bits of the `time_offset_value`
      syntax element. `time_offset_length` equal to 0 specifies that the `time_offset_value` syntax
      element is not present. `time_offset_length` should be the same for all pictures in the
      coded video sequence.
    """
    time_offset_value: int = ...
    """
    is used to compute clockTimestamp. The number of bits used to represent
      `time_offset_value` is equal to `time_offset_length`. When `time_offset_value` is not present,
      its value is inferred to be equal to 0.
    """

class AV1OBU(GObject.GPointer, metaclass=GObject.GType):
    """
    It is the general representation of AV1 OBU (Open Bitstream
    Unit). One OBU include its header and payload.
    """

    # gi Fields
    data: int = ...
    """
    references the current data chunk that holds the OBU
    """
    header: AV1OBUHeader | None = ...
    """
    a GstAV1OBUHeader OBU Header
    """
    obu_size: int = ...
    """
    size of the OBU, not include header size
    """
    obu_type: AV1OBUType = ...
    """
    the type of data structure contained in the OBU payload.
    """

class AV1OBUHeader(GObject.GPointer, metaclass=GObject.GType):
    """
    Collect info for OBU header and OBU extension header if
    obu_extension_flag == 1.
    """

    # gi Fields
    obu_extention_flag: bool = ...
    """
    indicates if OBU header extention is present.
    """
    obu_has_size_field: bool = ...
    """
    equal to 1 indicates that the obu_size syntax element will be
      present. `obu_has_size_field` equal to 0 indicates that the `obu_size` syntax element
      will not be present.
    """
    obu_spatial_id: int = ...
    """
    specifies the spatial level of the data contained in the OBU.
    """
    obu_temporal_id: int = ...
    """
    specifies the temporal level of the data contained in the OBU.
    """
    obu_type: AV1OBUType = ...
    """
    the type of data structure contained in the OBU payload.
    """

class AV1OperatingPoint(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    decoder_buffer_delay: int = ...
    """
    specifies the time interval between the arrival of the first bit
      in the smoothing buffer and the subsequent removal of the data that belongs to the
      first coded frame for operating point op, measured in units of 1/90000 seconds. The
      length of `decoder_buffer_delay` is specified by `buffer_delay_length_minus_1` + 1, in bits.
    """
    decoder_model_present_for_this_op: bool = ...
    """
    equal to one indicates that there is a decoder model
      associated with this operating point. `decoder_model_present_for_this_op` equal to zero
      indicates that there is not a decoder model associated.
    """
    encoder_buffer_delay: int = ...
    """
    specifies, in combination with `decoder_buffer_delay` syntax element,
      the first bit arrival time of frames to be decoded to the smoothing buffer.
      `encoder_buffer_delay` is measured in units of 1/90000 seconds. For a video sequence that
      includes one or more random access points the sum of `decoder_buffer_delay` and
      `encoder_buffer_delay` shall be kept constant.
    """
    idc: int = ...
    """
    contains a bitmask that indicates which spatial and temporal layers should be
      decoded. Bit k is equal to 1 if temporal layer k should be decoded (for k between
      0 and 7). Bit j+8 is equal to 1 if spatial layer j should be decoded (for j between
      0 and 3).
    """
    initial_display_delay_minus_1: int = ...
    """
    plus 1 specifies, for operating point i, the number of
      decoded frames that should be present in the buffer pool before the first presentable
      frame is displayed. This will ensure that all presentable frames in the sequence can
      be decoded at or before the time that they are scheduled for display.
    """
    initial_display_delay_present_for_this_op: bool = ...
    """
    equal to 1 indicates that
      `initial_display_delay_minus_1` is specified for this operating. 0 indicates that
      `initial_display_delay_minus_1` is not specified for this operating point.
    """
    low_delay_mode_flag: bool = ...
    """
    equal to 1 indicates that the smoothing buffer operates in low-delay
      mode for operating point op. In low-delay mode late decode times and buffer underflow
      are both permitted. `low_delay_mode_flag` equal to 0 indicates that the smoothing buffer
      operates in strict mode, where buffer underflow is not allowed.
    """
    seq_level_idx: int = ...
    """
    specifies the level that the coded video sequence conforms to.
    """
    seq_tier: int = ...
    """
    specifies the tier that the coded video sequence conforms to.
    """

class AV1Parser(GObject.GPointer, metaclass=GObject.GType):
    """
    For more details about the structures, you can refer to the AV1 Bitstream &
    Decoding Process Specification V1.0.0
    [specification](https://aomediacodec.github.io/av1-spec/av1-spec.pdf)

    It offers you bitstream parsing of low overhead bistream format (Section 5)
    or Annex B according to the setting of the parser. By calling the function of
    `gst_av1_parser_reset`, user can switch between bistream mode and Annex B mode.

    To retrieve OBUs and parse its headers, you should firstly call the function of
    `gst_av1_parser_identify_one_obu` to get the OBU type if succeeds or just discard
    the data if fails.

    Then, depending on the GstAV1OBUType of the newly parsed GstAV1OBU,
    you should call the differents functions to parse the structure details:

      * GST_AV1_OBU_SEQUENCE_HEADER: `gst_av1_parser_parse_sequence_header_obu`

      * GST_AV1_OBU_TEMPORAL_DELIMITER: `gst_av1_parser_parse_temporal_delimiter_obu`

      * GST_AV1_OBU_FRAME: `gst_av1_parser_parse_frame_obu`

      * GST_AV1_OBU_FRAME_HEADER: `gst_av1_parser_parse_frame_header_obu`

      * GST_AV1_OBU_TILE_GROUP: `gst_av1_parser_parse_tile_group_obu`

      * GST_AV1_OBU_METADATA: `gst_av1_parser_parse_metadata_obu`

      * GST_AV1_OBU_REDUNDANT_FRAME_HEADER: `gst_av1_parser_parse_frame_header_obu`

      * GST_AV1_OBU_TILE_LIST: `gst_av1_parser_parse_tile_list_obu`

    Note: Some parser functions are dependent on information provided in the sequence
    header and reference frame's information. It maintains a state inside itself, which
    contains all global vars and reference information during the whole parsing process.
    Calling `gst_av1_parser_reset` or a new sequence's arriving can clear and reset this
    inside state.

    After successfully handled a frame(for example, decode a frame successfully), you
    should call `gst_av1_parser_reference_frame_update` to update the parser's inside
    state(such as reference information, global segmentation information, etc).
    """

    # gi Fields
    @builtins.property
    def annex_b(self) -> bool: ...
    @builtins.property
    def frame_unit_consumed(self) -> int: ...
    @builtins.property
    def frame_unit_size(self) -> int: ...
    @builtins.property
    def seq_header(self) -> AV1SequenceHeaderOBU | None: ...
    @builtins.property
    def state(self) -> AV1ParserState | None: ...
    @builtins.property
    def temporal_unit_consumed(self) -> int: ...
    @builtins.property
    def temporal_unit_size(self) -> int: ...

    # gi Methods
    def free(self) -> None:
        """
            If parser is not None, frees its allocated memory.

        It cannot be used afterwards.
        """
    def identify_one_obu(self, data: int, size: int, obu: AV1OBU) -> tuple[AV1ParserResult, int]:
        """
            Identify one `obu`'s type from the incoming data stream. This function
        should be called first to know the type of `obu` before other parse APIs.
        """
    def parse_frame_header_obu(self, obu: AV1OBU, frame_header: AV1FrameHeaderOBU) -> AV1ParserResult:
        """
            Parse one frame header `obu` based on the `parser` context, store the result
        in the `frame`.
        """
    def parse_frame_obu(self, obu: AV1OBU, frame: AV1FrameOBU) -> AV1ParserResult:
        """
            Parse one frame `obu` based on the `parser` context, store the result
        in the `frame`.
        """
    def parse_metadata_obu(self, obu: AV1OBU, metadata: AV1MetadataOBU) -> AV1ParserResult:
        """
        Parse one meta data `obu` based on the `parser` context.
        """
    def parse_sequence_header_obu(self, obu: AV1OBU, seq_header: AV1SequenceHeaderOBU) -> AV1ParserResult:
        """
            Parse one sequence header `obu` based on the `parser` context, store the
        result in the `seq_header`.
        """
    def parse_temporal_delimiter_obu(self, obu: AV1OBU) -> AV1ParserResult:
        """
            Parse one temporal delimiter `obu` based on the `parser` context.
        The temporal delimiter is just delimiter and contains no content.
        """
    def parse_tile_group_obu(self, obu: AV1OBU, tile_group: AV1TileGroupOBU) -> AV1ParserResult:
        """
            Parse one tile group `obu` based on the `parser` context, store the result
        in the `tile_group`.
        """
    def parse_tile_list_obu(self, obu: AV1OBU, tile_list: AV1TileListOBU) -> AV1ParserResult:
        """
            Parse one tile list `obu` based on the `parser` context, store the result
        in the `tile_list`. It is for large scale tile coding mode.
        """
    def reference_frame_update(self, frame_header: AV1FrameHeaderOBU) -> AV1ParserResult:
        """
            Update the context of `frame_header` to parser's state. This function is
        used when we finish one frame's decoding/showing, and need to update info
        such as reference, global parameters.
        """
    def reset(self, annex_b: bool) -> None:
        """
        Reset the current GstAV1Parser's state totally.
        """
    def reset_annex_b(self) -> None:
        """
            Only reset the current GstAV1Parser's annex b context.
        The other part of the state is kept.
        """
    def set_operating_point(self, operating_point: int) -> AV1ParserResult:
        """
        Set the operating point to filter OBUs.
        """

class AV1ParserState(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    begin_first_frame: bool = ...
    current_frame_id: int = ...
    frame_height: int = ...
    frame_width: int = ...
    mi_col_starts: list | None = ...
    mi_cols: int = ...
    mi_row_starts: list | None = ...
    mi_rows: int = ...
    operating_point: int = ...
    operating_point_idc: int = ...
    prev_frame_id: int = ...
    ref_info: AV1ReferenceFrameInfo | None = ...
    render_height: int = ...
    render_width: int = ...
    seen_frame_header: int = ...
    sequence_changed: bool = ...
    tile_cols: int = ...
    tile_cols_log2: int = ...
    tile_rows: int = ...
    tile_rows_log2: int = ...
    tile_size_bytes: int = ...
    upscaled_width: int = ...

class AV1QuantizationParams(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    base_q_idx: int = ...
    """
    indicates the base frame qindex. This is used for Y AC coefficients and as
      the base value for the other quantizers.
    """
    delta_q_present: bool = ...
    """
    specifies whether quantizer index delta values are present in the bitstream.
    """
    delta_q_res: int = ...
    """
    specifies the left shift which should be applied to decoded quantizer index
      delta values.
    """
    delta_q_u_ac: int = ...
    """
    indicates the U AC quantizer relative to base_q_idx.
    """
    delta_q_u_dc: int = ...
    """
    indicates the U DC quantizer relative to base_q_idx.
    """
    delta_q_v_ac: int = ...
    """
    indicates the V AC quantizer relative to base_q_idx.
    """
    delta_q_v_dc: int = ...
    """
    indicates the V DC quantizer relative to base_q_idx.
    """
    delta_q_y_dc: int = ...
    """
    indicates the Y DC quantizer relative to base_q_idx.
    """
    diff_uv_delta: bool = ...
    """
    equal to 1 indicates that the U and V delta quantizer values are coded
      separately. `diff_uv_delta` equal to 0 indicates that the U and V delta quantizer values
      share a common value.
    """
    qm_u: int = ...
    """
    specifies the level in the quantizer matrix that should be used for chroma U plane decoding.
    """
    qm_v: int = ...
    """
    specifies the level in the quantizer matrix that should be used for chroma V plane decoding.
    """
    qm_y: int = ...
    """
    specifies the level in the quantizer matrix that should be used for luma plane decoding.
    """
    using_qmatrix: bool = ...
    """
    specifies that the quantizer matrix will be used to compute quantizers.
    """

class AV1ReferenceFrameInfo(GObject.GPointer, metaclass=GObject.GType):
    """
    All the info related to a reference frames.
    """

    # gi Fields
    entry: list | None = ...

class AV1ReferenceFrameInfoEntry(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    ref_bit_depth: int = ...
    ref_film_grain_params: AV1FilmGrainParams | None = ...
    ref_frame_height: int = ...
    ref_frame_id: int = ...
    ref_frame_type: AV1FrameType = ...
    ref_frame_width: int = ...
    ref_global_motion_params: AV1GlobalMotionParams | None = ...
    ref_lf_params: AV1LoopFilterParams | None = ...
    ref_mi_cols: int = ...
    ref_mi_rows: int = ...
    ref_order_hint: int = ...
    ref_render_height: int = ...
    ref_render_width: int = ...
    ref_segmentation_params: AV1SegmentationParams | None = ...
    ref_subsampling_x: int = ...
    ref_subsampling_y: int = ...
    ref_tile_info: AV1TileInfo | None = ...
    ref_upscaled_width: int = ...
    ref_valid: bool = ...

class AV1SegmentationParams(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    feature_data: list | None = ...
    """
    the value of according segmentation feature.
    """
    feature_enabled: list | None = ...
    """
    set to 1 when the feature of segmentation is enabled.
    """
    last_active_seg_id: int = ...
    """
    indicates the highest numbered segment id that has some enabled feature.
      This is used when decoding the segment id to only decode choices corresponding to used
      segments.
    """
    seg_id_pre_skip: int = ...
    """
    equal to 1 indicates that the segment id will be read before the skip
      syntax element. `seg_id_pre_skip` equal to 0 indicates that the skip syntax element will be
      read first.
    """
    segmentation_enabled: bool = ...
    """
    equal to 1 indicates that this frame makes use of the segmentation
      tool; `segmentation_enabled` equal to 0 indicates that the frame does not use segmentation.
    """
    segmentation_temporal_update: int = ...
    """
    equal to 1 indicates that the updates to the segmentation map
      are coded relative to the existing segmentation map. `segmentation_temporal_update` equal to
      0 indicates that the new segmentation map is coded without reference to the existing
      segmentation map.
    """
    segmentation_update_data: int = ...
    """
    equal to 1 indicates that new parameters are about to be
      specified for each segment. `segmentation_update_data` equal to 0 indicates that the
      segmentation parameters should keep their existing values.
    """
    segmentation_update_map: int = ...
    """
    equal to 1 indicates that the segmentation map are updated during
      the decoding of this frame. `segmentation_update_map` equal to 0 means that the segmentation
      map from the previous frame is used.
    """

class AV1SequenceHeaderOBU(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    additional_frame_id_length_minus_1: int = ...
    """
    is used to calculate the number of bits used to
      encode the frame_id syntax element.
    """
    bit_depth: int = ...
    """
    the bit depth of the stream.
    """
    color_config: AV1ColorConfig | None = ...
    """
    hold the color configuration.
    """
    decoder_model_info: AV1DecoderModelInfo | None = ...
    """
    holds information about the decoder model.
    """
    decoder_model_info_present_flag: bool = ...
    """
    specifies whether the decoder model info is present in
      the bitstream.
    """
    delta_frame_id_length_minus_2: int = ...
    """
    specifies the number of bits minus 2 used to encode
      delta_frame_id syntax elements.
    """
    enable_cdef: bool = ...
    """
    equal to 1 specifies that cdef filtering may be enabled. enable_cdef equal
      to 0 specifies that cdef filtering is disabled.
    """
    enable_dual_filter: bool = ...
    """
    equal to 1 indicates that the inter prediction filter type may be
      specified independently in the horizontal and vertical directions. If the flag is equal
      to 0, only one filter type may be specified, which is then used in both directions.
    """
    enable_filter_intra: bool = ...
    """
    equal to 1 specifies that the `use_filter_intra` syntax element may
      be present. `enable_filter_intra` equal to 0 specifies that the `use_filter_intra` syntax
      element will not be present.
    """
    enable_interintra_compound: bool = ...
    """
    equal to 1 specifies that the mode info for inter blocks may
      contain the syntax element interintra. `enable_interintra_compound` equal to 0 specifies
      that the syntax element interintra will not be present.
    """
    enable_intra_edge_filter: bool = ...
    """
    specifies whether the intra edge filtering process should be enabled.
    """
    enable_jnt_comp: bool = ...
    """
    equal to 1 indicates that the distance weights process may be used
      for inter prediction.
    """
    enable_masked_compound: bool = ...
    """
    equal to 1 specifies that the mode info for inter blocks may
      contain the syntax element `compound_type`. `enable_masked_compound` equal to 0 specifies
      that the syntax element `compound_type` will not be present.
    """
    enable_order_hint: bool = ...
    """
    equal to 1 indicates that tools based on the values of order hints
      may be used. `enable_order_hint` equal to 0 indicates that tools based on order hints
      are disabled.
    """
    enable_ref_frame_mvs: bool = ...
    """
    equal to 1 indicates that the `use_ref_frame_mvs` syntax element
      may be present. `enable_ref_frame_mvs` equal to 0 indicates that the `use_ref_frame_mvs`
      syntax element will not be present.
    """
    enable_restoration: bool = ...
    """
    equal to 1 specifies that loop restoration filtering may be enabled.
      enable_restoration equal to 0 specifies that loop restoration filtering is disabled.
    """
    enable_superres: bool = ...
    """
    equal to 1 specifies that the use_superres syntax element will be present
      in the uncompressed header. enable_superres equal to 0 specifies that the use_superres
      syntax element will not be present (instead use_superres will be set to 0 in the
      uncompressed header without being read).
    """
    enable_warped_motion: bool = ...
    """
    equal to 1 indicates that the allow_warped_motion syntax element
      may be present. `enable_warped_motion` equal to 0 indicates that the `allow_warped_motion`
      syntax element will not be present.
    """
    film_grain_params_present: int = ...
    """
    specifies whether film grain parameters are present in the bitstream.
    """
    frame_height_bits_minus_1: int = ...
    """
    specifies the number of bits minus 1 used for transmitting
      the frame height syntax elements.
    """
    frame_id_numbers_present_flag: bool = ...
    """
    specifies whether frame id numbers are present in the bitstream.
    """
    frame_width_bits_minus_1: int = ...
    """
    specifies the number of bits minus 1 used for transmitting
      the frame width syntax elements.
    """
    initial_display_delay_present_flag: int = ...
    """
    specifies whether initial display delay information
      is present in the bitstream or not.
    """
    max_frame_height_minus_1: int = ...
    """
    specifies the maximum frame height minus 1 for the frames
      represented by this sequenceheader.
    """
    max_frame_width_minus_1: int = ...
    """
    specifies the maximum frame width minus 1 for the frames
      represented by this sequenceheader.
    """
    num_planes: int = ...
    """
    the YUV plane number.
    """
    operating_points: list | None = ...
    """
    specifies the corresponding operating point for a set of operating
      parameters.
    """
    operating_points_cnt_minus_1: int = ...
    """
    indicates the number of operating points minus 1 present
      in this bitstream.
    """
    order_hint_bits: int = ...
    """
    specifies the number of bits used for the order_hint syntax element.
    """
    order_hint_bits_minus_1: int = ...
    """
    is used to compute OrderHintBits.
    """
    reduced_still_picture_header: int = ...
    """
    specifies that the syntax elements not needed by a still
      picture are omitted.
    """
    seq_choose_integer_mv: bool = ...
    """
    equal to 0 indicates that the seq_force_integer_mv syntax element
      will be present. `seq_choose_integer_mv` equal to 1 indicates that `seq_force_integer_mv`
      should be set equal to SELECT_INTEGER_MV.
    """
    seq_choose_screen_content_tools: bool = ...
    """
    equal to 0 indicates that the `seq_force_screen_content_tools`
      syntax element will be present. `seq_choose_screen_content_tools` equal to 1 indicates
      that `seq_force_screen_content_tools` should be set equal to SELECT_SCREEN_CONTENT_TOOLS.
    """
    seq_force_integer_mv: int = ...
    """
    equal to SELECT_INTEGER_MV indicates that the `force_integer_mv`
      syntax element will be present in the frame header (providing allow_screen_content_tools
      is equal to 1). Otherwise, `seq_force_integer_mv` contains the value for `force_integer_mv`.
    """
    seq_force_screen_content_tools: int = ...
    """
    equal to SELECT_SCREEN_CONTENT_TOOLS indicates that the
      `allow_screen_content_tools` syntax element will be present in the frame header. Otherwise,
      `seq_force_screen_content_tools` contains the value for `allow_screen_content_tools`.
    """
    seq_profile: AV1Profile = ...
    """
    specifies the features that can be used in the coded video sequence
    """
    still_picture: bool = ...
    """
    equal to 1 specifies that the bitstream contains only one coded frame.
    """
    timing_info: AV1TimingInfo | None = ...
    """
    holds the timing information.
    """
    timing_info_present_flag: bool = ...
    """
    specifies whether timing info is present in the bitstream.
    """
    use_128x128_superblock: bool = ...
    """
    when equal to 1, indicates that superblocks contain 128x128 luma
      samples. When equal to 0, it indicates that superblocks contain 64x64 luma samples.
      (The number of contained chroma samples depends on `subsampling_x` and `subsampling_y`).
    """

class AV1TileGroupOBU(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    entry: list | None = ...
    num_tiles: int = ...
    """
    specifies the total number of tiles in the frame.
    """
    tg_end: int = ...
    """
    specifies the zero-based index of the last tile in the current tile group.
      It is a requirement of bitstream conformance that the value of tg_end is greater
      than or equal to tg_start. It is a requirement of bitstream conformance that the
      value of tg_end for the last tile group in each frame is equal to num_tiles-1.
    """
    tg_start: int = ...
    """
    specifies the zero-based index of the first tile in the current tile group.
      It is a requirement of bitstream conformance that the value of `tg_start` is equal to
      the value of TileNum at the point that tile_group_obu is invoked.
    """
    tile_start_and_end_present_flag: bool = ...
    """
    specifies whether `tg_start` and `tg_end` are present
      in the bitstream. If `tg_start` and `tg_end` are not present in the bitstream, this
      tile group covers the entire frame. If `obu_type` is equal to GST_AV1_OBU_FRAME, it is a
      requirement of bitstream conformance that the value of `tile_start_and_end_present_flag`
      is equal to 0.
    """

class AV1TileGroupOBUEntry(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    mi_col_end: int = ...
    mi_col_start: int = ...
    mi_row_end: int = ...
    mi_row_start: int = ...
    tile_col: int = ...
    tile_offset: int = ...
    tile_row: int = ...
    tile_size: int = ...

class AV1TileInfo(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    context_update_tile_id: int = ...
    """
    specifies which tile to use for the CDF update.
    """
    height_in_sbs_minus_1: list | None = ...
    """
    specifies the height of a tile minus 1 in units of superblocks.
    """
    increment_tile_rows_log2: int = ...
    """
    is used to compute `tile_rows_log2`.
    """
    mi_col_starts: list | None = ...
    """
    is an array specifying the start column (in units of 4x4 luma samples) for
      each tile across the image.
    """
    mi_row_starts: list | None = ...
    """
    is an array specifying the start row (in units of 4x4 luma samples) for
      each tile down the image.
    """
    tile_cols: int = ...
    """
    specifies the number of tiles across the frame. It is a requirement of bitstream
      conformance that `tile_cols` is less than or equal to GST_AV1_MAX_TILE_COLS.
    """
    tile_cols_log2: int = ...
    """
    specifies the base 2 logarithm of the desired number of tiles across the frame.
    """
    tile_rows: int = ...
    """
    specifies the number of tiles down the frame. It is a requirement of bitstream
      conformance that `tile_rows` is less than or equal to GST_AV1_MAX_TILE_ROWS.
    """
    tile_rows_log2: int = ...
    """
    specifies the base 2 logarithm of the desired number of tiles down the frame.
    """
    tile_size_bytes: int = ...
    """
    specifies the number of bytes needed to code each tile size.
    """
    tile_size_bytes_minus_1: int = ...
    """
    is used to compute `tile_size_bytes`
    """
    uniform_tile_spacing_flag: int = ...
    """
    equal to 1 means that the tiles are uniformly spaced across the
      frame. (In other words, all tiles are the same size except for the ones at the right and
      bottom edge which can be smaller.) `uniform_tile_spacing_flag` equal to 0 means that the
      tile sizes are coded.
    """
    width_in_sbs_minus_1: list | None = ...
    """
    specifies the width of a tile minus 1 in units of superblocks.
    """

class AV1TileListOBU(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    entry: list | None = ...
    output_frame_height_in_tiles_minus_1: int = ...
    """
    plus one is the height of the output frame, in tile units.
    """
    output_frame_width_in_tiles_minus_1: int = ...
    """
    plus one is the width of the output frame, in tile units.
    """
    tile_count_minus_1: int = ...
    """
    plus one is the number of `tile_list_entry` in the list. It is a requirement
      of bitstream conformance that `tile_count_minus_1` is less than or equal to 511.
    """

class AV1TileListOBUEntry(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    anchor_frame_idx: int = ...
    anchor_tile_col: int = ...
    anchor_tile_row: int = ...
    coded_tile_data: int = ...
    tile_data_size_minus_1: int = ...

class AV1TimingInfo(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    equal_picture_interval: bool = ...
    """
    equal to 1 indicates that pictures should be displayed according
      to their output order with the number of ticks between two consecutive pictures (without
      dropping frames) specified by `num_ticks_per_picture_minus_1` + 1. `equal_picture_interval`
      equal to 0 indicates that the interval between two consecutive pictures is not specified.
    """
    num_ticks_per_picture_minus_1: int = ...
    """
    plus 1 specifies the number of clock ticks corresponding
      to output time between two consecutive pictures in the output order. It is a requirement
      of bitstream conformance that the value of `num_ticks_per_picture_minus_1` shall be in the
      range of 0 to (1 << 32) - 2, inclusive.
    """
    num_units_in_display_tick: int = ...
    """
    is the number of time units of a clock operating at the
      frequency `time_scale` Hz that corresponds to one increment of a clock tick counter.
      A clock tick, in seconds, is equal to num_units_in_display_tick divided by time_scale.
      It is a requirement of bitstream conformance that num_units_in_display_tick is greater
      than 0.
    """
    time_scale: int = ...
    """
    is the number of time units that pass in one second. It is a requirement of
      bitstream conformance that `time_scale` is greater than 0.
    """

class H264BufferingPeriod(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    nal_initial_cpb_removal_delay: list | None = ...
    nal_initial_cpb_removal_delay_offset: list | None = ...
    sps: H264SPS | None = ...
    vcl_initial_cpb_removal_delay: list | None = ...
    vcl_initial_cpb_removal_delay_offset: list | None = ...

class H264ClockTimestamp(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    cnt_dropped_flag: int = ...
    """
    specifies the skipping of one or more values of n_frames
      using the counting method specified by counting_type
    """
    counting_type: int = ...
    """
    specifies the method of dropping values of the n_frames
    """
    ct_type: int = ...
    """
    indicates the scan type, 0: progressive, 1: interlaced, 2: unknown,
      3: reserved
    """
    discontinuity_flag: int = ...
    """
    indicates whether the difference between the current
      value of clockTimestamp and the value of clockTimestamp computed from the
      previous clock timestamp can be interpreted as the time difference or not.
    """
    full_timestamp_flag: int = ...
    """
    equal to 1 specifies that the n_frames syntax element
      is followed by seconds_value, minutes_value, and hours_value (Since 1.18)
    """
    hours_flag: int = ...
    """
    equal to 1 specifies that `hours_value` is present when
      `full_timestamp_flag` is equal to 0 and `seconds_flag` is equal to 1 and
      `minutes_flag` is equal to 1
    """
    hours_value: int = ...
    minutes_flag: int = ...
    """
    equal to 1 specifies that `minutes_value` and hours_flag are
      present when `full_timestamp_flag` is equal to 0 and `seconds_flag` is
      equal to 1
    """
    minutes_value: int = ...
    """
    specifies the value of minutes to compute clockTimestamp
    """
    n_frames: int = ...
    """
    specifies the value of nFrames used to compute clockTimestamp
    """
    nuit_field_based_flag: int = ...
    """
    used in calculating clockTimestamp
    """
    seconds_flag: int = ...
    """
    equal to 1 specifies that `seconds_value` and minutes_flag are
      present when `full_timestamp_flag` is equal to 0
    """
    seconds_value: int = ...
    """
    specifies the value of seconds to compute clockTimestamp
    """
    time_offset: int = ...
    """
    specifies the value of tOffset used to compute clockTimestamp
    """

class H264ContentLightLevel(GObject.GPointer, metaclass=GObject.GType):
    """
    The upper bounds for the nominal target brightness light level
    as specified in CEA-861.3
    D.2.31
    """

    # gi Fields
    max_content_light_level: int = ...
    max_pic_average_light_level: int = ...

class H264DecRefPicMarking(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    adaptive_ref_pic_marking_mode_flag: int = ...
    bit_size: int = ...
    long_term_reference_flag: int = ...
    n_ref_pic_marking: int = ...
    no_output_of_prior_pics_flag: int = ...
    ref_pic_marking: list | None = ...

class H264DecoderConfigRecord(GObject.GPointer, metaclass=GObject.GType):
    """
    Contains AVCDecoderConfigurationRecord data as defined in ISO/IEC 14496-15
    """

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list | None: ...
    bit_depth_chroma_minus8: int = ...
    """
    Indicates bit depth of chroma component
    """
    bit_depth_luma_minus8: int = ...
    """
    Indicates bit depth of luma component
    """
    chroma_format: int = ...
    """
    chroma_format_idc defined in ISO/IEC 14496-10
    """
    chroma_format_present: bool = ...
    """
    True if chroma information is present. Otherwise below values
    have no meaning
    """
    configuration_version: int = ...
    """
    Indicates configurationVersion, must be 1
    """
    length_size_minus_one: int = ...
    """
    Indicates the length in bytes of the NAL unit length field
    """
    level_indication: int = ...
    """
    H.264 level indiction
    """
    pps: list | None = ...
    """
    Array of identified GstH264NalUnit from pictureParameterSetNALUnit.
    This array may contain non-PPS nal units such as SEI message
    """
    profile_compatibility: int = ...
    """
    H.264 profile compatibility
    """
    profile_indication: int = ...
    """
    H.264 profile indication
    """
    sps: list | None = ...
    """
    Array of identified GstH264NalUnit from sequenceParameterSetNALUnit.
    This array may contain non-SPS nal units such as SEI message
    """
    sps_ext: list | None = ...
    """
    Array of identified GstH264NalUnit from sequenceParameterSetExtNALUnit.
    """

    # gi Methods
    def free(self) -> None:
        """
        Free `config` data
        """

class H264FramePacking(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    content_interpretation_type: int = ...
    current_frame_is_frame0_flag: int = ...
    field_views_flag: int = ...
    frame0_flipped_flag: int = ...
    frame0_grid_position_x: int = ...
    frame0_grid_position_y: int = ...
    frame0_self_contained_flag: int = ...
    frame1_grid_position_x: int = ...
    frame1_grid_position_y: int = ...
    frame1_self_contained_flag: int = ...
    frame_packing_cancel_flag: int = ...
    frame_packing_id: int = ...
    frame_packing_repetition_period: int = ...
    frame_packing_type: int = ...
    quincunx_sampling_flag: int = ...
    spatial_flipping_flag: int = ...

class H264HRDParams(GObject.GPointer, metaclass=GObject.GType):
    """
    Defines the HRD parameters
    """

    # gi Fields
    bit_rate_scale: int = ...
    """
    specifies the maximum input bit rate of the
    SchedSelIdx-th CPB
    """
    bit_rate_value_minus1: list | None = ...
    cbr_flag: bytes | None = ...
    """
    Specifies if running in constant or intermittent bit rate mode
    """
    cpb_cnt_minus1: int = ...
    """
    plus 1 specifies the number of alternative
       CPB specifications in the bitstream
    """
    cpb_removal_delay_length_minus1: int = ...
    """
    specifies the length in bits of the
    dpb_output_delay syntax element
    """
    cpb_size_scale: int = ...
    """
    specifies the CPB size of the SchedSelIdx-th CPB
    `guint32` bit_rate_value_minus1: specifies the maximum input bit rate for the
    SchedSelIdx-th CPB
    """
    cpb_size_value_minus1: list | None = ...
    """
    is used together with cpb_size_scale to specify the
    SchedSelIdx-th CPB size
    """
    dpb_output_delay_length_minus1: int = ...
    """
    >0 specifies the length in bits of the time_offset syntax element.
    =0 specifies that the time_offset syntax element is not present
    """
    initial_cpb_removal_delay_length_minus1: int = ...
    """
    specifies the length in bits of
    the cpb_removal_delay syntax element
    """
    time_offset_length: int = ...
    """
    Length of the time offset
    """

class H264MasteringDisplayColourVolume(GObject.GPointer, metaclass=GObject.GType):
    """
    The colour volume (primaries, white point and luminance range) of display
    defined by SMPTE ST 2086.
    D.2.29
    """

    # gi Fields
    display_primaries_x: list | None = ...
    display_primaries_y: list | None = ...
    max_display_mastering_luminance: int = ...
    min_display_mastering_luminance: int = ...
    white_point_x: int = ...
    white_point_y: int = ...

class H264NalParser(GObject.GPointer, metaclass=GObject.GType):
    """
    H264 NAL Parser (opaque structure).
    """

    # gi Fields
    @builtins.property
    def last_pps(self) -> H264PPS | None: ...
    @builtins.property
    def last_sps(self) -> H264SPS | None: ...
    @builtins.property
    def pps(self) -> list | None: ...
    @builtins.property
    def sps(self) -> list | None: ...

    # gi Methods
    def free(self) -> None:
        """
        Frees `nalparser`
        """

class H264NalUnit(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the NAL unit headers
    """

    # gi Fields
    data: int = ...
    """
    The data array from which the NAL unit has been parsed,
     into which the offset and sc_offset apply.
    """
    extension_type: int = ...
    """
    the extension type for prefix NAL/MVC/SVC
     (Since: 1.6)
    """
    header_bytes: int = ...
    """
    The size of the NALU header in bytes. The NALU
     header is the 1-byte type code, and for extension / prefix NALs
     includes the extension header bytes. `offset` + `header_bytes` is
     therefore the first byte of the actual packet payload.
     (Since: 1.6)
    """
    idr_pic_flag: int = ...
    """
    calculated idr_pic_flag
    """
    offset: int = ...
    """
    The offset of the first byte of the NAL unit header,
     just after the start code.
    """
    ref_idc: int = ...
    """
    not equal to 0 specifies that the content of the NAL unit
     contains a sequence parameter set, a sequence parameter set
     extension, a subset sequence parameter set, a picture parameter
     set, a slice of a reference picture, a slice data partition of a
     reference picture, or a prefix NAL unit preceding a slice of a
     reference picture.
    """
    sc_offset: int = ...
    """
    The offset of the first byte of the start code of
     the NAL unit.
    """
    size: int = ...
    """
    The size of the NAL unit starting from `offset`, thus
     including the header bytes. e.g. `type` (nal_unit_type),
     but not the start code.
    """
    type: int = ...
    """
    A GstH264NalUnitType
    """
    valid: bool = ...
    """
    If the NAL unit is valid, which means it has
     already been parsed
    """

class H264NalUnitExtensionMVC(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    anchor_pic_flag: int = ...
    """
    If equal to 1, it specifies that the current
      access unit is an anchor access unit
    """
    inter_view_flag: int = ...
    """
    If equal to 0, it specifies that the current view
      component is not used for inter-view prediction by any other view
      component in the current access unit
    """
    non_idr_flag: int = ...
    """
    If equal to 0, it specifies that the current access
      unit is an IDR access unit
    """
    priority_id: int = ...
    """
    The priority identifier for the NAL unit
    """
    temporal_id: int = ...
    """
    The temporal identifier for the NAL unit
    """
    view_id: int = ...
    """
    The view identifier for the NAL unit
    """

class H264PPS(GObject.GPointer, metaclass=GObject.GType):
    """
    H264 Picture Parameter Set
    """

    # gi Fields
    bottom_right: list | None = ...
    chroma_qp_index_offset: int = ...
    constrained_intra_pred_flag: int = ...
    deblocking_filter_control_present_flag: int = ...
    entropy_coding_mode_flag: int = ...
    id: int = ...
    num_ref_idx_l0_active_minus1: int = ...
    num_ref_idx_l1_active_minus1: int = ...
    num_slice_groups_minus1: int = ...
    pic_init_qp_minus26: int = ...
    pic_init_qs_minus26: int = ...
    pic_order_present_flag: int = ...
    pic_scaling_matrix_present_flag: int = ...
    pic_size_in_map_units_minus1: int = ...
    redundant_pic_cnt_present_flag: int = ...
    run_length_minus1: list | None = ...
    scaling_lists_4x4: bytes | None = ...
    scaling_lists_8x8: bytes | None = ...
    second_chroma_qp_index_offset: int = ...
    sequence: H264SPS | None = ...
    slice_group_change_direction_flag: int = ...
    slice_group_change_rate_minus1: int = ...
    slice_group_id: int = ...
    slice_group_map_type: int = ...
    sps_id: int = ...
    top_left: list | None = ...
    transform_8x8_mode_flag: int = ...
    valid: bool = ...
    weighted_bipred_idc: int = ...
    weighted_pred_flag: int = ...

    # gi Methods
    def clear(self) -> None:
        """
        Clears all `pps` internal resources.
        """

class H264PicTiming(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    CpbDpbDelaysPresentFlag: int = ...
    """
    non-zero if linked
      GstH264VUIParams::nal_hrd_parameters_present_flag or
      GstH264VUIParams::vcl_hrd_parameters_present_flag is non-zero (Since: 1.18)
    """
    clock_timestamp: list | None = ...
    """
    a GstH264ClockTimestamp
    """
    clock_timestamp_flag: bytes | None = ...
    """
    equal to 1 indicates that a number of clock timestamp
      syntax elements are present
    """
    cpb_removal_delay: int = ...
    """
    specifies how many clock ticks to wait after removal from
      the CPB of the access unit associated with the most recent buffering period
      SEI message in a preceding access unit before removing from the
      buffer the access unit data associated with the picture timing SEI message
    """
    cpb_removal_delay_length_minus1: int = ...
    """
    specifies the length of `cpb_removal_delay`
      in bits (Since 1.18)
    """
    dpb_output_delay: int = ...
    """
    used to compute the DPB output time of the picture
    """
    dpb_output_delay_length_minus1: int = ...
    """
    specifies the length of `dpb_output_delay`
      in bits (Since 1.18)
    """
    pic_struct: int = ...
    """
    indicates whether a picture should be displayed as a frame or
      one or more fields
    """
    pic_struct_present_flag: int = ...
    """
    GstH264VUIParams::pic_struct_present_flag
    """
    time_offset_length: int = ...
    """
    specifies the length time_offset of
      GstH264ClockTimestamp in bits (Since 1.18)
    """

class H264PredWeightTable(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    chroma_log2_weight_denom: int = ...
    chroma_offset_l0: list | None = ...
    chroma_offset_l1: list | None = ...
    chroma_weight_l0: list | None = ...
    chroma_weight_l1: list | None = ...
    luma_log2_weight_denom: int = ...
    luma_offset_l0: list | None = ...
    luma_offset_l1: list | None = ...
    luma_weight_l0: list | None = ...
    luma_weight_l1: list | None = ...

class H264RecoveryPoint(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    broken_link_flag: int = ...
    changing_slice_group_idc: int = ...
    exact_match_flag: int = ...
    recovery_frame_cnt: int = ...

class H264RefPicListModification(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    modification_of_pic_nums_idc: int = ...

class H264RefPicMarking(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    difference_of_pic_nums_minus1: int = ...
    long_term_frame_idx: int = ...
    long_term_pic_num: int = ...
    max_long_term_frame_idx_plus1: int = ...
    memory_management_control_operation: int = ...

class H264RegisteredUserData(GObject.GPointer, metaclass=GObject.GType):
    """
    The User data registered by Rec. ITU-T T.35 SEI messag.
    """

    # gi Fields
    country_code: int = ...
    """
    an itu_t_t35_country_code.
    """
    country_code_extension: int = ...
    """
    an itu_t_t35_country_code_extension_byte.
      Should be ignored when `country_code` is not 0xff
    """
    data: int = ...
    """
    the data of itu_t_t35_payload_byte
      excluding `country_code` and `country_code_extension`
    """
    size: int = ...
    """
    the size of `data` in bytes
    """

class H264SEIMessage(GObject.GPointer, metaclass=GObject.GType):
    """
    Constains information about SEI message. The content depends on the
    `payloadType`.
    """

    # gi Fields
    payloadType: H264SEIPayloadType = ...
    """
    GstH264SEIPayloadType
    """

class H264SEIUnhandledPayload(GObject.GPointer, metaclass=GObject.GType):
    """
    Contains unhandled SEI payload data. This SEI may or may not
    be defined by spec
    """

    # gi Fields
    data: int = ...
    """
    payload raw data excluding payload type and payload size byte
    """
    payloadType: int = ...
    """
    Payload type
    """
    size: int = ...
    """
    the size of `data`
    """

class H264SPS(GObject.GPointer, metaclass=GObject.GType):
    """
    H264 Sequence Parameter Set (SPS)
    """

    # gi Fields
    bit_depth_chroma_minus8: int = ...
    bit_depth_luma_minus8: int = ...
    chroma_array_type: int = ...
    chroma_format_idc: int = ...
    constraint_set0_flag: int = ...
    constraint_set1_flag: int = ...
    constraint_set2_flag: int = ...
    constraint_set3_flag: int = ...
    constraint_set4_flag: int = ...
    constraint_set5_flag: int = ...
    crop_rect_height: int = ...
    crop_rect_width: int = ...
    crop_rect_x: int = ...
    crop_rect_y: int = ...
    delta_pic_order_always_zero_flag: int = ...
    direct_8x8_inference_flag: int = ...
    extension_type: int = ...
    fps_den_removed: int = ...
    fps_num_removed: int = ...
    frame_crop_bottom_offset: int = ...
    frame_crop_left_offset: int = ...
    frame_crop_right_offset: int = ...
    frame_crop_top_offset: int = ...
    frame_cropping_flag: int = ...
    frame_mbs_only_flag: int = ...
    gaps_in_frame_num_value_allowed_flag: int = ...
    height: int = ...
    id: int = ...
    """
    The ID of the sequence parameter set
    """
    level_idc: int = ...
    log2_max_frame_num_minus4: int = ...
    log2_max_pic_order_cnt_lsb_minus4: int = ...
    max_frame_num: int = ...
    mb_adaptive_frame_field_flag: int = ...
    num_ref_frames: int = ...
    num_ref_frames_in_pic_order_cnt_cycle: int = ...
    offset_for_non_ref_pic: int = ...
    offset_for_ref_frame: list | None = ...
    offset_for_top_to_bottom_field: int = ...
    pic_height_in_map_units_minus1: int = ...
    pic_order_cnt_type: int = ...
    pic_width_in_mbs_minus1: int = ...
    profile_idc: int = ...
    """
    indicate the profile to which the coded video sequence conforms
    """
    qpprime_y_zero_transform_bypass_flag: int = ...
    scaling_lists_4x4: bytes | None = ...
    scaling_lists_8x8: bytes | None = ...
    scaling_matrix_present_flag: int = ...
    separate_colour_plane_flag: int = ...
    valid: bool = ...
    vui_parameters: H264VUIParams | None = ...
    vui_parameters_present_flag: int = ...
    width: int = ...

    # gi Methods
    def clear(self) -> None:
        """
        Clears all `sps` internal resources.
        """

class H264SPSExtMVC(GObject.GPointer, metaclass=GObject.GType):
    """
    Represents the parsed ``seq_parameter_set_mvc_extension``.
    """

    # gi Fields
    level_value: H264SPSExtMVCLevelValue | None = ...
    """
    array of GstH264SPSExtMVCLevelValue
    """
    num_level_values_signalled_minus1: int = ...
    """
    plus 1 specifies the number of
      level values signalled for the coded video sequence.
    """
    num_views_minus1: int = ...
    """
    plus 1 specifies the maximum number of coded
      views in the coded video sequence
    """
    view: H264SPSExtMVCView | None = ...
    """
    array of GstH264SPSExtMVCView
    """

class H264SPSExtMVCLevelValue(GObject.GPointer, metaclass=GObject.GType):
    """
    Represents level values for a subset of the operation points for
    the coded video sequence.
    """

    # gi Fields
    applicable_op: H264SPSExtMVCLevelValueOp | None = ...
    """
    specifies the applicable operation point
    """
    level_idc: int = ...
    """
    specifies the level value signalled for the coded video
      sequence
    """
    num_applicable_ops_minus1: int = ...
    """
    plus 1 specifies the number of
      operation points to which the level indicated by level_idc applies
    """

class H264SPSExtMVCLevelValueOp(GObject.GPointer, metaclass=GObject.GType):
    """
    Represents an operation point for the coded video sequence.
    """

    # gi Fields
    num_target_views_minus1: int = ...
    num_views_minus1: int = ...
    target_view_id: int = ...
    temporal_id: int = ...

class H264SPSExtMVCView(GObject.GPointer, metaclass=GObject.GType):
    """
    Represents inter-view dependency relationships for the coded video
    sequence.
    """

    # gi Fields
    anchor_ref_l0: list | None = ...
    """
    specifies the view_id for inter-view prediction in
      the initialized RefPicList0 in decoding anchor view components.
    """
    anchor_ref_l1: list | None = ...
    """
    specifies the view_id for inter-view prediction in
      the initialized RefPicList1 in decoding anchor view components.
    """
    non_anchor_ref_l0: list | None = ...
    """
    specifies the view_id for inter-view prediction
      in the initialized RefPicList0 in decoding non-anchor view
      components.
    """
    non_anchor_ref_l1: list | None = ...
    """
    specifies the view_id for inter-view prediction
      in the initialized RefPicList1 in decoding non-anchor view
      components.
    """
    num_anchor_refs_l0: int = ...
    """
    specifies the number of view components for
      inter-view prediction in the initialized RefPicList0 in decoding
      anchor view components.
    """
    num_anchor_refs_l1: int = ...
    """
    specifies the number of view components for
      inter-view prediction in the initialized RefPicList1 in decoding
      anchor view components.
    """
    num_non_anchor_refs_l0: int = ...
    """
    specifies the number of view components
      for inter-view prediction in the initialized RefPicList0 in
      decoding non-anchor view components.
    """
    num_non_anchor_refs_l1: int = ...
    """
    specifies the number of view components
      for inter-view prediction in the initialized RefPicList1 in
      decoding non-anchor view components.
    """
    view_id: int = ...

class H264SliceHdr(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    bottom_field_flag: int = ...
    cabac_init_idc: int = ...
    colour_plane_id: int = ...
    dec_ref_pic_marking: H264DecRefPicMarking | None = ...
    delta_pic_order_cnt: list | None = ...
    delta_pic_order_cnt_bottom: int = ...
    direct_spatial_mv_pred_flag: int = ...
    disable_deblocking_filter_idc: int = ...
    field_pic_flag: int = ...
    first_mb_in_slice: int = ...
    frame_num: int = ...
    header_size: int = ...
    idr_pic_id: int = ...
    max_pic_num: int = ...
    n_emulation_prevention_bytes: int = ...
    n_ref_pic_list_modification_l0: int = ...
    n_ref_pic_list_modification_l1: int = ...
    num_ref_idx_active_override_flag: int = ...
    num_ref_idx_l0_active_minus1: int = ...
    num_ref_idx_l1_active_minus1: int = ...
    pic_order_cnt_bit_size: int = ...
    pic_order_cnt_lsb: int = ...
    pps: H264PPS | None = ...
    pps_id: int = ...
    pred_weight_table: H264PredWeightTable | None = ...
    redundant_pic_cnt: int = ...
    ref_pic_list_modification_flag_l0: int = ...
    ref_pic_list_modification_flag_l1: int = ...
    ref_pic_list_modification_l0: list | None = ...
    ref_pic_list_modification_l1: list | None = ...
    slice_alpha_c0_offset_div2: int = ...
    slice_beta_offset_div2: int = ...
    slice_group_change_cycle: int = ...
    slice_qp_delta: int = ...
    slice_qs_delta: int = ...
    sp_for_switch_flag: int = ...
    type: int = ...
    valid: bool = ...

class H264StereoVideoInfo(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    current_frame_is_left_view_flag: int = ...
    field_views_flag: int = ...
    left_view_self_contained_flag: int = ...
    next_frame_is_second_view_flag: int = ...
    right_view_self_contained_flag: int = ...
    top_field_is_left_view_flag: int = ...

class H264UserDataUnregistered(GObject.GPointer, metaclass=GObject.GType):
    """
    The User data unregistered SEI message syntax.
    """

    # gi Fields
    data: int = ...
    """
    the data of user_data_payload_byte
    """
    size: int = ...
    """
    the size of `data` in bytes
    """
    uuid: bytes | None = ...
    """
    an uuid_iso_iec_11578.
    """

class H264VUIParams(GObject.GPointer, metaclass=GObject.GType):
    """
    The structure representing the VUI parameters.
    """

    # gi Fields
    aspect_ratio_idc: int = ...
    aspect_ratio_info_present_flag: int = ...
    """
    True specifies that aspect_ratio_idc is present.
     False specifies that aspect_ratio_idc is not present
    `aspect_ratio_idc` specifies the value of the sample aspect ratio of the luma samples
    `sar_width` indicates the horizontal size of the sample aspect ratio
    `sar_height` indicates the vertical size of the sample aspect ratio
    """
    bitstream_restriction_flag: int = ...
    """
    True specifies that the following coded video sequence bitstream restriction
    parameters are present
    """
    chroma_loc_info_present_flag: int = ...
    """
    True specifies that chroma_sample_loc_type_top_field and
     chroma_sample_loc_type_bottom_field are present, False otherwise
    """
    chroma_sample_loc_type_bottom_field: int = ...
    chroma_sample_loc_type_top_field: int = ...
    """
    specify the location of chroma for top field
    `chroma_sample_loc_type_bottom_field` specify the location of chroma for bottom field
    """
    colour_description_present_flag: int = ...
    """
    True specifies that colour_primaries,
     transfer_characteristics and matrix_coefficients are present
    """
    colour_primaries: int = ...
    """
    indicates the chromaticity coordinates of the source primaries
    """
    fixed_frame_rate_flag: int = ...
    """
    True indicates that the temporal distance between the HRD output times
     of any two consecutive pictures in output order is constrained as specified in the spec, False
     otherwise.
    """
    log2_max_mv_length_horizontal: int = ...
    """
    indicate the maximum absolute value of a decoded horizontal
    motion vector component
    """
    log2_max_mv_length_vertical: int = ...
    """
    indicate the maximum absolute value of a decoded vertical
     motion vector component
    """
    low_delay_hrd_flag: int = ...
    """
    specifies the HRD operational mode
    """
    matrix_coefficients: int = ...
    """
    describes the matrix coefficients used in deriving luma and chroma signals
    """
    max_bits_per_mb_denom: int = ...
    """
    indicates the maximum number of coded bits of macroblock_layer
    """
    max_bytes_per_pic_denom: int = ...
    """
    indicates a number of bytes not exceeded by the sum of the sizes of
     the VCL NAL units associated with any coded picture in the coded video sequence.
    """
    max_dec_frame_buffering: int = ...
    """
    specifies the required size of the HRD decoded picture buffer in
     units of frame buffers.
    """
    motion_vectors_over_pic_boundaries_flag: int = ...
    """
    False indicates that no sample outside the
     picture boundaries and no sample at a fractional sample position, True indicates that one or more
     samples outside picture boundaries may be used in inter prediction
    """
    nal_hrd_parameters: H264HRDParams | None = ...
    nal_hrd_parameters_present_flag: int = ...
    """
    True if NAL HRD parameters exist in the bitstream
    """
    num_reorder_frames: int = ...
    """
    indicates the maximum number of frames, complementary field pairs,
     or non-paired fields that precede any frame,
    """
    num_units_in_tick: int = ...
    """
    is the number of time units of a clock operating at the frequency time_scale Hz
    time_scale: is the number of time units that pass in one second
    """
    overscan_appropriate_flag: int = ...
    """
    True indicates that the cropped decoded pictures
     output are suitable for display using overscan. False the cropped decoded pictures
     output contain visually important information
    """
    overscan_info_present_flag: int = ...
    """
    True overscan_appropriate_flag is present False otherwise
    """
    par_d: int = ...
    par_n: int = ...
    pic_struct_present_flag: int = ...
    """
    True specifies that picture timing SEI messages are present or not
    """
    sar_height: int = ...
    sar_width: int = ...
    time_scale: int = ...
    timing_info_present_flag: int = ...
    """
    True specifies that num_units_in_tick,
     time_scale and fixed_frame_rate_flag are present in the bitstream
    """
    transfer_characteristics: int = ...
    """
    indicates the opto-electronic transfer characteristic
    """
    vcl_hrd_parameters: H264HRDParams | None = ...
    vcl_hrd_parameters_present_flag: int = ...
    """
    True if VCL HRD parameters exist in the bitstream
    """
    video_format: int = ...
    """
    indicates the representation of the picture
    """
    video_full_range_flag: int = ...
    """
    indicates the black level and range of the luma and chroma signals
    """
    video_signal_type_present_flag: int = ...
    """
    True specifies that video_format, video_full_range_flag and
     colour_description_present_flag are present.
    """

class H265BufferingPeriod(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    au_cpb_removal_delay_delta_minus1: int = ...
    concatenation_flag: int = ...
    cpb_delay_offset: int = ...
    dpb_delay_offset: int = ...
    irap_cpb_params_present_flag: int = ...
    nal_initial_alt_cpb_removal_delay: bytes | None = ...
    nal_initial_alt_cpb_removal_offset: bytes | None = ...
    nal_initial_cpb_removal_delay: bytes | None = ...
    nal_initial_cpb_removal_offset: bytes | None = ...
    sps: H265SPS | None = ...
    vcl_initial_alt_cpb_removal_delay: bytes | None = ...
    vcl_initial_alt_cpb_removal_offset: bytes | None = ...
    vcl_initial_cpb_removal_delay: bytes | None = ...
    vcl_initial_cpb_removal_offset: bytes | None = ...

class H265ContentLightLevel(GObject.GPointer, metaclass=GObject.GType):
    """
    The upper bounds for the nominal target brightness light level
    as specified in CEA-861.3
    D.2.35
    """

    # gi Fields
    max_content_light_level: int = ...
    max_pic_average_light_level: int = ...

class H265DecoderConfigRecord(GObject.GPointer, metaclass=GObject.GType):
    """
    Contains HEVCDecoderConfigurationRecord data as defined in ISO/IEC 14496-15
    """

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list | None: ...
    avg_frame_rate: int = ...
    """
    average frame rate in units of frames per 256 seconds,
    or 0 when unspecified
    """
    bit_depth_chroma_minus8: int = ...
    """
    bit depth chroma minus 8
    """
    bit_depth_luma_minus8: int = ...
    """
    bit depth luma minus 8
    """
    chroma_format_idc: int = ...
    """
    chroma format indication
    """
    configuration_version: int = ...
    """
    Indicates configurationVersion, must be 1
    """
    constant_frame_rate: int = ...
    """
    constant frame rate.
    1: stream to which this configuration record applies is constante frame rate
    2: representation of each temporal layer in the stream is constant frame rate
    0: unspecified
    """
    general_frame_only_constraint_flag: int = ...
    """
    general_frame_only_constraint_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_inbld_flag: int = ...
    """
    general_inbld_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_interlaced_source_flag: int = ...
    """
    general_interlaced_source_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_intra_constraint_flag: int = ...
    """
    general_intra_constraint_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_level_idc: int = ...
    """
    general level indication
    """
    general_lower_bit_rate_constraint_flag: int = ...
    """
    general_lower_bit_rate_constraint_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_max_10bit_constraint_flag: int = ...
    """
    general_max_10bit_constraint_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_max_12bit_constraint_flag: int = ...
    """
    general_max_12bit_constraint_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_max_14bit_constraint_flag: int = ...
    """
    general_max_14bit_constraint_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_max_420chroma_constraint_flag: int = ...
    """
    general_max_420chroma_constraint_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_max_422chroma_constraint_flag: int = ...
    """
    general_max_422chroma_constraint_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_max_8bit_constraint_flag: int = ...
    """
    general_max_8bit_constraint_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_max_monochrome_constraint_flag: int = ...
    """
    general_max_monochrome_constraint_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_non_packed_constraint_flag: int = ...
    """
    general_non_packed_constraint_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_one_picture_only_constraint_flag: int = ...
    """
    general_one_picture_only_constraint_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_profile_compatibility_flags: bytes | None = ...
    """
    general profile compatibility flags
    """
    general_profile_idc: int = ...
    """
    general profile indiction
    """
    general_profile_space: int = ...
    """
    general profile space
    """
    general_progressive_source_flag: int = ...
    """
    general_progressive_source_flag parsed from
    HEVCDecoderConfigurationRecord.general_constraint_indicator_flags
    """
    general_tier_flag: int = ...
    """
    general tier flag
    """
    length_size_minus_one: int = ...
    """
    indicates the length in bytes of nal unit length field.
    This value shall be one of 0, 1, or 3 corresponding to a length
    encoded with 1, 2, or 4 bytes, respectively
    """
    min_spatial_segmentation_idc: int = ...
    """
    min spatial segmentation indication
    """
    nalu_array: list | None = ...
    """
    Array of GstH265DecoderConfigRecordNalUnitArray
    """
    num_temporal_layers: int = ...
    """
    0: unknown whether the stream is temporally scalable
    otherwise: the number of temporal layers
    """
    parallelism_type: int = ...
    """
    parallelism type
    """
    temporal_id_nested: int = ...
    """
    1: all SPSs that activated when the stream to this configuration record
      applies is decoded have sps_temporal_id_nesting_flags equal to 1
      and temporal sub-layer up-switching to any higher temporal layer can
      be performed at any semple
    0: unknown
    """

    # gi Methods
    def free(self) -> None:
        """
        Free `config` data
        """

class H265DecoderConfigRecordNalUnitArray(GObject.GPointer, metaclass=GObject.GType):
    """
    Contains NAL Unit array data as defined in ISO/IEC 14496-15
    """

    # gi Fields
    array_completeness: int = ...
    """
    1: all NAL units of the given type are in this array and none
      are in the stream.
    0: additional NAL units of the indicated type may be in the stream
    """
    nal_unit_type: H265NalUnitType = ...
    """
    Indicates the type of the NAL units in the following array.
    Shall be VPS, SPS, PPS, prefix SEI or suffix SEI
    """
    nalu: list | None = ...
    """
    Array of identified GstH265NalUnit
    """

class H265HRDParams(GObject.GPointer, metaclass=GObject.GType):
    """
    Defines the HRD parameters
    """

    # gi Fields
    au_cpb_removal_delay_length_minus1: int = ...
    """
    specifies the length, in bits, of the
      cpb_delay_offset and the au_cpb_removal_delay_minus1 syntax elements
    """
    bit_rate_scale: int = ...
    """
    maximum input bitrate
    """
    cpb_cnt_minus1: bytes | None = ...
    """
    specifies the number of alternative CPB specifications.
    """
    cpb_size_du_scale: int = ...
    """
    CPB size when operates in sub-picture level
    """
    cpb_size_scale: int = ...
    """
    CPB size when operates in access unit level
    """
    dpb_output_delay_du_length_minus1: int = ...
    dpb_output_delay_length_minus1: int = ...
    """
    specifies the length, in bits, of the
      dpb_delay_offset and the pic_dpb_output_delay syntax elements
    """
    du_cpb_removal_delay_increment_length_minus1: int = ...
    """
    specifies the length,
      in bits, of the nal_initial_cpb_removal_delay
    """
    elemental_duration_in_tc_minus1: list | None = ...
    """
    temporal distance in clock ticks
    """
    fixed_pic_rate_general_flag: bytes | None = ...
    """
    flag to indicate the presence of constraint
      on the temporal distance between the HRD output times of consecutive
      pictures in output order
    """
    fixed_pic_rate_within_cvs_flag: bytes | None = ...
    """
    same as fixed_pic_rate_general_flag
    """
    initial_cpb_removal_delay_length_minus1: int = ...
    """
    specifies the length, in bits, of the
      nal_initial_cpb_removal_delay, nal_initial_cpb_removal_offset,
      vcl_initial_cpb_removal_delay and vcl_initial_cpb_removal_offset.
    """
    low_delay_hrd_flag: bytes | None = ...
    """
    specifies the HRD operational mode
    """
    nal_hrd_parameters_present_flag: int = ...
    """
    indicate the presence of NAL HRD parameters
    """
    sub_pic_cpb_params_in_pic_timing_sei_flag: int = ...
    """
    specifies the length, in bits, of
      the cpb_delay_offset and the au_cpb_removal_delay_minus1 syntax elements.
    """
    sub_pic_hrd_params_present_flag: int = ...
    """
    indicate the presence of sub_pic_hrd_params
    """
    sublayer_hrd_params: list | None = ...
    """
    Sublayer HRD parameters.
    """
    tick_divisor_minus2: int = ...
    """
    is used to specify the clock sub-tick
    """
    vcl_hrd_parameters_present_flag: int = ...
    """
    indicate the presence of VCL HRD parameters
    """

class H265MasteringDisplayColourVolume(GObject.GPointer, metaclass=GObject.GType):
    """
    The colour volume (primaries, white point and luminance range) of display
    defined by SMPTE ST 2086.
    D.2.28
    """

    # gi Fields
    display_primaries_x: list | None = ...
    display_primaries_y: list | None = ...
    max_display_mastering_luminance: int = ...
    min_display_mastering_luminance: int = ...
    white_point_x: int = ...
    white_point_y: int = ...

class H265NalUnit(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the Nal unit headers
    """

    # gi Fields
    data: int = ...
    """
    The data from which the Nalu has been parsed
    """
    header_bytes: int = ...
    layer_id: int = ...
    """
    A nal unit layer id
    """
    offset: int = ...
    """
    The offset of the actual start of the nal unit
    """
    sc_offset: int = ...
    """
    The offset of the start code of the nal unit
    """
    size: int = ...
    """
    The size of the nal unit starting from `offset`
    """
    temporal_id_plus1: int = ...
    """
    A nal unit temporal identifier
    """
    type: int = ...
    """
    A GstH265NalUnitType
    """
    valid: bool = ...
    """
    If the nal unit is valid, which mean it has
    already been parsed
    """

class H265PPS(GObject.GPointer, metaclass=GObject.GType):
    """
    H265 Picture Parameter Set
    """

    # gi Fields
    PicHeightInCtbsY: int = ...
    PicWidthInCtbsY: int = ...
    beta_offset_div2: int = ...
    cabac_init_present_flag: int = ...
    cb_qp_offset: int = ...
    column_width_minus1: list | None = ...
    constrained_intra_pred_flag: int = ...
    cr_qp_offset: int = ...
    cu_qp_delta_enabled_flag: int = ...
    deblocking_filter_control_present_flag: int = ...
    deblocking_filter_disabled_flag: int = ...
    deblocking_filter_override_enabled_flag: int = ...
    dependent_slice_segments_enabled_flag: int = ...
    diff_cu_qp_delta_depth: int = ...
    entropy_coding_sync_enabled_flag: int = ...
    id: int = ...
    init_qp_minus26: int = ...
    lists_modification_present_flag: int = ...
    log2_parallel_merge_level_minus2: int = ...
    loop_filter_across_slices_enabled_flag: int = ...
    loop_filter_across_tiles_enabled_flag: int = ...
    num_extra_slice_header_bits: int = ...
    num_ref_idx_l0_default_active_minus1: int = ...
    num_ref_idx_l1_default_active_minus1: int = ...
    num_tile_columns_minus1: int = ...
    num_tile_rows_minus1: int = ...
    output_flag_present_flag: int = ...
    pps_3d_extension_flag: int = ...
    pps_extension_4bits: int = ...
    pps_extension_flag: int = ...
    pps_extension_params: H265PPSExtensionParams | None = ...
    pps_multilayer_extension_flag: int = ...
    pps_range_extension_flag: int = ...
    pps_scc_extension_flag: int = ...
    pps_scc_extension_params: H265PPSSccExtensionParams | None = ...
    row_height_minus1: list | None = ...
    scaling_list: H265ScalingList | None = ...
    scaling_list_data_present_flag: int = ...
    sign_data_hiding_enabled_flag: int = ...
    slice_chroma_qp_offsets_present_flag: int = ...
    slice_segment_header_extension_present_flag: int = ...
    sps: H265SPS | None = ...
    sps_id: int = ...
    tc_offset_div2: int = ...
    tiles_enabled_flag: int = ...
    transform_skip_enabled_flag: int = ...
    transquant_bypass_enabled_flag: int = ...
    uniform_spacing_flag: int = ...
    valid: bool = ...
    weighted_bipred_flag: int = ...
    weighted_pred_flag: int = ...

class H265PPSExtensionParams(GObject.GPointer, metaclass=GObject.GType):
    """
    Defines the GstH265SPSExtensionParams
    """

    # gi Fields
    cb_qp_offset_list: list | None = ...
    """
    specify offsets used in the derivation of qp cb.
    """
    chroma_qp_offset_list_enabled_flag: int = ...
    """
    equal to 1 specifies that the cu_chroma_qp_offset_flag may be
      present in the transform unit syntax.
    """
    chroma_qp_offset_list_len_minus1: int = ...
    """
    plus 1 specifies the number of cb_qp_offset_list[] and
      cr_qp_offset_list[] syntax elements that are present in the PPS.
    """
    cr_qp_offset_list: list | None = ...
    """
    specify offsets used in the derivation of qp cr.
    """
    cross_component_prediction_enabled_flag: int = ...
    """
    equal to 1 specifies that log2_res_scale_abs_plus1 and
      res_scale_sign_flag may be present in the transform unit syntax for pictures referring to the PPS.
    """
    diff_cu_chroma_qp_offset_depth: int = ...
    """
    specifies the difference between the luma coding tree block size and
      the minimum luma coding block size of coding units that convey cu_chroma_qp_offset_flag.
    """
    log2_max_transform_skip_block_size_minus2: int = ...
    """
    plus 2 specifies the maximum transform block size for which
      transform_skip_flag may be present in coded pictures referring to the PPS.
    """
    log2_sao_offset_scale_chroma: int = ...
    """
    the base 2 logarithm of the scaling parameter that is used to scale SAO
      offset values for chroma samples.
    """
    log2_sao_offset_scale_luma: int = ...
    """
    the base 2 logarithm of the scaling parameter that is used to scale sample
      adaptive offset (SAO) offset values for luma samples.
    """

class H265PPSSccExtensionParams(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    chroma_bit_depth_entry_minus8: int = ...
    """
    plus 8 specifies the bit depth of the chroma components of
      the entries of the palette predictor initializer.
    """
    luma_bit_depth_entry_minus8: int = ...
    """
    plus 8 specifies the bit depth of the luma component of the
      entries of the palette predictor initializer.
    """
    monochrome_palette_flag: int = ...
    """
    equal to 1 specifies that the pictures that refer to this PPS
      are monochrome. Equal to 0 specifies that the pictures that refer to this PPS have
      multiple components.
    """
    pps_act_cb_qp_offset_plus5: int = ...
    pps_act_cr_qp_offset_plus3: int = ...
    pps_act_y_qp_offset_plus5: int = ...
    pps_curr_pic_ref_enabled_flag: int = ...
    """
    equal to 1 specifies that a picture referring to the PPS may
      be included in a reference picture list of a slice of the picture itself.
    """
    pps_num_palette_predictor_initializer: int = ...
    """
    specifies the number of entries in the picture
      palette predictor initializer.
    """
    pps_palette_predictor_initializer: list | None = ...
    """
    specifies the value of the comp-th component of the
      i-th palette entry in the PPS that is used to initialize the array PredictorPaletteEntries.
    Defines the _GstH265PPSSccExtensionParams
    """
    pps_palette_predictor_initializers_present_flag: int = ...
    """
    equal to 1 specifies that the palette
      predictor initializers used for the pictures referring to the PPS are derived based on
      the palette predictor initializers specified by the PPS.
    """
    pps_slice_act_qp_offsets_present_flag: int = ...
    """
    equal to 1 specifies that slice_act_y_qp_offset,
      slice_act_cb_qp_offset, slice_act_cr_qp_offset are present in the slice header.
    `pps_act_y_qp_offset_plus5` `pps_act_cb_qp_offset_plus5` `pps_act_cr_qp_offset_plus3`:
      are used to determine the offsets that are applied to the quantization parameter values
      qp derived in clause 8.6.2 for the luma, Cb and Cr components, respectively, when
      tu_residual_act_flag[ xTbY ][ yTbY ] is equal to 1.
    """
    residual_adaptive_colour_transform_enabled_flag: int = ...
    """
    equal to 1 specifies that an adaptive
      colour transform may be applied to the residual in the decoding process.
    """

class H265Parser(GObject.GPointer, metaclass=GObject.GType):
    """
    It offers you bitstream parsing in HEVC mode and non-HEVC mode. To identify
    Nals in a bitstream and parse its headers, you should call:

      * `gst_h265_parser_identify_nalu` to identify the following nalu in
           non-HEVC bitstreams

      * `gst_h265_parser_identify_nalu_hevc` to identify the nalu in
           HEVC bitstreams

    Then, depending on the GstH265NalUnitType of the newly parsed GstH265NalUnit,
    you should call the differents functions to parse the structure:

      * From GST_H265_NAL_SLICE_TRAIL_N to GST_H265_NAL_SLICE_CRA_NUT: `gst_h265_parser_parse_slice_hdr`

      * `GST_H265_NAL_*_SEI`: `gst_h265_parser_parse_sei`

      * GST_H265_NAL_VPS: `gst_h265_parser_parse_vps`

      * GST_H265_NAL_SPS: `gst_h265_parser_parse_sps`

      * GST_H265_NAL_PPS: #`gst_h265_parser_parse_pps`

      * Any other: `gst_h265_parser_parse_nal`

    Note: You should always call `gst_h265_parser_parse_nal` if you don't
    actually need GstH265NalUnitType to be parsed for your personal use, in
    order to guarantee that the GstH265Parser is always up to date.

    For more details about the structures, look at the ITU-T H.265
    specifications, you can download them from:

      * ITU-T H.265: http://www.itu.int/rec/T-REC-H.265
    """

    # gi Fields
    @builtins.property
    def last_pps(self) -> H265PPS | None: ...
    @builtins.property
    def last_sps(self) -> H265SPS | None: ...
    @builtins.property
    def last_vps(self) -> H265VPS | None: ...
    @builtins.property
    def pps(self) -> list | None: ...
    @builtins.property
    def sps(self) -> list | None: ...
    @builtins.property
    def vps(self) -> list | None: ...

    # gi Methods
    def free(self) -> None:
        """
        Frees `parser`
        """
    def identify_and_split_nalu_hevc(
        self, data: int, offset: int, size: int, nal_length_size: int, nalus: list[H265NalUnit], consumed: int
    ) -> H265ParserResult:
        """
            Parses `data` for packetized (e.g., hvc1/hev1) bitstream and
        sets `nalus`. In addition to nal identifying process,
        this method scans start-code prefix to split malformed packet into
        actual nal chunks.
        """
    def identify_nalu(self, data: int, offset: int, size: int, nalu: H265NalUnit) -> H265ParserResult:
        """
        Parses `data` and fills `nalu` from the next nalu data from `data`
        """
    def identify_nalu_hevc(
        self, data: int, offset: int, size: int, nal_length_size: int, nalu: H265NalUnit
    ) -> H265ParserResult:
        """
        Parses `data` and sets `nalu`.
        """
    def identify_nalu_unchecked(self, data: int, offset: int, size: int, nalu: H265NalUnit) -> H265ParserResult:
        """
            Parses `data` and fills `nalu` from the next nalu data from `data`.

        This differs from `gst_h265_parser_identify_nalu` in that it doesn't
        check whether the packet is complete or not.

        Note: Only use this function if you already know the provided `data`
        is a complete NALU, else use `gst_h265_parser_identify_nalu`.
        """
    def insert_sei(self, au: Gst.Buffer, sei: Gst.Memory) -> Gst.Buffer | None:
        """
            Copy `au` into new GstBuffer and insert `sei` into the GstBuffer.
        The validation for completeness of `au` and `sei` is caller's responsibility.
        Both `au` and `sei` must be byte-stream formatted
        """
    def insert_sei_hevc(self, nal_length_size: int, au: Gst.Buffer, sei: Gst.Memory) -> Gst.Buffer | None:
        """
            Copy `au` into new GstBuffer and insert `sei` into the GstBuffer.
        The validation for completeness of `au` and `sei` is caller's responsibility.
        Nal prefix type of both `au` and `sei` must be packetized, and
        also the size of nal length field must be identical to `nal_length_size`
        """
    def link_slice_hdr(self, slice: H265SliceHdr) -> H265ParserResult:
        """
            Link SPS and PPS of `parser` to `slice`. `slice` must be valid and parsed
        already by `parser` or other GstH265Parser
        """
    def parse_decoder_config_record(self, data: int, size: int) -> tuple[H265ParserResult, H265DecoderConfigRecord]:
        """
            Parses HEVCDecoderConfigurationRecord data and fill into `config`.
        The caller must free `config` via `gst_h265_decoder_config_record_free`

        This method does not parse VPS, SPS and PPS and therefore the caller needs to
        parse each NAL unit via appropriate parsing method.
        """
    def parse_nal(self, nalu: H265NalUnit) -> H265ParserResult:
        """
            This function should be called in the case one doesn't need to
        parse a specific structure. It is necessary to do so to make
        sure `parser` is up to date.
        """
    def parse_pps(self, nalu: H265NalUnit, pps: H265PPS) -> H265ParserResult:
        """
        Parses `data`, and fills the `pps` structure.
        """
    def parse_sei(self, nalu: H265NalUnit, messages: list[H265SEIMessage]) -> H265ParserResult:
        """
        Parses `data`, create and fills the `messages` array.
        """
    def parse_slice_hdr(self, nalu: H265NalUnit, slice: H265SliceHdr) -> H265ParserResult:
        """
            Parses `data`, and fills the `slice` structure.
        The resulting `slice_hdr` structure shall be deallocated with
        `gst_h265_slice_hdr_free` when it is no longer needed
        """
    def parse_slice_hdr_ext(self, nalu: H265NalUnit, slice: H265SliceHdr, sps_ext: H265SPSEXT) -> H265ParserResult:
        """
            Parses `data`, and fills the `slice` structure.
        The resulting `slice_hdr` structure shall be deallocated with
        `gst_h265_slice_hdr_free` when it is no longer needed
        """
    def parse_sps(self, nalu: H265NalUnit, sps: H265SPS, parse_vui_params: bool) -> H265ParserResult:
        """
        Parses `data`, and fills the `sps` structure.
        """
    def parse_sps_ext(
        self, nalu: H265NalUnit, sps: H265SPS, sps_ext: H265SPSEXT, parse_vui_params: bool
    ) -> H265ParserResult:
        """
        Parses `data`, and fills the `sps` structure.
        """
    def parse_vps(self, nalu: H265NalUnit, vps: H265VPS) -> H265ParserResult:
        """
        Parses `data`, and fills the `vps` structure.
        """
    def update_pps(self, pps: H265PPS) -> H265ParserResult:
        """
            Replace internal Sequence Parameter Set struct corresponding to id of `pps`
        with `pps`. `nalparser` will mark `pps` as last parsed sps.
        """
    def update_sps(self, sps: H265SPS) -> H265ParserResult:
        """
            Replace internal Sequence Parameter Set struct corresponding to id of `sps`
        with `sps`. `nalparser` will mark `sps` as last parsed sps.
        """
    def update_vps(self, vps: H265VPS) -> H265ParserResult:
        """
            Replace internal Video Parameter Set struct corresponding to id of `vps`
        with `vps`. `nalparser` will mark `vps` as last parsed vps.
        """

class H265PicTiming(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    au_cpb_removal_delay_minus1: int = ...
    du_common_cpb_removal_delay_flag: int = ...
    du_common_cpb_removal_delay_increment_minus1: int = ...
    du_cpb_removal_delay_increment_minus1: int = ...
    duplicate_flag: int = ...
    num_decoding_units_minus1: int = ...
    num_nalus_in_du_minus1: int = ...
    pic_dpb_output_delay: int = ...
    pic_dpb_output_du_delay: int = ...
    pic_struct: int = ...
    source_scan_type: int = ...

class H265PredWeightTable(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    chroma_weight_l0_flag: bytes | None = ...
    chroma_weight_l1_flag: bytes | None = ...
    delta_chroma_log2_weight_denom: int = ...
    delta_chroma_offset_l0: list | None = ...
    delta_chroma_offset_l1: list | None = ...
    delta_chroma_weight_l0: list | None = ...
    delta_chroma_weight_l1: list | None = ...
    delta_luma_weight_l0: list | None = ...
    delta_luma_weight_l1: list | None = ...
    luma_log2_weight_denom: int = ...
    luma_offset_l0: list | None = ...
    luma_offset_l1: list | None = ...
    luma_weight_l0_flag: bytes | None = ...
    luma_weight_l1_flag: bytes | None = ...

class H265ProfileTierLevel(GObject.GPointer, metaclass=GObject.GType):
    """
    Define ProfileTierLevel parameters
    """

    # gi Fields
    frame_only_constraint_flag: int = ...
    """
    recognize the field_seq_flag
    """
    interlaced_source_flag: int = ...
    """
    flag to indicate the type of stream
    """
    intra_constraint_flag: int = ...
    """
    used to define profile extensions, see Annex A
    """
    level_idc: int = ...
    lower_bit_rate_constraint_flag: int = ...
    """
    used to define profile extensions, see Annex A
    """
    max_10bit_constraint_flag: int = ...
    """
    used to define profile extensions, see Annex A
    """
    max_12bit_constraint_flag: int = ...
    """
    used to define profile extensions, see Annex A
    """
    max_14bit_constraint_flag: int = ...
    """
    used to define profile extensions, see Annex A
    `level` idc: indicate the level which the CVS confirms
    """
    max_420chroma_constraint_flag: int = ...
    """
    used to define profile extensions, see Annex A
    """
    max_422chroma_constraint_flag: int = ...
    """
    used to define profile extensions, see Annex A
    """
    max_8bit_constraint_flag: int = ...
    """
    used to define profile extensions, see Annex A
    """
    max_monochrome_constraint_flag: int = ...
    """
    used to define profile extensions, see Annex A
    """
    non_packed_constraint_flag: int = ...
    """
    indicate the presence of frame packing
      arrangement sei message
    """
    one_picture_only_constraint_flag: int = ...
    """
    used to define profile extensions, see Annex A
    """
    profile_compatibility_flag: bytes | None = ...
    """
    compatibility flags
    """
    profile_idc: int = ...
    """
    profile id
    """
    profile_space: int = ...
    """
    specifies the context for the interpretation of
      general_profile_idc and general_profile_combatibility_flag
    """
    progressive_source_flag: int = ...
    """
    flag to indicate the type of stream
    """
    sub_layer_frame_only_constraint_flag: bytes | None = ...
    """
    recognize the sublayer
      specific field_seq_flag
    """
    sub_layer_interlaced_source_flag: bytes | None = ...
    """
    interlaced stream indicator for sublayer
    """
    sub_layer_level_idc: bytes | None = ...
    """
    indicate the sublayer specific level
    """
    sub_layer_level_present_flag: bytes | None = ...
    """
    sublayer level presence indicator.
    """
    sub_layer_non_packed_constraint_flag: bytes | None = ...
    """
    indicate the presence of
      frame packing arrangement sei message with in sublayers
    """
    sub_layer_profile_compatibility_flag: bytes | None = ...
    sub_layer_profile_idc: bytes | None = ...
    """
    conformant profile indicator for sublayers.
    `sub_layer_profile_compatibility_flag`[6][32]: compatibility flags for sublayers
    """
    sub_layer_profile_present_flag: bytes | None = ...
    """
    sublayer profile presence ind
    """
    sub_layer_profile_space: bytes | None = ...
    """
    profile space for sublayers
    """
    sub_layer_progressive_source_flag: bytes | None = ...
    """
    progressive stream indicator for sublayer
    """
    sub_layer_tier_flag: bytes | None = ...
    """
    tier flags for sublayers.
    """
    tier_flag: int = ...
    """
    the tier context for the interpretation of general_level_idc
    """

    # gi Methods
    def get_profile(self) -> H265Profile:
        """
        Return the H265 profile defined in `ptl`.
        """

class H265RecoveryPoint(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    broken_link_flag: int = ...
    exact_match_flag: int = ...
    recovery_poc_cnt: int = ...

class H265RefPicListModification(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    list_entry_l0: list | None = ...
    list_entry_l1: list | None = ...
    ref_pic_list_modification_flag_l0: int = ...
    ref_pic_list_modification_flag_l1: int = ...

class H265RegisteredUserData(GObject.GPointer, metaclass=GObject.GType):
    """
    The User data registered by Rec. ITU-T T.35 SEI message.
    """

    # gi Fields
    country_code: int = ...
    """
    an itu_t_t35_country_code.
    """
    country_code_extension: int = ...
    """
    an itu_t_t35_country_code_extension_byte.
      Should be ignored when `country_code` is not 0xff
    """
    data: int = ...
    """
    the data of itu_t_t35_payload_byte
      excluding `country_code` and `country_code_extension`
    """
    size: int = ...
    """
    the size of `data` in bytes
    """

class H265SEIMessage(GObject.GPointer, metaclass=GObject.GType):
    """
    Constains information about SEI message. The content depends on the
    `payloadType`.
    """

    # gi Fields
    payloadType: H265SEIPayloadType = ...
    """
    GstH265SEIPayloadType
    """

class H265SPS(GObject.GPointer, metaclass=GObject.GType):
    """
    H265 Sequence Parameter Set (SPS)
    """

    # gi Fields
    amp_enabled_flag: int = ...
    bit_depth_chroma_minus8: int = ...
    bit_depth_luma_minus8: int = ...
    chroma_array_type: int = ...
    chroma_format_idc: int = ...
    conf_win_bottom_offset: int = ...
    conf_win_left_offset: int = ...
    conf_win_right_offset: int = ...
    conf_win_top_offset: int = ...
    conformance_window_flag: int = ...
    crop_rect_height: int = ...
    crop_rect_width: int = ...
    crop_rect_x: int = ...
    crop_rect_y: int = ...
    fps_den: int = ...
    fps_num: int = ...
    height: int = ...
    id: int = ...
    """
    The ID of the sequence parameter set
    """
    log2_diff_max_min_luma_coding_block_size: int = ...
    log2_diff_max_min_pcm_luma_coding_block_size: int = ...
    log2_diff_max_min_transform_block_size: int = ...
    log2_max_pic_order_cnt_lsb_minus4: int = ...
    log2_min_luma_coding_block_size_minus3: int = ...
    log2_min_pcm_luma_coding_block_size_minus3: int = ...
    log2_min_transform_block_size_minus2: int = ...
    long_term_ref_pics_present_flag: int = ...
    lt_ref_pic_poc_lsb_sps: list | None = ...
    max_dec_pic_buffering_minus1: bytes | None = ...
    max_latency_increase_plus1: bytes | None = ...
    max_num_reorder_pics: bytes | None = ...
    max_sub_layers_minus1: int = ...
    max_transform_hierarchy_depth_inter: int = ...
    max_transform_hierarchy_depth_intra: int = ...
    num_long_term_ref_pics_sps: int = ...
    num_short_term_ref_pic_sets: int = ...
    pcm_enabled_flag: int = ...
    pcm_loop_filter_disabled_flag: int = ...
    pcm_sample_bit_depth_chroma_minus1: int = ...
    pcm_sample_bit_depth_luma_minus1: int = ...
    pic_height_in_luma_samples: int = ...
    pic_width_in_luma_samples: int = ...
    profile_tier_level: H265ProfileTierLevel | None = ...
    sample_adaptive_offset_enabled_flag: int = ...
    scaling_list: H265ScalingList | None = ...
    scaling_list_data_present_flag: int = ...
    scaling_list_enabled_flag: int = ...
    separate_colour_plane_flag: int = ...
    short_term_ref_pic_set: list | None = ...
    sps_3d_extension_flag: int = ...
    sps_extension_4bits: int = ...
    sps_extension_flag: int = ...
    sps_extension_params: H265SPSExtensionParams | None = ...
    sps_multilayer_extension_flag: int = ...
    sps_range_extension_flag: int = ...
    sps_scc_extension_flag: int = ...
    sps_scc_extension_params: H265SPSSccExtensionParams | None = ...
    strong_intra_smoothing_enabled_flag: int = ...
    sub_layer_ordering_info_present_flag: int = ...
    temporal_id_nesting_flag: int = ...
    temporal_mvp_enabled_flag: int = ...
    used_by_curr_pic_lt_sps_flag: bytes | None = ...
    valid: bool = ...
    vps: H265VPS | None = ...
    vps_id: int = ...
    vui_parameters_present_flag: int = ...
    vui_params: H265VUIParams | None = ...
    width: int = ...

class H265SPSEXT(GObject.GPointer, metaclass=GObject.GType):
    """
    H265 Sequence Parameter Set extension
    """

    # gi Fields
    short_term_ref_pic_set_ext: list | None = ...

class H265SPSExtensionParams(GObject.GPointer, metaclass=GObject.GType):
    """
    Defines the GstH265SPSExtensionParams
    """

    # gi Fields
    cabac_bypass_alignment_enabled_flag: int = ...
    """
    True specifies that a context-based adaptive binary
      arithmetic coding (CABAC) alignment process is used prior to bypass decoding of the syntax
      elements coeff_sign_flag[] and coeff_abs_level_remaining[]
    """
    explicit_rdpcm_enabled_flag: int = ...
    extended_precision_processing_flag: int = ...
    """
    True specifies that an extended dynamic range is used
      for coefficient parsing and inverse transform processing
    """
    high_precision_offsets_enabled_flag: int = ...
    """
    True specifies that weighted prediction offset values
      are signalled using a bit-depth-dependent precision.
    """
    implicit_rdpcm_enabled_flag: int = ...
    intra_smoothing_disabled_flag: int = ...
    """
    True specifies that  the filtering process of neighbouring
      samples is unconditionally disabled for intra prediction
    """
    persistent_rice_adaptation_enabled_flag: int = ...
    """
    True specifies that the Rice parameter derivation
      for the binarization of coeff_abs_level_remaining[] is initialized at the start of each
      sub-block using mode dependent statistics accumulated from previous sub-blocks.
    """
    transform_skip_context_enabled_flag: int = ...
    """
    True specifies tspecifies that a particular
      context is used for the parsing of the sig_coeff_flag for transform blocks with a skipped
      transform.
    """
    transform_skip_rotation_enabled_flag: int = ...
    """
    True specifies that a rotation is applied to
      the residual data block from intra 4X4 blocks coded using a transform skip operation.
    """

class H265SPSSccExtensionParams(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    delta_palette_max_predictor_size: int = ...
    """
    specifies the difference between the maximum allowed
      palette predictor size and the maximum allowed palette size.
    """
    intra_boundary_filtering_disabled_flag: int = ...
    """
    equal to 1 specifies that the intra boundary
      filtering process is unconditionally disabled for intra prediction.
    Defines the _GstH265SPSSccExtensionParams
    """
    motion_vector_resolution_control_idc: int = ...
    """
    controls the presence and inference of the
      use_integer_mv_flag that specifies the resolution of motion vectors for inter prediction.
    """
    palette_max_size: int = ...
    """
    specifies the maximum allowed palette size.
    """
    palette_mode_enabled_flag: int = ...
    """
    equal to 1 specifies that the decoding process for palette mode
      may be used for intra blocks. Equal to 0 specifies that the decoding process for palette
      mode is not applied.
    """
    sps_curr_pic_ref_enabled_flag: int = ...
    """
    equal to 1 specifies that a picture in the CVS may be
      included in a reference picture list of a slice of the picture itself.
    """
    sps_num_palette_predictor_initializer_minus1: int = ...
    """
    plus 1 specifies the number of entries in
      the sequence palette predictor initializer.
    """
    sps_palette_predictor_initializer: list | None = ...
    """
    specifies the value of the comp-th component of the
      i-th palette entry in the SPS that is used to initialize the array PredictorPaletteEntries.
    """
    sps_palette_predictor_initializers_present_flag: int = ...
    """
    equal to 1 specifies that the sequence
      palette predictors are initialized using the sps_palette_predictor_initializer specified
      in clause 7.3.2.2.3.
    """

class H265ScalingList(GObject.GPointer, metaclass=GObject.GType):
    """
    Defines the GstH265ScalingList
    """

    # gi Fields
    scaling_list_dc_coef_minus8_16x16: list | None = ...
    """
    this plus 8 specifies the DC
      Coefficient values for 16x16 scaling list
    """
    scaling_list_dc_coef_minus8_32x32: list | None = ...
    """
    this plus 8 specifies the DC
      Coefficient values for 32x32 scaling list
    """
    scaling_lists_16x16: bytes | None = ...
    """
    16x16 scaling list
    `guint8` scaling_lists_32x32: 32x32 scaling list
    """
    scaling_lists_32x32: bytes | None = ...
    scaling_lists_4x4: bytes | None = ...
    """
    4x4 scaling list
    """
    scaling_lists_8x8: bytes | None = ...
    """
    8x8 scaling list
    """

class H265ShortTermRefPicSet(GObject.GPointer, metaclass=GObject.GType):
    """
    Defines the GstH265ShortTermRefPicSet params
    """

    # gi Fields
    DeltaPocS0: list | None = ...
    """
    Derived value depending on inter_ref_pic_set_prediction_flag.
     See 7.4.8 Short-term reference picture set semantics
    """
    DeltaPocS1: list | None = ...
    """
    Derived value depending on inter_ref_pic_set_prediction_flag.
     See 7.4.8 Short-term reference picture set semantics
    """
    NumDeltaPocs: int = ...
    """
    sum of `NumNegativePics` and `NumPositivePics`.
    """
    NumDeltaPocsOfRefRpsIdx: int = ...
    """
    The value of NumDeltaPocs[RefRpsIdx].
     If inter_ref_pic_set_prediction_flag is equal to 0,
     this value should be ignored (Since: 1.18)
    """
    NumNegativePics: int = ...
    """
    Derived value depending on inter_ref_pic_set_prediction_flag.
     If inter_ref_pic_set_prediction_flag is equal to 0, this specifies
     the number of entries in the stRpsIdx-th candidate
     short-term RPS that have poc values less than the poc of the current picture.
    """
    NumPositivePics: int = ...
    """
    Derived value depending on inter_ref_pic_set_prediction_flag.
     If inter_ref_pic_set_prediction_flag is equal to 0, this specifies
     the number of entires in the stRpsIdx-th candidate
     short-term RPS that have poc values greater than the poc value of the current picture.
    """
    UsedByCurrPicS0: bytes | None = ...
    """
    Derived value depending on inter_ref_pic_set_prediction_flag.
     If inter_ref_pic_set_prediction_flag is equal to 0,
     equal to zero specifies that the i-th entry in the stRpsIdx-th
     candidate short-term RPS that has poc value less than of the current picture
     is not used for reference by the current picture
    """
    UsedByCurrPicS1: bytes | None = ...
    """
    Derived value depending on inter_ref_pic_set_prediction_flag.
     If inter_ref_pic_set_prediction_flag is equal to 0,
     equal to zero specifies that the i-th entry in the current
     candidate short-term RPS that has poc value greater than that of the current picture
     is not used for reference by the current picture.
    """
    abs_delta_rps_minus1: int = ...
    """
    delta_rps_sign and abs_delta_rps_minus1 together specify
     the value of the variable deltaRps
    """
    delta_idx_minus1: int = ...
    """
    plus 1 specifies the difference between the value of source and
     candidate short term RPS.
    """
    delta_rps_sign: int = ...
    """
    delta_rps_sign and abs_delta_rps_minus1 together specify
     the value of the variable deltaRps.
    """
    inter_ref_pic_set_prediction_flag: int = ...
    """
    True specifies that the stRpsIdx-th candidate
     short-term RPS is predicted from another candidate short-term RPS
    """

class H265ShortTermRefPicSetExt(GObject.GPointer, metaclass=GObject.GType):
    """
    Defines the extended GstH265ShortTermRefPicSetExt params
    """

    # gi Fields
    delta_poc_s0_minus1: list | None = ...
    """
    Specifies the negative picture order count delta for the i-th
     entry in the short-term RPS. See details in section 7.4.8 "Short-term reference
     picture set semantics" of the specification.
    """
    delta_poc_s1_minus1: list | None = ...
    """
    Specifies the positive picture order count delta for the i-th
     entry in the short-term RPS. See details in section 7.4.8 "Short-term reference
     picture set semantics" of the specification.
    """
    use_delta_flag: bytes | None = ...
    """
    Bit j equals to 1 specifies that the j-th entry in the source candidate
     short-term RPS is included in this candidate short-term RPS.
    """
    used_by_curr_pic_flag: bytes | None = ...

class H265SliceHdr(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    NumPocTotalCurr: int = ...
    """
    calculated NumPocTotalCurr which is used for
      decoding process for reference picture set
    """
    beta_offset_div2: int = ...
    """
    deblocking parameter offset for beta divided by 2 for the current slice.
    """
    cabac_init_flag: int = ...
    """
    specifies the method for determining the initialization table
      used in the initialization process for context variables.
    """
    cb_qp_offset: int = ...
    """
    a difference to be added to the value of pps_cb_qp_offset.
    """
    collocated_from_l0_flag: int = ...
    """
    equal to 1 specifies that the collocated picture
      used for temporal motion vector prediction is derived from reference picture list 0.
    """
    collocated_ref_idx: int = ...
    """
    the reference index of the collocated picture
      used for temporal motion vector prediction.
    """
    colour_plane_id: int = ...
    """
    specifies the colour plane associated with the current slice RBSP
      when separate_colour_plane_flag is equal to 1
    """
    cr_qp_offset: int = ...
    """
    a difference to be added to the value of pps_cr_qp_offset.
    """
    cu_chroma_qp_offset_enabled_flag: int = ...
    """
    equal to 1 if the cu_chroma_qp_offset_flag
      may be present in the transform unit syntax. (Since: 1.18)
    """
    deblocking_filter_disabled_flag: int = ...
    """
    equal to 1 specifies that the operation of
      the deblocking filter is not applied for the current slice.
    """
    deblocking_filter_override_flag: int = ...
    """
    equal to 1 if deblocking paramertes are present in the slice header.
    """
    delta_poc_msb_cycle_lt: list | None = ...
    """
    used to determine the value of the most significant bits
      of the picture order count value of the i-th entry in the long-term RPS of the current picture.
    """
    delta_poc_msb_present_flag: bytes | None = ...
    """
    equal to 1 if i-th delta_poc_msb_cycle_lt[] is present.
    """
    dependent_slice_segment_flag: int = ...
    """
    equal to 1 if the value of each slice segment header
      syntax element that is not present is inferred to be equal to the value of corresponding
      slice segment header syntax element in the slice header.
    """
    entry_point_offset_minus1: int = ...
    """
    the entry point offset in bytes.
    """
    first_slice_segment_in_pic_flag: int = ...
    """
    equal to 1 if this slice segment is
      the first slice segment of the picture in decoding order
    """
    five_minus_max_num_merge_cand: int = ...
    """
    specifies the maximum number of merging motion vector prediction (MVP)
      candidates supported in the slice.
    """
    header_size: int = ...
    """
    the calculated size of the slice_header() in bits.
    """
    long_term_ref_pic_set_size: int = ...
    """
    the calculated size of the branch
      `if( long_term_ref_pics_present_flag )` `inside `slice_segment_header`` syntax
      in bits. (Since: 1.22)
    """
    loop_filter_across_slices_enabled_flag: int = ...
    """
    equal to 1 specifies that in-loop filtering
      operation may be performed across the left and upper boundaries of the current slice.
    """
    lt_idx_sps: bytes | None = ...
    """
    the index of candidate long-term reference pictures
      specified in the active SPS.
    """
    mvd_l1_zero_flag: int = ...
    """
    equal to 1 if the mvd_coding sytanx structure is not parsed
    """
    n_emulation_prevention_bytes: int = ...
    """
    number of emulation prevention bytes (EPB)
      in this slice_header()
    """
    no_output_of_prior_pics_flag: int = ...
    """
    affects the output of previously-decoded pictures
      in the decoded picture buffer after the decoding of an IDR or a BLA picture
      that is not the first picture in the bitstream as specified in Annex C
    """
    num_entry_point_offsets: int = ...
    """
    specifies the number of entry_point_offset_minus1 syntax elements
      in the slice header.
    """
    num_long_term_pics: int = ...
    """
    the number of entries in the long-term RPS of the current picture
      that are directly signalled in the slice header.
    """
    num_long_term_sps: int = ...
    """
    the number of entries in the long-term RPS of current picture
      that are derived based on the syntax in active SPS.
    """
    num_ref_idx_active_override_flag: int = ...
    """
    equal to 1 specifies that the syntax elements
      num_ref_idx_l0_active_minus1 and num_ref_idx_l1_active_minus1 are present.
    """
    num_ref_idx_l0_active_minus1: int = ...
    """
    the maximum reference index for reference picture list 0
      that may be used to decode the slice.
    """
    num_ref_idx_l1_active_minus1: int = ...
    """
    the maximum reference index for reference picture list 1
      that may be used to decode the slice.
    """
    offset_len_minus1: int = ...
    """
    specifies the length of the entry_point_minus1 syntax elements
      in bits.
    """
    pic_order_cnt_lsb: int = ...
    """
    the picture order count modulo MaxPicOrderCntLsb for the current picture
    """
    pic_output_flag: int = ...
    """
    affects the decoded picture output and removal processes
      as specified in Annex C.
    """
    poc_lsb_lt: list | None = ...
    """
    the value of the picture order count modulo MaxPicOrderCntLsb
      of the each entry in the long-term RPS of the current picture.
    """
    pps: H265PPS | None = ...
    """
    a GstH265PPS
    """
    pps_id: int = ...
    pred_weight_table: H265PredWeightTable | None = ...
    """
    a GstH265PredWeightTable
    """
    qp_delta: int = ...
    """
    specifies the inital value of QPy to be used for the coding blocks in the slice.
    """
    ref_pic_list_modification: H265RefPicListModification | None = ...
    """
    a GstH265RefPicListModification
    """
    sao_chroma_flag: int = ...
    """
    equal to 1 if SAO is enabled for the chroma component in the current slice.
    """
    sao_luma_flag: int = ...
    """
    equal to 1 if SAO is enabled for the luma component in the current slice.
    """
    segment_address: int = ...
    """
    the address of the first CTB in the slice segment
    """
    short_term_ref_pic_set_idx: int = ...
    """
    the index of st_ref_pic_set syntax structure
      that is used for derivation of the short-term RPS of the current picture.
    """
    short_term_ref_pic_set_size: int = ...
    """
    the calculated size of short_term_ref_pic_set()
      in bits. (Since: 1.18)
    """
    short_term_ref_pic_set_sps_flag: int = ...
    """
    equal to 1 specifies that the short-term RPS
      of the current picture is derived based on the active SPS.
    """
    short_term_ref_pic_sets: H265ShortTermRefPicSet | None = ...
    """
    a GstH265ShortTermRefPicSet structure
    """
    slice_act_cb_qp_offset: int = ...
    """
    specify offsets to the quantization parameter values qP derived in
      clause 8.6.2 for Cb components. (Since: 1.18)
    """
    slice_act_cr_qp_offset: int = ...
    """
    specify offsets to the quantization parameter values qP derived in
      clause 8.6.2 for Cr components. (Since: 1.18)
    """
    slice_act_y_qp_offset: int = ...
    """
    specify offsets to the quantization parameter values qP derived in
      clause 8.6.2 for luma components. (Since: 1.18)
    """
    tc_offset_div2: int = ...
    """
    deblocking parameter offset for tC divided by 2 for the current slice.
    """
    temporal_mvp_enabled_flag: int = ...
    """
    whether temporal motion vector predictors can be used for inter prediction.
    """
    type: int = ...
    """
    slice type (B, P, or I)
    """
    use_integer_mv_flag: int = ...
    """
    equal to 1 specifies that the resolution of motion vectors for inter
      prediction in the current slice is integer. (Since: 1.18)
    """
    used_by_curr_pic_lt_flag: bytes | None = ...
    """
    equal to 0 if the entry in the long-term RPS
      of the current picture is not used for reference by the current picture.
    """

    # gi Methods
    def copy(self, src_slice: H265SliceHdr) -> bool:
        """
        Copies `src_slice` into `dst_slice`
        """
    def free(self) -> None:
        """
            slice_hdr: The GstH265SliceHdr to free
        Frees `slice_hdr` fields.
        """

class H265SubLayerHRDParams(GObject.GPointer, metaclass=GObject.GType):
    """
    Defines the Sublayer HRD parameters
    """

    # gi Fields
    bit_rate_du_value_minus1: list | None = ...
    """
    together with bit_rate_scale, it
      specifies the maximum input bit rate when the CPB operates at the
      sub-picture level
    """
    bit_rate_value_minus1: list | None = ...
    """
    together with bit_rate_scale, it specifies
      the maximum input bitrate when the CPB operates at the access
      unit level
    """
    cbr_flag: bytes | None = ...
    """
    indicate whether HSS operates in intermittent bit rate
      mode or constant bit rate mode.
    """
    cpb_size_du_value_minus1: list | None = ...
    """
    is used together with cpb_size_du_scale
      to specify the CPB size when the CPB operates at sub-picture
      level
    """
    cpb_size_value_minus1: list | None = ...
    """
    is used together with cpb_size_scale to
      specify the CPB size when the CPB operates at the access unit
      level
    """

class H265TimeCode(GObject.GPointer, metaclass=GObject.GType):
    """
    The time code SEI message provides time code information similar to that
    defined by SMPTE ST 12-1 (2014) for field(s) or frame(s) of the current
    picture.
    D.2.27
    """

    # gi Fields
    clock_timestamp_flag: bytes | None = ...
    cnt_dropped_flag: bytes | None = ...
    counting_type: bytes | None = ...
    discontinuity_flag: bytes | None = ...
    full_timestamp_flag: bytes | None = ...
    hours_flag: bytes | None = ...
    hours_value: bytes | None = ...
    minutes_flag: bytes | None = ...
    minutes_value: bytes | None = ...
    n_frames: list | None = ...
    num_clock_ts: int = ...
    seconds_flag: bytes | None = ...
    seconds_value: bytes | None = ...
    time_offset_length: bytes | None = ...
    time_offset_value: list | None = ...
    units_field_based_flag: bytes | None = ...

class H265UserDataUnregistered(GObject.GPointer, metaclass=GObject.GType):
    """
    The User data unregistered SEI message syntax.
    """

    # gi Fields
    data: int = ...
    """
    the data of user_data_payload_byte
    """
    size: int = ...
    """
    the size of `data` in bytes
    """
    uuid: bytes | None = ...
    """
    an uuid_iso_iec_11578.
    """

class H265VPS(GObject.GPointer, metaclass=GObject.GType):
    """
    Defines the VPS parameters
    """

    # gi Fields
    base_layer_available_flag: int = ...
    base_layer_internal_flag: int = ...
    cprms_present_flag: int = ...
    hrd_layer_set_idx: int = ...
    """
    index to the list of layer hrd params.
    """
    hrd_params: H265HRDParams | None = ...
    """
    the GstH265HRDParams list
    """
    id: int = ...
    """
    vps id
    `base_layer_internal_flag` and `base_layer_available_flag`:
      specify availability of base layer
    """
    max_dec_pic_buffering_minus1: bytes | None = ...
    """
    specifies the maximum required size
      of the decoded picture buffer
    """
    max_latency_increase_plus1: list | None = ...
    """
    is used to compute the value of
      VpsMaxLatencyPictures
    """
    max_layer_id: int = ...
    """
    specifies the maximum allowed value of nuh_layer_id
    """
    max_layers_minus1: int = ...
    """
    should be zero, but can be other values in future
    """
    max_num_reorder_pics: bytes | None = ...
    """
    indicates the maximum allowed number of
      pictures that can precede any picture in the CVS in decoding
      order
    """
    max_sub_layers_minus1: int = ...
    """
    specifies the maximum number of temporal sub-layers
    """
    num_hrd_parameters: int = ...
    """
    number of hrd_parameters present
    """
    num_layer_sets_minus1: int = ...
    """
    specifies the number of layer sets
    """
    num_ticks_poc_diff_one_minus1: int = ...
    """
    specifies the number of clock ticks
      corresponding to a difference of picture order count values equal
      to 1
    """
    num_units_in_tick: int = ...
    """
    number of time units in a tick
    """
    poc_proportional_to_timing_flag: int = ...
    """
    indicate whether the picture
      order count is proportional to output timin
    """
    profile_tier_level: H265ProfileTierLevel | None = ...
    """
    ProfileTierLevel info
    """
    sub_layer_ordering_info_present_flag: int = ...
    """
    indicates the presence of
      vps_max_dec_pic_buffering_minus1, vps_max_num_reorder_pics and
      vps_max_latency_increase_plus1
    """
    temporal_id_nesting_flag: int = ...
    """
    specifies whether inter prediction is
      additionally restricted
    """
    time_scale: int = ...
    """
    number of time units that pass in one second
    """
    timing_info_present_flag: int = ...
    """
    indicate the presence of
      num_units_in_tick, time_scale, poc_proportional_to_timing_flag
      and num_hrd_parameters
    """
    valid: bool = ...
    vps_extension: int = ...

class H265VUIParams(GObject.GPointer, metaclass=GObject.GType):
    """
    The structure representing the VUI parameters.
    """

    # gi Fields
    aspect_ratio_idc: int = ...
    aspect_ratio_info_present_flag: int = ...
    """
    True specifies that aspect_ratio_idc is present.
     False specifies that aspect_ratio_idc is not present
    `aspect_ratio_idc` specifies the value of the sample aspect ratio of the luma samples
    `sar_width` indicates the horizontal size of the sample aspect ratio
    `sar_height` indicates the vertical size of the sample aspect ratio
    """
    bitstream_restriction_flag: int = ...
    """
    True specifies that the following coded video sequence bitstream restriction
    parameters are present
    """
    chroma_loc_info_present_flag: int = ...
    """
    True specifies that chroma_sample_loc_type_top_field and
     chroma_sample_loc_type_bottom_field are present, False otherwise
    """
    chroma_sample_loc_type_bottom_field: int = ...
    chroma_sample_loc_type_top_field: int = ...
    """
    specify the location of chroma for top field
    `chroma_sample_loc_type_bottom_field` specify the location of chroma for bottom field
    """
    colour_description_present_flag: int = ...
    """
    True specifies that colour_primaries,
     transfer_characteristics and matrix_coefficients are present
    """
    colour_primaries: int = ...
    """
    indicates the chromaticity coordinates of the source primaries
    """
    def_disp_win_bottom_offset: int = ...
    def_disp_win_left_offset: int = ...
    def_disp_win_right_offset: int = ...
    def_disp_win_top_offset: int = ...
    default_display_window_flag: int = ...
    """
    True indicate that the default display window parameters present
    def_disp_win_left_offset:left offset of display rect
    def_disp_win_right_offset: right offset of display rect
    def_disp_win_top_offset: top offset of display rect
    def_disp_win_bottom_offset: bottom offset of display rect
    """
    field_seq_flag: int = ...
    """
    True indicate field and False indicate frame
    """
    frame_field_info_present_flag: int = ...
    """
    True indicate picture timing SEI messages are present for every
      picture and include the pic_struct, source_scan_type, and duplicate_flag syntax elements.
    """
    hrd_parameters_present_flag: int = ...
    """
    True if hrd parameters present in the bitstream
    """
    hrd_params: H265HRDParams | None = ...
    log2_max_mv_length_horizontal: int = ...
    """
    indicate the maximum absolute value of a decoded horizontal
    motion vector component
    """
    log2_max_mv_length_vertical: int = ...
    """
    indicate the maximum absolute value of a decoded vertical
     motion vector component
    """
    matrix_coefficients: int = ...
    """
    describes the matrix coefficients used in deriving luma and chroma signals
    """
    max_bits_per_min_cu_denom: int = ...
    """
    indicates an upper bound for the number of coded bits of coding_unit
     data for any coding block in any picture of the CVS
    """
    max_bytes_per_pic_denom: int = ...
    """
    indicates a number of bytes not exceeded by the sum of the sizes of
     the VCL NAL units associated with any coded picture in the coded video sequence.
    """
    min_spatial_segmentation_idc: int = ...
    """
    when not equal to 0, establishes a bound on the maximum possible size
     of distinct coded spatial segmentation regions in the pictures of the CVS
    """
    motion_vectors_over_pic_boundaries_flag: int = ...
    """
    False indicates that no sample outside the
     picture boundaries and no sample at a fractional sample position, True indicates that one or more
     samples outside picture boundaries may be used in inter prediction
    """
    neutral_chroma_indication_flag: int = ...
    """
    True indicate that the value of chroma samples is equla
     to 1<<(BitDepthchrom-1).
    """
    num_ticks_poc_diff_one_minus1: int = ...
    """
    plus 1 specifies the number of clock ticks corresponding to a
     difference of picture order count values equal to 1
    """
    num_units_in_tick: int = ...
    """
    is the number of time units of a clock operating at the frequency time_scale Hz
    """
    overscan_appropriate_flag: int = ...
    """
    True indicates that the cropped decoded pictures
     output are suitable for display using overscan. False the cropped decoded pictures
     output contain visually important information
    """
    overscan_info_present_flag: int = ...
    """
    True overscan_appropriate_flag is present False otherwise
    """
    par_d: int = ...
    par_n: int = ...
    parsed: bool = ...
    """
    True indicate that VUI parameters have been parsed (Since: 1.22)
    """
    poc_proportional_to_timing_flag: int = ...
    """
    True indicates that the picture order count value for each picture
     in the CVS that is not the first picture in the CVS, in decoding order, is proportional to the output
     time of the picture relative to the output time of the first picture in the CVS.
    """
    restricted_ref_pic_lists_flag: int = ...
    sar_height: int = ...
    sar_width: int = ...
    tiles_fixed_structure_flag: int = ...
    """
    True indicates that each PPS that is active in the CVS has the same value
      of the syntax elements num_tile_columns_minus1, num_tile_rows_minus1, uniform_spacing_flag,
      column_width_minus1, row_height_minus1 and loop_filter_across_tiles_enabled_flag, when present
    """
    time_scale: int = ...
    """
    is the number of time units that pass in one second
    """
    timing_info_present_flag: int = ...
    """
    True specifies that num_units_in_tick,
     time_scale and fixed_frame_rate_flag are present in the bitstream
    """
    transfer_characteristics: int = ...
    """
    indicates the opto-electronic transfer characteristic
    """
    video_format: int = ...
    """
    indicates the representation of the picture
    """
    video_full_range_flag: int = ...
    """
    indicates the black level and range of the luma and chroma signals
    """
    video_signal_type_present_flag: int = ...
    """
    True specifies that video_format, video_full_range_flag and
     colour_description_present_flag are present.
    """

class H266ALF(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 ALF parameters.
    """

    # gi Fields
    cc_cb_coeff_sign: bytes | None = ...
    """
    specifies the sign of the j-th coefficient of the
     signalled k-th cross-component filter for the Cb colour component.
    """
    cc_cb_filter_signal_flag: int = ...
    """
    specifies whether cross-component filters for the
     Cb colour component are signalled.
    """
    cc_cb_filters_signalled_minus1: int = ...
    """
    specifies the number of cross-component
     filters for the Cb colour component.
    """
    cc_cb_mapped_coeff_abs: bytes | None = ...
    """
    specifies the absolute value of the j-th mapped
     coefficient of the signalled k-th cross-component filter for the Cb colour
     component.
    """
    cc_cr_coeff_sign: bytes | None = ...
    """
    specifies the sign of the j-th coefficient of the
     signalled k-th cross-component filter for the Cr colour component.
    """
    cc_cr_filter_signal_flag: int = ...
    """
    specifies whether cross-component filters for the
     Cr colour component are signalled.
    """
    cc_cr_filters_signalled_minus1: int = ...
    """
    specifies the number of cross-component
     filters for the Cr colour component.
    """
    cc_cr_mapped_coeff_abs: bytes | None = ...
    """
    specifies the absolute value of the j-th mapped
     coefficient of the signalled k-th cross-component filter for the Cr colour
     component.
    """
    chroma_clip_flag: int = ...
    """
    specifies whether linear adaptive loop filtering is
     applied to chroma components.
    """
    chroma_clip_idx: bytes | None = ...
    """
    specifies the clipping index of the clipping value to use
     before multiplying by the j-th coefficient of the alternative chroma filter
     with index altIdx.
    """
    chroma_coeff_abs: bytes | None = ...
    """
    specifies the absolute value of the j-th chroma filter
     coefficient for the alternative chroma filter with index altIdx.
    """
    chroma_coeff_sign: bytes | None = ...
    """
    specifies the sign of the j-th chroma filter coefficient
     for the alternative chroma filter with index altIdx.
    """
    chroma_filter_signal_flag: int = ...
    """
    specifies whether a chroma filter is signalled.
    """
    chroma_num_alt_filters_minus1: int = ...
    """
    specifies the number of alternative filters
     for chroma components.
    """
    luma_clip_flag: int = ...
    """
    specifies whether linear adaptive loop filtering is applied
     to the luma component.
    """
    luma_clip_idx: bytes | None = ...
    """
    specifies the clipping index of the clipping value to use
     before multiplying by the j-th coefficient of the signalled luma filter.
    """
    luma_coeff_abs: bytes | None = ...
    """
    specifies the absolute value of the j-th coefficient of
     the signalled luma filter.
    """
    luma_coeff_delta_idx: bytes | None = ...
    """
    specifies the indices of the signalled adaptive loop
     filter luma coefficient deltas for the filter class.
    """
    luma_coeff_sign: bytes | None = ...
    """
    specifies the sign of the j-th luma coefficient of the
     filter.
    """
    luma_filter_signal_flag: int = ...
    """
    specifies whether a luma filter set is signalled.
    """
    luma_num_filters_signalled_minus1: int = ...
    """
    specifies the number of adpative loop
     filter classes for which luma coefficients can be signalled.
    """

class H266APS(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 Adaptation Parameter Set.
    """

    # gi Fields
    aps_id: int = ...
    """
    provides an identifier for the APS for reference by other syntax
     elements.
    """
    chroma_present_flag: int = ...
    """
    specifies whether the APS NAL unit could include
     chroma related syntax elements.
    """
    extension_data_flag: int = ...
    """
    could have any value and do not affect the decoding
     process now.
    """
    extension_flag: int = ...
    """
    specifies whether aps_extension_data_flag syntax elements
     are present.
    """
    params_type: H266APSType = ...
    """
    specifies the type of APS parameters carried in the APS as
     specified in Table 6.
    """
    valid: bool = ...
    """
    whether this APS is valid.
    """

class H266AUD(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 AU delimiter.
    """

    # gi Fields
    irap_or_gdr_flag: int = ...
    """
    specifies whether the AU containing the AU delimiter is
     an IRAP or GDR AU.
    """
    pic_type: int = ...
    """
    indicates sh_slice_type values that could be present in the AU.
    """

class H266BufferingPeriod(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 buffering period.
    """

    # gi Fields
    additional_concatenation_info_present_flag: int = ...
    """
    specifies whether the syntax
     element bp_max_initial_removal_delay_for_concatenation is present in the BP
     SEI message and the syntax element pt_delay_for_concatenation_ensured_flag
     is present in the PT SEI messages.
    """
    alt_cpb_params_present_flag: int = ...
    """
    specifies the presence of the syntax element
     bp_use_alt_cpb_params_flag.
    """
    concatenation_flag: int = ...
    """
    indicates whether the nominal CPB removal time of the
     current AU is determined relative to the nominal CPB removal time of the
     previous AU.
    """
    cpb_cnt_minus1: int = ...
    """
    specifies the number of syntax element pairs
     bp_nal_initial_cpb_removal_delay and bp_nal_initial_cpb_removal_offset
     of the i-th temporal sublayer.
    """
    cpb_initial_removal_delay_length_minus1: int = ...
    """
    specifies the length, in bits, of
     the initial cpb removal syntax elements.
    """
    cpb_removal_delay_delta_minus1: int = ...
    """
    specifies a CPB removal delay increment
     value relative to the nominal CPB removal time of the AU prevNonDiscardableAu.
    """
    cpb_removal_delay_delta_val: bytes | None = ...
    """
    specifies the i-th CPB removal delay delta.
    """
    cpb_removal_delay_deltas_present_flag: int = ...
    """
    specifies whether the BP SEI message
     contains CPB removal delay deltas.
    """
    cpb_removal_delay_length_minus1: int = ...
    """
    specifies the length, in bits, of the
     cpb removal syntax elements.
    """
    dpb_output_delay_du_length_minus1: int = ...
    """
    specifies the length, in bits, of the
     dpb output syntax element.
    """
    dpb_output_delay_length_minus1: int = ...
    """
    specifies the length, in bits, of the
     syntax dpb output syntax elements.
    """
    dpb_output_tid_offset: list | None = ...
    """
    specifies the difference between the DPB output times
     for the i-th sublayer representation.
    """
    du_cpb_params_in_pic_timing_sei_flag: int = ...
    """
    specifies whether DU level CPB removal
     delay parameters are present in PT SEI messages.
    """
    du_cpb_removal_delay_increment_length_minus1: int = ...
    """
    specifies the length, in bits,
     of the du cpb removal syntax elements.
    """
    du_dpb_params_in_pic_timing_sei_flag: int = ...
    """
    specifies whether DU level DPB output
     delay parameters are present in PT SEI messages.
    """
    du_hrd_params_present_flag: int = ...
    """
    specifies whether DU level HRD parameters are
     present and the HRD can be operated at the AU level or DU level.
    """
    max_initial_removal_delay_for_concatenation: int = ...
    """
    identify whether the nominal
     removal time from the CPB of the first AU of a following BP computed with
     bp_cpb_removal_delay_delta_minus1 applies.
    """
    max_sublayers_minus1: int = ...
    """
    specifies whether maximum number of temporal sublayers
     for which the initial CPB removal delay and the initial CPB removal offset
     are indicated in the BP SEI message.
    """
    nal_hrd_params_present_flag: int = ...
    """
    specifies whether a list of syntax element
     pairs bp_nal_initial_cpb_removal_delay and bp_nal_initial_cpb_removal_offset
     are present in the BP SEI message.
    """
    nal_initial_alt_cpb_removal_delay: bytes | None = ...
    """
    specify the j-th alternative initial CPB
     removal delay for the NAL HRD in units of a 90 kHz clock of the i-th
     temporal sublayer.
    """
    nal_initial_alt_cpb_removal_offset: bytes | None = ...
    """
    specify the j-th alternative initial CPB
     removal offset of the i-th temporal sublayer for the NAL HRD in units of a
     90 kHz clock.
    """
    nal_initial_cpb_removal_delay: bytes | None = ...
    """
    specify the j-th default initial CPB removal
     delay for the NAL HRD in units of a 90 kHz clock of the i-th temporal sublayer.
    """
    nal_initial_cpb_removal_offset: bytes | None = ...
    """
    specify the j-th default initial CPB removal
     offset of the i-th temporal sublayer for the NAL HRD in units of a 90 kHz
     clock.
    """
    num_cpb_removal_delay_deltas_minus1: int = ...
    """
    specifies the number of syntax elements
     bp_cpb_removal_delay_delta_val[i] in the BP SEI message.
    """
    sublayer_dpb_output_offsets_present_flag: int = ...
    """
    specifies whether DPB output time
     offsets are present for sublayer representation.
    """
    sublayer_initial_cpb_removal_delay_present_flag: int = ...
    """
    specifies that initial CPB
     removal delay related syntax elements are present for sublayer representation.
    """
    use_alt_cpb_params_flag: int = ...
    """
    could be used to derive the value of
     UseAltCpbParamsFlag.
    """
    vcl_hrd_params_present_flag: int = ...
    """
    specifies whether a list of syntax element
     pairs bp_vcl_initial_cpb_removal_delay and bp_vcl_initial_cpb_removal_offset
     are present in the BP SEI message.
    """
    vcl_initial_alt_cpb_removal_delay: bytes | None = ...
    """
    specify the j-th alternative initial CPB
     removal delay of the i-th temporal sublayer for the VCL HRD in units of a
     90 kHz clock.
    """
    vcl_initial_alt_cpb_removal_offset: bytes | None = ...
    """
    specify the j-th alternative initial CPB
     removal offset of the i-th temporal sublayer for the VCL HRD in units of a
     90 kHz clock.
    """
    vcl_initial_cpb_removal_delay: bytes | None = ...
    """
    specify the j-th default initial CPB removal
     delay of the i-th temporal sublayer for the VCL HRD in units of a 90 kHz clock.
    """
    vcl_initial_cpb_removal_offset: bytes | None = ...
    """
    specify the j-th default initial CPB removal
     offset of the i-th temporal sublayer for the VCL HRD in units of a 90 kHz clock.
    """

class H266DCI(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 decoding capability information.
    """

    # gi Fields
    extension_data_flag: int = ...
    """
    could have any value.
    """
    extension_flag: int = ...
    """
    specifies whether dci_extension_data_flag syntax elements
     are present.
    """
    num_ptls_minus1: int = ...
    """
    specifies the number of profile_tier_level syntax
     structures in the DCI NAL unit.
    """
    profile_tier_level: list | None = ...

class H266DPBParameters(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 DPB parameters.
    """

    # gi Fields
    max_dec_pic_buffering_minus1: bytes | None = ...
    """
    specifies the maximum required size of the
     DPB in units of picture storage buffers.
    """
    max_latency_increase_plus1: bytes | None = ...
    """
    used to compute the value of MaxLatencyPictures,
     which specifies the maximum number of pictures in the OLS.
    """
    max_num_reorder_pics: bytes | None = ...
    """
    specifies the maximum allowed number of pictures of
     the OLS that can precede any picture in the OLS in decoding order and
     follow that picture in output.
    """

class H266DUInfo(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 decoding unit info.
    """

    # gi Fields
    decoding_unit_idx: int = ...
    """
    specifies the index to the list of DUs in the current AU.
    """
    dpb_output_du_delay: int = ...
    """
    is used to compute the DPB output time of the AU.
    """
    dpb_output_du_delay_present_flag: int = ...
    """
    specifies the presence of the
     dui_dpb_output_du_delay syntax element in the DUI SEI message.
    """
    du_cpb_removal_delay_increment: bytes | None = ...
    """
    specifies the duration between the nominal
     CPB times of the last DU in decoding order in the current AU.
    """
    sublayer_delays_present_flag: bytes | None = ...
    """
    specifies whether
     dui_du_cpb_removal_delay_increment[i] is present for the sublayer.
    """

class H266DecoderConfigRecord(GObject.GPointer, metaclass=GObject.GType):
    """
    Contains VVCDecoderConfigurationRecord data as defined in ISO/IEC 14496-15
    """

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list | None: ...
    avg_frame_rate: int = ...
    """
    Average frame rate
    """
    bit_depth_minus8: int = ...
    """
    Bit depth minus 8
    """
    chroma_format_idc: int = ...
    """
    Chroma format indicator
    """
    constant_frame_rate: int = ...
    """
    Indicates if the frame rate is constant
    """
    length_size_minus_one: int = ...
    """
    indicates the length in bytes of nal unit length field.
    This value shall be one of 0, 1, or 3 corresponding to a length
    encoded with 1, 2, or 4 bytes, respectively
    """
    max_picture_height: int = ...
    """
    Maximum picture height
    """
    max_picture_width: int = ...
    """
    Maximum picture width
    """
    nalu_array: list | None = ...
    """
    Array of GstH266DecoderConfigRecordNalUnitArray
    """
    native_ptl: H266PTLRecord | None = ...
    """
    Profile, tier and level information
    """
    num_sublayers: int = ...
    """
    Number of sublayers
    """
    ols_idx: int = ...
    """
    Operating point layer set index
    """
    ptl_present_flag: int = ...
    """
    true: profile, tier and level information is present
    """

    # gi Methods
    def free(self) -> None:
        """
        Free `config` data
        """

class H266DecoderConfigRecordNalUnitArray(GObject.GPointer, metaclass=GObject.GType):
    """
    Contains NAL Unit array data as defined in ISO/IEC 14496-15
    """

    # gi Fields
    array_completeness: int = ...
    """
    1: all NAL units of the given type are in this array and none
      are in the stream.
    0: additional NAL units of the indicated type may be in the stream
    """
    nal_unit_type: H266NalUnitType = ...
    """
    Indicates the type of the NAL units in the following array.
    Shall be VPS, SPS, PPS, prefix APS or suffix APS
    """
    nalu: list | None = ...
    """
    Array of identified GstH266NalUnit
    """

class H266FrameFieldInfo(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 frame field information.
    """

    # gi Fields
    bottom_field_flag: int = ...
    """
    indicates whether the current picture is a bottom field.
    """
    display_elemental_periods_minus1: int = ...
    """
    indicates the number of elemental picture
     period intervals that the current coded picture or field occupies for the
     display model.
    `source_scan_type`; indicates whether the source scan type of the associated
     picture should be interpreted as progressive.
    """
    display_fields_from_frame_flag: int = ...
    """
    indicates whether the display model operates
     by sequentially displaying the individual fields of the frame with
     alternating parity.
    """
    duplicate_flag: int = ...
    """
    indicates whether the current picture is indicated to be a
     duplicate of a previous picture in output order.
    """
    field_pic_flag: int = ...
    """
    indicates whether the display model considers the current
     picture as a field.
    """
    paired_with_next_field_flag: int = ...
    """
    indicates whether the current picture is
     considered paired with the next picture as the two fields of a frame.
    """
    pairing_indicated_flag: int = ...
    """
    indicates whether the current picture is considered
     paired with the next picture in output order or with the previous picture
     in output order as the two fields of a frame.
    """
    source_scan_type: int = ...
    top_field_first_flag: int = ...
    """
    indicates that the first field of the frame that is
     displayed by the display model is the top field.
    """
    valid: bool = ...
    """
    whether this frame field info is valid.
    """

class H266GeneralConstraintsInfo(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 general constraints info.
    """

    # gi Fields
    all_layers_independent_constraint_flag: int = ...
    """
    specifies whether the
     vps_all_independent_layers_flag for all pictures in OlsInScope
     shall be equal to 1.
    """
    all_rap_pictures_constraint_flag: int = ...
    """
    specifies whether all pictures in
     OlsInScope are GDR pictures with ph_recovery_poc_cnt equal to 0 or IRAP
     pictures.
    """
    intra_only_constraint_flag: int = ...
    """
    specifies whether sh_slice_type for all slices
     in OlsInScope shall be equal to 2.
    """
    no_act_constraint_flag: int = ...
    """
    specifies whether sps_act_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_affine_motion_constraint_flag: int = ...
    """
    specifies whether sps_affine_enabled_flag
     for all pictures in OlsInScope shall be equal to 0.
    """
    no_alf_constraint_flag: int = ...
    """
    specifies whether sps_alf_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_amvr_constraint_flag: int = ...
    """
    specifies whether sps_amvr_enabled_flag for
     all pictures in OlsInScope shall be equal to 0.
    """
    no_aps_constraint_flag: int = ...
    """
    specifies whether there shall be no NAL unit with
     nuh_unit_type equal to PREFIX_APS_NUT or SUFFIX_APS_NUT present in
     OlsInScope.
    """
    no_bcw_constraint_flag: int = ...
    """
    specifies whether sps_bcw_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_bdof_constraint_flag: int = ...
    """
    specifies whether sps_bdof_enabled_flag for
     all pictures in OlsInScope shall be equal to 0.
    """
    no_bdpcm_constraint_flag: int = ...
    """
    specifies whether sps_bdpcm_enabled_flag for
     all pictures in OlsInScope shall be equal to 0.
    """
    no_ccalf_constraint_flag: int = ...
    """
    specifies whether sps_ccalf_enabled_flag for
     all pictures in OlsInScope shall be equal to 0.
    """
    no_cclm_constraint_flag: int = ...
    """
    specifies whether sps_cclm_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_chroma_qp_offset_constraint_flag: int = ...
    """
    specifies whether
     pps_cu_chroma_qp_offset_list_enabled_flag for all pictures in OlsInScope
     shall be equal to 0.
    """
    no_ciip_constraint_flag: int = ...
    """
    specifies whether sps_ciip_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_cra_constraint_flag: int = ...
    """
    specifies whether there shall be no NAL unit with
     nuh_unit_type equal to CRA_NUT present in OlsInScope.
    """
    no_cu_qp_delta_constraint_flag: int = ...
    """
    specifies whether
     pps_cu_qp_delta_enabled_flag for all pictures in OlsInScope shall be
     equal to 0.
    """
    no_dep_quant_constraint_flag: int = ...
    """
    specifies whether sps_dep_quant_enabled_flag
     for all pictures in OlsInScope shall be equal to 0.
    """
    no_dmvr_constraint_flag: int = ...
    """
    specifies whether sps_dmvr_enabled_flag for
     all pictures in OlsInScope shall be equal to 0.
    """
    no_explicit_scaling_list_constraint_flag: int = ...
    """
    specifies whether
     sps_explicit_scaling_list_enabled_flag for all pictures in OlsInScope shall
     be equal to 0.
    """
    no_extended_precision_processing_constraint_flag: int = ...
    """
    specifies whether
     sps_extended_precision_flag for all pictures in OlsInScope shall be
     equal to 0.
    """
    no_gdr_constraint_flag: int = ...
    """
    specifies whether sps_gdr_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_gpm_constraint_flag: int = ...
    """
    specifies whether sps_gpm_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_ibc_constraint_flag: int = ...
    """
    specifies whether sps_ibc_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_idr_constraint_flag: int = ...
    """
    specifies whether there shall be no NAL unit with
     nuh_unit_type equal to IDR_W_RADL or IDR_N_LP present in OlsInScope.
    """
    no_idr_rpl_constraint_flag: int = ...
    """
    specifies whether sps_idr_rpl_present_flag for
     all pictures in OlsInScope shall be equal to 0.
    """
    no_isp_constraint_flag: int = ...
    """
    specifies whether sps_isp_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_joint_cbcr_constraint_flag: int = ...
    """
    specifies whether sps_joint_cbcr_enabled_flag
     for all pictures in OlsInScope shall be equal to 0.
    """
    no_ladf_constraint_flag: int = ...
    """
    specifies whether sps_ladf_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_lfnst_constraint_flag: int = ...
    """
    specifies whether sps_lfnst_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_lmcs_constraint_flag: int = ...
    """
    specifies whether sps_lmcs_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_luma_transform_size_64_constraint_flag: int = ...
    """
    specifies whether
     sps_max_luma_transform_size_64_flag for all pictures in OlsInScope shall
     be equal to 0.
    """
    no_mip_constraint_flag: int = ...
    """
    specifies whether sps_mip_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_mixed_nalu_types_in_pic_constraint_flag: int = ...
    """
    specifies whether the
     pps_mixed_nalu_types_in_pic_flag for all pictures in OlsInScope shall
     be equal to 0.
    """
    no_mmvd_constraint_flag: int = ...
    """
    specifies whether sps_mmvd_enabled_flag for
     all pictures in OlsInScope shall be equal to 0.
    """
    no_mrl_constraint_flag: int = ...
    """
    specifies whether sps_mrl_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_mts_constraint_flag: int = ...
    """
    specifies whether sps_mts_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_mtt_constraint_flag: int = ...
    """
    specifies whether
     sps_max_mtt_hierarchy_depth_intra_slice_luma,
     sps_max_mtt_hierarchy_depth_inter_slice, and
     sps_max_mtt_hierarchy_depth_intra_slice_chroma for all pictures in
     OlsInScope shall be equal to 0.
    """
    no_palette_constraint_flag: int = ...
    """
    specifies whether sps_palette_enabled_flag
     for all pictures in OlsInScope shall be equal to 0.
    """
    no_partition_constraints_override_constraint_flag: int = ...
    """
    specifies whether
     sps_partition_constraints_override_enabled_flag for all pictures in
     OlsInScope shall be equal to 0.
    """
    no_persistent_rice_adaptation_constraint_flag: int = ...
    """
    specifies whether
     sps_persistent_rice_adaptation_enabled_flag for all pictures in OlsInScope
     shall be equal to 0.
    """
    no_prof_constraint_flag: int = ...
    """
    specifies whether sps_affine_prof_enabled_flag for
     all pictures in OlsInScope shall be equal to 0.
    """
    no_qtbtt_dual_tree_intra_constraint_flag: int = ...
    """
    specifies whether
     sps_qtbtt_dual_tree_intra_flag for all pictures in OlsInScope shall
     be equal to 0.
    """
    no_radl_constraint_flag: int = ...
    """
    specifies whether there shall be no NAL unit with
     nuh_unit_type equal to RADL_NUT present in OlsInScope.
    """
    no_rasl_constraint_flag: int = ...
    """
    specifies whether there shall be no NAL unit with
     nuh_unit_type equal to RASL_NUT present in OlsInScope.
    """
    no_rectangular_slice_constraint_flag: int = ...
    """
    specifies whether pps_rect_slice_flag
     for all pictures in OlsInScope shall be equal to 0.
    """
    no_ref_pic_resampling_constraint_flag: int = ...
    """
    specifies whether
     sps_ref_pic_resampling_enabled_flag for all pictures in OlsInScope shall
     be equal to 0.
    """
    no_ref_wraparound_constraint_flag: int = ...
    """
    specifies whether
     sps_ref_wraparound_enabled_flag for all pictures in OlsInScope shall be
     equal to 0.
    """
    no_res_change_in_clvs_constraint_flag: int = ...
    """
    specifies whether
     sps_res_change_in_clvs_allowed_flag for all pictures in OlsInScope shall
     be equal to 0.
    """
    no_reverse_last_sig_coeff_constraint_flag: int = ...
    """
    specifies whether
     sps_reverse_last_sig_coeff_enabled_flag for all pictures in OlsInScope
     shall be equal to 0.
    """
    no_rrc_rice_extension_constraint_flag: int = ...
    """
    specifies whether
     sps_rrc_rice_extension_flag for all pictures in OlsInScope shall be
     equal to 0.
    """
    no_sao_constraint_flag: int = ...
    """
    specifies whether sps_sao_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_sbt_constraint_flag: int = ...
    """
    specifies whether sps_sbt_enabled_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    no_sbtmvp_constraint_flag: int = ...
    """
    specifies whether sps_sbtmvp_enabled_flag
     for all pictures in OlsInScope shall be equal to 0.
    """
    no_sign_data_hiding_constraint_flag: int = ...
    """
    specifies whether
     sps_sign_data_hiding_enabled_flag for all pictures in OlsInScope shall
     be equal to 0.
    """
    no_smvd_constraint_flag: int = ...
    """
    specifies whether sps_smvd_enabled_flag for
     all pictures in OlsInScope shall be equal to 0.
    """
    no_stsa_constraint_flag: int = ...
    """
    specifies whether there shall be no NAL unit with
     nuh_unit_type equal to STSA_NUT present in OlsInScope.
    """
    no_subpic_info_constraint_flag: int = ...
    """
    specifies whether sps_subpic_info_present_flag
     for all pictures in OlsInScope shall be equal to 0.
    """
    no_temporal_mvp_constraint_flag: int = ...
    """
    specifies whether
     sps_temporal_mvp_enabled_flag for all pictures in OlsInScope shall be
     equal to 0.
    """
    no_trail_constraint_flag: int = ...
    """
    specifies whether there shall be no NAL unit
     with nuh_unit_type equal to TRAIL_NUT present in OlsInScope.
    """
    no_transform_skip_constraint_flag: int = ...
    """
    specifies whether
     sps_transform_skip_enabled_flag for all pictures in OlsInScope shall
     be equal to 0.
    """
    no_ts_residual_coding_rice_constraint_flag: int = ...
    """
    specifies whether
     sps_ts_residual_coding_rice_present_in_sh_flag for all pictures in
     OlsInScope should be equal to 0.
    """
    no_virtual_boundaries_constraint_flag: int = ...
    """
    specifies whether
     sps_virtual_boundaries_enabled_flag for all pictures in OlsInScope shall
     be equal to 0.
    """
    no_weighted_prediction_constraint_flag: int = ...
    """
    specifies whether
     sps_weighted_pred_flag and sps_weighted_bipred_flag for all pictures in
     OlsInScope shall both be equal to 0.
    """
    one_au_only_constraint_flag: int = ...
    """
    specifies whether there is only one
     AU in OlsInScope.
    """
    one_slice_per_pic_constraint_flag: int = ...
    """
    specifies whether each picture in
     OlsInScope shall contain only one slice.
    """
    one_slice_per_subpic_constraint_flag: int = ...
    """
    specifies whether the value of
     pps_single_slice_per_subpic_flag for all pictures in OlsInScope shall
     be equal to 1.
    """
    one_tile_per_pic_constraint_flag: int = ...
    """
    specifies whether each picture in
     OlsInScope shall contain only one tile.
    """
    pic_header_in_slice_header_constraint_flag: int = ...
    """
    specifies whether each picture
     in OlsInScope shall contain only one slice and the value of
     sh_picture_header_in_slice_header_flag in each slice in OlsInScope shall
     be equal to 1.
    """
    present_flag: int = ...
    """
    specifies whether additional syntax elements are present.
    """
    reserved_zero_bit: bytes | None = ...
    """
    the reserved bits.
    """
    sixteen_minus_max_bitdepth_constraint_idc: int = ...
    """
    specifies whether
     sps_bitdepth_minus8 plus 8 for all pictures in OlsInScope shall be in the
     range of 0 to 16 - gci_sixteen_minus_max_bitdepth_constraint_idc, inclusive.
    """
    three_minus_max_chroma_format_constraint_idc: int = ...
    """
    specifies whether the
     sps_chroma_format_idc for all pictures in OlsInScope shall be in the range
     of 0 to 3 - gci_three_minus_max_chroma_format_constraint_idc, inclusive.
    """
    three_minus_max_log2_ctu_size_constraint_idc: int = ...
    """
    specifies whether
     sps_log2_ctu_size_minus5 for all pictures in OlsInScope shall be in the
     range of 0 to 3-gci_three_minus_max_log2_ctu_size_constraint_idc, inclusive.
    """

class H266GeneralHRDParameters(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 HDR parameters.
    """

    # gi Fields
    bit_rate_scale: int = ...
    """
    specifies the maximum input bit rate of the CPB.
    """
    cpb_size_du_scale: int = ...
    """
    specifies the CPB size of the CPB at du level.
    """
    cpb_size_scale: int = ...
    """
    specifies the CPB size of the CPB.
    """
    general_du_hrd_params_present_flag: int = ...
    """
    specifies whether DU level HRD
     parameters are present and the HRD could operate at the AU level or DU level.
    """
    general_nal_hrd_params_present_flag: int = ...
    """
    specifies whether NAL HRD parameters
     are present in the general_timing_hrd_parameters syntax structure.
    """
    general_same_pic_timing_in_all_ols_flag: int = ...
    """
    specifies whether the
     non-scalable-nested PT SEI message in each AU applies to the AU for any
     OLS in the bitstream and no scalable-nested PT SEI messages are present.
    """
    general_vcl_hrd_params_present_flag: int = ...
    """
    specifies whether VCL HRD parameters
     are present in the general_timing_hrd_parameters syntax structure.
    """
    hrd_cpb_cnt_minus1: int = ...
    """
    plus 1 specifies the number of alternative CPB
     delivery schedules.
    """
    num_units_in_tick: int = ...
    """
    the number of time units of a clock operating at the
     frequency time_scale.
    """
    tick_divisor_minus2: int = ...
    """
    specifies the clock sub-tick.
    """
    time_scale: int = ...
    """
    number of time units that pass in one second.
    """

class H266HRDParams(GObject.GPointer, metaclass=GObject.GType): ...

class H266LMCS(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 LMCS parameters.
    """

    # gi Fields
    delta_abs_crs: int = ...
    """
    specifies the absolute codeword value of the variable
     lmcsDeltaCrs.
    """
    delta_abs_cw: bytes | None = ...
    """
    specifies the absolute delta codeword value for the ith bin.
    """
    delta_cw_prec_minus1: int = ...
    """
    specifies the number of bits used for the
     representation of the syntax lmcs_delta_abs_cw[i].
    """
    delta_max_bin_idx: int = ...
    """
    specifies the delta value between 15 and the maximum bin
     index LmcsMaxBinIdx used in the luma mapping with chroma scaling construction
     process.
    """
    delta_sign_crs_flag: int = ...
    """
    specifies the sign of the variable lmcsDeltaCrs.
    """
    delta_sign_cw_flag: bytes | None = ...
    """
    specifies the sign of the variable lmcsDeltaCW[i].
    """
    min_bin_idx: int = ...
    """
    minimum bin index used in the luma mapping with chroma scaling
     construction process.
    """

class H266NalUnit(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 Nal unit headers.
    """

    # gi Fields
    data: int = ...
    """
    The data from which the Nalu has been parsed.
    """
    header_bytes: int = ...
    """
    The size of the NALU header in bytes.
    """
    layer_id: int = ...
    """
    A nal unit layer id.
    """
    offset: int = ...
    """
    The offset of the actual start of the nal unit.
    """
    sc_offset: int = ...
    """
    The offset of the start code of the nal unit.
    """
    size: int = ...
    """
    The size of the nal unit starting from `offset`.
    """
    temporal_id_plus1: int = ...
    """
    A nal unit temporal identifier.
    """
    type: int = ...
    """
    A GstH266NalUnitType.
    """
    valid: bool = ...
    """
    If the nal unit is valid, which mean it has
     already been parsed.
    """

class H266OLSHRDParameters(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 OLS HDR parameters.
    """

    # gi Fields
    elemental_duration_in_tc_minus1: list | None = ...
    """
    specifies the temporal distance between
     the elemental units that specify the HRD output times of consecutive
     pictures in output order.
    """
    fixed_pic_rate_general_flag: bytes | None = ...
    """
    indicates the temporal distance between the
     HRD output times of consecutive pictures in output order is constrained as
     specified in this clause using the variable DpbOutputElementalInterval[n].
    """
    fixed_pic_rate_within_cvs_flag: bytes | None = ...
    """
    indicates the temporal distance between the
     HRD output times of consecutive pictures in output order is constrained as
     specified in this clause using the variable DpbOutputElementalInterval[n].
    """
    low_delay_hrd_flag: bytes | None = ...
    """
    specifies the HRD operational mode as specified
     in Annex C.
    """
    nal_sub_layer_hrd_parameters: list | None = ...
    """
    sub layer nal GstH266SubLayerHRDParameters.
    """
    vcl_sub_layer_hrd_parameters: list | None = ...
    """
    sub layer vcl GstH266SubLayerHRDParameters.
    """

class H266OPI(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 operating point information.
    """

    # gi Fields
    extension_data_flag: int = ...
    """
    could have any value.
    """
    extension_flag: int = ...
    """
    specifies whether opi_extension_data_flag syntax elements
     are present.
    """
    htid_info_present_flag: int = ...
    """
    specifies whether opi_htid_plus1 is present.
    """
    htid_plus1: int = ...
    """
    specifies that all the pictures in the current CVS and the next
     CVSs in decoding order up to and not including the next CVS for which
     opi_htid_plus1 is provided in an OPI NAL unit are IRAP pictures or GDR
     pictures with ph_recovery_poc_cnt equal to 0.
    """
    ols_idx: int = ...
    """
    specifies that the current CVS and the next CVSs in decoding order
     up to and not including the next CVS for which opi_ols_idx is provided in
     an OPI NAL unit do not contain any other layers than those included in the
     OLS with OLS index equal to opi_ols_idx.
    """
    ols_info_present_flag: int = ...
    """
    specifies whether opi_ols_idx is present in the OPI.
    """

class H266PPS(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 PPS.
    """

    # gi Fields
    alf_info_in_ph_flag: int = ...
    """
    specifies whether ALF information could be present in
     the PH syntax structure and not present in slice headers.
    """
    cabac_init_present_flag: int = ...
    """
    specifies whether sh_cabac_init_flag is present
     in slice headers.
    """
    cb_beta_offset_div2: int = ...
    """
    specify the default deblocking parameter offsets for
     beta and that are applied to the Cb component.
    """
    cb_qp_offset: int = ...
    """
    specify the offsets to the luma quantization parameter Qp'Y
     used for deriving Qp'Cb.
    """
    cb_qp_offset_list: bytes | None = ...
    """
    specify offsets used in the derivation of Qp'Cb.
    """
    cb_tc_offset_div2: int = ...
    """
    specify the default deblocking parameter offsets for tC
        (divided by 2) that are applied to the Cb component.
    """
    chroma_qp_offset_list_len_minus1: int = ...
    """
    specifies whether number of
     pps_cb_qp_offset_list[i], pps_cr_qp_offset_list[i],
     and pps_joint_cbcr_qp_offset_list[i], syntax elements that are present.
    """
    chroma_tool_offsets_present_flag: int = ...
    """
    specifies whether chroma tool offsets
     related syntax elements are present in the PPS.
    """
    conf_win_bottom_offset: int = ...
    """
    specify bottom offset of the cropping window that
     is applied to pictures.
    """
    conf_win_left_offset: int = ...
    """
    specify left offset of the cropping window that
     is applied to pictures.
    """
    conf_win_right_offset: int = ...
    """
    specify right offset of the cropping window that
     is applied to pictures.
    """
    conf_win_top_offset: int = ...
    """
    specify top offset of the cropping window that
     is applied to pictures.
    """
    conformance_window_flag: int = ...
    """
    specifies whether the conformance cropping window
     offset parameters follow next in the PPS.
    """
    cr_beta_offset_div2: int = ...
    """
    specify the default deblocking parameter offsets for
     beta that are applied to the Cr component.
    """
    cr_qp_offset: int = ...
    """
    specify the offsets to the luma quantization parameter Qp'Y
     used for deriving Qp'Cr.
    """
    cr_qp_offset_list: bytes | None = ...
    """
    specify offsets used in the derivation of Qp'Cr.
    """
    cr_tc_offset_div2: int = ...
    """
    specify the default deblocking parameter offsets for tC
     (divided by 2) that are applied to the Cr component.
    """
    crop_rect_height: int = ...
    """
    the cropped height of the picture.
    """
    crop_rect_width: int = ...
    """
    the cropped width of the picture.
    """
    crop_rect_x: int = ...
    """
    the x offset of the cropped window.
    """
    crop_rect_y: int = ...
    """
    the y offset of the cropped window.
    """
    cu_chroma_qp_offset_list_enabled_flag: int = ...
    """
    specifies whether the
     ph_cu_chroma_qp_offset_subdiv_intra_slice and
     ph_cu_chroma_qp_offset_subdiv_inter_slice syntax elements are present in PH.
    """
    cu_qp_delta_enabled_flag: int = ...
    """
    specifies whether either or both of the
     ph_cu_qp_delta_subdiv_intra_slice and ph_cu_qp_delta_subdiv_inter_slice
     syntax elements are present in PH.
    """
    dbf_info_in_ph_flag: int = ...
    """
    specifies whether deblocking filter information is
     present in the PH syntax structure and not present in slice headers.
    """
    deblocking_filter_control_present_flag: int = ...
    """
    specifies whether presence of
     deblocking filter control syntax elements in the PPS.
    """
    deblocking_filter_disabled_flag: int = ...
    """
    specifies whether the deblocking filter is
     disabled unless overridden for a picture or slice by information.
    """
    deblocking_filter_override_enabled_flag: int = ...
    """
    specifies whether the deblocking
     behaviour could be overridden in the picture level or slice level.
    """
    exp_slice_height_in_ctus_minus1: list | None = ...
    """
    specifies the height of the j-th rectangular
     slice in the tile containing the i-th slice.
    """
    extension_data_flag: int = ...
    """
    could have any value.
    """
    extension_flag: int = ...
    """
    specifies whether pps_extension_data_flag syntax elements
     are present.
    """
    height: int = ...
    """
    the calculated height of the picture.
    """
    init_qp_minus26: int = ...
    """
    specifies the initial value of SliceQp Y for each slice.
    """
    joint_cbcr_qp_offset_list: bytes | None = ...
    """
    specify offsets used in the derivation of Qp'CbCr.
    """
    joint_cbcr_qp_offset_present_flag: int = ...
    """
    specifies whether
     pps_joint_cbcr_qp_offset_value and pps_joint_cbcr_qp_offset_list[i] are
     present in the PPS.
    """
    joint_cbcr_qp_offset_value: int = ...
    """
    specifies the offset to the luma quantization
     parameter Qp'Y used for deriving Qp'CbCr.
    """
    log2_ctu_size_minus5: int = ...
    """
    specifies the luma coding tree block size of each CTU.
    """
    loop_filter_across_slices_enabled_flag: int = ...
    """
    specifies whether in-loop filtering
     operations across slice boundaries are enabled.
    """
    loop_filter_across_tiles_enabled_flag: int = ...
    """
    specifies whether in-loop filtering
     operations across tile boundaries are enabled for pictures.
    """
    luma_beta_offset_div2: int = ...
    """
    specify the default deblocking parameter offsets
     for beta that are applied to the luma component.
    """
    luma_tc_offset_div2: int = ...
    """
    specify the default deblocking parameter offsets for
     tC (divided by 2) that are applied to the luma component.
    """
    mixed_nalu_types_in_pic_flag: int = ...
    """
    specifies whether each picture referring to
     the PPS has more than one VCL NAL unit and the VCL NAL units do not have
     the same value of nal_unit_type.
    """
    no_pic_partition_flag: int = ...
    """
    specifies whether no picture partitioning is applied
     to each picture referring to the PPS.
    """
    num_exp_slices_in_tile: list | None = ...
    """
    specifies the number of explicitly provided slice
     heights for the slices in the containing the i-th slice.
    """
    num_exp_tile_columns_minus1: int = ...
    """
    specifies the number of explicitly provided
     tile column widths.
    """
    num_exp_tile_rows_minus1: int = ...
    """
    specifies the number of explicitly provided tile
     row heights.
    """
    num_ref_idx_default_active_minus1: bytes | None = ...
    """
    specifies the inferred value of the
     variable NumRefIdxActive[0] for P or B slices.
    """
    num_slices_in_pic_minus1: int = ...
    """
    specifies the number of rectangular slices in
     each picture.
    """
    num_slices_in_subpic: list | None = ...
    """
    slice number in subpicture.
    """
    num_subpics_minus1: int = ...
    """
    shall be equal to sps_num_subpics_minus1.
    """
    num_tile_columns: int = ...
    """
    total tile number in columns.
    """
    num_tile_rows: int = ...
    """
    total tile number in rows.
    """
    num_tiles_in_pic: int = ...
    """
    total tile number of the picture.
    """
    output_flag_present_flag: int = ...
    """
    specifies whether the ph_pic_output_flag syntax
     element could be present in PH syntax structures referring to the PPS.
    """
    pic_height_in_ctbs_y: int = ...
    """
    PicHeightInCtbsY specify picture height count in CTBs.
    """
    pic_height_in_luma_samples: int = ...
    """
    specifies the height of each decoded picture
     referring to the PPS in units of luma samples.
    """
    pic_size_in_ctbs_y: int = ...
    """
    picture size count in CTBs.
    """
    pic_width_in_ctbs_y: int = ...
    """
    PicWidthInCtbsY specify picture width count in CTBs.
    """
    pic_width_in_luma_samples: int = ...
    """
    specifies the width of each decoded picture
     referring to the PPS in units of luma samples.
    """
    pic_width_minus_wraparound_offset: int = ...
    """
    specifies the difference between the
     picture width and the offset used for computing the horizontal wrap-around
     position in units of MinCbSizeY luma samples.
    """
    picture_header_extension_present_flag: int = ...
    """
    specifies whether PH extension syntax
     elements are present in PH syntax structures.
    """
    pps_id: int = ...
    """
    provides an identifier for the PPS for reference by other
     syntax elements.
    """
    qp_delta_info_in_ph_flag: int = ...
    """
    specifies whether QP delta information is present
     in the PH syntax structure and not present in slice headers.
    """
    rect_slice_flag: int = ...
    """
    specifies whether the raster-san slice mode is in use for
     each picture referring to the PPS.
    """
    ref_wraparound_enabled_flag: int = ...
    """
    specifies whether the horizontal wrap-around
     motion compensation is enabled.
    """
    rpl1_idx_present_flag: int = ...
    """
    specifies whether rpl_sps_flag[1] and rpl_idx[1] are
     present in the PH.
    """
    rpl_info_in_ph_flag: int = ...
    """
    specifies whether RPL information is present in the
     PH syntax structure and not present in slice headers.
    """
    sao_info_in_ph_flag: int = ...
    """
    specifies whether SAO filter information could be
     present in the PH syntax structure and not present in slice headers.
    """
    scaling_win_bottom_offset: int = ...
    """
    specify the bottom offsets that are applied to
     the picture size for scaling ratio calculation.
    """
    scaling_win_left_offset: int = ...
    """
    specify the left offsets that are applied to the
     picture size for scaling ratio calculation.
    """
    scaling_win_right_offset: int = ...
    """
    specify the right offsets that are applied to the
     picture size for scaling ratio calculation.
    """
    scaling_win_top_offset: int = ...
    """
    specify the top offsets that are applied to the
     picture size for scaling ratio calculation.
    """
    scaling_window_explicit_signalling_flag: int = ...
    """
    specifies whether the scaling
     window offset parameters are present in the PPS.
    """
    single_slice_per_subpic_flag: int = ...
    """
    specifies whether each subpicture consists of
     one and only one rectangular slice.
    """
    slice_chroma_qp_offsets_present_flag: int = ...
    """
    specifies whether the sh_cb_qp_offset
     and sh_cr_qp_offset syntax elements are present in slice headers.
    """
    slice_header_extension_present_flag: int = ...
    """
    specifies whether slice header
     extension syntax elements are present in the slice headers.
    """
    slice_height_in_ctus: list | None = ...
    """
    slice height count in CTUs.
    """
    slice_height_in_tiles_minus1: list | None = ...
    """
    specifies the height of the i-th rectangular
     slice in units of tile rows.
    """
    slice_top_left_ctu_x: list | None = ...
    """
    specifying the top left CTU index in X direction.
    """
    slice_top_left_ctu_y: list | None = ...
    """
    specifying the top left CTU index in Y direction.
    """
    slice_top_left_tile_idx: list | None = ...
    """
    SliceTopLeftTileIdx specifying the tile index of
     the tile containing the first CTU in the slice.
    """
    slice_width_in_tiles_minus1: list | None = ...
    """
    specifies the width of the i-th rectangular
     slice in units of tile columns.
    """
    sps: H266SPS | None = ...
    """
    the GstH266SPS this PPS refers to.
    """
    sps_id: int = ...
    """
    specifies the SPS referred to by this PPS.
    """
    subpic_id: list | None = ...
    """
    specifies the subpicture ID of the i-th subpicture.
    """
    subpic_id_len_minus1: int = ...
    """
    shall be equal to sps_subpic_id_len_minus1.
    """
    subpic_id_mapping_present_flag: int = ...
    """
    specifies whether the subpicture ID mapping
     is signalled in the PPS.
    """
    tile_col_bd_val: list | None = ...
    """
    TileColBdVal specifying the location of the i-th tile
     column boundary in units of CTBs
    """
    tile_column_width_minus1: list | None = ...
    """
    specifies the width of the i-th tile column in
     units of CTBs.
    """
    tile_idx_delta_present_flag: int = ...
    """
    specifies whether all pictures are partitioned
     into rectangular slice rows and rectangular slice columns in slice raster
     order.
    """
    tile_idx_delta_val: list | None = ...
    """
    specifies the difference between the tile index of the
     tile containing the first CTU in the (i+1)-th rectangular slice and the tile
     index of the tile containing the first CTU in the i-th rectangular slice.
    """
    tile_row_bd_val: list | None = ...
    """
    TileRowBdVal specifying the location of the j-th tile row
     boundary in units of CTBs.
    """
    tile_row_height_minus1: list | None = ...
    """
    specifies the height of the i-th tile row in units
     of CTBs.
    """
    valid: bool = ...
    """
    whether this PPS is valid.
    """
    weighted_bipred_flag: int = ...
    """
    specifies whether explicit weighted prediction is
     applied to B slices.
    """
    weighted_pred_flag: int = ...
    """
    specifies whether weighted prediction is applied to P
     slices.
    """
    width: int = ...
    """
    the calculated width of the picture.
    """
    wp_info_in_ph_flag: int = ...
    """
    specifies whether weighted prediction information could
     be present in the PH syntax structure and not present in slice headers.
    """

class H266PTLRecord(GObject.GPointer, metaclass=GObject.GType):
    """
    Contains VvcPTLRecord data as defined in ISO/IEC 14496-15
    """

    # gi Fields
    general_constraint_info: bytes | None = ...
    """
    Array containing general constraint information.
    """
    general_level_idc: int = ...
    """
    General level id.
    """
    general_profile_idc: int = ...
    """
    General profile id.
    """
    general_sub_profile_idc: list | None = ...
    """
    Array containing general sub-profile ids.
    """
    general_tier_flag: int = ...
    """
    General tier flag.
    """
    num_bytes_constraint_info: int = ...
    """
    Number of bytes for constraint information.
    """
    ptl_frame_only_constraint_flag: int = ...
    """
    Frame-only constraint flag.
    """
    ptl_multilayer_enabled_flag: int = ...
    """
    Multilayer enabled flag.
    """
    ptl_num_sub_profiles: int = ...
    """
    Number of sub-profiles.
    """
    ptl_sublayer_level_present_flag: bytes | None = ...
    """
    Array indicating presence of sublayer level.
    """
    sublayer_level_idc: bytes | None = ...
    """
    Array containing sublayer level ids.
    """

class H266Parser(GObject.GPointer, metaclass=GObject.GType):
    """
    To identify Nals in a bitstream and parse its headers, you should call:

      * `gst_h266_parser_identify_nalu` to identify the following nalu in
           VVC bitstreams

    Then, depending on the GstH266NalUnitType of the newly parsed GstH266NalUnit,
    you should call the different functions to parse the structure:

      * From GST_H266_NAL_SLICE_TRAIL to GST_H266_NAL_SLICE_GDR:
        `gst_h266_parser_parse_slice_hdr`

      * `GST_H266_NAL_*_SEI`: `gst_h266_parser_parse_sei`

      * GST_H266_NAL_VPS: `gst_h266_parser_parse_vps`

      * GST_H266_NAL_SPS: `gst_h266_parser_parse_sps`

      * GST_H266_NAL_PPS: #`gst_h266_parser_parse_pps`

      * Any other: `gst_h266_parser_parse_nal`

    Note: You should always call `gst_h266_parser_parse_nal` if you don't
    actually need GstH266NalUnitType to be parsed for your personal use, in
    order to guarantee that the GstH266Parser is always up to date.

    For more details about the structures, look at the ITU-T H.266
    specifications, you can download them from:

      * ITU-T H.266: http://www.itu.int/rec/T-REC-H.266
    """

    # gi Fields
    @builtins.property
    def active_pps(self) -> H266PPS | None: ...
    @builtins.property
    def active_sps(self) -> H266SPS | None: ...
    @builtins.property
    def active_vps(self) -> H266VPS | None: ...
    @builtins.property
    def aps(self) -> list | None: ...
    @builtins.property
    def buffering_period(self) -> H266SEIMessage | None: ...
    @builtins.property
    def ctb_addr_in_slice(self) -> list | None: ...
    @builtins.property
    def ctb_to_tile_col_bd(self) -> list | None: ...
    @builtins.property
    def ctb_to_tile_row_bd(self) -> list | None: ...
    @builtins.property
    def last_aps(self) -> list | None: ...
    @builtins.property
    def last_buffering_period(self) -> H266SEIMessage | None: ...
    @builtins.property
    def last_pps(self) -> H266PPS | None: ...
    @builtins.property
    def last_sps(self) -> H266SPS | None: ...
    @builtins.property
    def last_vps(self) -> H266VPS | None: ...
    @builtins.property
    def num_ctus_in_slice(self) -> list | None: ...
    @builtins.property
    def ph(self) -> H266PicHdr | None: ...
    @builtins.property
    def pps(self) -> list | None: ...
    @builtins.property
    def slice_start_offset(self) -> list | None: ...
    @builtins.property
    def sps(self) -> list | None: ...
    @builtins.property
    def vps(self) -> list | None: ...

    # gi Methods
    def free(self) -> None:
        """
        Frees the `parser`
        """
    def identify_and_split_nalu_vvc(
        self, data: int, offset: int, size: int, nal_length_size: int, nalus: list[H266NalUnit], consumed: int
    ) -> H266ParserResult:
        """
            Parses `data` for packetized (e.g., vvc1/vvi1) bitstream and
        sets `nalus`. In addition to nal identifying process,
        this method scans start-code prefix to split malformed packet into
        actual nal chunks.
        """
    def identify_nalu(self, data: int, offset: int, size: int, nalu: H266NalUnit) -> H266ParserResult:
        """
        Parses `data` and fills `nalu` from the next nalu data from `data`
        """
    def identify_nalu_unchecked(self, data: int, offset: int, size: int, nalu: H266NalUnit) -> H266ParserResult:
        """
            Parses `data` and fills `nalu` from the next nalu data from `data`.

        This differs from `gst_h266_parser_identify_nalu` in that it doesn't
        check whether the packet is complete or not.

        Note: Only use this function if you already know the provided `data`
        is a complete NALU, else use `gst_h266_parser_identify_nalu`.
        """
    def identify_nalu_vvc(
        self, data: int, offset: int, size: int, nal_length_size: int, nalu: H266NalUnit
    ) -> H266ParserResult:
        """
        Parses `data` and sets `nalu`.
        """
    def parse_aps(self, nalu: H266NalUnit, aps: H266APS) -> H266ParserResult:
        """
        Parses `data`, and fills the `aps` structure.
        """
    def parse_aud(self, nalu: H266NalUnit, aud: H266AUD) -> H266ParserResult:
        """
        Parses `data`, and fills the `aud` structure.
        """
    def parse_dci(self, nalu: H266NalUnit, dci: H266DCI) -> H266ParserResult:
        """
        Parses `data`, and fills the `dci` structure.
        """
    def parse_decoder_config_record(self, data: int, size: int) -> tuple[H266ParserResult, H266DecoderConfigRecord]:
        """
            Parses VVCDecoderConfigurationRecord data and fill into `config`.
        The caller must free `config` via `gst_h266_decoder_config_record_free`

        This method does not parse APS, VPS, SPS and PPS and therefore the caller needs to
        parse each NAL unit via appropriate parsing method.
        """
    def parse_nal(self, nalu: H266NalUnit) -> H266ParserResult:
        """
            This function should be called in the case one doesn't need to
        parse a specific structure. It is necessary to do so to make
        sure `parser` is up to date.
        """
    def parse_opi(self, nalu: H266NalUnit, opi: H266OPI) -> H266ParserResult:
        """
        Parses `data`, and fills the `opi` structure.
        """
    def parse_picture_hdr(self, nalu: H266NalUnit, picture: H266PicHdr) -> H266ParserResult:
        """
        Parses `data`, and fills the `ph` structure.
        """
    def parse_pps(self, nalu: H266NalUnit, pps: H266PPS) -> H266ParserResult:
        """
        Parses `data`, and fills the `pps` structure.
        """
    def parse_sei(self, nalu: H266NalUnit, messages: list[H266SEIMessage]) -> H266ParserResult:
        """
        Parses `data`, create and fills the `messages` array.
        """
    def parse_slice_hdr(self, nalu: H266NalUnit, slice: H266SliceHdr) -> H266ParserResult:
        """
        Parses `data`, and fills the `sh` structure.
        """
    def parse_sps(self, nalu: H266NalUnit, sps: H266SPS) -> H266ParserResult:
        """
        Parses `data`, and fills the `sps` structure.
        """
    def parse_vps(self, nalu: H266NalUnit, vps: H266VPS) -> H266ParserResult:
        """
        Parses `data`, and fills the `vps` structure.
        """

class H266PicHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 picture header.
    """

    # gi Fields
    alf_aps_id_chroma: int = ...
    alf_aps_id_luma: bytes | None = ...
    """
    specifies the aps_adaptation_parameter_set_id of the i-th
     ALF APS.
    """
    alf_cb_enabled_flag: int = ...
    """
    specifies whether the adaptive loop filter is enabled
     for the Cb colour component of the current picture.
    """
    alf_cc_cb_aps_id: int = ...
    """
    specifies the aps_adaptation_parameter_set_id of the ALF APS
     that the Cb colour component of the slices in the current picture refers to.
    """
    alf_cc_cb_enabled_flag: int = ...
    """
    specifies whether the cross-component adaptive loop
     filter for the Cb colour component is enabled for the current picture
    """
    alf_cc_cr_aps_id: int = ...
    """
    specifies the aps_adaptation_parameter_set_id of the ALF APS
     that the Cr colour component of the slices in the current picture refers to.
    """
    alf_cc_cr_enabled_flag: int = ...
    """
    specifies that the cross-compoent adaptive loop
     filter for the Cr colour component is enabled for the current picture.
    """
    alf_cr_enabled_flag: int = ...
    """
    specifies whether the adaptive loop filter is enabled
     for the Cr colour component of the current picture.
    `alf_aps_id_chroma`;: specifies the aps_adaptation_parameter_set_id of the
     ALF APS.
    """
    alf_enabled_flag: int = ...
    """
    specifies whether the adaptive loop filter is enabled for
     the current picture.
    """
    bdof_disabled_flag: int = ...
    """
    specifies that the bi-directional optical flow inter
     prediction based inter bi-prediction is disabled for the current picture.
    """
    cb_beta_offset_div2: int = ...
    """
    specify the deblocking parameter offsets for beta that
     are applied to the Cb component for the slices in the current picture.
    """
    cb_tc_offset_div2: int = ...
    """
    specify the deblocking parameter offsets for tC that
     are applied to the Cb component for the slices in the current picture.
    """
    chroma_residual_scale_flag: int = ...
    """
    specifies whether chroma residual scaling is
     enabled and could be used for the current picture.
    """
    collocated_from_l0_flag: int = ...
    """
    specifies whether the collocated picture used for
     temporal motion vector prediction is derived from RPL 0.
    """
    collocated_ref_idx: int = ...
    """
    specifies the reference index of the collocated picture
     used for temporal motion vector prediction.
    """
    cr_beta_offset_div2: int = ...
    """
    specify the deblocking parameter offsets for beta that
     are applied to the Cr component for the slices in the current picture.
    """
    cr_tc_offset_div2: int = ...
    """
    specify the deblocking parameter offsets for tC that
     are applied to the Cr component for the slices in the current picture.
    """
    cu_chroma_qp_offset_subdiv_inter_slice: int = ...
    """
    specifies the maximum cbSubdiv value
     of coding units in inter slice that convey cu_chroma_qp_offset_flag.
    """
    cu_chroma_qp_offset_subdiv_intra_slice: int = ...
    """
    specifies the maximum cbSubdiv value
     of coding units in intra slice that convey cu_chroma_qp_offset_flag.
    """
    cu_qp_delta_subdiv_inter_slice: int = ...
    """
    specifies the maximum cbSubdiv value of
     coding units that in inter slice convey cu_qp_delta_abs and
     cu_qp_delta_sign_flag.
    """
    cu_qp_delta_subdiv_intra_slice: int = ...
    """
    specifies the maximum cbSubdiv value of
     coding units in intra slice that convey cu_qp_delta_abs and
     cu_qp_delta_sign_flag.
    """
    deblocking_filter_disabled_flag: int = ...
    """
    specifies whether the deblocking filter is
     disabled for the current picture.
    """
    deblocking_params_present_flag: int = ...
    """
    specifies whether the deblocking parameters
     could be present in the PH syntax structure.
    """
    dmvr_disabled_flag: int = ...
    """
    specifies whether the decoder motion vector refinement
     based inter bi-prediction is disabled for the current picture.
    """
    explicit_scaling_list_enabled_flag: int = ...
    """
    specifies that the explicit scaling list
     is enabled for the current picture.
    """
    extension_data_byte: bytes | None = ...
    """
    could have any value.
    """
    extension_length: int = ...
    """
    specifies the length of the PH extension data in bytes.
    """
    extra_bit: bytes | None = ...
    gdr_or_irap_pic_flag: int = ...
    """
    specifies whethers the current picture is a GDR or
     IRAP picture.
    """
    gdr_pic_flag: int = ...
    """
    specifies whether the current picture is a GDR picture.
    """
    inter_slice_allowed_flag: int = ...
    """
    specifies whether all coded slices of the picture
     have sh_slice_type equal to 2.
    """
    intra_slice_allowed_flag: int = ...
    """
    specifies whether all coded slices of the picture
     have sh_slice_type equal to 0 or 1.
    """
    joint_cbcr_sign_flag: int = ...
    """
    specifies whether the collocated residual samples of
     both chroma components have inverted signs.
    """
    lmcs_aps_id: int = ...
    """
    specifies the aps_adaptation_parameter_set_id of the LMCS APS.
    """
    lmcs_enabled_flag: int = ...
    """
    specifies whether LMCS is enabled for the current picture.
    """
    log2_diff_max_bt_min_qt_inter_slice: int = ...
    """
    specifies the difference between the
     base 2 logarithm of the maximum size in luma samples of a luma coding block
     that can be split using a binary split and the base 2 logarithm of the
     minimum size in luma samples of a luma leaf block resulting from quadtree
     splitting of a CTU.
    """
    log2_diff_max_bt_min_qt_intra_slice_chroma: int = ...
    """
    specifies the difference between
     the base 2 logarithm of the maximum size in luma samples of a chroma coding
     block that can be split using a binary split and the base 2 logarithm of the
     minimum size in luma samples of a chroma leaf block resulting from quadtree
     splitting of a chroma CTU with treeType equal to DUAL_TREE_CHROMA.
    """
    log2_diff_max_bt_min_qt_intra_slice_luma: int = ...
    """
    specifies the difference between
     the base 2 logarithm of the maximum size in luma samples of a luma coding
     block that can be split using a binary split and the base 2 logarithm of
     the minimum size in luma samples of a luma leaf block resulting from
     quadtree splitting of a CTU.
    """
    log2_diff_max_tt_min_qt_inter_slice: int = ...
    """
    specifies the difference between the
     base 2 logarithm of the maximum size in luma samples of a luma coding block
     that can be split using a ternary split and the base 2 logarithm of the
     minimum size in luma samples of a luma leaf block resulting from quadtree
     splitting of a CTU.
    """
    log2_diff_max_tt_min_qt_intra_slice_chroma: int = ...
    """
    specifies the difference between
     the base 2 logarithm of the maximum size in luma samples of a chroma coding
     block that can be split using a ternary split and the base 2 logarithm of
     the minimum size in luma samples of a chroma leaf block resulting from
     quadtree splitting of a chroma CTU with treeType equal to DUAL_TREE_CHROMA.
    """
    log2_diff_max_tt_min_qt_intra_slice_luma: int = ...
    """
    specifies the difference between
     the base 2 logarithm of the maximum size in luma samples of a luma coding
     block that can be split using a ternary split and the base 2 logarithm of
     the minimum size in luma samples of a luma leaf block resulting from
     quadtree splitting of a CTU.
    """
    log2_diff_min_qt_min_cb_inter_slice: int = ...
    """
    specifies the difference between the
     base 2 logarithm of the minimum size in luma samples of a luma leaf block
     resulting from quadtree splitting of a CTU and the base 2 logarithm of the
     minimum luma coding block size in luma samples for luma CUs.
    """
    log2_diff_min_qt_min_cb_intra_slice_chroma: int = ...
    """
    specifies the difference between
     the base 2 logarithm of the minimum size in luma samples of a chroma leaf
     block resulting from quadtree splitting of a chroma CTU with treeType equal
     to DUAL_TREE_CHROMA and the base 2 logarithm of the minimum coding block
     size in luma samples for chroma CUs with treeType equal to DUAL_TREE_CHROMA.
    """
    log2_diff_min_qt_min_cb_intra_slice_luma: int = ...
    """
    specifies the difference between
     the base 2 logarithm of the minimum size in luma samples of a luma leaf
     block resulting from quadtree splitting of a CTU and the base 2 logarithm
     of the minimum coding block size in luma samples for luma CUs.
    """
    luma_beta_offset_div2: int = ...
    """
    specify the deblocking parameter offsets for beta that
     are applied to the luma component for the slices in the current picture.
    """
    luma_tc_offset_div2: int = ...
    """
    specify the deblocking parameter offsets for tC that
     are applied to the luma component for the slices in the current picture.
    """
    max_mtt_hierarchy_depth_inter_slice: int = ...
    """
    specifies the maximum hierarchy depth
     for coding units resulting from multi-type tree splitting of a quadtree leaf.
    """
    max_mtt_hierarchy_depth_intra_slice_chroma: int = ...
    """
    specifies the maximum hierarchy
     depth for chroma coding units resulting from multi-type tree splitting of a
     chroma quadtree leaf with treeType equal to DUAL_TREE_CHROMA.
    """
    max_mtt_hierarchy_depth_intra_slice_luma: int = ...
    """
    specifies the maximum hierarchy
     depth for coding units resulting from multi-type tree splitting of a
     quadtree leaf.
    """
    mmvd_fullpel_only_flag: int = ...
    """
    specifies whether the merge mode with motion vector
     difference uses only integer sample precision for the current picture.
    """
    mvd_l1_zero_flag: int = ...
    """
    specifies whether the mvd_coding syntax structure is parsed.
    """
    non_ref_pic_flag: int = ...
    """
    specifies whether the current picture is never used as
     a reference picture.
    """
    num_alf_aps_ids_luma: int = ...
    """
    specifies the number of ALF APSs that the slices in
     the current picture refers to.
    """
    num_hor_virtual_boundaries: int = ...
    """
    specifies the number of
     ph_virtual_boundary_pos_y_minus1[i].
    """
    num_ver_virtual_boundaries: int = ...
    """
    specifies the number of
     ph_virtual_boundary_pos_x_minus1[i] syntax elements.
    """
    partition_constraints_override_flag: int = ...
    """
    specifies that partition constraint
     parameters are present.
    """
    pic_order_cnt_lsb: int = ...
    """
    specifies the picture order count modulo
     MaxPicOrderCntLsb for the current picture.
    """
    pic_output_flag: int = ...
    """
    affects the decoded picture output and removal processes
     as specified in Annex C.
    """
    poc_msb_cycle_present_flag: int = ...
    """
    specifies that the syntax element
     ph_poc_msb_cycle_val is present in the PH.
    """
    poc_msb_cycle_val: int = ...
    """
    specifies the value of the POC MSB cycle of the current
     picture.
    """
    pps: H266PPS | None = ...
    pps_id: int = ...
    """
    specifies the value of pps_pic_parameter_set_id for the PPS in use.
    """
    pred_weight_table: H266PredWeightTable | None = ...
    """
    prediction weight table of GstH266PredWeightTable.
    """
    prof_disabled_flag: int = ...
    """
    specifies whether prediction refinement with optical
     flow is disabled for the current picture.
    """
    qp_delta: int = ...
    """
    specifies the initial value of QpY to be used for the coding
     blocks in the picture.
    """
    recovery_poc_cnt: int = ...
    """
    specifies the recovery point of decoded pictures in
     output order.
    `extra_bit`[16]: could have any value.
    """
    ref_pic_lists: H266RefPicLists | None = ...
    """
    reference lists of GstH266RefPicLists.
    """
    sao_chroma_enabled_flag: int = ...
    """
    specifies whether SAO is enabled for the chroma
     component of the current picture.
    """
    sao_luma_enabled_flag: int = ...
    """
    specifies whether SAO is enabled for the luma
     component of the current picture.
    """
    scaling_list_aps_id: int = ...
    """
    specifies the aps_adaptation_parameter_set_id of the
     scaling list APS.
    """
    temporal_mvp_enabled_flag: int = ...
    """
    specifies whether temporal motion vector
     predictor is enabled for the current picture.
    """
    valid: bool = ...
    """
    whether this picture header is valid.
    """
    virtual_boundaries_present_flag: int = ...
    """
    specifies whether information of virtual
     boundaries is signalled in the PH syntax structure.
    """
    virtual_boundary_pos_x_minus1: list | None = ...
    """
    specifies the location of the i-th vertical
     virtual boundary in units of luma samples divided by 8.
    """
    virtual_boundary_pos_y_minus1: list | None = ...
    """
    specifies the location of the i-th horizontal
     virtual boundary in units of luma samples divided by 8.
    """

class H266PicTiming(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 picture timing.
    """

    # gi Fields
    cpb_alt_timing_info_present_flag: int = ...
    """
    specifies whether the cpb alt syntax
     elements could be present in the PT SEI message.
    """
    cpb_removal_delay_delta_enabled_flag: bytes | None = ...
    """
    specifies whether
     pt_cpb_removal_delay_delta_idx[i] is present in the PT SEI message.
    """
    cpb_removal_delay_delta_idx: bytes | None = ...
    """
    specifies the index of the CPB removal delta.
    """
    cpb_removal_delay_minus: int = ...
    """
    calculate the number of clock ticks between the
     nominal CPB removal times of the AU associated with the PT SEI message.
    """
    cpb_removal_delay_minus1: bytes | None = ...
    """
    is used to calculate the number of clock ticks
     between the nominal CPB removal times of the AU.
    """
    delay_for_concatenation_ensured_flag: int = ...
    """
    specifies that the difference between
     the final arrival time and the CPB removal time of the AU is present.
    """
    display_elemental_periods_minus1: int = ...
    """
    indicates the number of elemental picture
     period intervals.
    """
    dpb_output_delay: int = ...
    """
    is used to compute the DPB output time of the AU.
    """
    dpb_output_du_delay: int = ...
    """
    is used to compute the DPB output time of the AU when
     DecodingUnitHrdFlag is equal to 1.
    """
    du_common_cpb_removal_delay_flag: int = ...
    """
    specifies whether the syntax elements
     pt_du_common_cpb_removal_delay_increment_minus1[i] are present.
    """
    du_common_cpb_removal_delay_increment_minus1: bytes | None = ...
    """
    specifies the duration between
     the nominal CPB removal times of any two consecutive DUs in decoding order
     in the AU.
    """
    du_cpb_removal_delay_increment_minus1: bytes | None = ...
    """
    specifies the duration between the
     nominal CPB removal times of the (i + 1)-th DU and the i-th DU.
    """
    nal_cpb_alt_initial_removal_delay_delta: bytes | None = ...
    """
    specifies whether alternative
     initial CPB removal delay delta for the i-th sublayer for the j-th CPB for
     the NAL HRD in units of a 90 kHz clock.
    """
    nal_cpb_alt_initial_removal_offset_delta: bytes | None = ...
    """
    specifies whether alternative
     initial CPB removal offset delta for the i-th sublayer for the j-th CPB for
     the NAL HRD in units of a 90 kHz clock.
    """
    nal_cpb_delay_offset: bytes | None = ...
    """
    specifies an offset to be used in the derivation of
     the nominal CPB removal times of the AU.
    """
    nal_dpb_delay_offset: bytes | None = ...
    """
    specifies an offset to be used in the derivation of
     the DPB output times of the IRAP AU.
    """
    num_decoding_units_minus1: int = ...
    """
    specifies the number of DUs in the AU the PT SEI
     message is associated with.
    """
    num_nalus_in_du_minus1: list | None = ...
    """
    specifies the number of NAL units in the i-th DU of
     the AU the PT SEI message is associated with.
    """
    sublayer_delays_present_flag: bytes | None = ...
    """
    specifies whether cpb removal values are
     present for the sublayer with TemporalId equal to i.
    """
    vcl_cpb_alt_initial_removal_delay_delta: bytes | None = ...
    """
    specifies the alternative initial
     CPB removal delay delta for the i-th sublayer for the j-th CPB for the VCL
     HRD in units of a 90 kHz clock.
    """
    vcl_cpb_alt_initial_removal_offset_delta: bytes | None = ...
    """
    specifies the alternative initial
     CPB removal offset delta for the i-th sublayer for the j-th CPB for the VCL
     HRD in units of a 90 kHz clock.
    """
    vcl_cpb_delay_offset: bytes | None = ...
    """
    specifies an offset to be used in the derivation of
     the nominal CPB removal times of the AU.
    """
    vcl_dpb_delay_offset: bytes | None = ...
    """
    specifies an offset to be used in the derivation of
     the DPB output times of the IRAP AU.
    """

class H266PredWeightTable(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 weight table parameters.
    """

    # gi Fields
    chroma_weight_l0_flag: bytes | None = ...
    """
    specifies whether weighting factors for the chroma
     prediction values of list 0 prediction using RefPicList[0][i] are present.
    """
    chroma_weight_l1_flag: bytes | None = ...
    """
    specifies whether weighting factors for the chroma
     prediction values of list 1 prediction using RefPicList[1][i] are present.
    """
    delta_chroma_log2_weight_denom: int = ...
    """
    the difference of the base 2 logarithm of
     the denominator for all chroma weighting factors.
    """
    delta_chroma_offset_l0: list | None = ...
    """
    the difference of the additive offset applied to
     the chroma prediction values for list 0 prediction.
    """
    delta_chroma_offset_l1: list | None = ...
    """
    the difference of the additive offset applied to
     the chroma prediction values for list 1 prediction.
    """
    delta_chroma_weight_l0: list | None = ...
    """
    the difference of the weighting factor applied to
     the chroma prediction values for list 0 prediction.
    """
    delta_chroma_weight_l1: list | None = ...
    """
    the difference of the weighting factor applied to
     the chroma prediction values for list 1 prediction.
    """
    delta_luma_weight_l0: list | None = ...
    """
    the difference of the weighting factor applied to the
     luma prediction value for list 0 prediction using RefPicList[0][i].
    """
    delta_luma_weight_l1: list | None = ...
    """
    the difference of the weighting factor applied to the
     luma prediction value for list 1 prediction using RefPicList[1][i].
    """
    luma_log2_weight_denom: int = ...
    """
    the base 2 logarithm of the denominator for all
     luma weighting factors.
    """
    luma_offset_l0: list | None = ...
    """
    the additive offset applied to the luma prediction value
     for list 0 prediction using RefPicList[0][i].
    """
    luma_offset_l1: list | None = ...
    """
    the additive offset applied to the luma prediction value
     for list 1 prediction using RefPicList[1][i].
    """
    luma_weight_l0_flag: bytes | None = ...
    """
    specifies whether weighting factors for the luma
     component of list 0 prediction using RefPicList[0][i] are present.
    """
    luma_weight_l1_flag: bytes | None = ...
    """
    specifies whether weighting factors for the luma
     component of list 1 prediction using RefPicList[1][i] are present.
    """
    num_l0_weights: int = ...
    """
    specifies the number of weights signalled for entries in
     RPL 0 when pps_wp_info_in_ph_flag is equal to 1.
    """
    num_l1_weights: int = ...
    """
    specifies the number of weights signalled for entries in
     RPL 1.
    """

class H266ProfileTierLevel(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 profile, tier and level.
    """

    # gi Fields
    frame_only_constraint_flag: int = ...
    """
    specifies whether sps_field_seq_flag for all
     pictures in OlsInScope shall be equal to 0.
    """
    general_constraints_info: H266GeneralConstraintsInfo | None = ...
    """
    the GstH266GeneralConstraintsInfo contains
     the general constraints info.
    """
    level_idc: int = ...
    """
    indicates a level to which OlsInScope conforms
     as specified in Annex A.
    """
    multilayer_enabled_flag: int = ...
    """
    specifies whether the CVSs of OlsInScope
     might contain more than one layer.
    """
    num_sub_profiles: int = ...
    """
    specifies the number of the general_sub_profile_idc[i]
     syntax elements.
    """
    profile_idc: H266Profile = ...
    """
    the profile id.
    """
    ptl_reserved_zero_bit: int = ...
    sub_profile_idc: list | None = ...
    """
    specifies the i-th interoperability indicator registered
     as specified by Rec. ITU-T T.35
    """
    sublayer_level_idc: bytes | None = ...
    """
    the sublayer level idc.
    """
    sublayer_level_present_flag: bytes | None = ...
    """
    specifies whether level information is present
     in the profile_tier_level syntax structure for the sublayer representation.
    """
    tier_flag: int = ...
    """
    specifies the main tier or high tier.
    """

class H266RefPicListStruct(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 reference picture list.
    """

    # gi Fields
    abs_delta_poc_st: list | None = ...
    """
    specifies the value of the variable
     AbsDeltaPocSt[listIdx][rplsIdx][i].
    """
    delta_poc_val_st: list | None = ...
    """
    the calculated DeltaPocValSt value.
    """
    ilrp_idx: bytes | None = ...
    """
    specifies the index of the ILRP entry of the i-th entry in the
     ref_pic_list_struct.
    """
    inter_layer_ref_pic_flag: bytes | None = ...
    """
    specifies whether the i-th entry in the
     ref_pic_list_struct(listIdx, rplsIdx) syntax structure is an ILRP entry.
    """
    ltrp_in_header_flag: int = ...
    """
    specifies whether the POC LSBs of the LTRP entries
     indicated in the ref_pic_list_struct(listIdx, rplsIdx) syntax structure
     are present in the same syntax structure.
    """
    num_inter_layer_pic: int = ...
    """
    the number of inter layer reference picture.
    """
    num_long_term_pic: int = ...
    """
    the number long term reference picture.
    """
    num_ref_entries: int = ...
    """
    specifies the number of entries in the
     ref_pic_list_struct(listIdx, rplsIdx) syntax structure.
    """
    num_short_term_pic: int = ...
    """
    the number of short term reference picture.
    """
    rpls_poc_lsb_lt: bytes | None = ...
    """
    specifies the value of the picture order count modulo
     MaxPicOrderCntLsb of the picture referred to.
    """
    st_ref_pic_flag: bytes | None = ...
    """
    specifies whether the i-th entry in the
     ref_pic_list_struct(listIdx, rplsIdx) syntax structure is an STRP entry.
    """
    strp_entry_sign_flag: bytes | None = ...
    """
    specifies whether DeltaPocValSt[listIdx][rplsIdx]
     is greater than or equal to 0.
    """

class H266RefPicLists(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 reference picture lists.
    """

    # gi Fields
    delta_poc_msb_cycle_lt: list | None = ...
    """
    specifies the value of the variable FullPocLt[i][j].
    """
    delta_poc_msb_cycle_present_flag: bytes | None = ...
    """
    specifies whether
     delta_poc_msb_cycle_lt[i][j] is present.
    """
    poc_lsb_lt: list | None = ...
    """
    specifies the value of the picture order count
     modulo MaxPicOrderCntLsb.
    """
    rpl_idx: bytes | None = ...
    """
    specifies the index into the list of the
     ref_pic_list_struct(listIdx, rplsIdx) syntax structures.
    """
    rpl_ref_list: list | None = ...
    """
    reference list of GstH266RefPicListStruct.
    """
    rpl_sps_flag: bytes | None = ...
    """
    specifies whether RPL i in ref_pic_lists is derived based
     on one of the ref_pic_list_struct(listIdx, rplsIdx) syntax structures
     with listIdx equal to i in the SPS.
    """

class H266RegisteredUserData(GObject.GPointer, metaclass=GObject.GType):
    """
    The User data registered by Rec. ITU-T T.35 SEI message.
    """

    # gi Fields
    country_code: int = ...
    """
    an itu_t_t35_country_code.
    """
    country_code_extension: int = ...
    """
    an itu_t_t35_country_code_extension_byte.
      Should be ignored when `country_code` is not 0xff
    """
    data: int = ...
    """
    the data of itu_t_t35_payload_byte
      excluding `country_code` and `country_code_extension`
    """
    size: int = ...
    """
    the size of `data` in bytes
    """

class H266SEIMessage(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 sei message.
    """

    # gi Fields
    payloadType: H266SEIPayloadType = ...
    """
    the payload type of GstH266SEIPayloadType.
    """

class H266SPS(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 SPS.
    """

    # gi Fields
    act_enabled_flag: int = ...
    """
    specifies whether the adaptive colour transform is enabled
     for the CLVS.
    """
    affine_amvr_enabled_flag: int = ...
    """
    specifies whether adaptive motion vector
     difference resolution is enabled for the CLVS.
    """
    affine_enabled_flag: int = ...
    """
    specifies whether the affine model based motion
     compensation is enabled for the CLVS.
    """
    affine_prof_enabled_flag: int = ...
    """
    specifies whether the affine motion compensation
     refined with optical flow is enabled for the CLVS.
    """
    alf_enabled_flag: int = ...
    """
    specifies whether ALF is enabled for the CLVS.
    """
    amvr_enabled_flag: int = ...
    """
    specifies whether adaptive motion vector difference
     resolution is enabled for the CVLS.
    """
    bcw_enabled_flag: int = ...
    bdof_control_present_in_ph_flag: int = ...
    """
    specifies whether ph_bdof_disabled_flag
     could be present in PH syntax structures referring to the SPS.
    """
    bdof_enabled_flag: int = ...
    """
    specifies whether the bi-directional optical flow inter
     prediction is enabled for the CLVS.
    """
    bdpcm_enabled_flag: int = ...
    """
    specifies whether intra_bdpcm_luma_flag and
     intra_bdpcm_chroma_flag could be present in the coding unit syntax for
     intra coding units.
    """
    bitdepth_minus8: int = ...
    """
    specifies the bit depth of the samples of the luma and
     chroma arrays.
    """
    ccalf_enabled_flag: int = ...
    """
    specifies whether CCALF is enabled for the CLVS.
    """
    cclm_enabled_flag: int = ...
    """
    specifies whether the cross-component linear model intra
     prediction from luma component to chroma component is enabled for the CLVS.
    """
    chroma_format_idc: int = ...
    """
    specifies the chroma sampling relative to the
     luma sampling.
    """
    chroma_horizontal_collocated_flag: int = ...
    """
    specifies whether prediction processes
     operate in a manner designed for chroma sample positions that are not
     horizontally shifted relative to corresponding luma sample positions.
    """
    chroma_qp_table: list | None = ...
    chroma_vertical_collocated_flag: int = ...
    """
    specifies whether prediction processes
     operate in a manner designed for chroma sample positions that are not
     vertically shifted relative to corresponding luma sample positions.
    """
    ciip_enabled_flag: int = ...
    """
    specifies whether ciip_flag could be present in the
     coding unit syntax for inter coding units.
    """
    conf_win_bottom_offset: int = ...
    """
    specify bottom offset of the cropping window that
     is applied to pictures.
    """
    conf_win_left_offset: int = ...
    """
    specify left offset of the cropping window that
     is applied to pictures.
    """
    conf_win_right_offset: int = ...
    """
    specify right offset of the cropping window that
     is applied to pictures.
    """
    conf_win_top_offset: int = ...
    """
    specify top offset of the cropping window that
     is applied to pictures.
    """
    conformance_window_flag: int = ...
    """
    indicates whether the conformance cropping window
     offset parameters follow next in the SPS.
    """
    crop_rect_height: int = ...
    """
    the cropped height of the picture.
    """
    crop_rect_width: int = ...
    """
    the cropped width of the picture.
    """
    crop_rect_x: int = ...
    """
    the x offset of the cropped window.
    """
    crop_rect_y: int = ...
    """
    the y offset of the cropped window.
    """
    ctu_size: int = ...
    """
    the calculated ctu size.
    """
    delta_qp_diff_val: bytes | None = ...
    """
    specifies a delta value used to derive the output
     coordinate of the j-th pivot point of the i-th chroma QP mapping table.
    """
    delta_qp_in_val_minus1: bytes | None = ...
    """
    specifies a delta value used to derive the input
     coordinate of the j-th pivot point of the i-th chroma QP mapping table.
    """
    dep_quant_enabled_flag: int = ...
    """
    specifies whether dependent quantization is enabled
     for the CLVS.
    """
    dmvr_control_present_in_ph_flag: int = ...
    """
    specifies whether ph_dmvr_disabled_flag
     could be present in PH syntax structures referring to the SPS.
    """
    dmvr_enabled_flag: int = ...
    """
    specifies whether decoder motion vector refinement based
     inter bi-prediction is enabled for the CLVS.
    """
    dpb: H266DPBParameters | None = ...
    """
    DPB parameters of GstH266DPBParameters.
    """
    entropy_coding_sync_enabled_flag: int = ...
    """
    specifies whether a specific synchronization
     process for context variables is invoked before decoding the CTU.
    """
    entry_point_offsets_present_flag: int = ...
    """
    specifies whether signalling for entry
     point offsets for tiles or tile-specific CTU rows could be present in the
     slice headers of pictures.
    """
    explicit_mts_inter_enabled_flag: int = ...
    """
    specifies whether mts_idx could be present
     in the inter coding unit syntax of the CLVS.
    """
    explicit_mts_intra_enabled_flag: int = ...
    """
    specifies whether mts_idx could be present
     in the intra coding unit syntax of the CLVS.
    """
    explicit_scaling_list_enabled_flag: int = ...
    """
    specifies whether the use of an explicit
     scaling list is enabled for the CLVS.
    """
    extension_7_flags: bytes | None = ...
    """
    specifies whether no sps_extension_data_flag syntax
     elements are present.
    """
    extension_flag: int = ...
    """
    specifies whether the syntax elements
     sps_range_extension_flag and sps_extension_7bits are present.
    """
    extra_ph_bit_present_flag: bytes | None = ...
    """
    specifies whether the i-th extra bit is present
     in PH syntax structures.
    """
    extra_sh_bit_present_flag: bytes | None = ...
    """
    specifies whether the i-th extra bit is present
     in the slice headers of pictures.
    """
    field_seq_flag: int = ...
    """
    indicates whether the CLVS conveys pictures that
     represent fields.
    """
    five_minus_max_num_subblock_merge_cand: int = ...
    """
    specifies the maximum number of
     subblock-based merging motion vector prediction candidates supported.
    """
    fps_den: int = ...
    """
    the calculated FPS denominator.
    """
    fps_num: int = ...
    """
    the calculated FPS numerator.
    """
    gdr_enabled_flag: int = ...
    """
    specifies whether GDR pictures are enabled and could
     be present in the CLVS.
    """
    general_hrd_params: H266GeneralHRDParameters | None = ...
    """
    HRD parameters of GstH266GeneralHRDParameters.
    """
    gpm_enabled_flag: int = ...
    """
    specifies whether the geometric partition based motion
     compensation is enabled for the CLVS.
    """
    ibc_enabled_flag: int = ...
    """
    specifies whether the IBC prediction mode is enabled for
     the CLVS.
    """
    idr_rpl_present_flag: int = ...
    """
    specifies whether RPL syntax elements could be present
     in slice headers of slices with nal_unit_type equal to IDR_N_LP or IDR_W_RADL.
    """
    independent_subpics_flag: int = ...
    """
    specifies whether all subpicture boundaries in
     the CLVS are treated as picture boundaries and there is no loop filtering
     across the subpicture boundaries.
    """
    inter_layer_prediction_enabled_flag: int = ...
    """
    specifies whether inter-layer prediction
     is enabled for the CLVS and ILRPs might be used for inter prediction of one
     or more coded pictures in the CLVS.
    """
    isp_enabled_flag: int = ...
    """
    specifies whether intra prediction with subpartitions is
     enabled for the CLVS.
    """
    joint_cbcr_enabled_flag: int = ...
    """
    specifies whether the joint coding of chroma
     residuals is enabled for the CLVS.
    """
    ladf_delta_threshold_minus1: list | None = ...
    """
    is used to compute the values of
     SpsLadfIntervalLowerBound[i].
    """
    ladf_enabled_flag: int = ...
    """
    specifies whether sps_num_ladf_intervals_minus2,
     sps_ladf_lowest_interval_qp_offset, sps_ladf_qp_offset[i], and
     sps_ladf_delta_threshold_minus1[i] are present in the SPS.
    """
    ladf_lowest_interval_qp_offset: int = ...
    """
    specifies the offset used to derive the
     variable qP.
    """
    ladf_qp_offset: list | None = ...
    """
    specifies the offset array used to derive the variable qP.
    """
    lfnst_enabled_flag: int = ...
    """
    specifies whether lfnst_idx could be present in intra
     coding unit syntax.
    """
    lmcs_enabled_flag: int = ...
    """
    specifies whether LMCS is enabled for the CLVS.
    """
    log2_ctu_size_minus5: int = ...
    """
    specifies the luma coding tree block size of each CTU.
    """
    log2_diff_max_bt_min_qt_inter_slice: int = ...
    """
    specifies the default difference between
     the base 2 logarithm of the maximum size in luma samples of a luma coding
     block that can be split using a binary split and the base 2 logarithm of the
     minimum size in luma samples of a luma leaf block resulting from quadtree
     splitting of a CTU in slices with sh_slice_type equal to 0(B) or 1(P).
    """
    log2_diff_max_bt_min_qt_intra_slice_chroma: int = ...
    """
    specifies the default difference
     between the base 2 logarithm of the maximum size in luma samples of a chroma
     coding block that can be split using a binary split and the base 2 logarithm
     of the minimum size in luma samples of a chroma leaf block resulting from
     quadtree splitting of a chroma CTU in slices with sh_slice_type equal to 2(I).
    """
    log2_diff_max_bt_min_qt_intra_slice_luma: int = ...
    """
    specifies the default difference
     between the base 2 logarithm of the maximum size in luma samples of a luma
     coding block that can be split using a binary split and the base 2 logarithm
     of the minimum size in luma samples of a luma leaf block resulting from
     quadtree splitting.
    """
    log2_diff_max_tt_min_qt_inter_slice: int = ...
    """
    specifies the default difference between
     the base 2 logarithm of the maximum size in luma samples of a luma coding
     block that can be split using a ternary split and the base 2 logarithm of
     the minimum size in luma samples of a luma leaf block resulting from quadtree
     splitting of a CTU in slices with sh_slice_type equal to 0(B) or 1(P).
    """
    log2_diff_max_tt_min_qt_intra_slice_chroma: int = ...
    """
    specifies the default difference
     between the base 2 logarithm of the maximum size in luma samples of a chroma
     coding block that can be split using a ternary split and the base 2 logarithm
     of the minimum size in luma samples of a chroma leaf block resulting from
     quadtree splitting of a chroma CTU in slices with sh_slice_type equal to 2(I).
    """
    log2_diff_max_tt_min_qt_intra_slice_luma: int = ...
    """
    specifies the default difference
     between the base 2 logarithm of the maximum size in luma samples of a luma
     coding block that can be split using a ternary split and the base 2
     logarithm of the minimum size in luma samples of a luma leaf block resulting
     from quadtree splitting.
    """
    log2_diff_min_qt_min_cb_inter_slice: int = ...
    """
    specifies the default difference between
     the base 2 logarithm of the minimum size in luma samples of a luma leaf block
     resulting from quadtree splitting of a CTU and the base 2 logarithm of the
     minimum luma coding block size in luma samples for luma CUs in slices with
     sh_slice_type equal to 0(B) or 1(P).
    """
    log2_diff_min_qt_min_cb_intra_slice_chroma: int = ...
    """
    specifies the default difference
     between the base 2 logarithm of the minimum size in luma samples of a chroma
     leaf block resulting from quadtree splitting of a chroma CTU and the base 2
     logarithm of the minimum coding block size in luma samples for chroma CUs
     in slices with sh_slice_type equal to 2(I).
    """
    log2_diff_min_qt_min_cb_intra_slice_luma: int = ...
    """
    specifies the default difference
     between the base 2 logarithm of the minimum size in luma samples of a luma
     leaf block resulting from quadtree splitting of a CTU and the base 2
     logarithm of the minimum coding block size in luma samples.
    """
    log2_max_pic_order_cnt_lsb_minus4: int = ...
    """
    specifies the value of the variable
     MaxPicOrderCntLsb.
    """
    log2_min_luma_coding_block_size_minus2: int = ...
    """
    specifies the minimum luma coding
     block size.
    """
    log2_parallel_merge_level_minus2: int = ...
    """
    specifies the value of the variable
     Log2ParMrgLevel.
    """
    log2_transform_skip_max_size_minus2: int = ...
    """
    specifies the maximum block size used
     for transform skip.
    """
    long_term_ref_pics_flag: int = ...
    loop_filter_across_subpic_enabled_flag: bytes | None = ...
    """
    specifies whether in-loop filtering
     operations across subpicture boundaries is enabled and might be performed
     across the boundaries of the i-th subpicture.
    """
    max_height: int = ...
    """
    the calculated max height of the picture.
    """
    max_luma_transform_size_64_flag: int = ...
    """
    specifies whether the maximum transform
     size in luma samples is equal to 64.
    """
    max_mtt_hierarchy_depth_inter_slice: int = ...
    """
    specifies the default maximum hierarchy
     depth for coding units resulting from multi-type tree splitting of a quadtree
     leaf in slices with sh_slice_type equal to 0 (B) or 1 (P).
    """
    max_mtt_hierarchy_depth_intra_slice_chroma: int = ...
    """
    specifies the default maximum
     hierarchy depth for chroma coding units resulting from multi-type tree
     splitting of a chroma quadtree leaf in slices with sh_slice_type equal to 2(I).
    """
    max_mtt_hierarchy_depth_intra_slice_luma: int = ...
    """
    specifies the default maximum
     hierarchy depth for coding units resulting from multi-type tree splitting
     of a quadtree leaf in slices with sh_slice_type equal to 2(I).
    """
    max_num_merge_cand_minus_max_num_gpm_cand: int = ...
    """
    specifies the maximum number of
     geometric partitioning merge mode candidates supported in the SPS.
    """
    max_sublayers_minus1: int = ...
    """
    specifies the maximum number of temporal sublayers
     that could be present in each CLVS referring to the SPS.
    """
    max_width: int = ...
    """
    the calculated max width of the picture.
    """
    min_qp_prime_ts: int = ...
    """
    specifies whether minimum allowed quantization parameter
     for transform skip mode.
    """
    mip_enabled_flag: int = ...
    """
    specifies whether the matrix-based intra prediction is
     enabled for the CLVS.
    """
    mmvd_enabled_flag: int = ...
    """
    specifies whether merge mode with motion vector
     difference is enabled for the CLVS.
    """
    mmvd_fullpel_only_enabled_flag: int = ...
    """
    specifies whether the merge mode with motion
     vector difference using only integer sample precision is enabled for the CLVS.
    """
    mrl_enabled_flag: int = ...
    """
    specifies whether intra prediction with multiple reference
     lines is enabled for the CLVS.
    """
    mts_enabled_flag: int = ...
    """
    specifies whether sps_explicit_mts_intra_enabled_flag
     and sps_explicit_mts_inter_enabled_flag are present in the SPS.
    """
    nuh_layer_id: int = ...
    """
    specifies the identifier of the layer to which a VCL NAL unit
     belongs or the identifier of a layer to which a non-VCL NAL unit applies.
    """
    num_extra_ph_bytes: int = ...
    """
    specifies the number of bytes of extra bits in the PH
     syntax structure for coded pictures.
    """
    num_extra_sh_bytes: int = ...
    """
    specifies the number of bytes of extra bits in the
     slice headers for coded pictures.
    """
    num_hor_virtual_boundaries: int = ...
    """
    specifies the number of
     sps_virtual_boundary_pos_y_minus1[i] syntax elements.
    """
    num_ladf_intervals_minus2: int = ...
    """
    specifies the number of
     sps_ladf_delta_threshold_minus1[i] and sps_ladf_qp_offset[i] syntax
     elements that are present in the SPS.
    """
    num_points_in_qp_table_minus1: bytes | None = ...
    """
    specifies the number of points used to
     describe the i-th chroma QP mapping table.
    """
    num_ref_pic_lists: bytes | None = ...
    """
    specifies the number of the ref_pic_list_struct(listIdx,
     rplsIdx) syntax structures with listIdx equal to i included in the SPS.
    """
    num_subpics_minus1: int = ...
    """
    specifies the number of subpictures in each picture
     in the CLVS.
    """
    num_ver_virtual_boundaries: int = ...
    """
    specifies the number of
     sps_virtual_boundary_pos_x_minus1[i] syntax elements.
    """
    ols_hrd_params: H266OLSHRDParameters | None = ...
    """
    OLS HRD parameters of GstH266OLSHRDParameters.
    """
    palette_enabled_flag: int = ...
    """
    specifies whether the palette prediction mode is
     enabled for the CLVS.
    """
    partition_constraints_override_enabled_flag: int = ...
    """
    specifies the presence of
     ph_partition_constraints_override_flag in PH syntax structures.
    """
    pic_height_max_in_luma_samples: int = ...
    """
    specifies the maximum height, in units of
     luma samples, of each decoded picture.
    """
    pic_width_max_in_luma_samples: int = ...
    """
    specifies the maximum width, in units of
     luma samples, of each decoded picture.
    """
    poc_msb_cycle_flag: int = ...
    """
    specifies whether the ph_poc_msb_cycle_present_flag
     syntax element is present in PH syntax structures.
    """
    poc_msb_cycle_len_minus1: int = ...
    """
    specifies the length, in bits, of the
     ph_poc_msb_cycle_val syntax elements.
    """
    prof_control_present_in_ph_flag: int = ...
    """
    specifies whether ph_prof_disabled_flag
     could be present in PH syntax structures.
    bcw_enabled_flag: specifies whether bi-prediction with CU weights is enabled
     for the CLVS.
    """
    profile_tier_level: H266ProfileTierLevel | None = ...
    """
    the profile tier and level in GstH266ProfileTierLevel.
    """
    ptl_dpb_hrd_params_present_flag: int = ...
    """
    specifies whether a profile_tier_level
     syntax structure and a dpb_parameters syntax structure are present
     in the SPS.
    """
    qp_table_start_minus26: list | None = ...
    """
    specifies the starting luma and chroma QP used to
     describe the i-th chroma QP mapping table.
    """
    qtbtt_dual_tree_intra_flag: int = ...
    """
    specifies whether each CTU is split into coding
     units with 64x64 luma samples using an implicit quadtree split, and these
     coding units are the root of two separate coding_tree syntax structure for
     luma and chroma.
    """
    range_extension_flag: int = ...
    """
    specifies whether the sps_range_extension syntax
     structure is present.
    """
    range_params: H266SPSRangeExtensionParams | None = ...
    """
    range parameters of GstH266SPSRangeExtensionParams.
    """
    ref_pic_list_struct: list | None = ...
    """
    the reference list of GstH266RefPicListStruct.
    """
    ref_pic_resampling_enabled_flag: int = ...
    """
    specifies whether reference picture
     resampling is enabled.
    """
    ref_wraparound_enabled_flag: int = ...
    """
    specifies whether horizontal wrap-around motion
     compensation is enabled for the CLVS.
    """
    res_change_in_clvs_allowed_flag: int = ...
    """
    specifies whether the picture spatial
     resolution might change within a CLVS.
    """
    rpl1_same_as_rpl0_flag: int = ...
    """
    specifies whether the syntax element
     sps_num_ref_pic_lists[1] and the syntax structure ref_pic_list_struct(1,
        rplsIdx) are present.
    """
    same_qp_table_for_chroma_flag: int = ...
    """
    specifies whether only one chroma QP mapping
     table is signalled and this table applies to Cb and Cr residuals and
     additionally to joint Cb-Cr residuals.
    """
    sao_enabled_flag: int = ...
    """
    specifies whether SAO is enabled for the CLVS.
    """
    sbt_enabled_flag: int = ...
    """
    specifies whether subblock transform for inter-predicted
     CUs is enabled for the CLVS.
    """
    sbtmvp_enabled_flag: int = ...
    """
    specifies whether subblock-based temporal motion vector
     predictors are enabled and might be used in decoding of pictures with all
     slices having sh_slice_type not equal to I in the CLVS.
    """
    scaling_matrix_designated_colour_space_flag: int = ...
    """
    specifies whether the colour
     space of the scaling matrices is the colour space that does not use a colour
     space conversion for the decoded residuals.
    """
    scaling_matrix_for_alternative_colour_space_disabled_flag: int = ...
    """
    specifies whether
     scaling matrices are disabled and not applied to blocks of a coding unit.
    """
    scaling_matrix_for_lfnst_disabled_flag: int = ...
    """
    specifies whether scaling matrices
     are disabled for blocks coded with LFNST for the CLVS.
    """
    sign_data_hiding_enabled_flag: int = ...
    """
    specifies whether sign bit hiding is enabled
     for the CLVS.
    """
    six_minus_max_num_ibc_merge_cand: int = ...
    """
    specifies the maximum number of IBC
     merging block vector prediction (BVP) candidates.
    """
    six_minus_max_num_merge_cand: int = ...
    """
    specifies the maximum number of merging motion
     vector prediction (MVP) candidates supported in the SPS.
    """
    smvd_enabled_flag: int = ...
    """
    specifies whether symmetric motion vector difference
     is enabled for the CLVS.
    """
    sps_6param_affine_enabled_flag: int = ...
    """
    specifies whether the 6-parameter affine
     model based motion compensation is enabled for the CLVS.
    """
    sps_id: int = ...
    """
    provides an identifier for the SPS for reference by other
     syntax elements.
    """
    sublayer_cpb_params_present_flag: int = ...
    """
    specifies whether the
     ols_timing_hrd_parameters syntax structure in the SPS includes HRD
     parameters for sublayer representations.
    """
    sublayer_dpb_params_flag: int = ...
    """
    used to control the presence of
     dpb_max_dec_pic_buffering_minus1[i], dpb_max_num_reorder_pics[i],
     and dpb_max_latency_increase_plus1[i] syntax elements in the
     dpb_parameters syntax strucure.
    """
    subpic_ctu_top_left_x: list | None = ...
    """
    specifies horizontal position of top-left CTU of
     i-th subpicture in unit of CtbSizeY.
    """
    subpic_ctu_top_left_y: list | None = ...
    """
    specifies vertical position of top-left CTU of
     i-th subpicture in unit of CtbSizeY.
    """
    subpic_height_minus1: list | None = ...
    """
    specifies the height of the i-th subpicture in
     units of CtbSizeY.
    """
    subpic_id: list | None = ...
    """
    specifies the subpicture ID of the i-th subpicture.
    """
    subpic_id_len_minus1: int = ...
    """
    specifies the number of bits used to represent the
     syntax element sps_subpic_id[i].
    """
    subpic_id_mapping_explicitly_signalled_flag: int = ...
    """
    specifies whether the
     subpicture ID mapping is explicitly signalled.
    """
    subpic_id_mapping_present_flag: int = ...
    """
    specifies whether the subpicture ID mapping
     is signalled in the SPS.
    """
    subpic_info_present_flag: int = ...
    """
    specifies whether subpicture information is
     present for the CLVS and there might be one or more than one subpicture
     in each picture of the CLVS.
    """
    subpic_same_size_flag: int = ...
    """
    specifies whether all subpictures in the CLVS have
     the same width and height.
    """
    subpic_treated_as_pic_flag: bytes | None = ...
    """
    specifies whether the i-th subpicture of each
     coded picture in the CLVS is treated as a picture.
    """
    subpic_width_minus1: list | None = ...
    """
    specifies the width of the i-th subpicture in units
     of CtbSizeY.
    """
    temporal_mvp_enabled_flag: int = ...
    """
    specifies whether temporal motion vector
     predictors are enabled for the CLVS.
    """
    timing_hrd_params_present_flag: int = ...
    """
    specifies whether the SPS contains a
     general_timing_hrd_parameters syntax structure and an
     ols_timing_hrd_parameters syntax structure.
    """
    transform_skip_enabled_flag: int = ...
    """
    specifies whether transform_skip_flag could be
     present in the transform unit syntax.
    """
    valid: bool = ...
    """
    whether the SPS is valid.
    """
    virtual_boundaries_enabled_flag: int = ...
    """
    specifies whether disabling in-loop
     filtering across virtual boundaries is enabled for the CLVS.
    """
    virtual_boundaries_present_flag: int = ...
    """
    specifies whether information of virtual
     boundaries is signalled in the SPS.
    """
    virtual_boundary_pos_x_minus1: list | None = ...
    """
    specifies the location of the i-th vertical
     virtual boundary in units of luma samples divided by 8.
    """
    virtual_boundary_pos_y_minus1: list | None = ...
    """
    specifies the location of the i-th horizontal
     virtual boundary in units of luma samples divided by 8.
    """
    vps: H266VPS | None = ...
    """
    the GstH266VPS this SPS refers to.
    """
    vps_id: int = ...
    """
    specifies the VPS referred to by this SPS.
    """
    vui_parameters_present_flag: int = ...
    """
    specifies whether the syntax structure
     vui_payload is present in the SPS RBSP syntax structure.
    """
    vui_params: H266VUIParams | None = ...
    """
    VUI parameters of GstH266VUIParams.
    """
    vui_payload_size_minus1: int = ...
    """
    specifies the number of RBSP bytes in the
     vui_payload syntax structure.
    """
    weighted_bipred_flag: int = ...
    """
    specifies whether explicit weighted prediction might
     be applied to B slices referring to the SPS.
    `guint8` long_term_ref_pics_flag: specifies whether no LTRP is used for inter
     prediction of any coded picture in the CLVS.
    """
    weighted_pred_flag: int = ...
    """
    specifies whether weighted prediction might be applied
     to P slices referring to the SPS.
    """

class H266SPSRangeExtensionParams(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 SPS range extension parameters.
    """

    # gi Fields
    extended_precision_flag: int = ...
    """
    specifies whether an extended dynamic range is
     used for transform coefficients.
    """
    persistent_rice_adaptation_enabled_flag: int = ...
    """
    specifies whether Rice parameter
     derivation is initialized at the start of each TU using statistics
     accumulated from previous TUs.
    """
    reverse_last_sig_coeff_enabled_flag: int = ...
    """
    specifies whether
     sh_reverse_last_sig_coeff_flag is present in slice_header syntax
     structures referring to the SPS.
    """
    rrc_rice_extension_flag: int = ...
    """
    specifies whether an alternative Rice parameter
     derivation is used.
    """
    ts_residual_coding_rice_present_in_sh_flag: int = ...
    """
    specifies whether
     sh_ts_residual_coding_rice_idx_minus1 may be present in slice_header
     syntax structures referring to the SPS.
    """

class H266ScalableNesting(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 scalable nesting.
    """

    # gi Fields
    all_layers_flag: int = ...
    """
    specifies whether the scalable-nested SEI messages apply to
     all layers that have nuh_layer_id greater than or equal to the nuh_layer_id
     of the current SEI NAL unit.
    """
    layer_id: bytes | None = ...
    """
    specifies the nuh_layer_id value of the i-th layer to which the
     scalable-nested SEI messages apply.
    """
    num_layers_minus1: int = ...
    """
    specifies the number of layers to which the
     scalable-nested SEI messages apply.
    """
    num_olss_minus1: int = ...
    """
    specifies the number of OLSs to which the scalable-nested
     SEI messages apply.
    """
    num_seis_minus1: int = ...
    """
    specifies the number of scalable-nested SEI messages.
    """
    num_subpics_minus1: int = ...
    """
    specifies the number of subpictures in each picture in
     the multiSubpicLayers.
    """
    ols_flag: int = ...
    """
    specifies whether the scalable-nested SEI messages apply to
     specific OLSs.
    """
    ols_idx_delta_minus1: bytes | None = ...
    """
    is used to derive the variable NestingOlsIdx[i].
    """
    subpic_flag: int = ...
    """
    specifies whether the scalable-nested SEI messages that apply
     to specified OLSs or layers apply only to specific subpictures of the
     specified OLSs or layers.
    """
    subpic_id: bytes | None = ...
    """
    indicates the subpicture ID of the i-th subpicture in each
     picture in the multiSubpicLayers.
    """
    subpic_id_len_minus1: int = ...
    """
    specifies the number of bits used to represent the
     syntax element sn_subpic_id[i].
    """

class H266ScalingList(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 scaling list parameters.
    """

    # gi Fields
    copy_mode_flag: bytes | None = ...
    """
    specifies whether the values of the scaling list are the
     same as the values of a reference scaling list.
    """
    dc_coef: list | None = ...
    """
    used to derive the value of the variable ScalingMatrixDcRec.
    """
    delta_coef: list | None = ...
    """
    specifies the difference between the current matrix coefficient
     ScalingList.
    """
    pred_id_delta: bytes | None = ...
    """
    specifies the reference scaling list used to derive the
     predicted scaling matrix scalingMatrixPred.
    """
    pred_mode_flag: bytes | None = ...
    """
    specifies whether the values of the scaling list can be
     predicted from a reference scaling list.
    """
    scaling_list: bytes | None = ...
    """
    the calculated scaling list coefs.
    """
    scaling_list_DC: bytes | None = ...
    """
    the scaling list DC coef.
    """

class H266SliceHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 slice header.
    """

    # gi Fields
    alf_aps_id_chroma: int = ...
    """
    specifies the aps_adaptation_parameter_set_id of the ALF
     APS that the chroma component of the slice refers to.
    """
    alf_aps_id_luma: bytes | None = ...
    """
    specifies the aps_adaptation_parameter_set_id of the i-th
     ALF APS that the luma component of the slice refers to.
    """
    alf_cb_enabled_flag: int = ...
    """
    specifies whether ALF is enabled for the Cb colour
     component of the current slice.
    """
    alf_cc_cb_aps_id: int = ...
    """
    specifies the aps_adaptation_parameter_set_id that the Cb
     colour component of the slice refers to.
    """
    alf_cc_cb_enabled_flag: int = ...
    """
    specifies whether CCALF is enabled for the Cb
     colour component.
    """
    alf_cc_cr_aps_id: int = ...
    """
    specifies the aps_adaptation_parameter_set_id that the Cr
     colour component of the slice refers to.
    """
    alf_cc_cr_enabled_flag: int = ...
    """
    specifies whether CCALF is enabled for the Cr
     colour component of the current slice.
    """
    alf_cr_enabled_flag: int = ...
    """
    specifies whether ALF is enabled for the Cr colour
     component of the current slice.
    """
    alf_enabled_flag: int = ...
    """
    specifies whether ALF is enabled for the Y, Cb, or Cr
     colour component of the current slice.
    """
    cabac_init_flag: int = ...
    """
    specifies the method for determining the initialization
     table used in the initialization process for context variables.
    """
    cb_beta_offset_div2: int = ...
    """
    specify the deblocking parameter offsets for beta that
     are applied to the Cb component for the current slice.
    """
    cb_qp_offset: int = ...
    """
    specifies a difference to be added to the value of
     pps_cb_qp_offset when determining the value of the Qp'Cb quantization.
    """
    cb_tc_offset_div2: int = ...
    """
    specify the deblocking parameter offsets for tC that
     are applied to the Cb component for the current slice.
    """
    collocated_from_l0_flag: int = ...
    """
    specifies whether the collocated picture used for
     temporal motion vector prediction is derived from RPL 0.
    """
    collocated_ref_idx: int = ...
    """
    specifies the reference index of the collocated picture
     used for temporal motion vector prediction.
    """
    cr_beta_offset_div2: int = ...
    """
    specify the deblocking parameter offsets for beta that
     are applied to the Cr component for the current slice.
    """
    cr_qp_offset: int = ...
    """
    specifies a difference to be added to the value of
     pps_cr_qp_offset when determining the value of the Qp' Cr quantization.
    """
    cr_tc_offset_div2: int = ...
    """
    specify the deblocking parameter offsets for tC that
     are applied to the Cr component for the current slice.
    """
    cu_chroma_qp_offset_enabled_flag: int = ...
    """
    specifies whether the
     cu_chroma_qp_offset_flag could be present in the transform unit and palette
     coding syntax of the current slice.
    """
    deblocking_filter_disabled_flag: int = ...
    """
    specifies whether the deblocking filter is
     disabled for the current slice.
    """
    deblocking_params_present_flag: int = ...
    """
    specifies whether the deblocking parameters
     could be present in the slice header.
    """
    dep_quant_used_flag: int = ...
    """
    specifies whether dependent quantization is used.
    """
    entry_offset_len_minus1: int = ...
    """
    specifies the length, in bits, of the
     sh_entry_point_offset_minus1[i] syntax elements.
    """
    entry_point_offset_minus1: list | None = ...
    """
    specifies the i-th entry point offset in bytes.
    """
    entry_point_start_ctu: list | None = ...
    explicit_scaling_list_used_flag: int = ...
    """
    specifies that the explicit scaling list
     is used in the scaling process for transform coefficients.
    """
    extra_bit: bytes | None = ...
    """
    could have any value.
    """
    header_size: int = ...
    """
    size of the slice_header in bits.
    """
    joint_cbcr_qp_offset: int = ...
    """
    specifies a difference to be added to the value of
     pps_joint_cbcr_qp_offset_value when determining the value of the Qp'CbCr.
    """
    lmcs_used_flag: int = ...
    """
    specifies that luma mapping is used for the current slice
     and chroma scaling could be used for the current slice.
    """
    luma_beta_offset_div2: int = ...
    """
    specify the deblocking parameter offsets for beta that
     are applied to the luma component for the current slice.
    """
    luma_tc_offset_div2: int = ...
    """
    specify the deblocking parameter offsets for tC that
     are applied to the luma component for the current slice.
    """
    n_emulation_prevention_bytes: int = ...
    """
    number of emulation prevention bytes (EPB) in
     this slice_header.
    """
    no_output_of_prior_pics_flag: int = ...
    """
    affects the output of previously-decoded
     pictures in the DPB.
    """
    num_alf_aps_ids_luma: int = ...
    """
    specifies the number of ALF APSs.
    """
    num_entry_points: int = ...
    """
    NumEntryPoints specifies the number of entry points in
     the current slice.
    """
    num_ref_idx_active: bytes | None = ...
    """
    the calculated NumRefIdxActive value.
    """
    num_ref_idx_active_minus1: bytes | None = ...
    """
    is used for the derivation of the variable
     NumRefIdxActive[i].
    """
    num_ref_idx_active_override_flag: int = ...
    """
    specifies whether the syntax element
     sh_num_ref_idx_active_minus1[0] is present for P and B slices.
    """
    num_tiles_in_slice_minus1: int = ...
    """
    specifies the number of tiles in the slice.
    """
    picture_header: H266PicHdr | None = ...
    """
    the picture header of GstH266PicHdr.
    """
    picture_header_in_slice_header_flag: int = ...
    pred_weight_table: H266PredWeightTable | None = ...
    """
    prediction weight table of GstH266PredWeightTable.
    """
    qp_delta: int = ...
    """
    specifies the initial value of QpY to be used for the coding
     blocks in the slice.
    """
    ref_pic_lists: H266RefPicLists | None = ...
    """
    reference pictures lists of GstH266RefPicLists.
    """
    reverse_last_sig_coeff_flag: int = ...
    """
    specifies whether the coordinates of the last
     significant coefficient are coded relative to ( 0, 0 ) for each transform
     block of the current slice.
    """
    sao_chroma_used_flag: int = ...
    """
    specifies whether SAO is used for the chroma component
     in the current slice.
    """
    sao_luma_used_flag: int = ...
    """
    specifies whether SAO is used for the luma component
     in the current slice.
    """
    sign_data_hiding_used_flag: int = ...
    """
    specifies whether sign bit hiding is not used
     for the current slice.
    """
    slice_address: int = ...
    """
    specifies the slice address of the slice.
    """
    slice_header_extension_data_byte: bytes | None = ...
    """
    could have any value.
    """
    slice_header_extension_length: int = ...
    """
    specifies the length of the slice header
     extension data in bytes.
    """
    slice_qp_y: int = ...
    slice_type: int = ...
    """
    specifies the coding type of the slice.
    """
    subpic_id: int = ...
    """
    specifies the subpicture ID of the subpicture.
    """
    ts_residual_coding_disabled_flag: int = ...
    """
    specifies whether the residual_coding
     syntax structure is used to parse the residual samples of a transform skip
     block for the current slice.
    """
    ts_residual_coding_rice_idx_minus1: int = ...
    """
    specifies whether Rice parameter used
     for the residual_ts_coding syntax structure in the current slice.
    """

class H266SubLayerHRDParameters(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 sub layer HDR parameters.
    """

    # gi Fields
    bit_rate: list | None = ...
    """
    the calculated bit rate.
    """
    bit_rate_du_value_minus1: list | None = ...
    """
    specifies the maximum input bit rate for the CPB.
    """
    bit_rate_value_minus1: list | None = ...
    """
    specifies the maximum input bit rate for the CPB.
    """
    cbr_flag: bytes | None = ...
    """
    specifies whether to decode this bitstream by the HRD using
     the CPB specification.
    """
    cpb_size: list | None = ...
    """
    the calculated cpb size.
    """
    cpb_size_du_value_minus1: list | None = ...
    """
    together with cpb_size_du_scale to specify
     the CPB size.
    """
    cpb_size_value_minus1: list | None = ...
    """
    together with cpb_size_scale to specify the CPB size.
    """

class H266SubPicLevelInfo(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 subpicture level information.
    """

    # gi Fields
    cbr_constraint_flag: int = ...
    """
    specifies whether to decode the sub-bitstreams
     resulting from extraction of any subpicture sequence.
    """
    explicit_fraction_present_flag: int = ...
    """
    specifies whether the syntax elements
     sli_ref_level_fraction_minus1[ i ] are present.
    """
    max_sublayers_minus1: int = ...
    """
    specifies the maximum number of temporal sublayers in
     the subpicture sequences.
    """
    non_subpic_layers_fraction: bytes | None = ...
    """
    indicates the i-th fraction of the bitstream
     level limits associated with layers in targetCvss.
    """
    num_ref_levels_minus1: int = ...
    """
    specifies the number of reference levels signalled
     for each subpicture sequences.
    """
    num_subpics_minus1: int = ...
    """
    specifies the number of subpictures in the pictures in
     the multiSubpicLayers in targetCvss.
    """
    ref_level_fraction_minus1: bytes | None = ...
    """
    specifies the i-th fraction of the level limits
     for the subpictures.
    """
    ref_level_idc: bytes | None = ...
    """
    indicates the i-th level to which each subpicture sequence
     conforms as specified in Annex A.
    """
    sublayer_info_present_flag: int = ...
    """
    specifies whether the level information for
     subpicture sequences is present for sublayer representation.
    """

class H266VPS(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 VPS.
    """

    # gi Fields
    all_independent_layers_flag: int = ...
    """
    specifies whether all layers specified by the
     VPS are independently coded without using inter-layer prediction.
    """
    default_ptl_dpb_hrd_max_tid_flag: int = ...
    """
    specifies whether the syntax elements
     vps_ptl_max_tid, vps_dpb_max_tid, and vps_hrd_max_tid are present.
    """
    direct_ref_layer_flag: bytes | None = ...
    """
    specifies whether the layer with index j is not a
     direct reference layer for the layer with index i.
    """
    dpb: list | None = ...
    """
    the DPB parameters using GstH266DPBParameters.
    """
    dpb_max_tid: bytes | None = ...
    """
    specifies the TemporalId of the highest sublayer representation
     for which the DPB parameters could be present in the i-th dpb_parameters
     syntax strutcure in the VPS.
    """
    each_layer_is_an_ols_flag: int = ...
    """
    specifies whether each OLS specified by the VPS
     contains only one layer and each layer specified by the VPS is an OLS with
     the single included layer being the only output layer.
    """
    extension_data: bytes | None = ...
    """
    could have any value, it is not defined in this version of
     this Specification.
    """
    extension_flag: int = ...
    """
    specifies whether no vps_extension_data_flag syntax elements
     are present in the VPS RBSP syntax structure.
    """
    general_hrd_params: H266GeneralHRDParameters | None = ...
    """
    HRD parameters in GstH266GeneralHRDParameters.
    """
    hrd_max_tid: bytes | None = ...
    """
    specifies the TemporalId of the highest sublayer representation
     for which the HRD parameters are contained in the i-th
     ols_timing_hrd_parameters syntax structure.
    """
    independent_layer_flag: bytes | None = ...
    """
    specifies whether the layer with index i does not
     use inter-layer prediction.
    """
    layer_id: bytes | None = ...
    """
    specifies the nuh_layer_id value of the i-th layer.
    """
    layer_id_in_ols: bytes | None = ...
    """
    specifies the nuh_layer_id value of the j-th layer in
     the i-th OLS.
    """
    max_layers_minus1: int = ...
    """
    specifies the number of layers specified by the VPS.
    """
    max_sublayers_minus1: int = ...
    """
    specifies the maximum number of temporal sublayers
     that may be present in a layer specified by the VPS.
    """
    max_tid_il_ref_pics_plus1: bytes | None = ...
    """
    specifies whether the pictures of the j-th layer
     that are neither IRAP pictures nor GDR pictures with ph_recovery_poc_cnt
     equal to 0 are not used as ILRPs for decoding of pictures of the i-th layer.
    """
    max_tid_ref_present_flag: bytes | None = ...
    """
    specifies whether the syntax element
     vps_max_tid_il_ref_pics_plus1 could be present.
    """
    multi_layer_ols_idx: list | None = ...
    """
    specifies the index to the list of multi-layer OLSs
     for the i-th OLS.
    """
    num_dpb_params_minus1: int = ...
    """
    specifies the number of dpb_parameters syntax
     strutcures in the VPS.
    """
    num_layers_in_ols: list | None = ...
    """
    specifies the number of layers in the i-th OLS.
    """
    num_multi_layer_olss: int = ...
    """
    specifies the number of multi-layer OLSs (i.e., OLSs
     that contain more than one layer).
    """
    num_ols_timing_hrd_params_minus1: int = ...
    """
    specifies the number of
     ols_timing_hrd_parameters syntax structures present in the VPS.
    """
    num_output_layer_sets_minus2: int = ...
    """
    specifies the total number of OLSs
     specified by the VPS when vps_ols_mode_idc is equal to 2.
    """
    num_output_layers_in_ols: list | None = ...
    """
    specifies the number of output layers in
     the i-th OLS.
    """
    num_ptls_minus1: int = ...
    """
    specifies the number of profile_tier_level
     syntax structures in the VPS.
    """
    num_sub_layers_in_layer_in_ols: bytes | None = ...
    """
    specifies the number of sublayers in the
     j-th layer in the i-th OLS.
    """
    ols_dpb_bitdepth_minus8: bytes | None = ...
    """
    specifies the greatest allowed value of
     sps_bitdepth_minus8 for all SPSs.
    """
    ols_dpb_chroma_format: bytes | None = ...
    """
    specifies the greatest allowed value of
     sps_chroma_format_idc for all SPSs.
    """
    ols_dpb_params_idx: bytes | None = ...
    """
    specifies the index of the dpb_parameters syntax
     structure that applies to the i-th multi-layer OLS.
    """
    ols_dpb_pic_height: list | None = ...
    """
    specifies the height of each picture storage buffer
     for the i-th multi-layer OLS.
    """
    ols_dpb_pic_width: list | None = ...
    """
    specifies the width of each picture storage buffer for
     the i-th multi-layer OLS.
    """
    ols_hrd_params: list | None = ...
    """
    OLS HRD parameters using GstH266OLSHRDParameters.
    """
    ols_mode_idc: int = ...
    """
    specifies whether the total number of OLSs specified by the
     VPS is equal to vps_max_layers_minus1 + 1.
    """
    ols_output_layer_flag: bytes | None = ...
    """
    specifies whether the layer with nuh_layer_id
     equal to vps_layer_id[j] is an output layer of the i-th OLS when
     vps_ols_mode_idc is equal to 2.
    """
    ols_ptl_idx: bytes | None = ...
    """
    specifies the index of the profile_tier_level syntax
     structure that applies to the i-th OLS.
    """
    ols_timing_hrd_idx: bytes | None = ...
    """
    specifies the index of the ols_timing_hrd_parameters
     syntax structure that applies to the i-th multi-layer OLS.
    """
    output_layer_id_in_ols: bytes | None = ...
    """
    specifies the nuh_layer_id value of the j-th
     output layer in the i-th OLS.
    """
    profile_tier_level: list | None = ...
    """
    specifies the profile, tier and level using
     GstH266ProfileTierLevel.
    """
    pt_present_flag: bytes | None = ...
    """
    specifies whether profile, tier, and general constraints
     information are present in the i-th profile_tier_level syntax structure
     in the VPS.
    """
    ptl_max_tid: bytes | None = ...
    """
    specifies the TemporalId of the highest sublayer representation.
    """
    sublayer_cpb_params_present_flag: int = ...
    """
    specifies that the i-th
     ols_timing_hrd_parameters syntax structure in the VPS contains HRD
     parameters for the sublayer representations.
    """
    sublayer_dpb_params_present_flag: int = ...
    """
    is used to control the presence of
     dpb_max_dec_pic_buffering_minus1[j], dpb_max_num_reorder_pics[j], and
     dpb_max_latency_increase_plus1[j] syntax elements in the dpb_parameters
     syntax strucures in the VPS.
    """
    timing_hrd_params_present_flag: int = ...
    """
    specifies whether the VPS contains a
     general_timing_hrd_parameters syntax structure and other HRD parameters.
    """
    total_num_olss: int = ...
    """
    specifies the total number of OLSs specified by the VPS.
    """
    valid: bool = ...
    """
    whether this VPS is valid.
    """
    vps_id: int = ...
    """
    provides an identifier for the VPS for reference by other syntax
     elements.
    """

class H266VUIParams(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure defining the H266 VUI parameters.
    """

    # gi Fields
    aspect_ratio_constant_flag: int = ...
    """
    specifies whether the ratio is constant.
    """
    aspect_ratio_idc: int = ...
    """
    specifies the value of the sample aspect ratio of
     the luma samples.
    """
    aspect_ratio_info_present_flag: int = ...
    """
    specifies whether aspect_ratio_idc is present.
    """
    chroma_loc_info_present_flag: int = ...
    """
    specifies whether chroma_sample_loc_type_frame,
     chroma_sample_loc_type_top_field and chroma_sample_loc_type_bottom_field
     are present.
    """
    chroma_sample_loc_type_bottom_field: int = ...
    """
    specify the location of chroma for the
     bottom field.
    """
    chroma_sample_loc_type_frame: int = ...
    """
    specify the location of chroma for the frame.
    """
    chroma_sample_loc_type_top_field: int = ...
    """
    specify the location of chroma for the
     top field.
    """
    colour_description_present_flag: int = ...
    """
    specifies whether colour_primaries,
     transfer_characteristics and matrix_coefficients are present.
    """
    colour_primaries: int = ...
    """
    indicates the chromaticity coordinates of the source
     primaries.
    """
    full_range_flag: int = ...
    """
    indicates the full range of the color.
    """
    interlaced_source_flag: int = ...
    """
    flag to indicate the interlaced type of stream.
    """
    matrix_coeffs: int = ...
    """
    describes the matrix coefficients used in deriving
     luma and chroma signals.
    """
    non_packed_constraint_flag: int = ...
    """
    indicate the presence of frame packing
     arrangement sei message
    """
    non_projected_constraint_flag: int = ...
    """
    flag to indicate the projected support.
    """
    overscan_appropriate_flag: int = ...
    """
    indicates whether the cropped decoded pictures
     output are suitable for display using overscan.
    """
    overscan_info_present_flag: int = ...
    """
    specify whether the overscan_appropriate_flag
     is present.
    """
    par_d: int = ...
    """
    calculated aspect ratio denominator value.
    """
    par_n: int = ...
    """
    calculated aspect ratio numerator value.
    """
    progressive_source_flag: int = ...
    """
    flag to indicate the progressive type of stream.
    """
    sar_height: int = ...
    """
    indicates the vertical size of the sample aspect ratio.
    """
    sar_width: int = ...
    """
    indicates the horizontal size of the sample aspect ratio.
    """
    transfer_characteristics: int = ...
    """
    indicates the opto-electronic
     transfer characteristic.
    """

class JpegFrameComponent(GObject.GPointer, metaclass=GObject.GType):
    """
    Component-specification parameters.
    """

    # gi Fields
    horizontal_factor: int = ...
    """
    Horizontal sampling factor (Hi)
    """
    identifier: int = ...
    """
    Component identifier (Ci)
    """
    quant_table_selector: int = ...
    """
    Quantization table destination selector (Tqi)
    """
    vertical_factor: int = ...
    """
    Vertical sampling factor (Vi)
    """

class JpegFrameHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    Frame header.
    """

    # gi Fields
    components: list | None = ...
    """
    Image components
    """
    height: int = ...
    """
    Number of lines (Y)
    """
    num_components: int = ...
    """
    Number of image components in frame (Nf)
    """
    sample_precision: int = ...
    """
    Sample precision (P)
    """
    width: int = ...
    """
    Number of samples per line (X)
    """

class JpegHuffmanTable(GObject.GPointer, metaclass=GObject.GType):
    """
    Huffman table.
    """

    # gi Fields
    huf_bits: bytes | None = ...
    """
    Number of Huffman codes of length i + 1 (Li)
    """
    huf_values: bytes | None = ...
    valid: bool = ...
    """
    If the Huffman table is valid, which means it has already
      been parsed
    """

class JpegHuffmanTables(GObject.GPointer, metaclass=GObject.GType):
    """
    Helper data structure that holds all AC/DC Huffman tables used to
    decode an image.
    """

    # gi Fields
    ac_tables: list | None = ...
    """
    AC Huffman tables
    """
    dc_tables: list | None = ...
    """
    DC Huffman tables
    """

class JpegQuantTable(GObject.GPointer, metaclass=GObject.GType):
    """
    Quantization table.
    """

    # gi Fields
    quant_precision: int = ...
    """
    Quantization table element precision (Pq)
    """
    quant_table: list | None = ...
    """
    Quantization table elements (Qk)
    """
    valid: bool = ...
    """
    If the quantization table is valid, which means it has
      already been parsed
    """

class JpegQuantTables(GObject.GPointer, metaclass=GObject.GType):
    """
    Helper data structure that holds all quantization tables used to
    decode an image.
    """

    # gi Fields
    quant_tables: list | None = ...
    """
    All quantization tables
    """

class JpegScanComponent(GObject.GPointer, metaclass=GObject.GType):
    """
    Component-specification parameters.
    """

    # gi Fields
    ac_selector: int = ...
    """
    AC entropy coding table destination selector (Taj)
    """
    component_selector: int = ...
    """
    Scan component selector (Csj)
    """
    dc_selector: int = ...
    """
    DC entropy coding table destination selector (Tdj)
    """

class JpegScanHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    Scan header.
    """

    # gi Fields
    @builtins.property
    def _reserved1(self) -> int: ...
    @builtins.property
    def _reserved2(self) -> int: ...
    @builtins.property
    def _reserved3(self) -> int: ...
    @builtins.property
    def _reserved4(self) -> int: ...
    components: list | None = ...
    """
    Image components
    """
    num_components: int = ...
    """
    Number of image components in scan (Ns)
    """

class JpegSegment(GObject.GPointer, metaclass=GObject.GType):
    """
    A structure that contains the type of a segment, its offset and its size.
    """

    # gi Fields
    data: int = ...
    """
    the data containing the jpeg segment starting at `offset`
    """
    marker: JpegMarker = ...
    """
    The type of the segment that starts at `offset`
    """
    offset: int = ...
    """
    The offset to the segment start in bytes. This is the
      exact start of the segment, no marker code included
    """
    size: int = ...
    """
    The size of the segment in bytes, or -1 if the end was not
      found. It is the exact size of the segment, without the sync byte and
      marker code but including any length bytes.
    """

    # gi Methods
    def parse_frame_header(self) -> tuple[bool, JpegFrameHdr]:
        """
            Parses the `frame_hdr` JPEG frame header structure members from `segment`.

        The caller must make sure there is enough data for the whole segment
        available.
        """
    def parse_huffman_table(self) -> tuple[bool, JpegHuffmanTables]:
        """
            Parses the JPEG Huffman table structure members from `segment`.

        The caller must make sure there is enough data for the whole segment
        available.

        Note: `huf_tables` represents the complete set of possible Huffman
        tables. However, the parser will only write to the Huffman table
        specified by the table destination identifier (Th). While doing so,
        the `valid` flag of the specified Huffman table will also be set to
        True;
        """
    def parse_quantization_table(self) -> tuple[bool, JpegQuantTables]:
        """
            Parses the JPEG quantization table structure members from `segment`.

        The caller must make sure there is enough data for the whole segment
        available.

        Note: `quant_tables` represents the complete set of possible
        quantization tables. However, the parser will only write to the
        quantization table specified by the table destination identifier
        (Tq). While doing so, the `valid` flag of the specified quantization
        table will also be set to True.
        """
    def parse_restart_interval(self) -> tuple[bool, int]:
        """
            The caller must make sure there is enough data for the whole segment
        available.
        """
    def parse_scan_header(self) -> tuple[bool, JpegScanHdr]:
        """
            Parses the `scan_hdr` JPEG scan header structure members from `segment`.

        The caller must make sure there is enough data for the whole segment
        available.
        """

class LcevcMeta(GObject.GPointer, metaclass=GObject.GType):
    """
    LCEVC data for LCEVC codecs
    """

    # gi Fields
    enhancement_data: Gst.Buffer | None = ...  # type: ignore
    """
    the parsed LCEVC enhancement data
    """
    id: int = ...
    """
    the id of the LCEVC meta
    """
    meta: Gst.Meta | None = ...  # type: ignore
    """
    parent GstMeta
    """

    # gi Methods
    @staticmethod
    def get_info() -> Gst.MetaInfo:
        """
        Gets the GstMetaInfo of the LCEVC meta.
        """

class Mpeg4GroupOfVOP(GObject.GPointer, metaclass=GObject.GType):
    """
    The group of video object plane structure as defined in:
    6.2.4 Group of Video Object Plane
    """

    # gi Fields
    broken_link: int = ...
    closed: int = ...
    hours: int = ...
    minutes: int = ...
    seconds: int = ...

class Mpeg4Packet(GObject.GPointer, metaclass=GObject.GType):
    """
    A structure that contains the type of a packet, its offset and its size
    """

    # gi Fields
    data: int = ...
    """
    the data containing packet starting at `offset`
    """
    marker_size: int = ...
    """
    The size in bit of the resync marker.
    """
    offset: int = ...
    """
    offset of the start of the packet (without the 3 bytes startcode), but
    including the GstMpeg4StartCode byte.
    """
    size: int = ...
    """
    The size in bytes of the packet or G_MAXUINT if the end wasn't found.
    """
    type: Mpeg4StartCode = ...
    """
    the type of the packet that start at `offset`
    """

class Mpeg4SpriteTrajectory(GObject.GPointer, metaclass=GObject.GType):
    """
    The sprite trajectory structure as defined in:
    7.8.4 Sprite reference point decoding and
    6.2.5.4 Sprite coding
    """

    # gi Fields
    sprite_ref_points: list | None = ...
    vop_ref_points: list | None = ...

class Mpeg4VideoObjectLayer(GObject.GPointer, metaclass=GObject.GType):
    """
    The video object layer structure as defined in:
    6.2.3 Video Object Layer
    """

    # gi Fields
    aspect_ratio_info: Mpeg4AspectRatioInfo = ...
    bit_rate: int = ...
    bits_per_pixel: int = ...
    chroma_format: Mpeg4ChromaFormat = ...
    complexity_estimation_disable: int = ...
    composition_method: int = ...
    control_parameters: int = ...
    data_partitioned: int = ...
    enhancement_type: int = ...
    first_half_bitrate: int = ...
    first_half_vbv_buffer_size: int = ...
    first_half_vbv_occupancy: int = ...
    fixed_vop_rate: int = ...
    fixed_vop_time_increment: int = ...
    height: int = ...
    interlaced: int = ...
    intra_quant_mat: bytes | None = ...
    is_object_layer_identifier: int = ...
    latter_half_bitrate: int = ...
    latter_half_vbv_buffer_size: int = ...
    latter_half_vbv_occupancy: int = ...
    linear_composition: int = ...
    load_intra_quant_mat: int = ...
    load_non_intra_quant_mat: int = ...
    low_delay: int = ...
    low_latency_sprite_enable: int = ...
    newpred_enable: int = ...
    no_gray_quant_update: int = ...
    no_of_sprite_warping_points: int = ...
    non_intra_quant_mat: bytes | None = ...
    not_8_bit: int = ...
    obmc_disable: int = ...
    par_height: int = ...
    par_width: int = ...
    priority: int = ...
    quant_precision: int = ...
    quant_type: int = ...
    quarter_sample: int = ...
    random_accessible_vol: int = ...
    reduced_resolution_vop_enable: int = ...
    resync_marker_disable: int = ...
    reversible_vlc: int = ...
    sadct_disable: int = ...
    scalability: int = ...
    shape: Mpeg4VideoObjectLayerShape = ...
    shape_extension: int = ...
    short_hdr: Mpeg4VideoPlaneShortHdr | None = ...
    sprite_brightness_change: int = ...
    sprite_enable: Mpeg4SpriteEnable = ...
    sprite_height: int = ...
    sprite_left_coordinate: int = ...
    sprite_top_coordinate: int = ...
    sprite_warping_accuracy: int = ...
    sprite_width: int = ...
    vbv_buffer_size: int = ...
    vbv_parameters: int = ...
    verid: int = ...
    video_object_type_indication: int = ...
    vop_time_increment_bits: int = ...
    vop_time_increment_resolution: int = ...
    width: int = ...

class Mpeg4VideoObjectPlane(GObject.GPointer, metaclass=GObject.GType):
    """
    The Video object plane structure as defined in:
    6.2.5 Video Object Plane and Video Plane with Short Header
    """

    # gi Fields
    alternate_vertical_scan_flag: int = ...
    background_composition: int = ...
    change_conv_ratio_disable: int = ...
    coded: int = ...
    coding_type: Mpeg4VideoObjectCodingType = ...
    constant_alpha: int = ...
    constant_alpha_value: int = ...
    fcode_backward: int = ...
    fcode_forward: int = ...
    height: int = ...
    horizontal_mc_spatial_ref: int = ...
    id: int = ...
    id_for_prediction: int = ...
    id_for_prediction_indication: int = ...
    intra_dc_vlc_thr: int = ...
    load_backward_shape: int = ...
    mb_height: int = ...
    mb_num: int = ...
    mb_width: int = ...
    modulo_time_base: int = ...
    quant: int = ...
    reduced_resolution: int = ...
    ref_select_code: int = ...
    rounding_type: int = ...
    shape_coding_type: int = ...
    size: int = ...
    time_increment: int = ...
    top_field_first: int = ...
    vertical_mc_spatial_ref: int = ...
    width: int = ...

class Mpeg4VideoPacketHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    The video packet header structure as defined in:
    6.2.5.2 Video Plane with Short Header
    """

    # gi Fields
    header_extension_code: int = ...
    macroblock_number: int = ...
    quant_scale: int = ...
    size: int = ...
    """
    Size of the header in bit.
    """

class Mpeg4VideoPlaneShortHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    The video plane short header structure as defined in:
    6.2.5.2 Video Plane with Short Header
    """

    # gi Fields
    document_camera_indicator: int = ...
    full_picture_freeze_release: int = ...
    gob_frame_id: int = ...
    gob_header_empty: int = ...
    gob_number: int = ...
    num_gobs_in_vop: int = ...
    num_macroblocks_in_gob: int = ...
    pei: int = ...
    picture_coding_type: int = ...
    psupp: int = ...
    quant_scale: int = ...
    size: int = ...
    source_format: int = ...
    split_screen_indicator: int = ...
    temporal_reference: int = ...
    vop_height: int = ...
    vop_quant: int = ...
    vop_width: int = ...

class Mpeg4VideoSignalType(GObject.GPointer, metaclass=GObject.GType):
    """
    The video signal type structure as defined in:
    6.2.2 Visual Object Sequence and Visual Object.
    """

    # gi Fields
    color_description: int = ...
    color_primaries: int = ...
    format: int = ...
    matrix_coefficients: int = ...
    range: int = ...
    transfer_characteristics: int = ...
    type: int = ...

class Mpeg4VisualObject(GObject.GPointer, metaclass=GObject.GType):
    """
    The visual object structure as defined in:
    6.2.2 Visual Object Sequence and Visual Object
    """

    # gi Fields
    is_identifier: int = ...
    priority: int = ...
    type: Mpeg4VisualObjectType = ...
    verid: int = ...

class Mpeg4VisualObjectSequence(GObject.GPointer, metaclass=GObject.GType):
    """
    The visual object sequence structure as defined in:
    6.2.2 Visual Object Sequence and Visual Object
    """

    # gi Fields
    level: Mpeg4Level = ...
    profile: Mpeg4Profile = ...
    profile_and_level_indication: int = ...

class MpegVideoGop(GObject.GPointer, metaclass=GObject.GType):
    """
    The Mpeg Video Group of Picture structure.
    """

    # gi Fields
    broken_link: int = ...
    """
    Broken link
    """
    closed_gop: int = ...
    """
    Closed Gop
    """
    drop_frame_flag: int = ...
    """
    Drop Frame Flag
    """
    frame: int = ...
    """
    Frame (0-59)
    """
    hour: int = ...
    """
    Hour (0-23)
    """
    minute: int = ...
    """
    Minute (O-59)
    """
    second: int = ...
    """
    Second (0-59)
    """

class MpegVideoMeta(GObject.GPointer, metaclass=GObject.GType):
    """
    Extra buffer metadata describing the contents of a MPEG1/2 Video frame

    Can be used by elements (mainly decoders) to avoid having to parse
    Mpeg video 1/2 packets if it can be done upstream.

    The various fields are only valid during the lifetime of the GstMpegVideoMeta.
    If elements wish to use those for longer, they are required to make a copy.
    """

    # gi Fields
    meta: Gst.Meta | None = ...  # type: ignore
    """
    parent GstMeta
    """
    num_slices: int = ...
    picext: MpegVideoPictureExt | None = ...
    """
    the GstMpegVideoPictureExt if present in the buffer.
    """
    pichdr: MpegVideoPictureHdr | None = ...
    """
    the GstMpegVideoPictureHdr if present in the buffer.
    """
    quantext: MpegVideoQuantMatrixExt | None = ...
    """
    the GstMpegVideoQuantMatrixExt if present in the buffer
    """
    sequencedispext: MpegVideoSequenceDisplayExt | None = ...
    """
    the GstMpegVideoSequenceDisplayExt if present in the
    buffer.
    """
    sequenceext: MpegVideoSequenceExt | None = ...
    """
    the GstMpegVideoSequenceExt if present in the buffer
    """
    sequencehdr: MpegVideoSequenceHdr | None = ...
    """
    the GstMpegVideoSequenceHdr if present in the buffer
    """
    slice_offset: int = ...

    # gi Methods
    @staticmethod
    def get_info() -> Gst.MetaInfo: ...

class MpegVideoPacket(GObject.GPointer, metaclass=GObject.GType):
    """
    A structure that contains the type of a packet, its offset and its size
    """

    # gi Fields
    data: int = ...
    """
    the data containing the packet starting at `offset`
    """
    offset: int = ...
    """
    the offset of the packet start in bytes from `data`. This is the
        start of the packet itself without the sync code
    """
    size: int = ...
    """
    The size in bytes of the packet or -1 if the end wasn't found. This
        is the size of the packet itself without the sync code
    """
    type: int = ...
    """
    the type of the packet that start at `offset`, as a GstMpegVideoPacketTypeCode
    """

    # gi Methods
    def parse_gop(self) -> tuple[bool, MpegVideoGop]:
        """
            Parses the `gop` MPEG Video Group of Picture structure members from
        video `packet`
        """
    def parse_picture_extension(self) -> tuple[bool, MpegVideoPictureExt]:
        """
            Parse the `ext` MPEG Video Picture Extension structure members from
        video `packet`
        """
    def parse_picture_header(self) -> tuple[bool, MpegVideoPictureHdr]:
        """
            Parsers the `pichdr` MPEG Video Picture Header structure members
        from video `packet`
        """
    def parse_quant_matrix_extension(self) -> tuple[bool, MpegVideoQuantMatrixExt]:
        """
            Parses the `quant` MPEG Video Quantization Matrix Extension
        structure members from video `packet`
        """
    def parse_sequence_display_extension(self) -> tuple[bool, MpegVideoSequenceDisplayExt]:
        """
            Parses the `seqext` MPEG Video Sequence Display Extension structure
        members from video `packet`
        """
    def parse_sequence_extension(self) -> tuple[bool, MpegVideoSequenceExt]:
        """
            Parses the `seqext` MPEG Video Sequence Extension structure members
        from video `packet`
        """
    def parse_sequence_header(self) -> tuple[bool, MpegVideoSequenceHdr]:
        """
            Parses the `seqhdr` MPEG Video Sequence Header structure members
        from video `packet`
        """
    def parse_sequence_scalable_extension(self) -> tuple[bool, MpegVideoSequenceScalableExt]:
        """
            Parses the `seqscaleext` MPEG Video Sequence Scalable Extension structure
        members from video `packet`
        """
    def parse_slice_header(
        self, seq_hdr: MpegVideoSequenceHdr, seqscaleext: MpegVideoSequenceScalableExt
    ) -> tuple[bool, MpegVideoSliceHdr]:
        """
        Parses the `GstMpegVideoSliceHdr`  structure members from `data`
        """

class MpegVideoPictureExt(GObject.GPointer, metaclass=GObject.GType):
    """
    The Mpeg2 Video Picture Extension structure.
    """

    # gi Fields
    alternate_scan: int = ...
    """
    Alternate Scan
    """
    burst_amplitude: int = ...
    chroma_420_type: int = ...
    """
    Chroma 420 Type
    """
    composite_display: int = ...
    concealment_motion_vectors: int = ...
    """
    Concealment Motion Vectors
    """
    f_code: bytes | None = ...
    field_sequence: int = ...
    frame_pred_frame_dct: int = ...
    """
    Frame
    """
    intra_dc_precision: int = ...
    """
    Intra DC precision
    """
    intra_vlc_format: int = ...
    """
    Intra Vlc Format
    """
    picture_structure: int = ...
    """
    Structure of the picture
    """
    progressive_frame: int = ...
    """
    True if the frame is progressive False otherwise
    """
    q_scale_type: int = ...
    """
    Q Scale Type
    """
    repeat_first_field: int = ...
    """
    Repeat First Field
    """
    sub_carrier: int = ...
    sub_carrier_phase: int = ...
    top_field_first: int = ...
    """
    Top field first
    """
    v_axis: int = ...

class MpegVideoPictureHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    The Mpeg2 Video Picture Header structure.
    """

    # gi Fields
    f_code: bytes | None = ...
    """
    F code
    """
    full_pel_backward_vector: int = ...
    """
    the full pel backward flag
     of the frame: 0 or 1.
    """
    full_pel_forward_vector: int = ...
    """
    the full pel forward flag of
     the frame: 0 or 1.
    """
    pic_type: int = ...
    """
    Type of the frame
    """
    tsn: int = ...
    """
    Temporal Sequence Number
    """
    vbv_delay: int = ...

class MpegVideoQuantMatrixExt(GObject.GPointer, metaclass=GObject.GType):
    """
    The Quant Matrix Extension structure that exposes quantization
    matrices in zigzag scan order. i.e. the original encoded scan
    order.
    """

    # gi Fields
    chroma_intra_quantiser_matrix: bytes | None = ...
    chroma_non_intra_quantiser_matrix: bytes | None = ...
    intra_quantiser_matrix: bytes | None = ...
    load_chroma_intra_quantiser_matrix: int = ...
    load_chroma_non_intra_quantiser_matrix: int = ...
    load_intra_quantiser_matrix: int = ...
    load_non_intra_quantiser_matrix: int = ...
    non_intra_quantiser_matrix: bytes | None = ...

class MpegVideoSequenceDisplayExt(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    colour_description_flag: int = ...
    """
    True if colour information was provided
    """
    colour_primaries: int = ...
    """
    Valid if colour_description_flag is set
    """
    display_horizontal_size: int = ...
    """
    width of decoded frame sub-region to display
    """
    display_vertical_size: int = ...
    """
    height of decoded frame sub-region to display
    """
    matrix_coefficients: int = ...
    """
    Valid if colour_description_flag is set
    """
    transfer_characteristics: int = ...
    """
    Valid if colour_description_flag is set
    """
    video_format: int = ...
    """
    3-bit video_format field indicating PAL/NTSC etc.
    """

class MpegVideoSequenceExt(GObject.GPointer, metaclass=GObject.GType):
    """
    The Mpeg2 Video Sequence Extension structure.
    """

    # gi Fields
    bitrate_ext: int = ...
    """
    The bitrate
    """
    chroma_format: int = ...
    """
    indicates the chrominance format
    """
    fps_d_ext: int = ...
    """
    Framerate denominator code
    """
    fps_n_ext: int = ...
    """
    Framerate nominator code
    """
    horiz_size_ext: int = ...
    """
    Horizontal size
    """
    level: int = ...
    """
    mpeg2 decoder level
    """
    low_delay: int = ...
    """
    True if the sequence doesn't contain any B-pictures, False
    otherwise
    """
    profile: int = ...
    """
    mpeg2 decoder profile
    """
    profile_level_escape_bit: int = ...
    """
    Escape bit. If set, the meaning of the
       `profile` and `level` fields is different.
    """
    progressive: int = ...
    """
    True if the frames are progressive False otherwise
    """
    vbv_buffer_size_extension: int = ...
    """
    VBV buffer size
    """
    vert_size_ext: int = ...
    """
    Vertical size
    """

class MpegVideoSequenceHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    The Mpeg2 Video Sequence Header structure.
    """

    # gi Fields
    aspect_ratio_info: int = ...
    bitrate: int = ...
    """
    the real bitrate of the Mpeg video stream in bits per second, 0 if VBR stream
    """
    bitrate_value: int = ...
    """
    Value of the bitrate as is in the stream (400bps unit)
    """
    constrained_parameters_flag: int = ...
    """
    True if this stream uses constrained parameters.
    """
    fps_d: int = ...
    """
    Calculated Framerate denominator
    """
    fps_n: int = ...
    """
    Calculated Framrate nominator
    """
    frame_rate_code: int = ...
    height: int = ...
    """
    Height of each frame
    """
    intra_quantizer_matrix: bytes | None = ...
    """
    intra-quantization table, in zigzag scan order
    """
    load_intra_quantiser_matrix: int = ...
    """
    True indicates the presence of intra_quantiser_matrix
    """
    load_non_intra_quantiser_matrix: int = ...
    """
    True indicates the presence of non_intra_quantiser_matrix
    """
    non_intra_quantizer_matrix: bytes | None = ...
    """
    non-intra quantization table, in zigzag scan order
    """
    par_h: int = ...
    """
    Calculated Pixel Aspect Ratio height
    """
    par_w: int = ...
    """
    Calculated Pixel Aspect Ratio width
    """
    vbv_buffer_size_value: int = ...
    width: int = ...
    """
    Width of each frame
    """

class MpegVideoSequenceScalableExt(GObject.GPointer, metaclass=GObject.GType):
    """
    The Sequence Scalable Extension structure.
    """

    # gi Fields
    horizontal_subsampling_factor_m: int = ...
    horizontal_subsampling_factor_n: int = ...
    layer_id: int = ...
    lower_layer_prediction_horizontal_size: int = ...
    lower_layer_prediction_vertical_size: int = ...
    mux_to_progressive_sequence: int = ...
    picture_mux_enable: int = ...
    picture_mux_factor: int = ...
    picture_mux_order: int = ...
    scalable_mode: int = ...
    vertical_subsampling_factor_m: int = ...
    vertical_subsampling_factor_n: int = ...

class MpegVideoSliceHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    The Mpeg2 Video Slice Header structure.
    """

    # gi Fields
    header_size: int = ...
    intra_slice: int = ...
    """
    Equal to one if all the macroblocks are intra macro blocks.
    """
    mb_column: int = ...
    mb_row: int = ...
    priority_breakpoint: int = ...
    """
    Point where the bitstream shall be partitioned
    """
    quantiser_scale_code: int = ...
    """
    Quantiser value (range: 1-31)
    """
    slice_ext_flag: int = ...
    """
    Slice Extension flag
    """
    slice_picture_id: int = ...
    """
    Intended to aid recovery on severe bursts of
      errors for certain types of applications
    """
    slice_picture_id_enable: int = ...
    """
    controls the semantics of slice_picture_id
    """
    vertical_position: int = ...
    """
    slice vertical position
    """
    vertical_position_ext: int = ...

class VC1AdvancedSeqHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure for the advanced profile sequence headers specific parameters.
    """

    # gi Fields
    aspect_horiz_size: int = ...
    aspect_ratio: int = ...
    aspect_ratio_flag: int = ...
    aspect_vert_size: int = ...
    bitrate: int = ...
    bitrtq_postproc: int = ...
    color_format_flag: int = ...
    color_prim: int = ...
    colordiff_format: int = ...
    disp_horiz_size: int = ...
    disp_vert_size: int = ...
    display_ext: int = ...
    entrypoint: VC1EntryPointHdr | None = ...
    finterpflag: int = ...
    fps_d: int = ...
    fps_n: int = ...
    framerate: int = ...
    framerate_flag: int = ...
    frameratedr: int = ...
    framerateexp: int = ...
    framerateind: int = ...
    frameratenr: int = ...
    frmrtq_postproc: int = ...
    hrd_param: VC1HrdParam | None = ...
    hrd_param_flag: int = ...
    interlace: int = ...
    level: VC1Level = ...
    matrix_coef: int = ...
    max_coded_height: int = ...
    max_coded_width: int = ...
    par_d: int = ...
    par_n: int = ...
    postprocflag: int = ...
    psf: int = ...
    pulldown: int = ...
    tfcntrflag: int = ...
    transfer_char: int = ...

class VC1BDU(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure that represents a Bitstream Data Unit.
    """

    # gi Fields
    data: int = ...
    offset: int = ...
    sc_offset: int = ...
    size: int = ...
    type: VC1StartCode = ...

class VC1BitPlanes(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    acpred: int = ...
    directmb: int = ...
    fieldtx: int = ...
    forwardmb: int = ...
    mvtypemb: int = ...
    overflags: int = ...
    size: int = ...
    skipmb: int = ...

class VC1EntryPointHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure for entrypoint header, this will be used only in advanced profiles
    """

    # gi Fields
    broken_link: int = ...
    closed_entry: int = ...
    coded_height: int = ...
    coded_size_flag: int = ...
    coded_width: int = ...
    dquant: int = ...
    extended_dmv: int = ...
    extended_mv: int = ...
    fastuvmc: int = ...
    hrd_full: bytes | None = ...
    loopfilter: int = ...
    overlap: int = ...
    panscan_flag: int = ...
    quantizer: int = ...
    range_mapuv: int = ...
    range_mapuv_flag: int = ...
    range_mapy: int = ...
    range_mapy_flag: int = ...
    refdist_flag: int = ...
    vstransform: int = ...

class VC1FrameHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure that represent picture in any profile or mode.
    You should look at `ptype` and `profile` to know what is currently
    in use.
    """

    # gi Fields
    dquant: int = ...
    halfqp: int = ...
    header_size: int = ...
    interpfrm: int = ...
    pqindex: int = ...
    pquant: int = ...
    pquantizer: int = ...
    profile: int = ...
    ptype: VC1PictureType = ...
    transacfrm: int = ...
    transdctab: int = ...
    vopdquant: VC1VopDquant | None = ...

class VC1FrameLayer(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    framesize: int = ...
    key: int = ...
    next_framelayer_offset: int = ...
    skiped_p_frame: int = ...
    timestamp: int = ...

class VC1HrdParam(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    bit_rate_exponent: int = ...
    buffer_size_exponent: int = ...
    hrd_buffer: list | None = ...
    hrd_num_leaky_buckets: int = ...
    hrd_rate: list | None = ...

class VC1PicAdvanced(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    acpred: int = ...
    bf: int = ...
    bfraction: int = ...
    cbptab: int = ...
    condover: int = ...
    directmb: int = ...
    dmvrange: int = ...
    fcm: VC1FrameCodingMode = ...
    fieldtx: int = ...
    forwardmb: int = ...
    fptype: int = ...
    icbptab: int = ...
    imvtab: int = ...
    intcomp: int = ...
    intcompfield: int = ...
    lumscale: int = ...
    lumscale2: int = ...
    lumshift: int = ...
    lumshift2: int = ...
    mbmodetab: int = ...
    mvbptab2: int = ...
    mvbptab4: int = ...
    mvmode: int = ...
    mvmode2: int = ...
    mvrange: int = ...
    mvswitch4: int = ...
    mvtab: int = ...
    mvtypemb: int = ...
    numref: int = ...
    overflags: int = ...
    postproc: int = ...
    ps_height: int = ...
    ps_hoffset: int = ...
    ps_present: int = ...
    ps_voffset: int = ...
    ps_width: int = ...
    refdist: int = ...
    reffield: int = ...
    rff: int = ...
    rndctrl: int = ...
    rptfrm: int = ...
    skipmb: int = ...
    tfcntr: int = ...
    tff: int = ...
    transacfrm2: int = ...
    ttfrm: int = ...
    ttmbf: int = ...
    uvsamp: int = ...

class VC1PicSimpleMain(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    bf: int = ...
    bfraction: int = ...
    cbptab: int = ...
    directmb: int = ...
    frmcnt: int = ...
    lumscale: int = ...
    lumshift: int = ...
    mvmode: int = ...
    mvmode2: int = ...
    mvrange: int = ...
    mvtab: int = ...
    mvtypemb: int = ...
    rangeredfrm: int = ...
    respic: int = ...
    skipmb: int = ...
    transacfrm2: int = ...
    ttfrm: int = ...
    ttmbf: int = ...

class VC1Picture(GObject.GPointer, metaclass=GObject.GType): ...

class VC1SeqHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure for sequence headers in any profile.
    """

    # gi Fields
    advanced: VC1AdvancedSeqHdr | None = ...
    mb_height: int = ...
    mb_stride: int = ...
    mb_width: int = ...
    profile: VC1Profile = ...
    struct_c: VC1SeqStructC | None = ...

class VC1SeqLayer(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    numframes: int = ...
    struct_a: VC1SeqStructA | None = ...
    struct_b: VC1SeqStructB | None = ...
    struct_c: VC1SeqStructC | None = ...

class VC1SeqStructA(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    horiz_size: int = ...
    vert_size: int = ...

class VC1SeqStructB(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    cbr: int = ...
    framerate: int = ...
    hrd_buffer: int = ...
    hrd_rate: int = ...
    level: VC1Level = ...

class VC1SeqStructC(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    bitrate: int = ...
    bitrtq_postproc: int = ...
    coded_height: int = ...
    coded_width: int = ...
    dquant: int = ...
    extended_mv: int = ...
    fastuvmc: int = ...
    finterpflag: int = ...
    framerate: int = ...
    frmrtq_postproc: int = ...
    loop_filter: int = ...
    maxbframes: int = ...
    multires: int = ...
    overlap: int = ...
    profile: VC1Profile = ...
    quantizer: int = ...
    rangered: int = ...
    res_sprite: int = ...
    slice_code: int = ...
    syncmarker: int = ...
    vstransform: int = ...
    wmvp: int = ...

class VC1SliceHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    Structure that represents slice layer in advanced profile.
    """

    # gi Fields
    header_size: int = ...
    slice_addr: int = ...

class VC1VopDquant(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    abspq: int = ...
    altpquant: int = ...
    dqbedge: int = ...
    dqbilevel: int = ...
    dqprofile: int = ...
    dquantfrm: int = ...
    pqdiff: int = ...
    unused: int = ...

class Vp8FrameHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    Frame header.
    """

    # gi Fields
    clamping_type: int = ...
    """
    specifies if the decoder is required to clamp the
      reconstructed pixel values
    """
    color_space: int = ...
    """
    defines the YUV color space of the sequence
    """
    copy_buffer_to_alternate: int = ...
    """
    determines if the alternate reference is
      replaced by another reference
    """
    copy_buffer_to_golden: int = ...
    """
    determines if the golden reference is
      replaced by another reference
    """
    data_chunk_size: int = ...
    """
    the size in bytes of the Uncompressed Data Chunk
    """
    filter_type: int = ...
    """
    determines whether the normal or the simple loop
      filter is used
    """
    first_part_size: int = ...
    """
    the size in bytes of the first partition (control
      partition), excluding the uncompressed data chunk
    """
    header_size: int = ...
    """
    the size in bits of the Frame Header, thus excluding
      any Uncompressed Data Chunk bytes
    """
    height: int = ...
    """
    the frame height in pixels
    """
    horiz_scale_code: int = ...
    """
    horizontal scale code value
    """
    key_frame: int = ...
    """
    indicates whether the frame is a key frame (1), or an
      inter frame (0)
    """
    log2_nbr_of_dct_partitions: int = ...
    """
    determines the number of separate
      partitions containing the DCT coefficients of the macroblocks
    """
    loop_filter_level: int = ...
    """
    controls the deblocking filter
    """
    mb_no_skip_coeff: int = ...
    """
    enables (0) or disables (1) the skipping of
      macroblocks containing no non-zero coefficients
    """
    mode_probs: Vp8ModeProbs | None = ...
    """
    probabilities used for intra-prediction mode decoding
      tree (see GstVp8ModeProbs)
    """
    mv_probs: Vp8MvProbs | None = ...
    """
    probabilities used for motion vector decoding
      (see GstVp8MvProbs)
    """
    partition_size: list | None = ...
    """
    determines the size of each separate partition
      containing the DCT coefficients of the macroblocks, including the
      very last one (calculated size)
    """
    prob_gf: int = ...
    """
    indicates the probability that the golden reference frame
      is used for inter-prediction
    """
    prob_intra: int = ...
    """
    indicates the probability of an intra macroblock
    """
    prob_last: int = ...
    """
    indicates the probability that the last reference frame
      is used for inter-prediction
    """
    prob_skip_false: int = ...
    """
    indicates the probability that the macroblock is
      not skipped
    """
    quant_indices: Vp8QuantIndices | None = ...
    """
    dequantization indices (see GstVp8QuantIndices)
    """
    rd_count: int = ...
    """
    number of bits left in range decoder "Value" (`rd_value`)
    """
    rd_range: int = ...
    """
    last range decoder value for "Range"
    """
    rd_value: int = ...
    """
    last range decoder value for "Value"
    """
    refresh_alternate_frame: int = ...
    """
    determines if the current decoded frame
      refreshes the alternate reference frame
    """
    refresh_entropy_probs: int = ...
    """
    determines whether updated token
      probabilities are used only for this frame or until further update
    """
    refresh_golden_frame: int = ...
    """
    determines if the current decoded frame
      refreshes the golden frame
    """
    refresh_last: int = ...
    """
    determines if the current decoded frame refreshes
      the last frame reference buffer
    """
    sharpness_level: int = ...
    """
    controls the deblocking filter
    """
    show_frame: int = ...
    """
    indicates whether the frame is meant to be displayed (1),
      or not (0)
    """
    sign_bias_alternate: int = ...
    """
    controls the sign of motion vectors when the
      alternate frame is referenced
    """
    sign_bias_golden: int = ...
    """
    controls the sign of motion vectors when the
      golden frame is referenced
    """
    token_probs: Vp8TokenProbs | None = ...
    """
    token probabilities (see GstVp8TokenProbs)
    """
    version: int = ...
    """
    version number
    """
    vert_scale_code: int = ...
    """
    vertical scale code value
    """
    width: int = ...
    """
    the frame width in pixels
    """

class Vp8MbLfAdjustments(GObject.GPointer, metaclass=GObject.GType):
    """
    MB-level loop filter adjustments.
    """

    # gi Fields
    loop_filter_adj_enable: int = ...
    """
    indicates if the MB-level loop filter
      adjustment is on for the current frame
    """
    mb_mode_delta: list | None = ...
    """
    indicates the adjustment delta value corresponding
      to a certain MB prediction mode
    """
    mode_ref_lf_delta_update: int = ...
    """
    indicates if the delta values used in an
      adjustment are updated in the current frame
    """
    ref_frame_delta: list | None = ...
    """
    indicates the adjustment delta value
      corresponding to a certain used reference frame
    """

class Vp8ModeProbs(GObject.GPointer, metaclass=GObject.GType):
    """
    Probabilities used for intra-prediction mode decoding tree.

    Each probability value in thie structure is live across frames,
    until they are reset to their default values on key frame.
    """

    # gi Fields
    uv_prob: bytes | None = ...
    """
    indicates the branch probabilities of the chroma
      intra-prediction mode decoding tree
    """
    y_prob: bytes | None = ...
    """
    indicates the branch probabilities of the luma
      intra-prediction mode decoding tree
    """

class Vp8MvProbs(GObject.GPointer, metaclass=GObject.GType):
    """
    Probabilities used for motion vector decoding, with cumulative
    updates applied.

    Each probability value in this matrix is live across frames, until
    they are reset to their default values on key frame.
    """

    # gi Fields
    prob: bytes | None = ...
    """
    MV probability
    """

class Vp8Parser(GObject.GPointer, metaclass=GObject.GType):
    """
    For more details about the structures, you can refer to the
    specifications: VP8-rfc6386.pdf
    """

    # gi Fields
    mb_lf_adjust: Vp8MbLfAdjustments | None = ...
    """
    MB-level loop filter adjustments
    """
    mode_probs: Vp8ModeProbs | None = ...
    """
    probabilities used for intra-prediction mode decoding tree.
    """
    mv_probs: Vp8MvProbs | None = ...
    """
    probabilities used for motion vector decoding
    """
    segmentation: Vp8Segmentation | None = ...
    """
    segmentation feature data
    """
    token_probs: Vp8TokenProbs | None = ...
    """
    token probabilities
    """

    # gi Methods
    def init(self) -> None:
        """
        Initializes the supplied `parser` structure with its default values.
        """
    def parse_frame_header(self, frame_hdr: Vp8FrameHdr, data: int, size: int) -> Vp8ParserResult:
        """
            Parses the VP8 bitstream contained in `data`, and fills in `frame_hdr`
        with the information. The supplied `data` shall point to a complete
        frame since there is no sync code specified for VP8 bitstreams. Thus,
        the `size` argument shall represent the whole frame size.
        """

class Vp8QuantIndices(GObject.GPointer, metaclass=GObject.GType):
    # gi Fields
    uv_ac_delta: int = ...
    uv_dc_delta: int = ...
    y2_ac_delta: int = ...
    y2_dc_delta: int = ...
    y_ac_qi: int = ...
    y_dc_delta: int = ...

class Vp8RangeDecoder(GObject.GPointer, metaclass=GObject.GType):
    """
    Range decoder.
    """

    # gi Fields
    @builtins.property
    def _gst_reserved(self) -> list | None: ...
    buf: int = ...
    """
    the original bitstream buffer start
    """
    buf_size: int = ...
    """
    the original bitstream buffer size
    """

    # gi Methods
    def get_pos(self) -> int: ...
    def get_state(self, state: Vp8RangeDecoderState) -> None: ...
    def init(self, buf: int, buf_size: int) -> bool: ...
    def read(self, prob: int) -> int: ...
    def read_literal(self, bits: int) -> int: ...

class Vp8RangeDecoderState(GObject.GPointer, metaclass=GObject.GType):
    """
    Range decoder state.
    """

    # gi Fields
    count: int = ...
    """
    number of bits left in "Value" for decoding
    """
    range: int = ...
    """
    current "Range" value
    """
    value: int = ...
    """
    current "Value" value
    """

class Vp8Segmentation(GObject.GPointer, metaclass=GObject.GType):
    """
    Segmentation feature data.
    """

    # gi Fields
    lf_update_value: list | None = ...
    """
    indicates the update value for the loop filter level
    """
    quantizer_update_value: list | None = ...
    """
    indicates the update value for the segment
      quantizer
    """
    segment_feature_mode: int = ...
    """
    indicates the feature data update mode (0:
      delta, 1: absolute value)
    """
    segment_prob: bytes | None = ...
    """
    indicates the branch probabilities of the segment_id
      decoding tree
    """
    segmentation_enabled: int = ...
    """
    enables the segmentation feature for the
      current frame
    """
    update_mb_segmentation_map: int = ...
    """
    determines if the MB segmentation map
      is updated in the current frame
    """
    update_segment_feature_data: int = ...
    """
    indicates if the segment feature data
      is updated in the current frame
    """

class Vp8TokenProbs(GObject.GPointer, metaclass=GObject.GType):
    """
    Token probabilities, with cumulative updates applied.

    Each probability value in this matrix is live across frames, until
    they are reset to their default values on key frame.
    """

    # gi Fields
    prob: bytes | None = ...
    """
    token probability
    """

class Vp9FrameHdr(GObject.GPointer, metaclass=GObject.GType):
    """
    Frame header
    """

    # gi Fields
    allow_high_precision_mv: int = ...
    """
    allow hight precision motion vector
    """
    display_height: int = ...
    """
    display height
    """
    display_size_enabled: int = ...
    """
    display size enabled (cropping)
    """
    display_width: int = ...
    """
    display width
    """
    error_resilient_mode: int = ...
    """
    error resilent mode
    """
    first_partition_size: int = ...
    """
    first partition size (after the uncompressed header)
    """
    frame_context_idx: int = ...
    """
    frame context index
    """
    frame_header_length_in_bytes: int = ...
    """
    length of uncompressed header
    """
    frame_parallel_decoding_mode: int = ...
    """
    enable or disable parallel decoding support.
    """
    frame_to_show: int = ...
    """
    which frame to show if show_existing_frame is true
    """
    frame_type: int = ...
    """
    frame type
    """
    height: int = ...
    """
    frame height
    """
    intra_only: int = ...
    """
    intra only frame
    """
    log2_tile_columns: int = ...
    """
    tile column indicator
    """
    log2_tile_rows: int = ...
    """
    tile row indicator
    """
    loopfilter: Vp9LoopFilter | None = ...
    """
    loopfilter values
    """
    lossless_flag: int = ...
    """
    lossless mode decode
    """
    mcomp_filter_type: int = ...
    """
    interpolation filter type
    """
    profile: int = ...
    """
    encoded profile
    """
    quant_indices: Vp9QuantIndices | None = ...
    """
    quantization indices
    """
    ref_frame_indices: list | None = ...
    """
    reference frame index
    """
    ref_frame_sign_bias: list | None = ...
    """
    sign bias for selecting altref,last and golden frames
    """
    refresh_frame_context: int = ...
    """
    refresh frame context indicator
    """
    refresh_frame_flags: int = ...
    """
    refresh reference frame flags
    """
    reset_frame_context: int = ...
    """
    reset frame context
    """
    segmentation: Vp9SegmentationInfo | None = ...
    """
    segmentation info
    """
    show_existing_frame: int = ...
    """
    display already decoded frame instead of doing the decoding
    """
    show_frame: int = ...
    """
    indicate whether it is a displayable frame or not
    """
    width: int = ...
    """
    frame width
    """

class Vp9LoopFilter(GObject.GPointer, metaclass=GObject.GType):
    """
    Loop filter values
    """

    # gi Fields
    filter_level: int = ...
    """
    indicates loop filter level for the current frame
    """
    mode_deltas: list | None = ...
    """
    Loop filter strength adjustments based on
      mode (zero, new mv)
    """
    mode_ref_delta_enabled: int = ...
    """
    indicate if filter adjust is on
    """
    mode_ref_delta_update: int = ...
    """
    indicates if the delta values used in an
      adjustment are updated in the current frame
    """
    ref_deltas: list | None = ...
    """
    Loop filter strength adjustments based on
     frame type (intra, inter)
    """
    sharpness_level: int = ...
    """
    indicates sharpness level for thecurrent frame
    """
    update_mode_deltas: bytes | None = ...
    """
    indicate with mode deltas are updated
    """
    update_ref_deltas: bytes | None = ...
    """
    indicate which ref deltas are updated
    """

class Vp9Parser(GObject.GPointer, metaclass=GObject.GType):
    """
    For more details about the structures, you can refer to the
    specifications:
    """

    # gi Fields
    bit_depth: int = ...
    """
    bit depth of the stream
    """
    color_range: int = ...
    """
    color range standard
    """
    color_space: int = ...
    """
    color space standard
    """
    mb_segment_tree_probs: bytes | None = ...
    """
    decoding tree probabilities
    """
    priv: object | None = ...
    """
    GstVp9ParserPrivate struct to keep track of state variables
    """
    segment_pred_probs: bytes | None = ...
    """
    segment prediction probabiilties
    """
    segmentation: list | None = ...
    """
    Segmentation info
    """
    subsampling_x: int = ...
    """
    horizontal subsampling
    """
    subsampling_y: int = ...
    """
    vertical subsampling
    """

    # gi Methods
    def free(self) -> None:
        """
        Frees `parser`.
        """
    def parse_frame_header(self, frame_hdr: Vp9FrameHdr, data: int, size: int) -> Vp9ParserResult:
        """
            Parses the VP9 bitstream contained in `data`, and fills in `frame_hdr`
        with the information. The `size` argument represent the whole frame size.
        """
    def parse_superframe_info(self, superframe_info: Vp9SuperframeInfo, data: int, size: int) -> Vp9ParserResult:
        """
            Parses the VP9 bitstream contained in `data`, and fills in `superframe_info`
        with the information. The `size` argument represent the whole superframe size.
        If `data` is not superframe but normal frame, the parser returns
        GST_VP9_PARSER_OK, frame_size[0] is set to `size` and frames_in_superframe is
        set to 1. Also this method does not validate vp9frame header and verifying
        the frame header is caller's responsibility.
        """

class Vp9QuantIndices(GObject.GPointer, metaclass=GObject.GType):
    """
    Dequantization indices.
    """

    # gi Fields
    uv_ac_delta: int = ...
    """
    indicates the delta value that is added to the
      baseline index to obtain the chroma AC coefficient dequantization
      index
    """
    uv_dc_delta: int = ...
    """
    indicates the delta value that is added to the
      baseline index to obtain the chroma DC coefficient dequantization
      index
    """
    y_ac_qi: int = ...
    """
    indicates the dequantization table index used for the
      luma AC coefficients
    """
    y_dc_delta: int = ...
    """
    indicates the delta value that is added to the
      baseline index to obtain the luma DC coefficient dequantization
      index
    """

class Vp9Segmentation(GObject.GPointer, metaclass=GObject.GType):
    """
    Segmentation info kept across multiple frames
    """

    # gi Fields
    chroma_ac_quant_scale: int = ...
    chroma_dc_quant_scale: int = ...
    """
    DC quant scale for chroma (U/V) component
    """
    filter_level: bytes | None = ...
    """
    loop filter level
    """
    luma_ac_quant_scale: int = ...
    """
    AC quant scale for luma(Y) component
    """
    luma_dc_quant_scale: int = ...
    """
    DC quant scale for luma(Y) component
    `chroma_ac_quant_scale` AC quant scale for chroma(U/V) component
    """
    reference_frame: int = ...
    """
    alternate reference frame
    """
    reference_frame_enabled: int = ...
    """
    alternate reference frame enablement
    """
    reference_skip: int = ...
    """
    a block skip mode that implies both the use of a (0,0)
      motion vector and that no residual will be coded
    """

class Vp9SegmentationInfo(GObject.GPointer, metaclass=GObject.GType):
    """
    Segmentation info
    """

    # gi Fields
    abs_delta: int = ...
    """
    interpretation of segment data values
    """
    data: list | None = ...
    """
    segment feature data
    """
    enabled: int = ...
    """
    enables the segmentation feature for the current frame
    """
    pred_probs: bytes | None = ...
    """
    prediction probabilities
    """
    temporal_update: int = ...
    """
    type of map update
    """
    tree_probs: bytes | None = ...
    """
    segment tree probabilities
    """
    update_data: int = ...
    """
    indicates if the segment feature data
      is updated in the current frame
    """
    update_map: int = ...
    """
    determines if segmentation is updated in the current frame
    """
    update_pred_probs: bytes | None = ...
    """
    determines if prediction probabilities updated or not
    """
    update_tree_probs: bytes | None = ...
    """
    determines if tree probabilities updated or not
    """

class Vp9SegmentationInfoData(GObject.GPointer, metaclass=GObject.GType):
    """
    Segmentation info for each segment
    """

    # gi Fields
    alternate_loop_filter: int = ...
    """
    alternate loop filter
    """
    alternate_loop_filter_enabled: int = ...
    """
    indicate alternate loop filter enabled at segment level
    """
    alternate_quantizer: int = ...
    """
    alternate quantizer value
    """
    alternate_quantizer_enabled: int = ...
    """
    indicate alternate quantizer enabled at segment level
    """
    reference_frame: int = ...
    """
    alternate reference frame
    """
    reference_frame_enabled: int = ...
    """
    indicate alternate reference frame at segment level
    """
    reference_skip: int = ...
    """
    a block skip mode that implies both the use of a (0,0)
      motion vector and that no residual will be coded.
    """

class Vp9SuperframeInfo(GObject.GPointer, metaclass=GObject.GType):
    """
    Superframe info
    """

    # gi Fields
    bytes_per_framesize: int = ...
    """
    indicates the number of bytes needed to code each frame size
    """
    frame_sizes: list | None = ...
    """
    specifies the size in bytes of frame number i (zero indexed) within this superframe
    """
    frames_in_superframe: int = ...
    """
    indicates the number of frames within this superframe
    """
    superframe_index_size: int = ...
    """
    indicates the total size of the superframe_index
    """

###############################################################
# Aliases
###############################################################

_lock = _thread._lock  # type: ignore
###############################################################
# Constants
###############################################################

AV1_CDEF_MAX: int = ...
AV1_DIV_LUT_BITS: int = ...
AV1_DIV_LUT_NUM: int = ...
AV1_DIV_LUT_PREC_BITS: int = ...
AV1_GM_ABS_ALPHA_BITS: int = ...
AV1_GM_ABS_TRANS_BITS: int = ...
AV1_GM_ABS_TRANS_ONLY_BITS: int = ...
AV1_GM_ALPHA_PREC_BITS: int = ...
AV1_GM_TRANS_ONLY_PREC_BITS: int = ...
AV1_GM_TRANS_PREC_BITS: int = ...
AV1_MAX_LOOP_FILTER: int = ...
AV1_MAX_NUM_CB_POINTS: int = ...
AV1_MAX_NUM_CR_POINTS: int = ...
AV1_MAX_NUM_PLANES: int = ...
AV1_MAX_NUM_POS_LUMA: int = ...
AV1_MAX_NUM_SPATIAL_LAYERS: int = ...
AV1_MAX_NUM_TEMPORAL_LAYERS: int = ...
AV1_MAX_NUM_Y_POINTS: int = ...
AV1_MAX_OPERATING_POINTS: int = ...
AV1_MAX_SEGMENTS: int = ...
AV1_MAX_TEMPORAL_GROUP_REFERENCES: int = ...
AV1_MAX_TEMPORAL_GROUP_SIZE: int = ...
AV1_MAX_TILE_AREA: int = ...
AV1_MAX_TILE_COLS: int = ...
AV1_MAX_TILE_COUNT: int = ...
AV1_MAX_TILE_ROWS: int = ...
AV1_MAX_TILE_WIDTH: int = ...
AV1_PRIMARY_REF_NONE: int = ...
AV1_REFS_PER_FRAME: int = ...
AV1_RESTORATION_TILESIZE_MAX: int = ...
AV1_SEG_LVL_ALT_Q: int = ...
AV1_SEG_LVL_MAX: int = ...
AV1_SEG_LVL_REF_FRAME: int = ...
AV1_SELECT_INTEGER_MV: int = ...
AV1_SELECT_SCREEN_CONTENT_TOOLS: int = ...
AV1_SUPERRES_DENOM_BITS: int = ...
AV1_SUPERRES_DENOM_MIN: int = ...
AV1_SUPERRES_NUM: int = ...
AV1_TOTAL_REFS_PER_FRAME: int = ...
AV1_WARPEDMODEL_PREC_BITS: int = ...
AV1_WARP_PARAM_REDUCE_BITS: int = ...
CAPS_FEATURE_META_GST_LCEVC_META: str = ...
H264_MAX_PPS_COUNT: int = ...
H264_MAX_SPS_COUNT: int = ...
H264_MAX_VIEW_COUNT: int = ...
H264_MAX_VIEW_ID: int = ...
H265_MAX_PPS_COUNT: int = ...
H265_MAX_SPS_COUNT: int = ...
H265_MAX_SUB_LAYERS: int = ...
H265_MAX_VPS_COUNT: int = ...
H265_RESERVED_IRAP_NAL_TYPE_MAX: int = ...
H265_RESERVED_IRAP_NAL_TYPE_MIN: int = ...
H265_RESERVED_NON_IRAP_NAL_TYPE_MAX: int = ...
H265_RESERVED_NON_IRAP_NAL_TYPE_MIN: int = ...
H265_RESERVED_NON_IRAP_SUBLAYER_NAL_TYPE_MAX: int = ...
H265_RESERVED_NON_IRAP_SUBLAYER_NAL_TYPE_MIN: int = ...
H265_RESERVED_NON_VCL_NAL_TYPE_MAX: int = ...
H265_RESERVED_NON_VCL_NAL_TYPE_MIN: int = ...
H265_UNSPECIFIED_NON_VCL_NAL_TYPE_MAX: int = ...
H265_UNSPECIFIED_NON_VCL_NAL_TYPE_MIN: int = ...
H266_MAX_APS_COUNT: int = ...
H266_MAX_CPB_CNT: int = ...
H266_MAX_CTUS_IN_PICTURE: int = ...
H266_MAX_DECODING_UNITS_IN_PIC_TIMING: int = ...
H266_MAX_DPB_SIZE: int = ...
H266_MAX_ENTRY_POINTS: int = ...
H266_MAX_HEIGHT: int = ...
H266_MAX_LAYERS: int = ...
H266_MAX_LUMA_PS: int = ...
H266_MAX_POINTS_IN_QP_TABLE: int = ...
H266_MAX_PPS_COUNT: int = ...
H266_MAX_REF_ENTRIES: int = ...
H266_MAX_REF_PIC_LISTS: int = ...
H266_MAX_SAMPLE_ARRAYS: int = ...
H266_MAX_SLICES_PER_AU: int = ...
H266_MAX_SLI_REF_LEVELS: int = ...
H266_MAX_SPS_COUNT: int = ...
H266_MAX_SUBLAYERS: int = ...
H266_MAX_SUB_PROFILES: int = ...
H266_MAX_TILES_PER_AU: int = ...
H266_MAX_TILE_COLUMNS: int = ...
H266_MAX_TOTAL_NUM_OLSS: int = ...
H266_MAX_VPS_COUNT: int = ...
H266_MAX_WIDTH: int = ...
H266_NUM_ALF_FILTERS: int = ...
JPEG2000_COLORSPACE_LIST: str = ...
JPEG2000_SAMPLING_LIST: str = ...
JPEG_MAX_FRAME_COMPONENTS: int = ...
JPEG_MAX_QUANT_ELEMENTS: int = ...
JPEG_MAX_SCAN_COMPONENTS: int = ...
VC1_BFRACTION_BASIS: int = ...
VC1_BFRACTION_PTYPE_BI: int = ...
VC1_BFRACTION_RESERVED: int = ...
VC1_MAX_HRD_NUM_LEAKY_BUCKETS: int = ...
VP8_MAX_REF_FRAMES: int = ...
VP9_FRAME_CONTEXTS_LOG2: int = ...
VP9_FRAME_MARKER: int = ...
VP9_MAX_FRAMES_IN_SUPERFRAME: int = ...
VP9_MAX_LOOP_FILTER: int = ...
VP9_MAX_MODE_LF_DELTAS: int = ...
VP9_MAX_PROB: int = ...
VP9_MAX_REF_LF_DELTAS: int = ...
VP9_MAX_SEGMENTS: int = ...
VP9_MAX_SHARPNESS: int = ...
VP9_PREDICTION_PROBS: int = ...
VP9_REFS_PER_FRAME: int = ...
VP9_REF_FRAMES: int = ...
VP9_REF_FRAMES_LOG2: int = ...
VP9_SEGMENT_ABSDATA: int = ...
VP9_SEGMENT_DELTADATA: int = ...
VP9_SEG_TREE_PROBS: int = ...
VP9_SUPERFRAME_MARKER: int = ...
VP9_SYNC_CODE: int = ...
_namespace: str = ...
_version: str = ...
